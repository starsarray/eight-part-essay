- [第 1 题：进程和线程的区别、核心区别、相同点、各自适用场景、线程切换和进程切换的差异、为什么线程切换更快](#第-1-题进程和线程的区别核心区别相同点各自适用场景线程切换和进程切换的差异为什么线程切换更快)
- [第 2 题：TCP 三次握手流程、为什么不是两次/四次、ACK/SEQ 有什么作用、为什么 ACK=SEQ+1、如果第三次握手 ACK 丢了会怎样](#第-2-题tcp-三次握手流程为什么不是两次四次ackseq-有什么作用为什么-ackseq1如果第三次握手-ack-丢了会怎样)
- [第 3 题：在浏览器输入一个 URL 会发生什么、从输入地址到页面展示的完整流程、涉及哪些网络概念](#第-3-题在浏览器输入一个-url-会发生什么从输入地址到页面展示的完整流程涉及哪些网络概念)
- [第 4 题：HTTP 和 HTTPS 的区别、HTTPS 比 HTTP 多了什么、为什么更安全](#第-4-题http-和-https-的区别https-比-http-多了什么为什么更安全)
- [第 5 题：HTTPS 建立连接 / 加密通信流程、TLS 握手过程、非对称加密和对称加密怎么配合、证书校验过程、如何提升 HTTPS 安全性](#第-5-题https-建立连接--加密通信流程tls-握手过程非对称加密和对称加密怎么配合证书校验过程如何提升-https-安全性)
- [第 6 题：MySQL 索引底层原理、B+ 树原理、为什么 MySQL 用 B+ 树不用红黑树](#第-6-题mysql-索引底层原理b-树原理为什么-mysql-用-b-树不用红黑树)
- [第 7 题：索引失效的场景、索引失效的例子、为什么会失效](#第-7-题索引失效的场景索引失效的例子为什么会失效)
- [第 8 题：慢 SQL 如何优化、SQL 执行慢怎么排查、慢日志怎么分析、怎么判断是否命中索引](#第-8-题慢-sql-如何优化sql-执行慢怎么排查慢日志怎么分析怎么判断是否命中索引)
- [第 9 题：JVM 内存划分、JVM 内存模型、堆/栈/方法区/程序计数器/本地方法栈](#第-9-题jvm-内存划分jvm-内存模型堆栈方法区程序计数器本地方法栈)
- [第 10 题：volatile 和 synchronized 的区别、volatile 的作用、能否保证原子性、可见性和有序性怎么理解](#第-10-题volatile-和-synchronized-的区别volatile-的作用能否保证原子性可见性和有序性怎么理解)
- [第 11 题：主流垃圾回收器及区别、CMS 和 G1 的区别、各自适用场景](#第-11-题主流垃圾回收器及区别cms-和-g1-的区别各自适用场景)
- [第 12 题：频繁 GC 的原因、Young GC / Full GC 频繁怎么排查、排查思路是什么](#第-12-题频繁-gc-的原因young-gc--full-gc-频繁怎么排查排查思路是什么)
- [第 13 题：TCP 四次挥手流程、为什么要 TIME\_WAIT、为什么 TIME\_WAIT 要等 2MSL、大量 TIME\_WAIT 的原因](#第-13-题tcp-四次挥手流程为什么要-time_wait为什么-time_wait-要等-2msl大量-time_wait-的原因)
- [第 14 题：select、poll、epoll 的区别、IO 多路复用是什么、这里的“复用”复用的是什么、没有多路复用会有什么问题](#第-14-题selectpollepoll-的区别io-多路复用是什么这里的复用复用的是什么没有多路复用会有什么问题)
- [第 15 题：协程是什么、协程和线程的区别、适用什么场景](#第-15-题协程是什么协程和线程的区别适用什么场景)
- [第 16 题：用户态和内核态的区别、系统调用过程、IO 是否涉及用户态和内核态切换](#第-16-题用户态和内核态的区别系统调用过程io-是否涉及用户态和内核态切换)
- [第 17 题：TCP 和 UDP 的区别、各自特点、各自适用场景](#第-17-题tcp-和-udp-的区别各自特点各自适用场景)
- [第 18 题：MySQL 如何保证事务、ACID 是什么、事务的一致性怎么理解](#第-18-题mysql-如何保证事务acid-是什么事务的一致性怎么理解)
- [第 19 题：MySQL 的 binlog、redo log、undo log 分别是什么、有什么作用、三者之间的区别和关系](#第-19-题mysql-的-binlogredo-logundo-log-分别是什么有什么作用三者之间的区别和关系)
- [第 20 题：MVCC 的原理、版本链、Read View、可重复读是怎么实现的](#第-20-题mvcc-的原理版本链read-view可重复读是怎么实现的)
- [第 21 题：MySQL 主从一致性怎么解决、主从同步流程、写完 binlog 后从库怎么同步](#第-21-题mysql-主从一致性怎么解决主从同步流程写完-binlog-后从库怎么同步)
- [第 22 题：MySQL 和 Redis 的一致性怎么保证、双写一致性问题怎么处理、延迟双删是什么](#第-22-题mysql-和-redis-的一致性怎么保证双写一致性问题怎么处理延迟双删是什么)
- [第 23 题：缓存击穿、缓存穿透、缓存雪崩分别是什么、怎么处理、恶意请求不存在数据怎么防御](#第-23-题缓存击穿缓存穿透缓存雪崩分别是什么怎么处理恶意请求不存在数据怎么防御)
- [第 24 题：HashMap 原理、底层数据结构、put 过程、是否线程安全](#第-24-题hashmap-原理底层数据结构put-过程是否线程安全)
- [第 25 题：HashMap 如何扩容、什么时候扩容、负载因子是什么、多线程扩容为什么可能形成环](#第-25-题hashmap-如何扩容什么时候扩容负载因子是什么多线程扩容为什么可能形成环)
- [第 26 题：Java 接口和抽象类的区别、各自适用场景](#第-26-题java-接口和抽象类的区别各自适用场景)
- [第 27 题：String、StringBuilder、StringBuffer 的区别、循环拼接字符串为什么优先用 StringBuilder、StringBuffer 是否线程安全](#第-27-题stringstringbuilderstringbuffer-的区别循环拼接字符串为什么优先用-stringbuilderstringbuffer-是否线程安全)
- [第 28 题：Java 里面如何实现线程安全、有哪些常见手段、锁 / CAS / 原子类 / ThreadLocal 分别适合什么场景](#第-28-题java-里面如何实现线程安全有哪些常见手段锁--cas--原子类--threadlocal-分别适合什么场景)
- [第 29 题：TCP 拥塞控制、慢启动、拥塞避免、快重传、快恢复分别是什么](#第-29-题tcp-拥塞控制慢启动拥塞避免快重传快恢复分别是什么)
- [第 30 题：上下文切换是什么、线程上下文切换和进程上下文切换差别在哪、为什么线程切换成本更低](#第-30-题上下文切换是什么线程上下文切换和进程上下文切换差别在哪为什么线程切换成本更低)
- [第 31 题：虚拟内存是什么、虚拟地址和物理地址的区别、为什么需要虚拟内存](#第-31-题虚拟内存是什么虚拟地址和物理地址的区别为什么需要虚拟内存)
- [第 32 题：程序地址空间是什么样的、堆和栈在内存中的分布、代码段 / 数据段 / 堆 / 栈分别是什么](#第-32-题程序地址空间是什么样的堆和栈在内存中的分布代码段--数据段--堆--栈分别是什么)
- [第 33 题：TCP 可靠性怎么实现、为什么 TCP 能保证有序、确认应答 / 重传 / 滑动窗口 / 流量控制 / 拥塞控制分别起什么作用](#第-33-题tcp-可靠性怎么实现为什么-tcp-能保证有序确认应答--重传--滑动窗口--流量控制--拥塞控制分别起什么作用)
- [第 34 题：HTTP/2 和 HTTP/3 的区别、HTTP/3 为什么基于 UDP、QUIC 带来了什么变化](#第-34-题http2-和-http3-的区别http3-为什么基于-udpquic-带来了什么变化)
- [第 35 题：一条 update 语句的执行流程、从 SQL 解析到存储引擎执行再到日志落盘的全过程](#第-35-题一条-update-语句的执行流程从-sql-解析到存储引擎执行再到日志落盘的全过程)
- [第 36 题：联合索引的底层结构、最左前缀原则、联合索引 $(a,b,c)$ 为什么查 b=1 and c=1 不一定走索引](#第-36-题联合索引的底层结构最左前缀原则联合索引-abc-为什么查-b1-and-c1-不一定走索引)
- [第 37 题：MyBatis 如何防止 SQL 注入、#{} 和 ${} 的区别、预编译为什么能防注入](#第-37-题mybatis-如何防止-sql-注入-和--的区别预编译为什么能防注入)
- [第 38 题：死锁是什么、死锁的必要条件、如何避免死锁、如何排查死锁](#第-38-题死锁是什么死锁的必要条件如何避免死锁如何排查死锁)
- [第 39 题：Linux 文件系统是什么样的、VFS / inode / dentry 分别是什么、文件查找过程是什么](#第-39-题linux-文件系统是什么样的vfs--inode--dentry-分别是什么文件查找过程是什么)
- [第 40 题：零拷贝是什么、Java 或 Linux 中怎么实现零拷贝、涉及哪些系统调用](#第-40-题零拷贝是什么java-或-linux-中怎么实现零拷贝涉及哪些系统调用)
- [第 41 题：IO 模型有哪些、阻塞 / 非阻塞 / 同步 / 异步的区别、什么是 IO 多路复用](#第-41-题io-模型有哪些阻塞--非阻塞--同步--异步的区别什么是-io-多路复用)
- [第 42 题：WebSocket 是怎么实现持久化双向通信的、为什么可以双向通信、心跳和保活怎么做](#第-42-题websocket-是怎么实现持久化双向通信的为什么可以双向通信心跳和保活怎么做)
- [第 43 题：RPC 和 HTTP 的区别、为什么很多内部服务用 RPC、不直接用 HTTP 的原因是什么](#第-43-题rpc-和-http-的区别为什么很多内部服务用-rpc不直接用-http-的原因是什么)
- [第 44 题：微服务里的熔断、降级、限流分别是什么、常见限流算法有哪些](#第-44-题微服务里的熔断降级限流分别是什么常见限流算法有哪些)
- [第 45 题：Kafka 如何保持高吞吐和高可用、分区和副本的作用、消费失败怎么处理、消息堆积怎么处理](#第-45-题kafka-如何保持高吞吐和高可用分区和副本的作用消费失败怎么处理消息堆积怎么处理)
- [第 46 题：分布式锁如何实现、Redis 分布式锁怎么做、SET NX EX 是什么、锁续期和误删问题怎么解决](#第-46-题分布式锁如何实现redis-分布式锁怎么做set-nx-ex-是什么锁续期和误删问题怎么解决)
- [第 47 题：布隆过滤器原理、哈希函数和 bitmap 怎么配合、为什么能防缓存穿透、有什么缺点](#第-47-题布隆过滤器原理哈希函数和-bitmap-怎么配合为什么能防缓存穿透有什么缺点)
- [第 48 题：DNS 解析过程是什么、域名是怎么查到 IP 的、如果二级域名没找到会发生什么](#第-48-题dns-解析过程是什么域名是怎么查到-ip-的如果二级域名没找到会发生什么)
- [第 49 题：RAG 怎么做、切片 / 向量化 / 召回 / 重排流程是什么、千万级知识库检索有什么性能瓶颈、怎么优化](#第-49-题rag-怎么做切片--向量化--召回--重排流程是什么千万级知识库检索有什么性能瓶颈怎么优化)
- [第 50 题：Java 里面怎么实现线程安全类、AQS 是什么、为什么 AQS 要用队列、公平锁和非公平锁怎么实现](#第-50-题java-里面怎么实现线程安全类aqs-是什么为什么-aqs-要用队列公平锁和非公平锁怎么实现)

### 第 1 题：进程和线程的区别、核心区别、相同点、各自适用场景、线程切换和进程切换的差异、为什么线程切换更快

🎯 【一句话总结】

**进程是资源分配的基本单位，线程是 CPU 调度的基本单位**；核心区别就在于**进程之间资源隔离，线程之间共享进程资源**，所以一般来说**线程更轻量、切换成本更低**。

🗣️ 【口述要点】

第一，先说核心区别，**进程**可以理解成一个独立运行的程序实例，它有自己独立的**虚拟地址空间、文件描述符、页表、各种系统资源**；而**线程**是进程里的执行流，同一个进程下的多个线程会共享**堆、方法区、全局变量、打开的文件**这些资源，但每个线程也有自己独立的**栈、程序计数器、寄存器上下文**。

第二，二者的相同点是，它们本质上都是操作系统做**并发执行和任务调度**的载体，都有自己的运行状态，比如就绪、运行、阻塞，也都会发生上下文切换。只不过进程更偏向“资源容器”，线程更偏向“执行单元”。

第三，适用场景上，一般如果要求**隔离性、稳定性、安全性**更强，比如多个独立服务、不同业务模块、防止相互影响，通常会优先用**多进程**；如果更关注**并发能力、响应速度、资源利用率**，比如 Web 服务器处理请求、线程池执行任务、后台异步处理，通常会优先用**多线程**。

第四，线程切换比进程切换更快，主要是因为**线程共享进程资源**，切换时通常只需要保存和恢复**线程私有的上下文**，比如寄存器、栈信息；而进程切换除了这些，还往往涉及**地址空间切换、页表切换、TLB 刷新、内核资源切换**，系统开销会更大。所以从调度成本上看，线程一般明显比进程轻。

### 第 2 题：TCP 三次握手流程、为什么不是两次/四次、ACK/SEQ 有什么作用、为什么 ACK=SEQ+1、如果第三次握手 ACK 丢了会怎样

🎯 【一句话总结】

**TCP 三次握手的目的，本质上是让通信双方互相确认“收发能力正常”，并同步初始序列号**；之所以不是两次，是因为两次没法保证服务端确认客户端的接收能力，四次又没有必要，三次刚好够。

🗣️ 【口述要点】

第一，先说标准流程。客户端先发一个 **SYN** 报文，请求建立连接，同时带上自己的初始 **SEQ=x**；服务端收到后，返回 **SYN+ACK**，表示自己收到了，同时也告诉客户端它自己的初始序列号，比如 **SEQ=y，ACK=x+1**；最后客户端再回一个 **ACK**，表示我也收到了你的 SYN，通常是 **SEQ=x+1，ACK=y+1**。到这一步，双方都确认连接可以建立了。

第二，**SEQ** 可以理解成这段数据的序号，用来保证**有序传输和重传控制**；**ACK** 是确认号，表示“我期望你下次从哪个序号开始发”。之所以常看到 **ACK=SEQ+1**，是因为 **SYN** 本身虽然不带业务数据，但它也会**消耗一个序号**，所以确认时要加 1。这个规则不只是握手，FIN 也一样会占一个序号。

第三，为什么不是两次握手，核心原因是**两次只能保证客户端能发、服务端能收，以及服务端能发，但不能保证客户端能收**。也就是说，服务端没法确认自己的响应有没有真正到达客户端。如果少了第三次 ACK，服务端就可能误以为连接已经建立，造成**半连接问题**或者历史报文引发的异常连接。

第四，为什么不是四次握手，因为三次已经足够完成**双方收发能力确认 + 初始序列号同步**，再多一次只是徒增开销，没有新增价值。至于第三次 ACK 如果丢了，客户端其实已经认为连接建立了，可以开始发数据；服务端因为没收到 ACK，会认为连接还没完全建立，此时它会**重传 SYN+ACK**。如果客户端收到了重传报文，会再发 ACK，服务端最终也能进入 ESTABLISHED 状态，所以一般可以恢复，只是会有一次额外重传。

### 第 3 题：在浏览器输入一个 URL 会发生什么、从输入地址到页面展示的完整流程、涉及哪些网络概念

🎯 【一句话总结】

**从浏览器输入 URL 到页面展示，本质上经历了“URL 解析、DNS 解析、建立连接、发送请求、服务端处理、浏览器渲染”这几大步骤**；面试里回答时，重点要把**网络链路**和**浏览器渲染链路**都讲出来。

🗣️ 【口述要点】

第一，浏览器拿到 URL 之后，先做**URL 解析**，判断协议是 **HTTP 还是 HTTPS**，再解析出域名、端口、请求路径这些信息。接着会先查**浏览器缓存、操作系统缓存、hosts 文件、本地 DNS 缓存**，如果都没有，就发起 **DNS 解析**，把域名解析成目标 IP，这里可能还会经过**递归查询、根 DNS、顶级域 DNS、权威 DNS** 这些过程。

第二，拿到 IP 之后，浏览器会和服务端建立连接。如果是 **HTTP**，通常就是 **TCP 三次握手**；如果是 **HTTPS**，那在 TCP 建立之后，还要再走一层 **TLS 握手**，完成证书校验、密钥协商，后面才能开始加密通信。这里涉及的网络概念主要有 **DNS、TCP、TLS、IP、端口、CDN、负载均衡** 这些。

第三，连接建立好之后，浏览器会发起 **HTTP 请求**，请求到达服务端可能会先经过**反向代理、网关、负载均衡**，再到具体应用服务。应用服务处理完业务逻辑后，可能会访问**数据库、缓存、搜索引擎、下游 RPC 服务**，最后把 **HTML、CSS、JS、图片** 等资源返回给浏览器。这里如果用了 **CDN、缓存、Keep-Alive、HTTP/2 多路复用**，性能会更好。

第四，浏览器拿到响应后进入渲染阶段：先解析 **HTML** 构建 **DOM 树**，解析 **CSS** 构建 **CSSOM 树**，然后合并形成 **Render Tree**，接着做 **布局 Layout**，最后做 **绘制 Paint** 和 **合成 Composite**，页面才真正显示出来。中间如果碰到 **JS**，还可能阻塞 DOM 解析或者触发重排重绘，所以前端性能优化很多都是围绕这条链路展开的。

### 第 4 题：HTTP 和 HTTPS 的区别、HTTPS 比 HTTP 多了什么、为什么更安全

🎯 【一句话总结】

**HTTP 是明文传输协议，HTTPS 是“HTTP + TLS”**；HTTPS 比 HTTP 多了一层**加密、身份认证、完整性校验**，所以它更安全，能防止数据被窃听、篡改和冒充。

🗣️ 【口述要点】

第一，最直接的区别是传输方式不同。**HTTP** 的数据默认是**明文传输**，中间链路上的人如果能抓包，就有机会看到请求内容；而 **HTTPS** 在 HTTP 和 TCP 之间加了一层 **TLS/SSL**，通信内容会先加密再传，所以即使被截获，也很难直接读懂。

第二，HTTPS 比 HTTP 主要多了三件事：

- **加密**：保护传输内容不被窃听；
- **证书认证**：确认你访问的服务端是不是“真的那个服务端”；
- **完整性校验**：防止传输过程中内容被人偷偷改掉。
   也就是说，HTTPS 解决的不只是“保密性”，还解决了“身份可信”和“数据没被篡改”这两个问题。

第三，为什么 HTTPS 更安全，本质上是因为它结合了**非对称加密、对称加密、数字证书、消息摘要**这些机制。握手阶段通常用**非对称加密或密钥协商**来安全地建立会话密钥，后续正式传输大量数据时，再用**对称加密**提高性能；同时客户端会校验证书链和域名，避免被中间人伪造服务端。

第四，当然 HTTPS 也不是完全没有代价。相比 HTTP，它会多一次 **TLS 握手**，证书管理也有成本，CPU 也会有一定加密开销；不过现在硬件性能和 TLS 优化都比较成熟了，而且安全收益远大于这点性能损耗，所以线上基本都会优先上 HTTPS。

### 第 5 题：HTTPS 建立连接 / 加密通信流程、TLS 握手过程、非对称加密和对称加密怎么配合、证书校验过程、如何提升 HTTPS 安全性

🎯 【一句话总结】

**HTTPS 的核心就是先通过 TLS 握手安全地协商出会话密钥，再用这个对称密钥进行后续加密通信**；它之所以安全，关键在于**证书校验 + 密钥协商 + 完整性保护**这三个环节。

🗣️ 【口述要点】

第一，先说整体流程。客户端先和服务端建立 **TCP 连接**，然后进入 **TLS 握手**。客户端会先发起 `ClientHello`，告诉服务端自己支持的 **TLS 版本、加密套件、随机数**；服务端返回 `ServerHello`，选择具体的加密套件，并把自己的**数字证书**和服务端随机数发给客户端。后面双方再基于握手参数协商出一个**会话密钥**，握手完成后，应用层的 HTTP 数据就用这个会话密钥做加密传输了。

第二，**非对称加密和对称加密**是配合使用的。面试里可以直接说：**非对称加密/密钥交换负责安全地协商密钥，对称加密负责高效传输数据**。因为非对称加密安全性高，但性能比较差，不适合加密大量业务数据；对称加密性能高，适合后续正式通信。所以 HTTPS 本质上是“握手阶段偏非对称，传输阶段偏对称”。

第三，证书校验过程也很重要。客户端收到服务端证书后，会重点校验这几件事：

- 证书是否在**有效期**内；
- 证书里的**域名**和当前访问域名是否匹配；
- 证书是否由受信任的 **CA** 签发；
- 证书链是否完整，根证书是否能追溯到系统内置信任链；
- 证书是否被吊销。
   如果这些校验不过，理论上客户端就应该报警或者直接拒绝连接，这一步主要就是防止**中间人攻击**。

第四，怎么进一步提升 HTTPS 安全性，主要体现在几个方面：

- 优先使用 **TLS 1.2 / TLS 1.3**，禁用老旧的 SSL、TLS 1.0/1.1；
- 选择更安全的加密套件，优先使用支持 **前向安全性** 的方案，比如 **ECDHE**；
- 开启 **HSTS**，强制浏览器只走 HTTPS；
- 做好证书和私钥管理，避免私钥泄露；
- 服务端关闭弱算法、弱证书和不安全重定向。

### 第 6 题：MySQL 索引底层原理、B+ 树原理、为什么 MySQL 用 B+ 树不用红黑树

🎯 【一句话总结】

**MySQL，尤其是 InnoDB 的索引底层主要是 B+ 树**；原因很简单，**B+ 树更适合磁盘和范围查询场景，能用更少的 IO 支持更稳定的查询性能**，这也是它比红黑树更适合数据库索引的核心原因。

🗣️ 【口述要点】

第一，先说索引本质。数据库索引可以理解成一套**加速查找的数据结构**，它的目标不是单纯“能查到”，而是要在磁盘场景下尽量减少 **IO 次数**。InnoDB 里的**聚簇索引**和**二级索引**底层通常都是 **B+ 树**。聚簇索引的叶子节点存的是**整行数据**，二级索引的叶子节点存的是**索引列值 + 主键值**，查二级索引后往往还要再回主键索引查一次，这就是常说的**回表**。

第二，**B+ 树**的特点主要有两个。一个是**非叶子节点只存键，不存完整数据**，所以单个节点能放更多 key，树的高度就更低；另一个是**所有数据都在叶子节点，并且叶子节点之间通过链表相连**，这样做范围查询、排序、分页的时候非常高效。面试里你可以直接说，B+ 树很适合数据库这种“**大量随机查 + 范围查**”的混合场景。

第三，为什么不用**红黑树**，核心原因是红黑树是**二叉树**，每个节点分叉太少，数据量一大，树就会很高。树一高，磁盘 IO 次数就会变多，而数据库查询最怕的就是随机 IO。相比之下，B+ 树是**多路平衡查找树**，一个节点可以挂很多分支，高度能控制得很低，通常查几次就到底了，这对磁盘型存储非常友好。

第四，再补一个常见追问，为什么不是 B 树而是 B+ 树。因为 **B 树**的非叶子节点也会存数据，导致每个节点能存的 key 更少，树更容易变高；而且 B+ 树所有数据都在叶子节点，天然更适合**范围扫描和顺序遍历**。所以数据库里最终更偏向 B+ 树，而不是红黑树或者普通 B 树。

### 第 7 题：索引失效的场景、索引失效的例子、为什么会失效

🎯 【一句话总结】

**索引失效本质上就是优化器觉得“没法高效利用索引”或者“用了索引反而更慢”**；常见原因主要集中在**联合索引没满足最左前缀、对索引列做了计算/函数/隐式转换、范围查询截断、模糊查询写法不当**这几类。

🗣️ 【口述要点】

第一，最常见的是**联合索引不满足最左前缀原则**。比如有联合索引 $(a,b,c)$，如果你查 `a=1 and b=2`，一般能走；但如果直接查 `b=2 and c=3`，因为跳过了最左边的 `a`，通常就没法按这个联合索引高效定位。所以面试里可以直接说，联合索引能不能生效，关键看是否从最左列开始连续匹配。

第二，对**索引列做函数、表达式计算、类型转换**，也很容易导致失效。比如 `where date(create_time)=...`、`where id+1=10`，或者字段是字符串你却拿数字去比，可能触发**隐式类型转换**。原因是索引里存的是原始值，一旦你对列做加工，优化器就很难直接利用原来的有序结构去查。

第三，**范围查询**和**模糊查询**也是高频场景。比如联合索引 $(a,b,c)$，如果条件里 `b > 10` 这种范围已经出现了，那后面的 `c` 往往就不能继续完整利用索引；再比如 `like '%abc'` 这种前面带百分号的写法，因为开头不确定，索引也基本用不上。但如果是 `like 'abc%'`，通常还是可以利用前缀索引能力的。

第四，还有一类是**优化器主动放弃索引**。比如表数据量本来就很小，或者查询返回的行太多，走索引后还要大量回表，优化器可能判断**全表扫描更划算**。所以“有索引”不等于“必走索引”，最终还是要看执行计划。

### 第 8 题：慢 SQL 如何优化、SQL 执行慢怎么排查、慢日志怎么分析、怎么判断是否命中索引

🎯 【一句话总结】

**慢 SQL 优化的核心思路就是先定位瓶颈，再看执行计划，最后有针对性地改 SQL、改索引、改表结构或者改架构**；不要一上来就拍脑袋加索引。

🗣️ 【口述要点】

第一，排查慢 SQL，我一般会先做**定位**。最常用的手段就是开 **慢查询日志**，先把执行时间长、扫描行数多的 SQL 找出来；如果是线上问题，也会结合 **APM、数据库监控、应用日志** 看是偶发慢，还是持续慢，是单条 SQL 问题，还是整体数据库压力大。

第二，定位到 SQL 之后，重点看 **`EXPLAIN` 执行计划**。这里我一般会重点关注几个字段：

- **type**：访问类型，至少希望到 `range`、`ref`，最好是 `const`、`eq_ref`；
- **key**：实际用了哪个索引；
- **rows**：预估扫描了多少行；
- **extra**：有没有 `Using filesort`、`Using temporary`、`Using where` 之类的信息。
   判断有没有命中索引，最直接就是看 **key 是否为空**，以及扫描行数是不是明显变少。

第三，常见优化手段主要有这几个方面：

- **SQL 改写**：避免 `select *`，只查需要的列；拆复杂 SQL；减少子查询和不必要的排序分组；
- **索引优化**：补合适的单列索引或联合索引，尽量用上**覆盖索引**，减少回表；
- **避免索引失效**：比如不要对索引列做函数、计算、隐式类型转换，联合索引注意最左前缀；
- **表结构优化**：字段类型尽量合理，避免过宽行，必要时分库分表或冷热分离。

第四，如果 SQL 本身没问题，还要看是不是**数据库整体负载**导致的，比如 CPU 打满、IO 高、锁等待严重、Buffer Pool 命中率低、连接数过多，或者事务太大导致阻塞。也就是说，SQL 慢不一定只是“语句写得差”，也可能是资源瓶颈或者并发冲突问题。

如果你要，我下一条继续给你 **第 9 题：JVM 内存划分、JVM 内存模型、堆/栈/方法区/程序计数器/本地方法栈**。

### 第 9 题：JVM 内存划分、JVM 内存模型、堆/栈/方法区/程序计数器/本地方法栈

🎯 【一句话总结】

**JVM 运行时内存区域**主要分成 **线程私有** 和 **线程共享** 两大类；面试里最核心的就是讲清楚 **堆、方法区、虚拟机栈、程序计数器、本地方法栈** 各自放什么、解决什么问题。

🗣️ 【口述要点】

第一，先从整体上看，JVM 内存可以分为两类：**线程私有**的有 **程序计数器、虚拟机栈、本地方法栈**；**线程共享**的有 **堆** 和 **方法区**。这个分类很关键，因为很多问题本质上就是看是线程执行过程中的问题，还是共享对象和类元数据的问题。

第二，**程序计数器**可以理解成当前线程正在执行的字节码位置指示器，线程切换之后能够依赖它恢复到正确的执行位置。**虚拟机栈**是每个线程执行 Java 方法时使用的，方法每调用一次就会创建一个**栈帧**，里面主要有**局部变量表、操作数栈、动态链接、返回地址**。平时常见的 **StackOverflowError**，一般就是递归太深或者栈帧太多导致的。

第三，**本地方法栈**和虚拟机栈类似，只不过它服务的是 **Native 方法**。而 **堆** 是 JVM 中最核心的内存区域，绝大多数**对象实例和数组**都在这里分配，它也是 **垃圾回收器重点管理的区域**。从经典分代角度看，堆一般又分成**新生代**和**老年代**，新生代里面还会有 **Eden** 和 **Survivor**。

第四，**方法区**主要存放**类信息、运行时常量池、静态变量、JIT 编译后的代码**等数据。它在规范里属于 JVM 运行时数据区的一部分，HotSpot 里早期通常用**永久代**来实现，到了 **JDK 8** 之后改成了**元空间**，也就是使用本地内存来存这部分数据。这里也要注意，**JVM 内存划分**和 **Java 内存模型 JMM** 不是一回事，前者说的是运行时区域怎么分，后者说的是多线程下**可见性、有序性、原子性**这些规则。

如果你要，我下一条继续给你 **第 10 题：volatile 和 synchronized 的区别**。

### 第 10 题：volatile 和 synchronized 的区别、volatile 的作用、能否保证原子性、可见性和有序性怎么理解

🎯 【一句话总结】

**volatile 和 synchronized 都能解决多线程问题，但侧重点完全不一样**；**volatile 主要保证可见性和一定程度的有序性，不保证原子性**，而 **synchronized** 是一种互斥锁，既能保证**可见性**，也能保证**原子性**。

🗣️ 【口述要点】

第一，先说 **volatile** 的作用。给变量加上 **volatile** 之后，一个线程修改这个变量，其他线程能够立刻看到最新值，这就是**可见性**。另外它还能通过**内存屏障**禁止特定场景下的指令重排，所以也能保证一定的**有序性**。但是它不能保证复合操作的**原子性**，比如 `i++` 本质上是读、改、写三个动作，多线程下还是会出问题。

第二，**synchronized** 的核心是**互斥**。同一时刻只允许一个线程进入同步代码块或者同步方法，所以它天然能保证临界区代码的**原子性**。同时，线程在释放锁之前对共享变量的修改，对后续获得同一把锁的线程也是可见的，所以它也具备**可见性**和**有序性**保证。

第三，这两个关键区别可以从适用场景上理解：如果只是一个状态位、开关变量，或者典型的**一个线程写、多个线程读**，用 **volatile** 就比较合适，开销小；但如果涉及**计数器、自增、复合判断、先检查再执行**这类操作，就必须考虑 **synchronized、Lock、CAS** 这些能保证原子性的手段，不能只靠 volatile。

第四，再解释一下三个概念。**可见性**，就是一个线程修改共享变量后，其他线程能不能及时看到；**原子性**，就是一组操作要么全做完，要么都不做，中间不能被打断；**有序性**，就是程序执行结果不能因为编译器优化或 CPU 重排而违背多线程语义。面试里把这三个词讲清楚，volatile 和 synchronized 的区别基本就说明白了。

### 第 11 题：主流垃圾回收器及区别、CMS 和 G1 的区别、各自适用场景

🎯 【一句话总结】

**主流垃圾回收器的选择，本质上是在“吞吐量、停顿时间、内存碎片、实现复杂度”之间做权衡**；如果面试官重点问 CMS 和 G1，可以直接回答：**CMS 更追求低停顿，G1 是面向服务端场景、兼顾吞吐和可预测停顿的更通用方案**。

🗣️ 【口述要点】

第一，先从大类上看，JVM 垃圾回收器主要分为两类：**新生代回收器**和**老年代回收器**，以及一些可以管理整个堆的**整堆回收器**。经典组合里，像 **Serial、ParNew、Parallel Scavenge** 更多用于新生代，**Serial Old、CMS、Parallel Old** 更多用于老年代，而 **G1、ZGC、Shenandoah** 这类则更偏向整堆管理。面试里通常重点讲 **Serial、Parallel、CMS、G1** 就够了。

第二，**CMS** 的特点是以**低停顿**为核心目标，它基于**标记-清除**算法，主要阶段包括初始标记、并发标记、重新标记、并发清除，其中大部分阶段可以和用户线程并发执行，所以停顿时间相对短，适合对响应时间敏感的系统。但它也有明显问题，比如**会产生内存碎片**，对 CPU 比较敏感，而且在并发回收过程中如果内存不够，还可能退化成 **Serial Old**，导致更长停顿。

第三，**G1** 可以理解成面向大堆内存场景设计的回收器，它把堆划分成很多大小相等的 **Region**，不再固定区分传统的新生代和老年代物理边界，而是通过 Region 动态管理。G1 的优势在于它能按收益优先回收部分 Region，尽量满足**可预测的停顿时间目标**，而且它在回收时会做**整理压缩**，所以比 CMS 更不容易产生内存碎片。

第四，适用场景上，一般来说：如果是比较老的系统、JDK 版本偏旧、追求较低停顿，历史上很多项目会用 **CMS**；但从现在的趋势看，**G1** 已经是更主流、更通用的服务端选择，尤其适合**堆比较大、既关注吞吐又关注停顿**的场景。如果再往新版本走，对超大堆和超低停顿要求更高时，还会考虑 **ZGC** 这类更先进的回收器。

### 第 12 题：频繁 GC 的原因、Young GC / Full GC 频繁怎么排查、排查思路是什么

🎯 【一句话总结】

**频繁 GC 本质上说明“对象分配速度”和“内存回收速度”失衡了**；排查时不要只盯着 JVM 参数，应该先区分是 **Young GC 频繁** 还是 **Full GC 频繁**，再分别看是**对象创建过快、内存泄漏、晋升异常、堆设置不合理**，还是代码层面有问题。

🗣️ 【口述要点】

第一，先分类型看。**Young GC 频繁**，通常说明新生代压力大，常见原因包括：短生命周期对象创建太多、接口高并发下临时对象暴增、对象太大直接进老年代，或者新生代本身配得太小。**Full GC 频繁** 往往更严重，通常意味着老年代空间紧张，可能是对象长期存活、内存泄漏、缓存没上限、大对象过多、晋升过快，或者元空间也在异常增长。

第二，排查时我一般先看**GC 日志**和监控指标，重点关注这几个点：

- GC 的频率和每次停顿时间；
- Young GC 后对象有没有被大量晋升到老年代；
- Full GC 前后老年代回收效果怎么样；
- 堆使用曲线是不是一直上升，GC 后回不来。
   如果 GC 后内存明显回落，更多是分配压力问题；如果 GC 后还是降不下来，就要高度怀疑**内存泄漏**或者长生命周期对象堆积。

第三，再往下会结合工具看对象分布。常用思路是通过 **jstat、jmap、jcmd、MAT、Arthas** 这些工具，先看哪个区域增长异常，再看**哪些对象实例数最多、谁在持有引用、引用链有没有一直不释放**。比如如果发现某些 **Map、缓存、ThreadLocal、连接对象、消息堆积对象** 持续增长，那就很可能是代码层面的问题，不是简单调参数能解决的。

第四，优化一般分两层。**JVM 层面**可以调整堆大小、新生代比例、晋升阈值、回收器类型；**代码层面**更关键，要减少不必要对象创建、控制大对象、给缓存加淘汰策略、及时释放资源、避免集合无界增长。面试里最好强调一句：**参数调优只能缓解，真正根因往往还是业务对象生命周期管理有问题**。

### 第 13 题：TCP 四次挥手流程、为什么要 TIME_WAIT、为什么 TIME_WAIT 要等 2MSL、大量 TIME_WAIT 的原因

🎯 【一句话总结】

**TCP 四次挥手的本质，是连接双方要分别关闭各自的发送通道，所以通常需要四次**；而 **TIME_WAIT** 的核心作用是**确保最后的 ACK 能被对方收到，同时让旧连接中的延迟报文彻底过期**。

🗣️ 【口述要点】

第一，先说四次挥手流程。假设客户端主动关闭连接，它先发送一个 **FIN**，表示“我这边没有数据要发了”；服务端收到后，先回一个 **ACK**，表示我知道你要关闭了，但这时候服务端可能还有数据没发完，所以它的发送通道还没关。等服务端数据发完之后，再发送自己的 **FIN**；客户端收到后，再回一个最终的 **ACK**。所以整个过程通常是 **FIN -> ACK -> FIN -> ACK**，这就是四次挥手。

第二，为什么不是三次，核心原因是 **TCP 是全双工的**。一方关闭发送能力，并不代表另一方也能立刻关闭，服务端收到第一个 FIN 时，可能还有剩余数据要发，所以它只能先确认，再等自己准备好了再发 FIN。也就是说，第二次 ACK 和第三次 FIN 往往不能简单合并，所以通常是四次。

第三，**TIME_WAIT** 是出现在主动关闭方上的。它存在的第一个原因，是为了保证最后一个 **ACK** 万一丢了，对方重发 FIN 时，本端还能重新发 ACK；第二个原因，是为了让网络中这个旧连接残留的报文在经过一段时间后自然消失，避免后面新的同名连接收到旧报文，造成数据错乱。

第四，为什么要等 **2MSL**，因为 **MSL** 是一个报文在网络中的最大生存时间。等 **2MSL**，可以确保“我发出去的最后 ACK”以及“对方如果重发的 FIN”这两个方向上的报文都能过期掉。线上如果出现大量 **TIME_WAIT**，常见原因一般是：短连接太多、HTTP 没做好连接复用、客户端频繁主动关闭、服务调用量大但连接池配置不合理。这个时候一般先考虑**Keep-Alive、连接池复用、减少短连接创建**，而不是一上来改系统内核参数。

### 第 14 题：select、poll、epoll 的区别、IO 多路复用是什么、这里的“复用”复用的是什么、没有多路复用会有什么问题

🎯 【一句话总结】

**IO 多路复用的核心，就是一个线程同时监听多个文件描述符，一旦某个连接就绪，再去处理它**；这里“复用”的本质是**复用同一个线程或进程去管理多个 IO 连接**，而不是一个连接重复用很多次。

🗣️ 【口述要点】

第一，先说什么是 **IO 多路复用**。传统阻塞 IO 下，一个连接往往要配一个线程去等数据，连接一多，线程数就会爆炸；而多路复用的做法是，让一个线程通过内核提供的机制，统一监听多个 **socket**，哪个连接可读、可写了，内核就通知它去处理。这样做的价值就是**减少线程数量、降低上下文切换成本、提升高并发连接处理能力**。

第二，**select、poll、epoll** 都是实现多路复用的方式，但能力层次不一样。**select** 的问题比较明显，它用的是位图或数组结构，默认监听的文件描述符数量有限，而且每次调用都要把整个集合从用户态拷贝到内核态，再轮询一遍，连接数一大性能就比较差。**poll** 相比 select 解决了描述符数量限制问题，但本质上还是**线性扫描**，所以连接多的时候性能还是会下降。

第三，**epoll** 是 Linux 下更高效的方案。它把“注册感兴趣的 fd”和“等待就绪事件”拆开了，内核会维护一个就绪队列，应用层不需要每次把所有 fd 全量传进去，也不用每次从头遍历全部连接。简单理解就是：**select 和 poll 更像每次都主动全表扫描，epoll 更像只拿已经准备好的结果**，所以在高并发、长连接场景下优势很明显。

第四，没有多路复用会有什么问题，最直接就是**一个连接一个线程**的模型扛不住高并发。线程多了之后，会带来大量**内存占用、上下文切换、调度开销**，而且大部分线程可能都只是阻塞在那里等 IO，CPU 利用率并不高。所以像 **Nginx、Redis、Netty** 这类高并发框架，底层都会大量依赖 **epoll** 这种机制。

### 第 15 题：协程是什么、协程和线程的区别、适用什么场景

🎯 【一句话总结】

**协程可以理解成比线程更轻量的用户态执行单元**；它和线程最大的区别在于，**线程由操作系统调度，协程通常由应用程序或运行时在用户态调度**，所以协程在高并发 IO 场景下切换成本更低。

🗣️ 【口述要点】

第一，先说协程是什么。协程本质上也是一种并发执行模型，但它不是操作系统直接调度的，而是由**用户态运行时**自己管理调度。你可以把它理解成“在线程内部再切出很多更轻量的任务”，一个线程里可以承载很多协程，它们通过**主动让出执行权**或者由运行时调度，实现看起来像并发的效果。

第二，协程和线程最大的区别主要体现在调度和开销上。**线程**是内核调度单位，切换时会涉及**内核态参与、寄存器保存恢复、上下文切换**；而**协程**一般在用户态切换，只需要保存少量运行现场，不涉及频繁的内核调度，所以开销更小。另外，线程通常是**抢占式调度**，协程更多是**协作式调度**，也就是说协程一般要在合适的点主动挂起和恢复。

第三，协程更适合**高并发、IO 密集型**场景。比如网络请求、RPC 调用、数据库访问、爬虫、网关、IM 长连接这类任务，很多时间其实都耗在等待 IO 返回上，这时候用协程可以在一个或少量线程里挂很多任务，资源利用率会比较高。像 **Go 的 goroutine、Kotlin 协程、Python asyncio**，本质上都是这个思路。

第四，协程也不是万能的。它比较不适合那种**纯 CPU 密集型**场景，因为 CPU 真正跑计算时，协程并不能让单核算得更快，这种场景还是得靠多线程、多进程或者多机并行。面试里可以直接说：**协程更擅长解决“高并发等待”的问题，不是解决“高强度计算”的问题**。

### 第 16 题：用户态和内核态的区别、系统调用过程、IO 是否涉及用户态和内核态切换

🎯 【一句话总结】

**用户态和内核态的核心区别就是权限不同**；用户态权限受限，不能直接访问硬件和关键资源，必须通过**系统调用**进入内核态完成相关操作，所以绝大多数 IO 都会涉及**用户态和内核态切换**。

🗣️ 【口述要点】

第一，先说区别。**用户态**是应用程序运行的状态，权限比较低，不能直接执行特权指令，也不能直接操作硬件设备；**内核态**是操作系统内核运行的状态，权限最高，可以管理内存、进程、文件系统、网络设备这些核心资源。这样设计的目的，本质上就是做**隔离和保护**，避免应用程序随便操作系统底层资源，影响整个系统稳定性。

第二，系统调用就是用户态程序向内核申请服务的一种标准方式。比如程序调用 `read`、`write`、`send`、`recv` 这些函数时，并不是自己直接去读磁盘或网卡，而是先从用户态切到内核态，由内核去完成真正的资源访问。内核处理完之后，再把结果返回给用户程序，最后再切回用户态继续执行。所以系统调用本质上就是一次**受控的权限切换**。

第三，IO 基本都会涉及用户态和内核态切换。以一次网络读取为例，应用线程调用 `read` 后先进入内核态，内核检查数据有没有准备好；如果没准备好，阻塞 IO 下线程会挂起，等网卡数据到达后，内核再把数据从内核缓冲区拷贝到用户缓冲区，最后返回用户态。所以这里至少会有**态切换**，很多场景还会伴随**数据拷贝**。

第四，面试里还可以顺手说明，IO 性能问题很多时候并不只是“磁盘慢、网络慢”，还包括**频繁系统调用、频繁上下文切换、用户态和内核态之间的数据拷贝**。这也是为什么后面会有 **零拷贝、mmap、epoll、异步 IO** 这些优化方案，本质上都是在减少切换和拷贝成本。

### 第 17 题：TCP 和 UDP 的区别、各自特点、各自适用场景

🎯 【一句话总结】

**TCP 和 UDP 的核心区别在于：TCP 面向连接、可靠，UDP 无连接、轻量但不保证可靠性**；所以一般来说，**对可靠性要求高用 TCP，对实时性和传输效率更敏感用 UDP**。

🗣️ 【口述要点】

第一，先说最本质的区别。**TCP** 是**面向连接**的，通信前要先三次握手建立连接，传输过程中有**确认应答、重传、流量控制、拥塞控制**这些机制，所以能保证数据**可靠、有序、不丢不重**地到达。**UDP** 是**无连接**的，发数据前不需要建连，也没有这些复杂控制机制，所以它更轻量，但默认不保证数据一定到、顺序一定对，也不保证不重复。

第二，从性能和开销上看，**TCP** 因为机制完整，所以头部更大、状态更多、延迟和开销也更高；**UDP** 头部比较小，没有建连和复杂确认流程，传输效率高，时延通常也更低。所以面试里可以直接说，TCP 更像“安全稳妥地送达”，UDP 更像“尽快发出去”。

第三，适用场景上，如果是**网页访问、数据库连接、文件传输、RPC 调用、消息可靠投递**这类业务，通常都用 **TCP**，因为这类场景最怕丢数据、乱序或者数据不完整；如果是**视频直播、语音通话、在线游戏、DNS 查询、某些监控上报**，很多时候更适合 **UDP**，因为这些场景往往更看重**实时性**，少量丢包通常比高延迟更能接受。

第四，也要注意，UDP “不可靠”不代表它不能做可靠传输。如果业务有需要，也可以在应用层自己实现**重传、排序、限速、纠错**这些能力。像 **QUIC/HTTP3** 就是基于 UDP 之上，在应用层实现了更灵活的可靠传输和拥塞控制，所以现在很多高性能协议也开始走这条路线。

### 第 18 题：MySQL 如何保证事务、ACID 是什么、事务的一致性怎么理解

🎯 【一句话总结】

**MySQL 保证事务，本质上是靠“锁 + 日志 + MVCC + 崩溃恢复机制”这套组合拳**；而事务的核心目标就是满足 **ACID**，也就是**原子性、一致性、隔离性、持久性**。

🗣️ 【口述要点】

第一，先说 **ACID**。

- **原子性**：事务里的操作要么全成功，要么全失败，不会只执行一半；
- **一致性**：事务执行前后，数据库都要从一个合法状态变成另一个合法状态，不能破坏业务约束；
- **隔离性**：多个事务并发执行时，彼此之间不能互相干扰到不该看到的数据；
- **持久性**：事务一旦提交，结果就要永久保存，即使数据库宕机也不能丢。

第二，MySQL 保证事务原子性和持久性，主要依赖**undo log、redo log**。**undo log** 用来支持回滚，如果事务执行失败或者主动 rollback，可以把数据恢复到修改前；**redo log** 用来保证提交后的数据不丢，哪怕数据页还没来得及刷盘，只要日志已经落盘，宕机恢复后也能把已提交的数据重做出来。

第三，隔离性主要依赖**锁机制和 MVCC**。像更新、删除这类写操作，InnoDB 会通过**行锁、间隙锁、临键锁**等机制控制并发；而普通读在很多场景下会走 **MVCC**，避免读写互斥，提高并发性能。不同隔离级别，比如 **读未提交、读已提交、可重复读、串行化**，本质上就是在一致性和并发能力之间做平衡。

第四，**一致性**其实是 ACID 里最容易被说空的一点。面试里可以这样说：一致性不是单靠某一个机制保证的，而是**原子性、隔离性、持久性再加上业务约束一起共同保证**的。比如转账场景里，A 扣钱、B 加钱必须是一个整体，事务提交后总金额不能凭空变多或变少，这就是一致性。也就是说，一致性更偏**最终业务结果合法**，其他三个特性更像支撑它落地的技术手段。

### 第 19 题：MySQL 的 binlog、redo log、undo log 分别是什么、有什么作用、三者之间的区别和关系

🎯 【一句话总结】

**binlog、redo log、undo log 都是 MySQL 里非常关键的日志，但职责完全不同**；可以简单理解成：**undo log 管回滚，redo log 管崩溃恢复，binlog 管归档和主从同步**。

🗣️ 【口述要点】

第一，先说 **undo log**。它记录的是数据被修改前的旧值，主要作用有两个：一个是事务回滚时可以把数据恢复回去，保证**原子性**；另一个是给 **MVCC** 提供历史版本支持，让事务在一致性读时能看到符合隔离级别的旧版本数据。所以 undo log 更像“反向日志”。

第二，**redo log** 是 InnoDB 层的物理日志，记录的是“某个数据页做了什么修改”。它的核心作用是保证**持久性**。因为 InnoDB 修改数据时，先改内存里的 Buffer Pool，再把修改以顺序写的方式记到 redo log，之后再择机刷盘。这样即使数据库突然宕机，只要 redo log 还在，重启后就能把已经提交但还没来得及落盘的数据恢复出来，这就是 **WAL**，也就是先写日志再写数据页。

第三，**binlog** 是 MySQL Server 层的日志，记录的是逻辑层面的数据库变更，比如一条 `insert`、`update`、`delete`。它主要用于**主从复制、数据归档、增量恢复**。也就是说，redo log 更关注“存储引擎怎么恢复数据页”，而 binlog 更关注“数据库发生过哪些业务变更”。

第四，三者的区别和关系可以这样总结：

- **undo log**：回滚用，保存旧版本；
- **redo log**：崩溃恢复用，保证已提交事务不丢；
- **binlog**：复制和归档用，记录逻辑操作。
   实际事务提交时，InnoDB 还要协调 **redo log** 和 **binlog** 的一致性，所以才会有 **两阶段提交**，目的是避免主从同步和本地存储状态不一致。

### 第 20 题：MVCC 的原理、版本链、Read View、可重复读是怎么实现的

🎯 【一句话总结】

**MVCC 本质上就是“多版本并发控制”**，它通过保存数据的多个历史版本，让读操作尽量不加锁，从而实现**读写不冲突**；在 InnoDB 里，核心就是 **隐藏字段 + undo log + Read View** 这套机制。

🗣️ 【口述要点】

第一，MVCC 的核心思想是：数据被修改时，不是简单覆盖掉旧值，而是保留历史版本。这样一个事务在读数据时，不一定读到当前最新值，而是读到**对自己可见的那个版本**。所以它能在保证隔离性的同时，减少读写之间的锁竞争，提升并发性能。

第二，InnoDB 里每行记录都会带一些隐藏字段，比较重要的是**事务 ID** 和**回滚指针**。当一行数据被更新时，旧版本不会立刻消失，而是通过 **undo log** 串起来，形成一条**版本链**。新版本指向旧版本，旧版本再指向更老版本，所以事务在做一致性读时，就可以顺着这条链找到适合自己的历史版本。

第三，**Read View** 可以理解成事务做快照读时生成的一份“可见性规则”。它里面会记录当前哪些事务已经提交、哪些还没提交，以及当前事务自己的 ID。事务读一行数据时，会拿这行记录的事务 ID 去和 Read View 做比较，判断这个版本对自己是不是可见；如果不可见，就顺着 undo log 继续找更老的版本，直到找到一个符合条件的版本为止。

第四，**可重复读** 之所以能实现，关键就在于同一个事务里多次快照读通常使用的是**同一份 Read View**。这样即使中间别的事务提交了更新，这个事务再次查询时，看到的还是第一次查询时那个一致性视图下的数据，所以结果是一致的。简单说就是：**不是数据库没有变化，而是当前事务看到的快照没变**。

### 第 21 题：MySQL 主从一致性怎么解决、主从同步流程、写完 binlog 后从库怎么同步

🎯 【一句话总结】

**MySQL 主从同步本质上是基于 binlog 的异步复制机制**；主库负责写 **binlog**，从库负责拉取并重放 binlog，所以主从一致性的核心问题，实际上就是**复制延迟、复制中断和读写路由时机控制**。

🗣️ 【口述要点】

第一，先说主从同步的基本流程。主库执行事务提交后，会把数据变更写入 **binlog**；从库这边会有一个 **IO 线程** 去主库拉取 binlog，写入自己的 **relay log**；然后从库再由 **SQL 线程** 或者多线程回放 relay log，把这些变更真正执行到本地数据上。也就是说，主库写完 binlog 并不代表从库立刻就一致了，中间还要经历“拉取 + 落 relay log + 回放”这个过程。

第二，主从为什么会不一致，最常见的原因就是**复制延迟**。比如主库写压力大、binlog 产生太快、从库执行能力跟不上，或者从库还承担了大量查询压力，这都会导致从库追不上主库。另外，如果网络抖动、从库异常、复制线程报错，也可能导致同步中断，这时候就不只是延迟，而是真正的数据不一致。

第三，解决主从一致性问题，业务上一般有几种常见思路。

- 对一致性要求高的读，**直接走主库**；
- 写后读场景做**延迟读路由控制**，比如写完一段时间内强制读主；
- 根据复制延迟指标做**读从库降级**，延迟过大时不让流量打到从库；
- 对金融、订单这类强一致场景，尽量避免依赖“异步复制后的从库数据”做关键判断。
   所以很多时候不是“彻底消灭延迟”，而是**业务上感知延迟并规避风险**。

第四，如果想进一步提升一致性，可以考虑**半同步复制**。它的思路是主库事务提交时，至少要等一个从库收到 binlog 并确认，主库才认为这次复制更安全。这样能降低主从数据丢失风险，但也会增加写延迟。所以面试里最好强调一句：**主从复制的本质是用性能换一致性，最终要看业务怎么权衡**。

### 第 22 题：MySQL 和 Redis 的一致性怎么保证、双写一致性问题怎么处理、延迟双删是什么

🎯 【一句话总结】

**MySQL 和 Redis 的一致性，本质上是缓存和数据库的双写一致性问题**；严格意义上很难做到绝对强一致，工程上通常追求的是**最终一致性**，而最常见、相对可靠的方案是**更新数据库后删除缓存**。

🗣️ 【口述要点】

第一，双写场景下最容易出问题的是操作顺序。如果先删缓存再更新数据库，在并发下可能会出现别的线程把旧数据重新查出来并回填缓存，导致缓存脏数据；如果先更新数据库再更新缓存，也可能因为第二步失败，导致数据库是新值、缓存还是旧值。所以面试里一般直接说：**常规推荐方案是“先更新数据库，再删除缓存”**，而不是直接双写两个地方。

第二，这个方案为什么相对更稳，因为删除缓存比更新缓存更简单，也更不容易引入复杂并发问题。流程一般是：先提交 MySQL 事务，确保数据库是最新值；成功后再删除 Redis 缓存。这样即使删缓存失败，最多就是后续读到旧缓存一段时间，还可以通过重试、消息补偿来修复，不会像“同时维护两个新值”那么复杂。

第三，**延迟双删** 是这个问题里的一个常见补充方案。做法是：更新数据库后先删一次缓存，过一小段时间再删一次。它主要是为了应对这样一种并发时序：你刚更新完数据库，另一个并发请求正好在你第一次删缓存前或删缓存后短时间内，把旧值查出来又写回缓存了。第二次延迟删除，就是为了把这类脏缓存再清掉一次。

第四，真正线上要提高一致性，通常不会只靠“删两次”。更稳的做法一般是配合**消息队列、重试机制、订阅 binlog、异步补偿、缓存过期时间兜底**一起做。也就是说，缓存一致性不是靠一个技巧完全解决的，而是靠**更新策略 + 异步修复 + 失效兜底**这套组合方案来控制风险。

### 第 23 题：缓存击穿、缓存穿透、缓存雪崩分别是什么、怎么处理、恶意请求不存在数据怎么防御

🎯 【一句话总结】

**这三个问题本质上都是缓存保护失效，但触发方式不一样**；**穿透**是查不存在的数据，**击穿**是热点 key 失效瞬间被并发打爆，**雪崩**是大量 key 同时失效导致请求整体压到数据库。

🗣️ 【口述要点】

第一，**缓存穿透** 指的是请求的数据在缓存里没有、数据库里也没有，这样每次请求都会直接打到数据库。常见场景是恶意攻击，比如不断请求不存在的用户 ID、订单号。处理方式一般有三种：

- 对不存在的数据也做**空值缓存**，避免每次都查库；
- 在入口层加 **布隆过滤器**，先快速判断数据大概率是否存在；
- 做好**参数校验、限流、防刷**，从接入层直接拦恶意请求。

第二，**缓存击穿** 一般是某个**热点 key** 过期了，但这时候并发请求非常高，所有请求同时穿过缓存去打数据库，导致数据库瞬时压力暴涨。这个问题的关键不是“不存在”，而是“热点且刚好失效”。常见处理方式是：

- 给热点 key 做**永不过期**或者逻辑过期；
- 对重建缓存过程加**互斥锁**，只允许一个线程回源；
- 热点数据提前预热，避免在高峰期失效。

第三，**缓存雪崩** 是指大量缓存 key 在同一时间集中失效，或者整个缓存服务挂了，导致大量请求同时落到数据库。这个问题影响面比击穿更大，通常是系统级风险。常见处理方式包括：

- 给缓存过期时间加**随机值**，避免同一批 key 同时过期；
- 做好多级缓存、服务降级、限流熔断；
- Redis 集群高可用，避免缓存整体不可用；
- 对核心数据做预热和兜底。

第四，针对你说的“恶意请求不存在数据怎么防御”，面试里最标准的回答就是：**布隆过滤器 + 空对象缓存 + 限流校验**。布隆过滤器挡第一层，大量不存在请求直接拦掉；漏过去的请求如果查库发现不存在，可以把空结果也缓存一段时间；再往外层加 IP、用户维度的限流和黑名单，基本就能把风险控制住。

### 第 24 题：HashMap 原理、底层数据结构、put 过程、是否线程安全

🎯 【一句话总结】

**HashMap 本质上是基于哈希表实现的键值对容器**；JDK 1.8 之后它的底层结构是 **数组 + 链表 + 红黑树**，核心目标就是在哈希冲突时尽量兼顾查询效率和空间利用率。

🗣️ 【口述要点】

第一，先说底层结构。HashMap 里有一个**数组**，数组的每个位置可以叫一个桶。往里放元素时，会先根据 key 计算出一个 **hash**，再根据这个 hash 定位到数组下标。如果这个桶里没有元素，就直接放进去；如果已经有元素了，就说明发生了**哈希冲突**，这时候会在桶里通过**链表**或者**红黑树**继续存。

第二，JDK 1.8 之后的优化点是：当某个桶里的冲突节点太多时，链表会转成**红黑树**，这样最坏查询效率能从链表的 $O(n)$ 优化到 $O(\log n)$。不过并不是一冲突就树化，通常要同时满足链表长度达到阈值、数组容量也达到一定大小，才会真正转成红黑树，否则更倾向于先扩容。

第三，`put` 的过程可以概括成几步：

- 先对 key 做 hash 计算；
- 根据 hash 定位桶下标；
- 如果桶为空，直接插入；
- 如果桶里已有节点，就判断 key 是否相同，相同则覆盖旧值；
- 如果 key 不同，就在链表或红黑树里继续查找并插入；
- 插入后如果元素总数超过阈值，还要触发**扩容**。
   所以 HashMap 的核心其实就是“**先定位桶，再在桶内解决冲突**”。

第四，**HashMap 不是线程安全的**。在多线程并发 put 的情况下，可能会出现数据覆盖、丢失，早期 JDK 里扩容时甚至可能形成链表环，导致死循环。如果是并发场景，一般要用 **ConcurrentHashMap**，或者在外层自己做同步控制，不能直接拿 HashMap 当线程安全容器用。

### 第 25 题：HashMap 如何扩容、什么时候扩容、负载因子是什么、多线程扩容为什么可能形成环

🎯 【一句话总结】

**HashMap 扩容的本质，就是当桶太满时，通过扩大数组容量来降低哈希冲突**；它通常在**元素个数超过阈值**时触发，而阈值就是 **容量 × 负载因子**。

🗣️ 【口述要点】

第一，HashMap 什么时候扩容，核心看**阈值 threshold**。默认情况下，HashMap 的初始容量是 16，默认**负载因子**是 0.75，所以阈值就是 $16 \times 0.75 = 12$。当实际元素个数超过这个值时，就会触发扩容。负载因子可以理解成“装多满才扩容”的一个平衡参数，太小会浪费空间，太大又会让冲突变多，所以 0.75 是一个比较常用的折中值。

第二，扩容时通常会把数组容量扩成原来的 **2 倍**。扩容不是简单复制，而是要把原来桶里的元素重新分布到新数组里，这个过程叫**rehash**。不过在 JDK 1.8 里做了优化，因为容量翻倍后，元素的新位置要么还在原下标，要么是“原下标 + 旧容量”，所以很多时候不需要重新计算特别复杂的 hash。

第三，为什么要扩容，本质上是为了降低冲突。如果桶里元素太多，链表或树就会变长，查询和插入性能都会下降。通过扩容把数据分散到更多桶里，可以让每个桶更稀疏，整体查找效率更稳定。所以 HashMap 的性能核心其实不是“无限加元素”，而是要把**容量、负载因子、冲突率**维持在一个合理范围内。

第四，至于多线程扩容为什么可能形成环，主要是 **JDK 1.7** 里链表迁移使用了**头插法**。多个线程同时扩容迁移同一个桶时，如果链表节点指针修改顺序交错，就可能把链表指向搞乱，最终形成闭环，后续遍历就会死循环。JDK 1.8 在实现上已经做了改进，但结论还是一样：**HashMap 本身不适合并发写场景**，多线程下要优先用 **ConcurrentHashMap**。

### 第 26 题：Java 接口和抽象类的区别、各自适用场景

🎯 【一句话总结】

**接口和抽象类都用于抽象，但抽象层级不一样**；**接口更偏行为规范和能力约定，抽象类更偏公共实现和模板沉淀**，所以二者不是谁替代谁，而是适用场景不同。

🗣️ 【口述要点】

第一，先说最核心的区别。**接口**本质上是在定义“你具备什么能力”，强调的是一种**规范和约束**；而**抽象类**更像是在定义“你本来就是这一类东西”，强调的是一种**继承关系和通用实现**。面试里可以简单概括成：**接口是 can-do，抽象类是 is-a**。

第二，从能力上看，接口里的方法默认更偏抽象定义，虽然从 JDK 8 开始也支持了 **default 方法** 和 **static 方法**，但整体上还是更强调“规范”；抽象类则可以有**成员变量、构造方法、普通方法、抽象方法**，更适合把一部分公共逻辑直接沉淀下来，子类只改差异点。

第三，适用场景上，如果你是想做**解耦、扩展、面向规范编程**，比如支付能力、存储能力、消息发送能力这类横向能力定义，一般优先用**接口**；如果多个子类之间确实有明显的**公共属性和通用实现**，并且你想复用代码、约束子类模板流程，那更适合用**抽象类**。

第四，再补一个常见点，Java 类只能**单继承**抽象类，但可以**实现多个接口**。所以从架构设计角度看，接口更适合作为对外扩展点，灵活性更高；抽象类更适合作为框架内部的骨架实现。实际项目里经常是**接口定义能力，抽象类沉淀共性，具体类完成落地**。

### 第 27 题：String、StringBuilder、StringBuffer 的区别、循环拼接字符串为什么优先用 StringBuilder、StringBuffer 是否线程安全

🎯 【一句话总结】

**这三个类最大的区别在于“是否可变”和“是否线程安全”**；**String 不可变，StringBuilder 可变但不线程安全，StringBuffer 可变且线程安全**，所以日常开发里，单线程拼接一般优先用 **StringBuilder**。

🗣️ 【口述要点】

第一，**String** 是**不可变对象**，一旦创建，它的内容就不能再改。表面上看像是在“拼接”，其实每次 `+` 操作本质上都可能创建一个新的字符串对象。这样做的好处是安全、便于复用、适合作为常量池对象和 HashMap key，但缺点就是频繁拼接时会产生很多中间对象，性能和内存都不太友好。

第二，**StringBuilder** 和 **StringBuffer** 都是**可变字符串**，内部通常是一个可扩容的字符数组，拼接时是在原对象上追加内容，不会像 String 那样频繁创建新对象。所以在循环拼接、大量字符串组装这类场景下，性能会明显更好。

第三，为什么循环拼接优先用 **StringBuilder**，核心原因就是它**没有额外的同步开销**。如果你在一个循环里不断用 `String` 做 `+`，会反复创建临时对象，GC 压力也会上来；而 StringBuilder 是直接在可变数组上追加，效率高得多。所以面试里可以直接说：**大量字符串拼接用 StringBuilder，本质上是为了减少对象创建和拷贝成本**。

第四，**StringBuffer 是线程安全的**，因为它很多方法都加了 **synchronized**，所以多线程并发操作同一个 StringBuffer 时，相对安全；但也正因为有锁，性能通常比 StringBuilder 差一些。所以一般单线程场景优先用 StringBuilder，只有在确实需要多线程共享拼接对象时，才会考虑 StringBuffer。

### 第 28 题：Java 里面如何实现线程安全、有哪些常见手段、锁 / CAS / 原子类 / ThreadLocal 分别适合什么场景

🎯 【一句话总结】

**Java 里实现线程安全，核心思路无非就三类：共享变量加控制、减少共享、或者干脆不共享**；常见手段主要就是 **锁、CAS、原子类、并发容器、ThreadLocal** 这些，具体选型要看是追求**强互斥、轻量更新，还是线程隔离**。

🗣️ 【口述要点】

第一，最直接的方式是用**锁**，比如 **synchronized**、**ReentrantLock**。锁适合那种需要保护**一段临界区代码**的场景，尤其是涉及多个变量、一组复合操作、先判断再修改这类逻辑。它的优点是语义强、容易保证正确性，缺点是高并发下可能有**阻塞、上下文切换、锁竞争**开销。

第二，第二类是 **CAS** 和基于 CAS 的**原子类**，比如 **AtomicInteger、AtomicLong、AtomicReference**。它们适合那种**单个变量的无锁更新**场景，比如计数器、自旋状态位、简单引用替换。优点是轻量、并发性能好，缺点是适用范围有限，复杂逻辑不好处理，而且会有**ABA 问题、自旋开销**这些典型问题。

第三，第三类是**减少共享**，也就是从设计上避免多个线程同时改同一份数据。典型方式就是 **ThreadLocal**，它本质上是给每个线程一份独立副本，线程之间互不干扰，所以天然规避了线程安全问题。它适合保存**线程上下文、用户信息、数据库连接、TraceId** 这类和当前线程强绑定的数据，但要注意使用完及时清理，不然在线程池场景下容易出现**内存泄漏或脏数据串用**。

第四，除了这些，实际项目里还会结合**并发容器**、**不可变对象**、**消息队列串行化处理**、**分段锁**这些手段一起用。面试里可以总结成一句：**简单变量优先 CAS / 原子类，复杂临界区优先锁，线程私有数据优先 ThreadLocal，能不共享就尽量不共享**。

### 第 29 题：TCP 拥塞控制、慢启动、拥塞避免、快重传、快恢复分别是什么

🎯 【一句话总结】

**TCP 拥塞控制的核心目标，是避免发送方一下子把网络打爆**；它本质上是在动态探测当前网络承载能力，然后通过 **慢启动、拥塞避免、快重传、快恢复** 这几个机制来调节发送速率。

🗣️ 【口述要点】

第一，先说整体思路。TCP 不会一上来就无限发数据，而是维护一个**拥塞窗口 cwnd**，发送方实际能发多少，不光要看接收方窗口，还要看当前拥塞窗口大小。也就是说，TCP 不只是关心“对方收不收得下”，还关心“网络扛不扛得住”，这就是拥塞控制。

第二，**慢启动** 发生在连接刚建立或者超时重传之后。它的思路是先小心试探，刚开始只发少量数据，然后每收到确认，拥塞窗口快速增大，近似指数增长。之所以叫慢启动，不是它增长慢，而是它相对于“一上来就发很多”来说更保守。这个阶段主要是快速摸清网络大概能承受多大流量。

第三，当拥塞窗口增长到一个阈值之后，就进入**拥塞避免**阶段，这时候窗口增长速度会放缓，从指数增长变成**线性增长**。这样做是因为到了一定规模后，继续翻倍涨就太激进了，更容易把网络打满，所以要稳一点，边发边试探。

第四，**快重传** 和 **快恢复** 是处理丢包时的优化机制。如果发送方连续收到多个重复 ACK，就说明某个报文大概率丢了，不用等超时，直接重传，这就是**快重传**。而 **快恢复** 的思路是：既然只是少量报文丢了，说明网络不一定已经严重拥塞，所以没必要像超时那样把窗口一下子降得特别低，而是适度收缩后继续进入拥塞避免阶段。简单说，**超时重传代表网络可能很堵，处理更保守；重复 ACK 更像局部丢包，处理相对温和**。

### 第 30 题：上下文切换是什么、线程上下文切换和进程上下文切换差别在哪、为什么线程切换成本更低

🎯 【一句话总结】

**上下文切换，本质上就是 CPU 从当前执行单元切到另一个执行单元之前，先保存现场，再恢复新现场**；线程切换通常比进程切换更轻，是因为**线程共享进程资源，不需要像进程那样频繁切换完整的地址空间和系统资源**。

🗣️ 【口述要点】

第一，先说什么是**上下文**。它可以理解成一个执行单元当前运行所依赖的现场信息，比如**寄存器内容、程序计数器、栈信息、调度状态**等。CPU 在某个线程或进程上运行时，如果因为时间片用完、发生阻塞、被高优先级任务抢占，就需要把当前现场保存起来，再切到另一个任务继续执行，这个过程就是**上下文切换**。

第二，**线程上下文切换**和**进程上下文切换**最大的区别，在于资源隔离级别不同。线程属于同一个进程，很多资源是共享的，比如**地址空间、堆、全局变量、文件描述符**；所以线程切换时，重点是切换**线程私有的寄存器、栈、程序计数器**这些内容。而进程之间资源隔离更强，各自有独立的**虚拟地址空间、页表、内核资源视图**，切换时通常要做更多底层工作。

第三，为什么线程切换成本更低，核心就是它少了一大块“资源环境切换”的成本。进程切换除了保存执行现场，还常常涉及**地址空间切换、页表切换、TLB 失效、缓存命中率下降**这些问题；而线程因为共享进程的大部分资源，一般不需要做这么重的切换，所以开销更小，恢复也更快。

第四，面试里最好补一句：虽然线程切换比进程轻，但**线程切多了照样很贵**。因为无论线程还是进程，只要切换频繁，都会带来调度开销、缓存失效、CPU 时间浪费。所以线上高并发系统不是线程越多越好，而是要控制线程数量，尽量通过**线程池、异步化、IO 多路复用、协程**来降低不必要的切换。

### 第 31 题：虚拟内存是什么、虚拟地址和物理地址的区别、为什么需要虚拟内存

🎯 【一句话总结】

**虚拟内存本质上是一种“地址抽象”机制**；程序看到的是连续的**虚拟地址空间**，真正落到哪块内存、甚至是否暂时放在磁盘上，由操作系统和硬件一起完成映射和管理。

🗣️ 【口述要点】

第一，先说概念。**虚拟地址**是程序运行时使用的地址，看起来是连续、独立的；**物理地址**才是真正的内存条上的地址。CPU 执行程序时访问的是虚拟地址，经过 **MMU** 和页表转换后，才定位到实际物理内存。所以程序并不是直接操作物理内存，而是先通过虚拟内存这层抽象。

第二，为什么需要虚拟内存，最核心有三个原因。

- 第一是**隔离性**：每个进程都有自己的独立地址空间，彼此不能随便访问对方内存，提高了系统安全性和稳定性；
- 第二是**简化编程**：程序看到的是连续地址，不用关心物理内存碎片和实际分布；
- 第三是**扩展性**：操作系统可以把暂时不用的页换到磁盘，让进程觉得自己拥有比物理内存更大的空间。

第三，虚拟内存一般按**页**来管理，常见是 4KB 一页。进程访问某个虚拟地址时，系统会先查页表，看这个虚拟页映射到哪个物理页；如果该页当前不在内存里，就会触发**缺页异常**，由操作系统把数据从磁盘换入内存，再恢复执行。所以虚拟内存的本质不只是“地址映射”，还包括**分页、换页、页面置换**这些机制。

第四，面试里可以强调一句：虚拟内存带来了灵活性和隔离性，但也不是没有代价。比如页表本身要占内存，地址翻译会有额外开销，缺页时还会触发磁盘 IO，性能可能抖动。所以很多高性能系统优化时，都会尽量减少**缺页、中断、频繁换页**这类问题。

### 第 32 题：程序地址空间是什么样的、堆和栈在内存中的分布、代码段 / 数据段 / 堆 / 栈分别是什么

🎯 【一句话总结】

**程序地址空间可以理解成进程看到的一整套虚拟内存布局**；从低地址到高地址，通常会包含 **代码段、数据段、堆、映射区、栈** 等区域，不同区域职责不同，其中**堆向高地址增长，栈通常向低地址增长**。

🗣️ 【口述要点】

第一，先说整体结构。一个进程启动后，操作系统会给它分配独立的**虚拟地址空间**。这块空间里不是乱放的，通常会按功能划分成几个区域：放程序指令的、放全局数据的、动态申请内存的、线程栈的、共享库映射的。也就是说，程序看到的是一整块连续空间，但内部实际上是按用途分区管理的。

第二，**代码段** 主要存放程序的机器指令，也就是可执行代码，一般是只读的，防止被程序随意修改；**数据段** 主要存放全局变量和静态变量，其中初始化和未初始化的数据通常还会再细分。这个区域的特点是生命周期跟进程基本一致，不像局部变量那样随函数调用进出而变化。

第三，**堆** 是程序运行时动态申请内存的区域，比如 `malloc`、`new` 申请出来的对象通常都在堆上。堆的特点是灵活、生命周期由程序控制，但管理成本也更高，容易出现**内存碎片、泄漏**这些问题。通常说的堆在地址空间里是**从低地址向高地址扩展**。

第四，**栈** 主要是给函数调用服务的，里面放的是**局部变量、返回地址、参数、保存的寄存器现场**等内容。每次函数调用都会压栈，函数返回时再弹栈，所以它分配释放非常快。栈一般是**从高地址向低地址增长**，和堆相对而行。面试里如果被追问，可以再补一句：当堆和栈不断扩展，如果最终相遇，就说明进程地址空间快耗尽了。

### 第 33 题：TCP 可靠性怎么实现、为什么 TCP 能保证有序、确认应答 / 重传 / 滑动窗口 / 流量控制 / 拥塞控制分别起什么作用

🎯 【一句话总结】

**TCP 之所以可靠，本质上是因为它不是“发出去就不管了”，而是围绕“编号、确认、重传、窗口、控制速率”建立了一整套机制**；有了这些机制，TCP 才能做到**可靠、有序、尽量高效**地传输数据。

🗣️ 【口述要点】

第一，TCP 能保证**有序**，核心靠的是 **序列号 SEQ**。发送方给每一段数据编号，接收方收到后会按序号重组数据，即使中间有的包先到、有的后到，最终交给应用层时也会按正确顺序输出。如果某一段丢了，后面的数据即使先到了，接收方也不会直接交上去，而是等缺失的那一段补齐，所以 TCP 才能保证字节流有序。

第二，**确认应答 ACK** 的作用是告诉发送方“哪些数据我已经收到了”。发送方发完数据后，不是默认一定成功，而是要等接收方确认；如果在规定时间内没收到 ACK，就会怀疑数据丢了，触发**重传机制**。所以 ACK 和重传是一组配套机制，一个负责确认到达，一个负责补救丢失。

第三，**滑动窗口** 是为了提高传输效率。没有窗口的话，发送一段就得等一次确认，效率会很低；有了滑动窗口后，发送方可以在窗口范围内连续发送多段数据，不用每发一段都停下来等。接收方确认之后，窗口再往前滑动，这样就把“可靠性”和“吞吐量”结合起来了。

第四，**流量控制** 和 **拥塞控制** 都是在控制发送速度，但关注点不一样。**流量控制** 是端到端的，重点看接收方来不来得及处理，防止把对方缓冲区打满；**拥塞控制** 是面向整个网络的，防止发送方把网络链路压垮。所以前者解决“对方收不收得下”，后者解决“网络扛不扛得住”。TCP 正是把这些机制放在一起，才能做到既可靠，又尽量高效。

### 第 34 题：HTTP/2 和 HTTP/3 的区别、HTTP/3 为什么基于 UDP、QUIC 带来了什么变化

🎯 【一句话总结】

**HTTP/2 和 HTTP/3 的核心区别，不在 HTTP 语义层，而在底层传输层**；**HTTP/2 基于 TCP，HTTP/3 基于 UDP + QUIC**，这样做的主要目的就是解决 TCP 场景下的**队头阻塞、连接建立慢、迁移能力差**这些问题。

🗣️ 【口述要点】

第一，先说 **HTTP/2**。它相对 HTTP/1.1 的主要提升是**二进制分帧、多路复用、头部压缩、服务端推送**。多路复用让多个请求可以复用同一个 TCP 连接，减少了连接数量和应用层的阻塞问题，所以性能比 HTTP/1.1 好很多。但它底层毕竟还是 TCP，只要 TCP 某个包丢了，后续数据在该连接上还是会受影响，这就是典型的**传输层队头阻塞**。

第二，**HTTP/3** 最大的变化，是把底层从 TCP 换成了 **QUIC**，而 QUIC 又是构建在 **UDP** 之上的。为什么基于 UDP，不是因为 UDP 天生更可靠，而是因为 UDP 更简单，协议控制权可以更多放到用户态和应用层，让 QUIC 自己去实现**可靠传输、拥塞控制、重传、加密握手**这些能力，这样演进速度更快，也更灵活。

第三，QUIC 带来的一个关键变化，是它把“连接管理”和“流控制”做得更细粒度。它支持**连接级别的多路复用**，但不同流之间丢包不会像 TCP 那样相互阻塞，所以能明显缓解队头阻塞问题。另外 QUIC 默认集成了 **TLS 1.3**，连接建立更快，很多场景下可以做到更少的握手轮次。

第四，QUIC 还有一个很实用的优势，就是**连接迁移能力**更好。比如移动端网络从 Wi-Fi 切到 4G，传统 TCP 往往要重建连接，而 QUIC 可以基于连接 ID 继续维持会话，不一定要完全重来。所以面试里可以总结成一句：**HTTP/3 不是简单换了个协议名，而是把底层传输能力整体升级了一遍**。

### 第 35 题：一条 update 语句的执行流程、从 SQL 解析到存储引擎执行再到日志落盘的全过程

🎯 【一句话总结】

**一条 update 语句的执行过程，本质上就是“先定位数据、再修改内存页、同时记录日志，最后在合适时机落盘”**；真正保证事务安全的关键，不是立刻把数据页刷盘，而是**先把相关日志写好**。

🗣️ 【口述要点】

第一，SQL 进入 MySQL 后，先经过 **连接器、解析器、优化器、执行器** 这一套流程。连接器负责鉴权和会话管理；解析器把 SQL 做词法和语法分析，确认这是不是一条合法的 `update`；优化器决定怎么执行，比如走哪个索引；最后执行器调用存储引擎接口，真正去修改数据。

第二，到了 InnoDB 这一层，执行器会先根据条件找到目标记录。如果有索引，就尽量通过索引定位；找到记录后，如果数据页不在内存里，会先从磁盘读到 **Buffer Pool**。然后 InnoDB 在内存页上修改这条记录，同时生成对应的 **undo log**，这样如果事务回滚，还能恢复旧值。

第三，修改内存页的同时，InnoDB 还会记录 **redo log**，表示这个数据页做了什么变更。这个阶段一般遵循 **WAL** 思想，也就是**先写日志，再择机刷数据页**，这样随机写磁盘页就转成了顺序写日志，性能更高。而 MySQL Server 层还会把这次更新写到 **binlog**，用于主从复制和归档恢复。

第四，真正事务提交时，会涉及 **redo log 和 binlog 的两阶段提交**。可以简单理解成：先把 redo log 写到 prepare 状态，再写 binlog，最后再把 redo log 标记成 commit。这样是为了保证两份日志逻辑一致，避免出现“本地恢复成功但主从不同步”或者“binlog 有了但本地事务没真正提交”的问题。至于数据页本身，不一定在事务提交时立刻刷盘，后面会由后台线程按策略慢慢刷下去。

### 第 36 题：联合索引的底层结构、最左前缀原则、联合索引 $(a,b,c)$ 为什么查 b=1 and c=1 不一定走索引

🎯 【一句话总结】

**联合索引本质上还是一棵 B+ 树，只不过它的排序规则变成了“先按 a，再按 b，再按 c”**；所以能不能高效使用联合索引，关键就在于是否满足**最左前缀原则**。

🗣️ 【口述要点】

第一，先说底层结构。联合索引和普通索引在结构上没有本质区别，底层一样是 **B+ 树**，只是树上节点存的不再是单个列值，而是多个列组成的**联合键**。比如索引是 $(a,b,c)$，那它在逻辑上就是先按 `a` 排序，`a` 相同再按 `b` 排序，`b` 也相同再按 `c` 排序，所以这个顺序非常关键。

第二，**最左前缀原则** 的意思就是：使用联合索引时，查询条件必须尽量从最左边的列开始，才能充分利用这棵树的有序性。比如 $(a,b,c)$ 这个索引，查 `a=1`、`a=1 and b=2`、`a=1 and b=2 and c=3`，通常都比较容易利用索引；但如果把最左边的 `a` 跳过去，直接从 `b` 或 `c` 开始，索引的整体有序结构就不太好用了。

第三，为什么查 `b=1 and c=1` 不一定走索引，核心原因就是这棵树不是按 `b,c` 排的，而是按 `a,b,c` 排的。因为没有先限定 `a`，数据库很难直接在树上快速定位一段连续区间，只能扫描很多不同 `a` 值下的 `b,c` 组合，这时候优化器可能判断**走索引收益不高**，还不如全表扫描更划算。所以不是说它“绝对不能走”，而是说**通常没法高效利用这个联合索引**。

第四，再补一个面试里常见的细节：最左前缀不是只认“写 SQL 的顺序”，而是看优化器最终能不能形成从左到右的有效匹配条件。也就是说，`where b=2 and a=1` 和 `where a=1 and b=2` 本质上通常是一样的，关键不是你写谁在前面，而是条件里有没有把最左列约束住。

### 第 37 题：MyBatis 如何防止 SQL 注入、#{} 和 ${} 的区别、预编译为什么能防注入

🎯 【一句话总结】

**MyBatis 防 SQL 注入的核心做法，就是优先使用 `#{}`，底层通过预编译和参数绑定把“SQL 结构”和“用户输入”隔离开**；而 `${}` 是字符串直接拼接，如果用户输入不可信，就有注入风险。

🗣️ 【口述要点】

第一，先说 **`#{}` 和 `${}` 的区别**。`#{}` 会把参数当成**占位符参数**来处理，最终生成的是带 `?` 的预编译 SQL，然后再由 JDBC 去做参数绑定；而 `${}` 是直接做**字符串替换**，相当于把用户传进来的内容原样拼到 SQL 里。所以从安全性上讲，**`#{}` 更安全，`${}` 风险更高**。

第二，为什么 **预编译** 能防 SQL 注入，核心就是它把 SQL 分成了两部分：一部分是**固定的 SQL 语法结构**，另一部分是**参数值**。数据库会先把 SQL 模板编译好，再把参数按值填进去，这样用户输入只能被当成普通数据，不能再改变原有 SQL 结构。比如用户传了 `' or 1=1 --`，在 `#{}` 场景下它只是一个字符串值，不会变成真正的逻辑条件。

第三，MyBatis 实际开发里，绝大部分场景都应该优先用 **`#{}`**，像 `where id = #{id}`、`where name = #{name}` 这种都没有问题。只有一些特殊场景，比如**动态表名、动态字段名、order by 字段名**，`#{}` 没法直接替代时，才可能需要用 `${}`，但这时候一定要自己做**白名单校验**，不能让用户随便传。

第四，面试里可以总结成一句：**防 SQL 注入最核心的不是框架本身，而是“不要把不可信输入直接拼进 SQL 结构里”**。MyBatis 之所以多数时候安全，是因为它默认推荐的 `#{}` 走的是预编译参数绑定；一旦开发者为了图方便到处用 `${}`，那框架也救不了。

### 第 38 题：死锁是什么、死锁的必要条件、如何避免死锁、如何排查死锁

🎯 【一句话总结】

**死锁本质上就是多个线程或事务互相等待对方释放资源，结果谁也走不下去**；它不是“卡一下”，而是如果不被外部打破，相关任务就会一直阻塞住。

🗣️ 【口述要点】

第一，死锁成立一般有四个必要条件，也叫经典四条件：

- **互斥**：资源同一时刻只能被一个执行单元占有；
- **请求并持有**：一个线程已经拿到部分资源，同时还在申请别的资源；
- **不可剥夺**：资源不能被别人强行抢走，只能自己释放；
- **循环等待**：多个线程形成环形依赖，A 等 B，B 等 C，C 又等 A。
   这四个条件同时满足，就可能形成死锁。

第二，如何避免死锁，核心思路就是主动破坏这四个条件中的一个或多个。工程里最常见的是：

- **固定加锁顺序**，比如所有线程都先锁 A 再锁 B，避免循环等待；
- 尽量**缩小锁粒度和持锁时间**，拿到锁就尽快做完释放；
- 避免一个线程持有锁时再去申请更多未知资源；
- 对获取锁设置**超时、tryLock**，拿不到就主动放弃重试。
   数据库层面也类似，尽量让事务短、索引命中准，减少锁范围和锁时间。

第三，怎么排查死锁，要分语言运行时和数据库两类看。Java 里如果怀疑线程死锁，可以用 **jstack** 看线程栈，通常能直接看到线程互相等待哪把锁；如果是 MySQL 死锁，可以看 **`show engine innodb status`**，里面会有最近一次死锁信息，包括哪两个事务、持有什么锁、在等什么锁。这个信息一般很关键。

第四，真正线上处理死锁，重点不是“出了死锁就重启”，而是要定位**资源竞争路径**。比如是不是事务里 update 顺序不一致、是不是缺少索引导致锁范围扩大、是不是锁和 RPC 混用导致持锁时间过长。也就是说，死锁表面看是锁问题，底层很多时候其实是**资源访问顺序和业务流程设计问题**。

### 第 39 题：Linux 文件系统是什么样的、VFS / inode / dentry 分别是什么、文件查找过程是什么

🎯 【一句话总结】

**Linux 文件系统本质上是一个分层的树状结构，核心目标是把“文件名”和“文件数据”解耦管理**；在内核里，常见的关键概念就是 **VFS、inode、dentry**，它们分别解决统一接口、文件元数据、路径查找这几个问题。

🗣️ 【口述要点】

第一，先说整体结构。Linux 把一切资源都尽量抽象成文件，整个文件系统从根目录 `/` 开始，往下是多级目录和文件。不同磁盘格式，比如 ext4、xfs、tmpfs，对上层应用来说尽量保持统一访问方式，这就是 **VFS** 的意义。**VFS** 可以理解成一个虚拟文件系统层，它对上提供统一的文件操作接口，对下屏蔽具体文件系统实现差异。

第二，**inode** 是文件的元数据结构，里面通常记录文件的**权限、大小、时间戳、数据块位置**等信息，但它**不直接保存文件名**。也就是说，真正标识一个文件内容和属性的是 inode，而不是文件名。文件名更多是路径解析阶段用来做映射的。

第三，**dentry** 可以理解成目录项，它的作用是把“文件名”和“inode”关联起来，用于路径查找和目录结构组织。简单说，inode 解决“这是什么文件、数据在哪”，dentry 解决“这个名字在这个目录下对应谁”。而且 dentry 会做缓存，所以频繁访问同一路径时，后续查找会更快。

第四，文件查找过程大致是这样的：当应用访问一个路径，比如 `/home/app/a.txt`，内核会从根目录开始一层层解析路径，先找 `home` 的 dentry，再找 `app`，再找 `a.txt`，每一层都可能通过目录项定位到对应 inode。如果命中了 dentry / inode 缓存，就能减少磁盘访问；如果没命中，才需要从磁盘把目录项和 inode 信息读上来。所以文件查找本质上就是一个**路径解析 + 元数据定位 + 数据块访问**的过程。

### 第 40 题：零拷贝是什么、Java 或 Linux 中怎么实现零拷贝、涉及哪些系统调用

🎯 【一句话总结】

**零拷贝的核心目标，不是“完全没有拷贝”，而是尽量减少用户态和内核态之间的数据拷贝，以及减少 CPU 参与搬运数据的次数**；它本质上是一种提升 IO 性能、降低 CPU 开销的优化手段。

🗣️ 【口述要点】

第一，为什么需要零拷贝，可以先看传统 IO 流程。比如读取文件再通过 socket 发送，通常要经历：磁盘数据读到内核缓冲区，再拷贝到用户缓冲区，应用再调用发送接口，把数据从用户缓冲区再拷贝回 socket 的内核缓冲区，最后网卡发出去。这里既有多次**态切换**，也有多次**数据拷贝**，CPU 会花很多时间在“搬数据”上。

第二，所谓**零拷贝**，就是尽量让数据少经过用户态，最好直接在内核态完成传输链路。Linux 里比较典型的方式有 **`sendfile`**、**`mmap`**、以及更底层依赖 **DMA / scatter-gather** 的方案。比如 `sendfile` 可以让文件数据直接从内核文件缓冲区传到 socket 缓冲区，中间不需要应用程序自己把数据读出来再写回去，这样就少了用户态参与。

第三，常见涉及的系统调用，主要有 **`read` / `write`** 这种传统方式，以及 **`mmap`**、**`sendfile`**、有些场景下还有 **`splice`**。其中 `mmap` 是把文件映射到进程虚拟地址空间，减少一次从内核到用户空间的拷贝；`sendfile` 更适合文件到网络这种场景，可以进一步减少系统调用次数和数据搬运成本。

第四，在 Java 里，零拷贝常见体现在 **NIO** 相关能力上，比如 `FileChannel.transferTo()`、`transferFrom()`，底层很多实现会利用操作系统的 `sendfile`。像 **Netty、Kafka、Nginx** 这类高性能组件，都会大量利用这类机制。面试里可以总结成一句：**零拷贝本质上不是“数据完全不动”，而是尽量让数据别在用户态和内核态之间来回折腾**。

### 第 41 题：IO 模型有哪些、阻塞 / 非阻塞 / 同步 / 异步的区别、什么是 IO 多路复用

🎯 【一句话总结】

**IO 模型的核心区别，不在于“有没有数据”，而在于“线程怎么等数据、数据准备好之后谁来搬数据”**；常见模型可以分成 **阻塞 IO、非阻塞 IO、IO 多路复用、信号驱动 IO、异步 IO**，其中面试最常考的是前四者和同步异步的区别。

🗣️ 【口述要点】

第一，先说最基础的几个模型。**阻塞 IO** 是指线程发起 IO 后，如果数据没准备好，就一直卡在那里不能干别的；**非阻塞 IO** 则是发起后如果没准备好，会立刻返回，线程可以继续做别的事情，但通常需要自己不断轮询。阻塞和非阻塞，描述的是**发起调用后，线程会不会被挂住**。

第二，**同步和异步** 的区别，核心看的是“数据拷贝阶段谁负责完成”。**同步 IO** 下，哪怕数据已经准备好了，最终也还是要调用线程自己去把数据从内核拷到用户空间，线程得亲自参与收尾；而**异步 IO** 是内核把数据准备好并拷贝完成之后，再主动通知应用，应用线程几乎不用自己等和搬。所以阻塞/非阻塞关注“等的时候卡不卡”，同步/异步关注“最后谁来完成这个 IO”。

第三，**IO 多路复用** 可以理解成一种“更高效地等多个 IO 就绪”的同步模型。它不是让 IO 变成异步，而是通过 **select、poll、epoll** 这种机制，让一个线程同时监听多个文件描述符，哪个就绪了再去处理。这样就避免了一个连接一个线程的浪费，特别适合高并发网络服务。

第四，面试里可以把这几个概念连起来说：

- 阻塞 IO：线程等一个连接，简单但扩展性差；
- 非阻塞 IO：线程不被挂住，但会频繁轮询；
- IO 多路复用：一个线程高效监听多个连接，本质上还是同步；
- 异步 IO：内核把准备和拷贝都做完，再通知应用。
   所以很多高性能框架，比如 **Nginx、Redis、Netty**，核心并不是“异步 IO”，而是大量用到了**非阻塞 socket + IO 多路复用**。

### 第 42 题：WebSocket 是怎么实现持久化双向通信的、为什么可以双向通信、心跳和保活怎么做

🎯 【一句话总结】

**WebSocket 本质上是在一次 HTTP 握手成功后，把连接升级成一个长期保持的全双工 TCP 连接**；一旦升级完成，客户端和服务端都可以主动发消息，所以它天然适合**实时双向通信**场景。

🗣️ 【口述要点】

第一，WebSocket 建立连接时，最开始还是走 **HTTP 请求**。客户端会发一个带 `Upgrade: websocket` 的请求，服务端如果支持，就返回 `101 Switching Protocols`，表示同意协议升级。升级完成之后，这条连接就不再是普通的一问一答式 HTTP 了，而是变成一条持续存在的 TCP 通道。

第二，为什么它可以**双向通信**，核心就是连接建立后，通信双方地位是对等的，不像传统 HTTP 那样必须“客户端发请求，服务端才能响应”。WebSocket 下，客户端可以主动发消息，服务端也可以在任意时刻主动推消息给客户端，所以很适合做**聊天、IM、推送、在线协作、实时行情**这类业务。

第三，WebSocket 为什么叫“持久化连接”，是因为它不是每次发消息都重新建连，而是一条连接长期复用。这样做减少了频繁握手和断开的成本，也降低了延迟。不过也正因为连接会长期存在，所以服务端要维护大量连接状态，对**连接数、内存、心跳、网络抖动恢复**这些能力要求更高。

第四，**心跳和保活** 很关键。因为很多时候连接表面没断，但中间链路、代理、NAT 映射可能已经失效了，所以客户端和服务端一般都会定期发送**ping/pong** 或业务层心跳包。它的作用主要有两个：一个是及时发现死连接并清理，另一个是维持中间网络设备不要过早回收这条长连接。实际项目里通常还会配合**超时检测、断线重连、连接状态上报**一起做。

### 第 43 题：RPC 和 HTTP 的区别、为什么很多内部服务用 RPC、不直接用 HTTP 的原因是什么

🎯 【一句话总结】

**RPC 和 HTTP 不是完全对立的关系，但在内部服务调用场景下，RPC 通常更强调“像调本地方法一样调远程服务”，而 HTTP 更偏通用的应用层协议**；很多内部系统选 RPC，核心是为了**性能、治理能力、接口约束和开发效率**。

🗣️ 【口述要点】

第一，先说区别。**HTTP** 是一种通用的应用层协议，关注的是报文格式、请求方法、状态码这些内容，天然适合浏览器和服务端、开放接口这类场景；而 **RPC** 更像一种远程调用框架，它关心的不只是“怎么传数据”，还包括**服务发现、负载均衡、超时重试、序列化、熔断限流、链路追踪**这些服务治理能力。简单说，HTTP 更像“协议”，RPC 更像“调用体系”。

第二，为什么很多内部服务喜欢用 RPC，因为内部调用通常更关注**高性能、低延迟、强治理**。RPC 一般会使用更高效的**二进制序列化**，通信协议也更轻量，传输和解析开销通常比纯文本 HTTP 更小；另外它往往天然集成注册中心、负载均衡、超时控制、重试机制，对微服务内部互调更友好。

第三，不直接用 HTTP 的原因，不是 HTTP 不能用，而是它在内部复杂服务治理场景下，很多能力要自己补。比如你要做服务发现、接口版本控制、失败重试、熔断、灰度发布、链路透传，HTTP 本身不会帮你解决，而 RPC 框架通常已经把这些能力内建好了。也就是说，内部服务选 RPC，很多时候看重的是**完整的工程化能力**，不只是性能。

第四，当然 HTTP 也有自己的优势，比如**标准统一、跨语言友好、调试方便、生态成熟、对外接口天然兼容**。所以实际项目里常见的做法是：**外部开放接口多用 HTTP / REST，内部服务之间更倾向 RPC**。面试里这样回答，一般比较符合大厂真实架构。

### 第 44 题：微服务里的熔断、降级、限流分别是什么、常见限流算法有哪些

🎯 【一句话总结】

**熔断、降级、限流本质上都是微服务的自我保护机制**；它们解决的问题不一样：**限流**防流量打爆系统，**熔断**防故障扩散，**降级**是在资源不足或依赖异常时优先保核心功能。

🗣️ 【口述要点】

第一，先说**限流**。限流就是在系统入口或者某个关键资源前，控制单位时间内允许通过的请求量，避免瞬时流量把服务、数据库、缓存打垮。它解决的是“请求太多”的问题，常见应用位置包括网关层、接口层、服务层、下游依赖层。面试里可以直接说：**限流是事前控制，把超出系统承载能力的流量提前拦住**。

第二，**熔断** 更像电路里的保险丝。当某个下游服务持续超时、报错率很高，或者已经明显不可用了，如果还继续疯狂调用，只会把当前服务线程、连接、资源全部拖死。这时候就要触发熔断，暂时不再请求下游，直接快速失败或者走兜底逻辑。它解决的是“依赖异常导致故障扩散”的问题。

第三，**降级** 是在系统压力大、依赖不稳定或者资源紧张时，主动牺牲一部分非核心能力，优先保住主链路。比如商品详情页先不展示推荐、评论、实时库存，或者返回默认值、缓存值、简化结果。它解决的是“资源不够时怎么保核心服务”的问题，所以本质上是一种业务层面的取舍。

第四，常见**限流算法** 主要有这几种：

- **固定窗口**：实现简单，但窗口切换时可能有突刺；
- **滑动窗口**：统计更平滑，精度更高；
- **漏桶算法**：按固定速率出水，适合平滑流量；
- **令牌桶算法**：按固定速率放令牌，请求拿到令牌才通过，既能限速也允许一定突发流量。
   实际工程里，网关和服务端最常用的通常还是**令牌桶**和**滑动窗口**。

### 第 45 题：Kafka 如何保持高吞吐和高可用、分区和副本的作用、消费失败怎么处理、消息堆积怎么处理

🎯 【一句话总结】

**Kafka 能做到高吞吐和高可用，核心靠的是“顺序磁盘写 + 分区并行 + 副本容灾 + 批量传输”**；而分区解决的是**扩展性和并发能力**，副本解决的是**容灾和可用性**。

🗣️ 【口述要点】

第一，先说为什么 Kafka 吞吐高。它不是随机写磁盘，而是大量依赖**顺序追加写**，同时配合 **Page Cache、批量发送、批量拉取、零拷贝**，把磁盘和网络效率都压得比较高。再加上 topic 可以拆成多个 **partition**，生产和消费都能并行处理，所以它天然适合高吞吐场景。

第二，**分区** 的作用主要有两个：一个是把一个 topic 的数据拆散，提升**并行读写能力**；另一个是为扩容提供基础，因为不同分区可以分布到不同 broker 上。需要注意的是，Kafka 只能保证**单分区内有序**，不能天然保证整个 topic 全局有序，所以业务如果特别依赖顺序，通常要把同一类 key 路由到同一个分区。

第三，**副本** 的作用是保证高可用。Kafka 一个分区会有一个 **leader** 和多个 **follower**，生产和消费通常都只跟 leader 交互，follower 负责同步数据。这样即使某个 broker 宕机，只要 ISR 里还有可用副本，就可以重新选 leader，服务还能继续，所以副本是 Kafka 容灾能力的关键。

第四，消费失败和消息堆积也都是高频问题。**消费失败** 一般要看业务是否允许重试，如果是临时异常，可以重试；如果是脏数据或业务不可恢复错误，通常要放到**死信队列**或异常队列，避免一直卡住。**消息堆积** 说明生产速度明显大于消费速度，这时候一般从几个方向排查：消费者处理逻辑是不是太慢、分区数够不够、消费组实例数够不够、下游依赖是不是拖慢了消费，必要时通过**扩分区、扩消费者、批量消费、异步处理、削峰限流**来解决。

### 第 46 题：分布式锁如何实现、Redis 分布式锁怎么做、SET NX EX 是什么、锁续期和误删问题怎么解决

🎯 【一句话总结】

**分布式锁的核心目标，是让多个节点在分布式环境下对同一资源实现互斥访问**；而基于 Redis 的常见做法，就是用 **`SET key value NX EX`** 原子地完成“加锁 + 过期时间”这两个动作。

🗣️ 【口述要点】

第一，先说 Redis 分布式锁怎么做。最基本的方式是执行：`SET lockKey requestId NX EX 30`。这里 **NX** 表示“只有 key 不存在时才设置成功”，也就是保证只有一个客户端能抢到锁；**EX 30** 表示给锁设置 30 秒过期时间，避免持锁服务宕机后锁永远不释放。所以这条命令的核心价值，是把**加锁和设置过期时间做成原子操作**。

第二，为什么 value 里通常要放一个唯一的 **requestId**，是因为解锁时不能直接 `DEL`。如果一个线程拿到锁后执行时间太长，锁过期了，被别的线程重新拿到，这时候前一个线程再来解锁，如果只是简单 `DEL`，就会把别人的锁删掉，这就是**误删问题**。所以正确做法是：解锁时先判断当前 value 是不是自己当初写进去的那个 requestId，只有匹配才删除，一般会通过 **Lua 脚本** 保证“校验 + 删除”的原子性。

第三，**锁续期** 是为了解决业务执行时间不确定的问题。比如你给锁设置了 30 秒过期，但任务实际跑了 60 秒，如果不续期，锁中途就会自动失效，其他节点可能会误以为资源空闲，又进来执行。解决思路通常是加一个**看门狗机制**或者后台续约线程，在业务还没执行完且锁还属于自己的情况下，周期性延长过期时间。

第四，面试里要注意强调：Redis 分布式锁适合那种**轻量互斥、允许少量概率性风险可控**的场景，但如果业务对一致性极其敏感，比如金融扣款、库存强一致扣减，只靠一个简单 Redis 锁还不够，往往还要结合**数据库约束、幂等设计、状态机校验**一起保证正确性。

### 第 47 题：布隆过滤器原理、哈希函数和 bitmap 怎么配合、为什么能防缓存穿透、有什么缺点

🎯 【一句话总结】

**布隆过滤器本质上是一种高效判断“某个元素可能存在、一定不存在”的概率型数据结构**；它特别适合放在缓存或数据库前面，提前拦截那些明显不存在的请求，从而防止**缓存穿透**。

🗣️ 【口述要点】

第一，先说原理。布隆过滤器底层通常是一个**位图 bitmap**，再配合多个**哈希函数**。一个元素写入时，会经过多个哈希函数计算出多个位置，然后把 bitmap 对应位置都置为 1；查询时，再对这个元素做同样的哈希，如果对应位置只要有一个是 0，就能确定这个元素**一定不存在**；如果这些位置全是 1，那只能说明它**可能存在**。

第二，哈希函数和 bitmap 的配合，本质上是在用很小的空间去表达大量元素的“存在痕迹”。多个哈希函数可以把同一个元素映射到多个 bit 位上，这样既能降低误判率，也能让整体空间利用率比较高。所以布隆过滤器最大的价值，就是**省内存、判断快、适合做大规模存在性过滤**。

第三，它为什么能防**缓存穿透**，因为对于那些根本不存在的数据请求，比如恶意构造的用户 ID、订单号，请求到了系统入口时，可以先过布隆过滤器；如果布隆过滤器判断“一定不存在”，就直接拦掉，不用再查 Redis，更不用打到 MySQL。这样大量无效请求就不会压垮后端存储。

第四，布隆过滤器也有明显缺点。最典型的是**有误判**，也就是它可能把“不存在”的元素误判成“可能存在”，但不会把存在的元素误判成不存在；另外，普通布隆过滤器**不适合删除元素**，因为多个元素可能共享同一个 bit 位，直接清零可能误伤别人。所以它通常适合那种**只增不减、允许少量误判**的场景。

### 第 48 题：DNS 解析过程是什么、域名是怎么查到 IP 的、如果二级域名没找到会发生什么

🎯 【一句话总结】

**DNS 解析本质上就是把人类可读的域名，一层层转换成机器可访问的 IP 地址**；它通常会经历**本地缓存查询 + 递归查询/迭代查询 + 权威 DNS 返回结果**这几个阶段。

🗣️ 【口述要点】

第一，先说整体流程。浏览器访问一个域名时，通常会先查**浏览器缓存、操作系统缓存、hosts 文件、本地 DNS 缓存**，如果本地都没有，再把请求发给配置好的**本地域名服务器**。本地 DNS 一般会帮你做后续递归解析，所以对客户端来说，它只问一次，但后面可能经历多轮查询。

第二，真正往下查的时候，通常会从**根域名服务器**开始，根服务器不会直接告诉你最终 IP，但会告诉你“.com”这类**顶级域服务器**在哪；接着去问顶级域服务器，它再告诉你目标域名对应的**权威 DNS** 在哪；最后去问权威 DNS，权威 DNS 才返回这个域名真正对应的 **A 记录 / AAAA 记录 / CNAME** 等结果。拿到 IP 之后，本地 DNS 和客户端通常还会缓存一段时间。

第三，域名是怎么查到 IP 的，本质上依赖的是一套**树状分层授权体系**。根管顶级域，顶级域再往下授权到具体域名的权威服务器，所以 DNS 不是一个中心化数据库，而是一个分布式、分层管理的系统。这样做的好处是可扩展性强，全球都能协同工作。

第四，如果你访问的是某个**二级域名**，比如 `api.example.com`，但权威 DNS 里找不到对应记录，通常就会返回 **NXDOMAIN** 或者“无此记录”的响应。再往上一层说，如果二级域名没有单独配置，但上层配置了**泛域名解析**，比如 `*.example.com`，那它就可能被泛解析规则接住，返回统一的 IP；如果没有泛解析，那最终就是解析失败。

### 第 49 题：RAG 怎么做、切片 / 向量化 / 召回 / 重排流程是什么、千万级知识库检索有什么性能瓶颈、怎么优化

🎯 【一句话总结】

**RAG 本质上就是“先检索，再生成”**；它的核心目标不是让大模型硬记知识，而是让模型在回答前，先从外部知识库里找到更相关的内容，再基于这些内容生成答案。

🗣️ 【口述要点】

第一，标准流程可以分成四步：**切片、向量化、召回、重排**。

- **切片**：先把原始文档拆成适合检索的片段，太长会影响召回精度，太短又会丢上下文；
- **向量化**：把每个片段转成向量，写入向量库；
- **召回**：用户提问后，把问题也向量化，从向量库里先粗筛出一批候选片段；
- **重排**：再用更强的模型或规则对候选结果精排，选出最相关的内容给大模型生成答案。
   这就是 RAG 最核心的链路。

第二，RAG 效果好不好，很多时候不在大模型本身，而在**切片质量和召回质量**。切片时一般要考虑**语义完整性、段落边界、标题层级、上下文重叠**；如果切得太碎，答案上下文不完整；如果切得太大，检索又不精准。所以切片不是简单按字数截断，而是要尽量兼顾语义和检索粒度。

第三，到了**千万级知识库**，性能瓶颈通常会出现在几个地方：

- 向量检索本身的计算量变大，召回延迟上升；
- 索引规模变大，内存压力和磁盘 IO 压力都更明显；
- 重排模型如果候选太多，耗时会很高；
- 多租户、多业务场景下过滤条件复杂，也会拖慢整体检索。
   所以规模一大，问题通常不只是“能不能查到”，更重要的是“能不能在可接受延迟内查准”。

第四，优化思路一般是分层做。常见手段包括：

- 做**分桶、分片、分租户索引**，减少单次检索范围；
- 先走**粗召回**，再做精排，避免所有数据都用重模型算；
- 结合**关键词检索 + 向量检索**做混合召回；
- 控制候选集规模，减少重排成本；
- 对热点问题做缓存，对离线数据提前建索引和预处理。
   所以 RAG 工程化的关键，不只是模型能力，更是**检索系统设计和性能治理能力**。

### 第 50 题：Java 里面怎么实现线程安全类、AQS 是什么、为什么 AQS 要用队列、公平锁和非公平锁怎么实现

🎯 【一句话总结】

**Java 里很多线程安全类的底层核心，其实都离不开 AQS**；AQS 可以理解成一个**基于状态位 + FIFO 等待队列**的同步器框架，像 **ReentrantLock、Semaphore、CountDownLatch** 这些并发工具，很多都是在它基础上实现的。

🗣️ 【口述要点】

第一，先说 Java 里怎么实现线程安全类。常见方式有几类：

- 用 **synchronized / Lock** 做互斥；
- 用 **CAS + volatile** 做无锁或轻量同步；
- 用 **原子类、并发容器** 封装线程安全操作；
- 用 **ThreadLocal** 做线程隔离。
   而如果要实现一个复杂的同步组件，比如可重入锁、共享锁、倒计时器，就往往会基于 **AQS** 来做。

第二，**AQS** 全称是 **AbstractQueuedSynchronizer**，它本质上是一个同步器框架。核心思想很简单：用一个 **state** 表示同步状态，比如锁有没有被占用、重入了几次、还剩多少许可；当线程竞争失败时，不是一直空转，而是把线程封装成节点，放进一个**双向队列**里排队，等前面的线程释放资源后再唤醒后继节点。

第三，为什么 AQS 要用**队列**，因为并发竞争下，总会有很多线程抢同一个资源，不可能都同时成功。用队列能把竞争失败的线程有序挂起来，避免无意义地持续抢占 CPU，也便于在资源释放时按规则唤醒后继线程。简单说，队列解决的是**高并发下“谁该等、谁该被唤醒、怎么有序调度”**的问题。

第四，**公平锁** 和 **非公平锁** 的区别，核心在于获取锁时要不要严格按排队顺序来。**公平锁** 会先检查等待队列里有没有前驱节点，有的话新线程不能插队，必须老老实实排队；**非公平锁** 则允许新线程在锁刚释放时直接先抢一次，抢到了就先执行，抢不到再入队。公平锁更讲顺序，但吞吐量通常低一些；非公平锁可能让个别线程等得更久，但整体性能一般更好，所以像 **ReentrantLock 默认就是非公平锁**。