[TOC]

## 用户态和内核态

### 用户态和内核态的区别？

内核态和用户态是操作系统中的两种运行模式。它们的主要区别在于权限和可执行的操作：

内核态：

- CPU可以执行所有指令，包括特权指令

- 可以访问所有内存空间和硬件资源

- 操作系统内核运行在此模式下

用户态：

- CPU只能执行非特权指令

- 只能访问受限的内存空间（用户空间）

- 普通应用程序运行在此模式下

## 进程管理

### 线程和进程的区别是什么？

**进程**是操作系统资源分配的基本单位，拥有独立的地址空间（包括代码、数据、堆栈）以及文件描述符等系统资源，进程之间相互隔离。

**线程**是CPU调度的基本单位，也称为轻量级进程，它依附于进程存在，同一个进程内的多个线程共享该进程的地址空间和资源，但每个线程有自己的栈和程序计数器。

两者的核心区别主要体现在以下几个方面：

- **资源开销**上，进程的创建、切换和销毁成本较高，因为需要分配独立的内存空间和数据结构；线程则开销小得多，只需分配栈和寄存器上下文。
- **内存空间**方面，进程间内存隔离，互不干扰；线程则共享进程内存，方便数据交换但需注意同步问题。
- **通信方式**不同，进程间通信（IPC）较复杂，需借助管道、消息队列等机制；线程间可直接读写共享变量，但需配合锁等同步手段。
- **健壮性**上，进程隔离性好，单个进程崩溃不影响其他进程；线程则可能因一个线程的非法操作导致整个进程退出。因此，多进程适合需要高稳定性和隔离性的场景，多线程则适用于高并发、资源共享的任务。

### 进程，线程，协程的区别是什么？

**进程**是操作系统资源分配的基本单位，拥有独立的地址空间（包括代码、数据、堆栈）以及文件描述符等系统资源，进程之间相互隔离。
**线程**是CPU调度的基本单位，也称为轻量级进程，它依附于进程存在，同一个进程内的多个线程共享该进程的地址空间和资源，但每个线程有自己的栈和程序计数器。
**协程**是用户态的轻量级线程，由程序自身控制调度，一个线程内可以存在多个协程，协程间的切换不涉及内核态，因此开销极小。

三者的核心区别主要体现在以下几个方面：

- **资源开销**：进程的创建、切换和销毁开销最大（需分配独立内存空间、建立数据结构、页表切换）；线程开销较小，共享进程资源，切换仅需保存/恢复寄存器、栈指针；协程开销最小，完全在用户态管理，切换只需保存/恢复少量上下文（如程序计数器、寄存器），无需系统调用。
- **调度方式**：进程与线程由操作系统内核**抢占式**调度，线程有优先级，时间片用完或被高优先级线程抢占；协程由程序**协作式**调度（主动让出控制权，如`yield`或`await`），开发者可自行控制执行顺序。
- **内存空间**：进程间内存独立，互不干扰；线程共享所属进程的地址空间，可直接访问相同变量，但需同步机制；同一线程内的协程共享该线程的堆内存，栈通常独立或复用。
- **通信与同步**：进程间通信（IPC）较复杂，需借助管道、消息队列、共享内存、Socket等；线程间可直接读写共享变量，但需锁、信号量等同步手段；协程通常通过通道（如Go的channel）或异步编程模型通信，同一线程内的协程无需加锁即可共享数据。
- **并发能力与数量**：进程数量受系统资源限制，通常较少；线程数量受内核限制（过多线程导致频繁上下文切换）；协程可轻松创建成千上万个，适合高并发场景（如IO密集型应用）。
- **健壮性**：进程隔离性好，单个进程崩溃不影响其他进程；线程隔离性差，一个线程的非法操作可能导致整个进程退出；协程的崩溃通常仅影响自身，但若未妥善处理可能波及所在线程。

因此，**进程**适合需要强隔离、多机分布的任务（如独立服务）；**线程**适合资源共享、频繁交互的任务（如Web服务器处理请求）；**协程**适合高并发、IO密集、需精细控制执行流的场景（如异步编程、微服务）。

### **为什么进程崩溃不会对其他进程产生很大影响**

主要是因为：

- **进程隔离性**：每个进程都有自己独立的内存空间，当一个进程崩溃时，其内存空间会被操作系统回收，不会影响其他进程的内存空间。这种进程间的隔离性保证了一个进程崩溃不会直接影响其他进程的执行。
- **进程独立性**：每个进程都是独立运行的，它们之间不会共享资源，如文件、网络连接等。因此，一个进程的崩溃通常不会对其他进程的资源产生影响。

### 为什么进程之下还要设计线程？

设计线程的主要目的是为了**提高系统的并发处理能力和资源利用率**，同时弥补进程模型的不足。

如果只有进程而没有线程，将面临以下问题：

- **单进程无法高效并发**：一个进程一次只能执行一个任务，若遇到I/O阻塞，整个进程会暂停，导致CPU资源浪费。
- **多进程开销巨大**：虽然多进程可以实现并发，但进程的创建、切换和销毁需要分配独立地址空间、页表等，成本高昂；进程间通信（IPC）复杂且效率低（如管道、共享内存等），难以满足高频数据交换的需求。
- **资源限制**：系统可创建的进程数量受内存和CPU资源限制，无法支撑大规模并发任务（如数千个网络连接）。

### 多线程比单线程的优势，劣势？

- **优势**：提高程序的运行效率，可以充分利用多核处理器的资源，同时处理多个任务，加快程序的执行速度。
- **劣势**：存在多线程数据竞争访问的问题，需要通过锁机制来保证线程安全，增加了加锁的开销，并且还会有死锁的风险。多线程会消耗更多系统资源，如CPU和内存，因为每个线程都需要占用一定的内存和处理时间。

### 进程切换和线程切换的区别？

1. 进程切换：进程切换涉及到更多的内容，包括整个进程的地址空间、全局变量、文件描述符等。因此，进程切换的开销通常比线程切换大。
2. 线程切换：线程切换只涉及到线程的堆栈、寄存器和程序计数器等，不涉及进程级别的资源，因此线程切换的开销较小。

### 线程切换为什么比进程切换快，节省了什么资源？

- **地址空间共享**：同一进程内的线程共享地址空间，切换时**不需要切换页表**和刷新TLB（快表），避免了内存映射切换带来的开销。而进程切换必须切换页表，导致TLB刷新和缓存失效，内存访问延迟增加。
- **上下文保存内容少**：线程切换只需保存和恢复**线程私有资源**（如寄存器、程序计数器、栈指针），而进程切换需要保存和恢复**进程级资源**（如进程控制块PCB、文件描述符表、信号处理等），数据量更大，耗时更长。
- **系统资源复用**：线程直接复用所属进程的资源（如打开的文件、内存映射），无需重新加载，节省了资源初始化和重建的时间。

### 线程切换详细过程是怎么样的？上下文保存在哪里？

线程切换的详细过程可以分为以下几个步骤：

- 上下文保存：当操作系统决定切换到另一个线程时，它首先会保存当前线程的上下文信息。上下文信息包括寄存器状态、程序计数器、堆栈指针等，用于保存线程的执行状态。
- 切换到调度器：操作系统将执行权切换到调度器（Scheduler）。调度器负责选择下一个要执行的线程，并根据调度算法做出决策。
- 上下文恢复：调度器选择了下一个要执行的线程后，它会从该线程保存的上下文信息中恢复线程的执行状态。
- 切换到新线程：调度器将执行权切换到新线程，使其开始执行。

上下文信息的保存通常由操作系统负责管理，具体保存在哪里取决于操作系统的实现方式。一般情况下，上下文信息会保存在线程的控制块（Thread Control Block，TCB）中。

TCB是操作系统用于管理线程的数据结构，包含了线程的状态、寄存器的值、堆栈信息等。当发生线程切换时，操作系统会通过切换TCB来保存和恢复线程的上下文信息。

### 进程的状态（五种状态），如何切换？

一个完整的进程状态的变迁如下图：

![img](https://cdn.xiaolincoding.com//picgo/1715669823633-dcd21d9d-1bc9-44b0-b708-7afda68c2257.webp)

进程五种状态的变迁

再来详细说明一下进程的状态变迁：

- *NULL -> 创建状态*：一个新进程被创建时的第一个状态；
- *创建状态 -> 就绪状态*：当进程被创建完成并初始化后，一切就绪准备运行时，变为就绪状态，这个过程是很快的；
- *就绪状态 -> 运行状态*：处于就绪状态的进程被操作系统的进程调度器选中后，就分配给 CPU 正式运行该进程；
- *运行状态 -> 结束状态*：当进程已经运行完成或出错时，会被操作系统作结束状态处理；
- *运行状态 -> 就绪状态*：处于运行状态的进程在运行过程中，由于分配给它的运行时间片用完，操作系统会把该进程变为就绪态，接着从就绪态选中另外一个进程运行；
- *运行状态 -> 阻塞状态*：当进程请求某个事件且必须等待时，例如请求 I/O 事件；
- *阻塞状态 -> 就绪状态*：当进程要等待的事件完成时，它从阻塞状态变到就绪状态；

### 进程上下文有哪些？

**主要包含了虚拟内存、栈、全局变量等用户空间的资源，内核堆栈、寄存器等内核空间的资源。**

### 进程间通讯有哪些方式？

Linux 内核提供了不少进程间通信的方式：

- 管道
- 消息队列
- 共享内存
- 信号
- 信号量
- socket

**管道**是最简单的 IPC 方式，数据从一端写入，从另一端读出，单向流动。匿名管道只能用于有亲缘关系的进程，比如父子进程。命名管道在文件系统里有个名字，不相关的进程也能用。Shell 里的 `cat file | grep xxx` 就是用管道把两个进程串起来。

**消息队列**允许进程发送和接收带类型的消息，消息在队列里排队，接收方可以按类型选择性地读取。比管道灵活，但每条消息有大小限制，Linux 默认是 8KB。

**共享内存**是最快的 IPC 方式，多个进程直接读写同一块物理内存，不需要数据拷贝。但因为没有内置同步机制，必须配合信号量或互斥锁使用，否则会出现竞态条件。数据库的缓冲池、高性能缓存系统经常用共享内存。

**信号量**本身不传递数据，是用来同步的。可以控制多个进程对共享资源的访问顺序，实现互斥或者限流。

**信号**是一种异步通知机制，用来告诉进程发生了某个事件。比如 `Ctrl+C` 会给前台进程发 `SIGINT`，`kill -9` 会发 `SIGKILL`。信号只能传递一个信号编号，不能带复杂数据。

**套接字**最灵活，既能在本机进程间通信，也能跨网络通信。Unix 域套接字用于本机，效率比 TCP 高。网络套接字用 TCP/UDP 协议，是分布式系统通信的基础。

### 管道有几种方式？

管道在Linux中有两种方式：匿名管道和命名管道。

- 匿名管道：是一种在父子进程或者兄弟进程之间进行通信的机制，只能用于具有亲缘关系的进程间通信，通常通过pipe系统调用创建。
- 命名管道：是一种允许无关的进程间进行通信的机制，基于文件系统，可以在不相关的进程之间进行通信。

### 信号和信号量有什么区别？

- 信号：一种处理异步事件的方式。信号是比较复杂的通信方式，用于通知接收进程有某种事件发生，除了用于进程外，还可以发送信号给进程本身。
- 信号量：进程间通信处理同步互斥的机制。是在多线程环境下使用的一种设施，它负责协调各个线程，以保证它们能够正确，合理的使用公共资源。

### 共享内存怎么实现的？

**共享内存的机制，就是拿出一块虚拟地址空间来，映射到相同的物理内存中**。这样这个进程写入的东西，另外一个进程马上就能看到了，都不需要拷贝来拷贝去，传来传去，大大提高了进程间通信的速度。

### 线程间通讯有什么方式？

Linux 提供五种主要线程同步/通信方式：

1. **互斥锁**：一把锁。线程访问共享资源前加锁，完成后解锁。加锁期间，其他试图加锁的线程会阻塞，直到锁被释放。
2. **条件变量**：用于实现“等待-唤醒”机制。常与互斥锁配合使用，允许线程在条件不满足时挂起等待，当条件成立时被另一线程唤醒。
3. **自旋锁**：基于CAS原子操作在用户态实现。锁被持有时，竞争失败的线程会“忙等待”（循环占用CPU），适用于锁持有时间短的场景，避免了线程上下文切换的开销。
4. **信号量**：基于一个整型计数器，通过原子操作P（减1，可能阻塞）和V（加1，可能唤醒）来控制对有限资源的访问次数。
5. **读写锁**：区分“读锁”和“写锁”。读锁可被多线程并发持有（共享），写锁只能被单线程独占。适用于读多写少的场景，能提高并发效率。

### 进程调度算法有哪些？

### **先来先服务（FCFS, First-Come, First-Served）**

**定义**：

- **FCFS** 是按进程到达的顺序进行调度，先来先执行。

**特点**：

- 非抢占式，即一旦进程开始执行，它将一直执行，直到完成。
- 调度简单，容易实现。
- **问题**：容易导致“饥饿”现象或**长任务阻塞短任务**，也容易引起 **队列效应**，即进程等待时间较长，导致 **平均等待时间** 高。

**适用场景**：

- 适用于进程执行时间较为均匀、没有突发较长进程的情况，或者任务间的执行时间差距较小的系统。

### 2. **短作业优先（SJF, Shortest Job First）**

**定义**：

- **SJF** 是一种非抢占式的调度算法，优先选择执行运行时间最短的进程。通过预测每个进程的执行时间来确定调度顺序。

**特点**：

- 如果执行时间已知，能够使 **平均等待时间最小**，即优化了系统的响应时间。
- 该算法会导致 **长进程饥饿**，如果总是有新的短进程到来，长进程就可能一直无法执行。
- 在实际情况中，预测每个进程的执行时间通常很困难。

**适用场景**：

- 对于已知作业长度、进程执行时间比较稳定的系统，或者执行任务相对较短的场景，SJF 可以发挥较好性能。

### 3. **最短剩余时间优先（SRTF, Shortest Remaining Time First）**

**定义**：

- **SRTF** 是抢占式的 **SJF** 算法，它选择剩余执行时间最短的进程。若一个新进程到来时其剩余时间比当前正在执行的进程短，则会抢占 CPU 执行。

**特点**：

- 与 SJF 相似，但具有抢占式特性。
- 可以有效地减少平均等待时间。
- 与 SJF 一样，可能导致 **长进程饥饿**，并且对于未知的执行时间预测存在挑战。

**适用场景**：

- 适用于已知剩余执行时间，且优先调度短任务的系统。

### 4. **优先级调度（Priority Scheduling）**

**定义**：

- 进程根据优先级进行调度。优先级高的进程先执行，优先级低的进程后执行。
- 优先级可以根据不同的标准设定，例如任务的重要性、所需资源等。

**特点**：

- 可实现抢占式或非抢占式调度。抢占式优先级调度在运行过程中，若一个新进程的优先级高于当前执行进程，则会抢占 CPU。
- 可能导致 **低优先级进程饥饿**，特别是在长时间不调整优先级的情况下。
- 动态优先级调度可以通过 ** aging 算法**（逐渐提高低优先级进程的优先级）来解决饥饿问题。

**适用场景**：

- 适用于多种任务重要性不同的场景，例如一些实时性较强的任务需要优先执行，而其他任务则可以稍后处理。

### 5. **轮转法（RR, Round Robin）**

**定义**：

- **RR** 是一种时间片轮转的调度算法。每个进程分配一个固定的时间片，当进程的时间片用尽后，它会被放回队列的末尾，等待下一次轮到它继续执行。

**特点**：

- 抢占式算法，每个进程的执行时间受到限制（即时间片的长度）。
- 适合 **响应时间要求较高** 的系统，能保证各个进程公平地获得 CPU 资源。
- 但如果时间片设置不合理（过短或过长），可能会导致 **过多的上下文切换**，从而增加开销。

**适用场景**：

- 适用于交互式系统（如操作系统的命令行和图形界面），或者对响应时间有较高要求的系统，如时间共享系统。

### 6. **多级反馈队列调度（Multilevel Feedback Queue）**

**定义**：

- MLFQ

  是一种结合多级队列与优先级调度的**抢占式**策略。在 MLFQ 中，当新进程到达时，它会被放入最高优先级的队列中。每个队列都对应一个时间片，进程会按队列中的时间片运行。当进程用完当前队列的时间片时：

  - 如果它还未完成，则会被移到下一个低优先级队列中，进入新的队列后，它的执行时间片通常会变长。
  - 如果进程在时间片内完成任务，它将从队列中移除。

**特点**：

- 结合了 **FCFS**、**SJF** 和 **优先级调度** 的优点，可以有效避免饥饿问题，同时尽量减少长任务的等待时间。
- 动态调整进程的优先级，使得短任务能够更快执行，长任务能逐步降级。
- 算法设计复杂，涉及多个队列和多次调度策略。

**适用场景**：

- 多任务系统或负载较重的系统，可以根据实际负载自动调整调度策略，保证较好的响应性和公平性。

### 总结与选择：

- 对于 **实时系统**，需要保证任务按时完成，常用的调度算法包括 **优先级调度** 和 **最短作业优先**。
- 对于 **交互式系统**，如多任务桌面操作系统，常用 **轮转法** 和 **多级反馈队列调度**。
- 对于需要 **平衡效率和公平性** 的情况， **多级反馈队列** 是一种比较理想的选择。

## 锁

### 为什么并发执行线程要加锁？

并发执行线程需要加锁主要是为了保护共享数据，防止出现"竞态条件"。

"竞态条件"是指当多个线程同时访问和操作同一块数据时，最终结果依赖于线程的执行顺序，这可能导致数据的不一致性。

通过加锁，我们可以确保在任何时刻只有一个线程能够访问共享数据，从而避免"竞态条件"，确保数据的一致性和完整性。

### 自旋锁是什么？应用在哪些场景？

自旋锁是一种基于忙等待的锁机制。当线程加锁失败时，它会持续循环（自旋）不断检查锁是否可用，直到成功获取锁，而不会像互斥锁那样让出CPU进行线程切换。

**工作原理**：自旋锁利用CPU提供的CAS（Compare And Swap）原子指令，在用户态完成加锁和解锁操作。CAS将“查看锁状态”和“设置锁持有者”两个步骤合并为一条硬件指令，保证操作的原子性。例如，用整数0表示锁空闲，pid表示线程ID，则`CAS(lock, 0, pid)`为加锁，`CAS(lock, pid, 0)`为解锁。

**实现细节**：忙等待通常用while循环实现，但更优的做法是使用CPU的PAUSE指令，以减少循环等待时的功耗。

**适用场景**：

- 适用于多核系统（单核CPU需要抢占式调度，否则自旋线程会独占CPU）。
- 适合锁持有时间极短的场景，如用户态协程、异步编程等，能避免线程切换开销。
- 若锁持有时间过长，自旋会长期占用CPU，降低系统效率。

**与互斥锁对比**：

- 互斥锁加锁失败时，线程会阻塞并触发上下文切换，适合锁持有时间较长的场景。
- 自旋锁加锁失败时，线程忙等待（不切换），适合锁持有时间极短的场景。

### 死锁发生条件是什么？

死锁只有**同时满足**以下四个条件才会发生：

- 互斥条件：互斥条件是指**多个线程不能同时使用同一个资源**。
- 持有并等待条件：持有并等待条件是指，当线程 A 已经持有了资源 1，又想申请资源 2，而资源 2 已经被线程 C 持有了，所以线程 A 就会处于等待状态，但是**线程 A 在等待资源 2 的同时并不会释放自己已经持有的资源 1**。
- 不可剥夺条件：不可剥夺条件是指，当线程已经持有了资源 ，**在自己使用完之前不能被其他线程获取**，线程 B 如果也想使用此资源，则只能在线程 A 使用完并释放后才能获取。
- 环路等待条件：环路等待条件指的是，在死锁发生的时候，**两个线程获取资源的顺序构成了环形链**。

### 如何避免死锁？

破坏四个死锁发生条件中的一个，最常见的并且可行的就是**使用资源有序分配法，来破环环路等待条件**。也就是说线程 A 和 线程 B 获取资源的顺序要一样，当线程 A 是先尝试获取资源 A，然后尝试获取资源 B 的时候，线程 B 同样也是先尝试获取资源 A，然后尝试获取资源 B。也就是说，线程 A 和 线程 B 总是以相同的顺序申请自己想要的资源。

### 讲一下银行家算法

一种用于**动态资源分配**的死锁避免算法。其核心思想是：**对于每次资源申请，系统都要实时判断其是否有死锁风险，如果存在风险，就拒绝该申请；仅在资源分配后系统仍处于安全状态时，才允许分配资源**，从而**避免死锁**。

银行家算法的实现：

- **安全状态检查**：每次分配资源前，模拟分配后的系统状态，判断系统是否存在一个 **安全序列**（所有进程能按顺序获得所需资源并完成）。
- **动态决策**：若安全状态成立，则正式分配资源；若无法找到安全序列，则拒绝资源请求，防止系统进入可能导致死锁的状态。

优点：

- 严格避免死锁，保证系统安全。
- 资源利用率高于死锁预防策略（如一次性分配）。

缺点：

- **静态需求假设**：进程需预先声明最大资源需求，现实中难以动态调整。
- **计算开销大**：每次请求需遍历所有进程，时间复杂度为 O(n²)。
- **不适用于不可抢占资源**：如打印机、独占设备。

### 乐观锁和悲观锁有什么区别？

乐观锁：

- 基本思想：乐观锁假设多个事务之间很少发生冲突，因此在读取数据时不会加锁，而是在更新数据时检查数据的版本（如使用版本号或时间戳），如果版本匹配则执行更新操作，否则认为发生了冲突。
- 使用场景：乐观锁适用于读多写少的场景，可以减少锁的竞争，提高并发性能。例如，数据库中的乐观锁机制可以用于处理并发更新同一行数据的情况。

悲观锁：

- 基本思想：悲观锁假设多个事务之间会频繁发生冲突，因此在读取数据时会加锁，防止其他事务对数据进行修改，直到当前事务完成操作后才释放锁。
- 使用场景：悲观锁适用于写多的场景，通过加锁保证数据的一致性。例如，数据库中的行级锁机制可以用于处理并发更新同一行数据的情况。

乐观锁适用于读多写少的场景，通过版本控制来处理冲突；而悲观锁适用于写多的场景，通过加锁来避免冲突。

## 内存管理

### 介绍一下操作系统内存管理

操作系统通过虚拟内存管理物理内存，为每个进程提供独立的虚拟地址空间。程序访问的是虚拟地址，由内存管理单元（MMU）结合页表实时转换为物理地址。

**虚拟内存的主要优势：**

1. **内存扩展**：利用程序运行的局部性原理，可将不常访问的内存页换出到磁盘（如Swap分区），使进程能使用超过物理内存大小的空间。
2. **进程隔离**：每个进程拥有独立的页表，虚拟地址空间相互隔离，避免进程间地址冲突，一个进程无法直接访问另一进程的内存。
3. **安全保护**：页表项包含读写权限、存在位等属性，硬件级检查可防止非法内存访问，提升系统安全性。

**内存分页管理：**
Linux将内存划分为固定大小的页（通常4KB）。虚拟地址和物理地址通过页表映射，页表本身也存储在内存中。MMU负责地址转换，当访问的虚拟页未在页表中映射时，触发**缺页异常**，内核介入分配物理内存、更新页表，之后恢复进程运行。

### 什么是虚拟内存和物理内存？

- 虚拟内存：是操作系统提供给每个运行中程序的一种地址空间，每个程序在运行时认为自己拥有的内存空间就是虚拟内存，其大小可以远远大于物理内存的大小。虚拟内存通过将程序的地址空间划分成若干个固定大小的页或段，并将这些页或者段映射到物理内存中的不同位置，从而使得程序在运行时可以更高效地利用物理内存。
- 物理内存：物理内存是计算机实际存在的内存，是计算机中的实际硬件部件。

### 讲一下页表？

页表是操作系统内存分页管理机制中的核心数据结构，用于记录虚拟地址与物理地址之间的映射关系。

**分页管理**
内存被划分为固定大小的块，称为“页”（Page）。Linux 中典型页大小为 4KB。虚拟地址空间和物理地址空间均按此粒度划分，每个进程拥有独立的虚拟地址空间，通过页表映射到物理内存。

**页表与地址转换**
页表存储在物理内存中，其每一项（页表项）记录了虚拟页号对应的物理页框号及访问权限等属性。CPU 中的内存管理单元（MMU）负责将虚拟地址转换为物理地址：

1. 虚拟地址被拆分为**页号**和**页内偏移**。
2. MMU 以页号为索引查询当前进程的页表，获取对应的物理页框号。
3. 物理页框号与页内偏移组合形成最终的物理地址。

**缺页异常**
若访问的虚拟页尚未在页表中建立有效映射（即页表项标记为不存在），MMU 会触发缺页异常，陷入内核。内核分配物理页、更新页表后恢复进程执行，整个过程对进程透明。

**优缺点**

- **优点**：页与页紧密排列，消除了外部内存碎片。
- **缺点**：分配的最小单位为一页，可能导致页内空间浪费（内部碎片），尤其当程序需求不足一页时。

页表机制实现了虚拟内存的核心功能：进程隔离、内存扩展和访问保护。

### 讲一下段表？

虚拟地址也可以通过**段表**与物理地址进行映射的，分段机制会把程序的虚拟地址分成 4 个段，每个段在段表中有一个项，在这一项找到段的基地址，再加上偏移量，于是就能找到物理内存中的地址。

### 虚拟地址是怎么转化到物理地址的？

虚拟地址到物理地址的转换由内存管理单元（MMU）硬件完成。MMU利用页表（Page Table）实现映射，页表将虚拟地址空间划分为固定大小的页（如4KB），每个页表项记录虚拟页与物理页框的对应关系。

**转换步骤**：

1. **地址分解**：MMU将虚拟地址拆分为**页号**和**页内偏移**。
2. **页表查询**：以页号为索引，在当前进程的页表中查找对应页表项，获取物理页框号。
3. **地址组合**：将物理页框号与页内偏移拼接，得到最终物理地址。

为提高效率，MMU还集成了TLB（快表）缓存近期使用的页表项，并可能采用多级页表结构以节省内存。若页表项无效，则触发缺页异常，由内核处理。

### 程序的内存布局是怎么样的？

程序在用户空间的内存布局（由低地址到高地址)通常包含以下区域：

1. **保留区**：最低地址区域，用于捕获空指针或非法小地址访问，防止程序跑飞。
2. **代码段（Text）**：存储二进制可执行代码。
3. **数据段（Data）**：存放已初始化的静态变量和全局变量。
4. **BSS段**：存放未初始化的静态变量和全局变量（加载时初始化为零）。
5. **堆（Heap）**：动态分配内存（如`malloc`），向高地址方向增长。
6. **文件映射段**：映射动态库、共享内存等，位于堆和栈之间。
7. **栈（Stack）**：存储局部变量、函数调用上下文，向低地址方向增长，大小固定（通常8MB，可配置）。

其中，堆和文件映射段的内存是动态分配的（如`malloc`、`mmap`）。

### 堆和栈的区别？

- **分配方式**：堆是动态分配内存，由程序员手动申请和释放内存，通常用于存储动态数据结构和对象。栈是静态分配内存，由编译器自动分配和释放内存，用于存储函数的局部变量和函数调用信息。
- **内存管理**：堆需要程序员手动管理内存的分配和释放，如果管理不当可能会导致内存泄漏或内存溢出。栈由编译器自动管理内存，遵循后进先出的原则，变量的生命周期由其作用域决定，函数调用时分配内存，函数返回时释放内存。
- **大小和速度**：堆通常比栈大，内存空间较大，动态分配和释放内存需要时间开销。栈大小有限，通常比较小，内存分配和释放速度较快，因为是编译器自动管理。

### fork()会复制哪些东西？

- fork 阶段会复制父进程的页表（虚拟内存）
- fork 之后，如果发生了写时复制，就会复制物理内存

### 介绍copy on write(写时复制)

写时复制（Copy-on-Write，COW）是一种优化资源复用的技术，常用于 `fork` 系统调用。其核心思想是：**当多个任务需要共享同一份数据时，不立即复制物理内存，而是仅复制页表，使父子进程的虚拟地址映射到同一物理内存，并将该内存页权限设为只读**。这样既节省了物理内存，也避免了不必要的复制开销。

当其中任一进程尝试写入共享页时，CPU 会因权限不符而触发写保护中断。内核在中断处理中，会为执行写入的进程分配新的物理页，将原页内容复制过来，并更新其页表映射到新页，同时将新旧页的权限恢复为可读写。此后，父子进程各自拥有独立的物理页，写入操作在副本上进行。

**目的**：避免 `fork` 时一次性复制父进程整个地址空间造成的性能损耗和内存浪费，适用于进程创建后立即执行 `exec` 的场景，此时许多共享页根本无需复制。

### copy on write节省了什么资源？

节省了物理内存的资源，因为 fork 的时候，子进程不需要复制父进程的物理内存，避免了不必要的内存复制开销，子进程只需要复制父进程的页表，这时候父子进程的页表指向的都是共享的物理内存。

只有当父子进程任何有一方对这片共享的物理内存发生了修改操作，才会触发写时复制机制，这时候才会复制发生修改操作的物理内存。

### malloc 1KB和1MB 有什么区别？

`malloc` 分配内存时，glibc 根据申请大小选择不同系统调用：

- **小于 128 KB**（如 1 KB）：使用 `brk()`。
  通过移动堆顶指针分配，释放后内存不立即归还系统，而是由 `malloc` 缓存复用，适合频繁分配的小内存，效率高，但可能导致内存碎片，进程内存占用只增不减。
- **大于 128 KB**（如 1 MB）：使用 `mmap()`。
  在文件映射段分配匿名内存，释放时立即 `munmap` 归还给系统，适合大块内存，避免长期占用堆空间，但每次分配/释放开销较大（需处理页表映射）。

**区别总结**：
1 KB 分配由堆管理，速度快、可复用；1 MB 分配由内存映射实现，释放彻底，但系统调用开销更高。阈值（128 KB）可配置，不同 glibc 版本可能不同。

### 介绍一下brk，mmap

`brk` 和 `mmap` 是 Linux 中用于动态内存分配的两个系统调用，`malloc` 底层基于它们实现。

- **brk**
  通过修改 `program break`（堆顶指针）来分配或释放堆内存。将堆顶向高地址移动可扩大堆空间，向低地址移动则缩小堆。释放的内存由 `malloc` 缓存复用，不立即归还系统，适合小块内存分配（如 <128 KB），效率高，但可能引发内存碎片，且堆内存只增不减。
- **mmap**
  通过“私有匿名映射”在文件映射区分配一块内存。该内存与堆独立，释放时通过 `munmap` 立即归还系统，适合大块内存分配（如 >128 KB）。每次分配/释放需处理页表映射，开销较大，但释放彻底，避免长期占用内存。此外，`mmap` 也常用于文件映射（将文件直接映射到内存）。

### 操作系统内存不足的时候会发生什么？

1. **缺页中断触发**
   程序访问已分配但未映射物理内存的虚拟地址时，CPU 产生缺页中断，内核尝试分配物理内存。
2. **内存回收**
   若无空闲物理内存，内核开始回收内存：
   - **后台回收（kswapd）**：内核线程异步回收，不阻塞进程。
   - **直接回收**：同步回收，阻塞进程直至获得足够内存。
3. **OOM Killer**
   若回收后仍无法满足需求，触发 OOM 机制，选择并杀死占用内存较高的进程，释放内存。

**可回收内存类型**：

- **文件页**：缓存磁盘数据或文件。
  - 干净页：直接释放。
  - 脏页：需先写回磁盘再释放。
- **匿名页**：无文件载体（如堆、栈），通过 Swap 换出到磁盘，再次访问时换入。

**回收策略**：基于 LRU 算法，维护活跃（active）和不活跃（inactive）链表，优先回收不常访问的内存页。

### 页面置换有哪些算法？

页面置换算法用于在缺页异常且物理内存已满时，选择换出哪个页面到磁盘，以腾出空间加载所需页面。其核心目标是**尽可能减少页面换入换出次数**，即降低缺页率。

常见算法如下：

1. **最佳页面 置换算法（OPT）**

- **思想**：置换未来最长时间内不再访问的页面。
- **特点**：理想化但无法实现（需预知未来访问序列），仅作为衡量其他算法性能的参考标准。

2. **先进先出 置换算法（FIFO）**

- **思想**：置换在内存中驻留时间最长的页面。
- **特点**：实现简单，但性能较差，可能出现**Belady异常**（分配的物理页增多，缺页率反而上升）。

3. **最近最久未使用 置换算法（LRU）**

- **思想**：置换最长时间未被访问的页面，利用“历史”推测“未来”。
- **特点**：性能较好，近似OPT，但硬件开销大（需维护全局链表并实时更新），实际应用较少。

4. **时钟页面 置换算法（Clock/NRU）**

- **思想**：将页面组织成环形链表，表针指向最老页面。每个页有访问位（0/1）。缺页时扫描：访问位为0则置换，为1则清零并移动指针继续扫描。
- **特点**：是对FIFO的改进，性能接近LRU，实现成本低，在现代操作系统中应用广泛。

5. **最不常用 置换算法（LFU）**

- **思想**：置换访问次数最少的页面。每个页维护访问计数器，缺页时淘汰计数值最小的页。
- **特点**：需硬件计数器支持，且可能“饿死”新进入但访问频繁的页（可通过定期衰减计数器值缓解）。

## 中断

### 什么是中断？

中断是CPU暂停当前任务，转而处理其他事件，处理完毕后再返回继续执行原任务的过程。它是硬件和操作系统实现并发、响应事件的核心机制。

1. **外部中断**

由CPU外部设备引发，通过特定引脚通知CPU。

- **可屏蔽中断**：通过INTR引脚传入，来自硬盘、网卡等外设。CPU可选择忽略或不立即处理，不影响系统基本运行。
- **不可屏蔽中断**：通过NMI引脚传入，如电源故障、硬件线路错误。此类中断至关重要，CPU必须立即响应，无法屏蔽。

2. **内部中断（异常）**

由CPU执行指令时内部检测到的事件引发，与外部硬件无关。

- **陷阱（Trap）**：**有意触发**的异常，用于请求系统服务或调试。执行特殊指令（如`int 0x80`）主动引发，处理完成后返回**下一条指令**继续执行。典型例子：系统调用。
- **故障（Fault）**：**可修复的意外**，由指令执行过程中检测到问题（如缺页、除零）引发。若能修复，处理程序将重新执行**引发故障的指令**；若无法修复，则报告错误。典型例子：缺页异常。
- **终止（Abort）**：**不可修复的致命错误**，如硬件故障或系统表损坏。处理程序直接终止当前程序，不再返回。

### 讲讲中断的流程

中断是CPU暂停当前程序，转而处理紧急事件，处理完成后再返回原程序继续执行的机制。其基本流程如下：

1. **中断请求**
   硬件设备（如外设）或软件（如系统调用）向CPU发送中断信号。
2. **中断响应**
   CPU在每条指令执行结束时检查中断信号。若检测到中断且未被屏蔽，则：
   - **保护现场**：硬件自动将程序计数器（PC）、状态寄存器等关键上下文压入栈中保存。
   - **查找中断向量**：根据中断号，从中断向量表（IDT）获取对应的中断处理程序入口地址。
3. **中断处理**
   CPU跳转到中断处理程序执行：
   - 保存更多寄存器状态（软件完成）。
   - 识别中断类型并进行相应处理（如读写磁盘、处理网络数据包）。
   - 清除中断请求，恢复允许中断（若需要）。
4. **中断返回**
   执行中断返回指令（如 `iret`）：
   - 恢复之前保存的上下文。
   - 返回到原程序被中断的位置继续执行。

### 中断的作用是什么？

中断使得计算机系统具备应对对处理突发事件的能力，提高了CPU的工作效率，如果没有中断系统，CPU就只能按照原来的程序编写的先后顺序，对各个外设进行查询和处理，即轮询工作方式，轮询方法貌似公平，但实际工作效率却很低，却不能及时响应紧急事件

## 网络 i/o

### 你了解过哪些io模型？

**1. 阻塞 I/O**
应用程序发起 I/O 操作后会被阻塞，直到操作完成才返回结果。适用于对实时性要求不高的场景。

**2. 非阻塞 I/O**
应用程序发起 I/O 操作后立即返回，不会被阻塞，但需要**不断轮询**或使用 `select`/`poll`/`epoll` 等系统调用来检查 I/O 是否完成。适合需要多路复用的场景，如同时处理多个 socket 连接的服务器。

**3. I/O 复用模型**
通过 `select`、`poll`、`epoll` 等系统调用，**应用程序**可以同时**等待**多个 I/O 操作，当其中任何一个准备就绪时被通知。适用于高并发服务端程序。

**4. 信号驱动 I/O 模型**
应用程序发起 I/O 操作后可继续其他工作，当 I/O 完成时**操作系统发送信号通知**。适合需要异步通知的场景，可提高并发能力。

**5. 异步 I/O 模型**
应用程序发起 I/O 操作后立即做其他事，由**内核完成全部 I/O**（包括数据复制），完成后通知应用程序。适合大量并发连接和高性能场景，能减少系统调用次数，提升效率。

### 服务器处理并发请求有哪几种方式？

- 单线程web服务器方式：web服务器一次处理一个请求，结束后读取并处理下一个请求，性能比较低，一次只能处理一个请求。
- 多进程/多线程web服务器：web服务器生成多个进程或线程并行处理多个用户请求，进程或线程可以按需或事先生成。有的web服务器应用程序为每个用户请求生成一个单独的进程或线程来进行响应，不过，一旦并发请求数量达到成千上万时，多个同时运行的进程或线程将会消耗大量的系统资源。（即每个进程只能响应一个请求，并且一个进程对应一个线程）
- I/O多路复用web服务器：web服务器可以I/O多路复用，达到只用一个线程就能监听和处理多个客户端的 i/o 事件。
- 多路复用多线程web服务器：将多进程和多路复用的功能结合起来形成的web服务器架构，其避免了让一个进程服务于过多的用户请求，并能充分利用多CPU主机所提供的计算能力。（这种架构可以理解为有多个进程，并且一个进程又生成多个线程，每个线程处理一个请求）

### 讲一下io多路复用

IO 多路复用是一种高效的 IO 处理方式，允许单个线程同时监视多个 socket（或其他文件描述符）的 IO 事件。当其中任何一个 socket 准备好进行读写操作时，线程就会被通知并处理。这种方式避免了为每个连接创建独立线程或进程，减少了线程上下文切换的开销，能够高效地处理大量并发连接。常见的实现包括 select、poll 和 epoll。

### select、poll、epoll 的区别是什么？

三者都是 **I/O 多路复用**系统调用函数，用于**单线程/进程**同时监听多个**文件描述符**（socket）的 I/O 事件。

**select**

- 使用**固定长度**的 `fd_set` 位图（默认 1024 位）表示监听集合，有**最大描述符数量限制**（通常 1024）。
- 每次调用需将**整个集合**从用户态拷贝到内核态，内核通过**遍历**集合检查事件，再将整个集合拷回用户态。用户态再次遍历找到就绪描述符。
- 效率随监听数增加而**线性下降**，且**两次遍历**和**两次拷贝**开销大。

**poll**

- 使用**动态数组**（`pollfd` 结构体链表）存储监听描述符，**突破了 select 的 1024 限制**（但仍受系统文件描述符上限影响）。
- 同样需要将**整个数组**拷贝到内核，内核**遍历**所有描述符检查事件，再拷回用户态。
- 没有最大数量限制，但依然存在**线性遍历**和**拷贝开销**，性能随并发数增长而下降。

**epoll**

- **专为处理大量描述符而设计**，解决了 select/poll 的**性能瓶颈**。
- **数据结构**：在内核中维护一个**红黑树**存储所有监听描述符，通过 `epoll_ctl` 增删改，**无需每次传全量集合**。
- **事件通知**：就绪描述符通过**回调机制**加入内核**就绪链表**，`epoll_wait` **只返回就绪的描述符**，无需遍历全部。
- **拷贝**：仅需将**就绪事件**拷贝到用户态，数据量小，效率高。
- **无上限**：最大监听数受系统文件描述符限制（可调），**适合 C10K 甚至更高并发**。

### epoll 的 边缘触发和水平触发有什么区别？

- **水平触发（LT，level-triggered）**：默认模式。只要描述符有数据可读（或可写），每次 `epoll_wait` 都会返回该描述符，直到数据处理完。编程简单，类似 select/poll。
- **边缘触发（ET，edge-triggered）**：仅当描述符状态发生变化（如从无数据变为有数据）时通知一次，后续即使数据未读完也不再通知。要求应用程序**必须一次性将数据读完**（通常配合非阻塞 I/O 循环读写直到返回 `EAGAIN`），否则可能遗漏数据。ET 模式能减少 `epoll_wait` 调用次数，效率更高，但编程复杂。

### redis，nginx，netty 是依赖什么做的这么高性能？

它们高性能的核心是**基于 I/O 多路复用**（如 epoll）实现的 **Reactor 网络模型**，能够高效处理海量并发连接。但在具体架构上各有不同：

- **Redis（6.0 之前）**：采用 **单 Reactor 单进程**模型。所有事件（连接、读写）都在一个进程中处理，利用 Reactor 分发。由于业务操作基于内存，速度极快，单进程避免了锁竞争，实现简单高效。
- **Netty**：采用 **多 Reactor 多线程**模型。主 Reactor 负责接收新连接，分发给多个子 Reactor（工作线程）处理读写和业务逻辑，充分利用多核 CPU，分工明确。
- **Nginx**：采用 **多 Reactor 多进程**模型（略有变体）。多个子进程各自运行一个 Reactor，通过**互斥锁**控制仅一个子进程能 `accept` 新连接，避免惊群；连接建立后完全由该子进程的 Reactor 负责后续 I/O 和请求处理，无需跨进程通信，性能极高。

### 零拷贝是什么？

**零拷贝**是一种避免在用户态与内核态之间进行多余数据拷贝的 I/O 优化技术，主要为了减少上下文切换次数和 CPU 数据拷贝开销，从而大幅提升文件传输等场景的性能。

**传统 I/O 方式**（以文件发送到网络为例）的流程为：硬盘 → 内核缓冲区（DMA 拷贝）→ 用户缓冲区（CPU 拷贝）→ 内核 Socket 缓冲区（CPU 拷贝）→ 网卡（DMA 拷贝）。其开销包括 4 次上下文切换（读、写各两次系统调用）和 4 次数据拷贝（2 次 DMA + 2 次 CPU 拷贝）。

**零拷贝方式**（如 `sendfile`）的流程为：硬盘 → 内核缓冲区（DMA 拷贝）→ 内核 Socket 缓冲区（仅需修改描述符，无需 CPU 拷贝）→ 网卡（DMA 拷贝）。其开销仅需 2 次上下文切换（一次 `sendfile` 调用）和 2 次数据拷贝（均为 DMA 拷贝，CPU 不参与数据搬移）。

相比传统方式，零拷贝减少 2 次上下文切换并完全消除 CPU 拷贝，性能可提升一倍以上，特别适合大文件传输、Web 服务器等场景。