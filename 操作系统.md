## 用户态和内核态

### 用户态和内核态的区别？

内核态和用户态是操作系统中的两种运行模式。它们的主要区别在于权限和可执行的操作：

内核态：

- CPU可以执行所有指令，包括特权指令

- 可以访问所有内存空间和硬件资源

- 操作系统内核运行在此模式下

用户态：

- CPU只能执行非特权指令

- 只能访问受限的内存空间（用户空间）

- 普通应用程序运行在此模式下

## 进程管理

### 线程和进程的区别是什么？

**进程**是操作系统资源分配的基本单位，拥有独立的地址空间（包括代码、数据、堆栈）以及文件描述符等系统资源，进程之间相互隔离。

**线程**是CPU调度的基本单位，也称为轻量级进程，它依附于进程存在，同一个进程内的多个线程共享该进程的地址空间和资源，但每个线程有自己的栈和程序计数器。

两者的核心区别主要体现在以下几个方面：

- **资源开销**上，进程的创建、切换和销毁成本较高，因为需要分配独立的内存空间和数据结构；线程则开销小得多，只需分配栈和寄存器上下文。
- **内存空间**方面，进程间内存隔离，互不干扰；线程则共享进程内存，方便数据交换但需注意同步问题。
- **通信方式**不同，进程间通信（IPC）较复杂，需借助管道、消息队列等机制；线程间可直接读写共享变量，但需配合锁等同步手段。
- **健壮性**上，进程隔离性好，单个进程崩溃不影响其他进程；线程则可能因一个线程的非法操作导致整个进程退出。因此，多进程适合需要高稳定性和隔离性的场景，多线程则适用于高并发、资源共享的任务。

### 进程，线程，协程的区别是什么？

**进程**是操作系统资源分配的基本单位，拥有独立的地址空间（包括代码、数据、堆栈）以及文件描述符等系统资源，进程之间相互隔离。
**线程**是CPU调度的基本单位，也称为轻量级进程，它依附于进程存在，同一个进程内的多个线程共享该进程的地址空间和资源，但每个线程有自己的栈和程序计数器。
**协程**是用户态的轻量级线程，由程序自身控制调度，一个线程内可以存在多个协程，协程间的切换不涉及内核态，因此开销极小。

三者的核心区别主要体现在以下几个方面：

- **资源开销**：进程的创建、切换和销毁开销最大（需分配独立内存空间、建立数据结构、页表切换）；线程开销较小，共享进程资源，切换仅需保存/恢复寄存器、栈指针；协程开销最小，完全在用户态管理，切换只需保存/恢复少量上下文（如程序计数器、寄存器），无需系统调用。
- **调度方式**：进程与线程由操作系统内核**抢占式**调度，线程有优先级，时间片用完或被高优先级线程抢占；协程由程序**协作式**调度（主动让出控制权，如`yield`或`await`），开发者可自行控制执行顺序。
- **内存空间**：进程间内存独立，互不干扰；线程共享所属进程的地址空间，可直接访问相同变量，但需同步机制；同一线程内的协程共享该线程的堆内存，栈通常独立或复用。
- **通信与同步**：进程间通信（IPC）较复杂，需借助管道、消息队列、共享内存、Socket等；线程间可直接读写共享变量，但需锁、信号量等同步手段；协程通常通过通道（如Go的channel）或异步编程模型通信，同一线程内的协程无需加锁即可共享数据。
- **并发能力与数量**：进程数量受系统资源限制，通常较少；线程数量受内核限制（过多线程导致频繁上下文切换）；协程可轻松创建成千上万个，适合高并发场景（如IO密集型应用）。
- **健壮性**：进程隔离性好，单个进程崩溃不影响其他进程；线程隔离性差，一个线程的非法操作可能导致整个进程退出；协程的崩溃通常仅影响自身，但若未妥善处理可能波及所在线程。

因此，**进程**适合需要强隔离、多机分布的任务（如独立服务）；**线程**适合资源共享、频繁交互的任务（如Web服务器处理请求）；**协程**适合高并发、IO密集、需精细控制执行流的场景（如异步编程、微服务）。

### **为什么进程崩溃不会对其他进程产生很大影响**

主要是因为：

- **进程隔离性**：每个进程都有自己独立的内存空间，当一个进程崩溃时，其内存空间会被操作系统回收，不会影响其他进程的内存空间。这种进程间的隔离性保证了一个进程崩溃不会直接影响其他进程的执行。
- **进程独立性**：每个进程都是独立运行的，它们之间不会共享资源，如文件、网络连接等。因此，一个进程的崩溃通常不会对其他进程的资源产生影响。

### 为什么进程之下还要设计线程？

设计线程的主要目的是为了**提高系统的并发处理能力和资源利用率**，同时弥补进程模型的不足。

如果只有进程而没有线程，将面临以下问题：

- **单进程无法高效并发**：一个进程一次只能执行一个任务，若遇到I/O阻塞，整个进程会暂停，导致CPU资源浪费。
- **多进程开销巨大**：虽然多进程可以实现并发，但进程的创建、切换和销毁需要分配独立地址空间、页表等，成本高昂；进程间通信（IPC）复杂且效率低（如管道、共享内存等），难以满足高频数据交换的需求。
- **资源限制**：系统可创建的进程数量受内存和CPU资源限制，无法支撑大规模并发任务（如数千个网络连接）。

### 多线程比单线程的优势，劣势？

- **优势**：提高程序的运行效率，可以充分利用多核处理器的资源，同时处理多个任务，加快程序的执行速度。
- **劣势**：存在多线程数据竞争访问的问题，需要通过锁机制来保证线程安全，增加了加锁的开销，并且还会有死锁的风险。多线程会消耗更多系统资源，如CPU和内存，因为每个线程都需要占用一定的内存和处理时间。

### 进程切换和线程切换的区别？

1. 进程切换：进程切换涉及到更多的内容，包括整个进程的地址空间、全局变量、文件描述符等。因此，进程切换的开销通常比线程切换大。
2. 线程切换：线程切换只涉及到线程的堆栈、寄存器和程序计数器等，不涉及进程级别的资源，因此线程切换的开销较小。

### 线程切换为什么比进程切换快，节省了什么资源？

- **地址空间共享**：同一进程内的线程共享地址空间，切换时**不需要切换页表**和刷新TLB（快表），避免了内存映射切换带来的开销。而进程切换必须切换页表，导致TLB刷新和缓存失效，内存访问延迟增加。
- **上下文保存内容少**：线程切换只需保存和恢复**线程私有资源**（如寄存器、程序计数器、栈指针），而进程切换需要保存和恢复**进程级资源**（如进程控制块PCB、文件描述符表、信号处理等），数据量更大，耗时更长。
- **系统资源复用**：线程直接复用所属进程的资源（如打开的文件、内存映射），无需重新加载，节省了资源初始化和重建的时间。