# Android 端跑酷游戏 AI Agent 项目面试宝典

**—— 第十八届全国大学生软件创新大赛 全国一等奖项目**

## 1. 项目概览（Opening）

**Q1: 请简单介绍一下你的这个项目。**

> **参考回答：**
> 这是一个基于 Android 平台的通用跑酷游戏 AI Agent 系统。项目获得了第十八届全国大学生软件创新大赛全国一等奖。
> 它的核心目标是利用计算机视觉和深度学习技术，让手机自动游玩《地铁跑酷》、《神庙逃亡》等跑酷类游戏。
> **技术上**，我采用 **TensorFlow Lite** 在端侧部署 AI 模型，利用 **MediaProjection** API 实时截取屏幕画面，通过 **OpenCV/图像处理** 进行预处理，最后使用 **AccessibilityService（无障碍服务）** 模拟人手操作，实现了一套“感知-决策-执行”的闭环系统。不仅支持自动跑图，还能通过 OCR 识别分数并记录，用于后续的模型评估。

**Q2: 这个项目的难点在哪里？**

> **参考回答：**
> 主要难点在于 **端侧的实时性** 和 **无需 Root 权限的通用性**。
>
> 1. **实时性**：跑酷游戏对延迟要求极高。我需要在 Android 设备上完成“截屏 -> 图像预处理 -> 模型推理 -> 模拟触控”的全流程，必须将端到端延迟控制在毫秒级。为此我开启了 TFLite 的 **NNAPI 加速** 和 **多线程推理**，并对图像进行了 **ROI（感兴趣区域）裁剪** 以减少计算量。
> 2. **通用性**：为了适配不同游戏，我设计了配置化的架构，针对不同游戏加载不同的 `.tflite` 模型和裁剪参数。
> 3. **系统限制**：在不 Root 的情况下，如何高效截屏和模拟操作是挑战。我深入研究了 Android 的 `MediaProjection` 和 `AccessibilityService` 机制解决了这个问题。

***

## 2. 核心技术深度追问（Deep Dive）

### 🧠 A. AI 模型与部署

**Q3: 你的 AI 模型是怎么在手机上运行的？**

> **参考回答：**
> 我使用了 **TensorFlow Lite (TFLite)** 框架。
>
> - **模型加载**：模型文件（如 `model_subway.tflite`）直接打包在 APK 的 assets 中。
> - **推理引擎**：在 `AI.java` 中，我配置了 `Interpreter.Options`，开启了 **4 线程（setNumThreads(4)）** 并尝试启用 **NNAPI (Android Neural Networks API)** 代理，利用手机的 NPU/GPU 进行硬件加速。
> - **输入输出**：模型的输入是 `224x224` 的 RGB 图像（ByteBuffer 格式），输出是动作的概率分布（Logits）。我在 Java 层实现了一个 Softmax 函数将 Logits 转为概率，选取置信度最高的动作。

**Q4: 图像预处理是怎么做的？直接把全屏传给模型吗？**

> **参考回答：**
> 不是的，直接处理全屏图像既慢又包含干扰信息。
> 我在 `AI.java` 中实现了 **ROI (Region of Interest) 裁剪**。
>
> - **裁剪**：针对《地铁跑酷》等竖屏游戏，我只截取屏幕中间偏下的核心区域（例如去除顶部的分数栏和底部的广告栏），这样能让模型更专注于障碍物。
> - **缩放与归一化**：将裁剪后的 Bitmap 缩放到模型需要的 `224x224` 尺寸，并将像素值从 `[0, 255]` 归一化到浮点数，存入 `ByteBuffer` 传给 TFLite。

### 👁️ B. 视觉感知（屏幕捕获）

**Q5: 你是如何获取手机屏幕画面的？为什么不用** **`adb screencap`？**

> **参考回答：**
> 为了保证高帧率和低延迟，我使用了 Android 原生的 **MediaProjection API**，配合 `ImageReader` 和 `VirtualDisplay`。
>
> - **架构设计**：我把截屏逻辑放在了一个前台服务 `ScreenCaptureService` 中，防止被系统杀后台。
> - **流程**：
>   1. 申请录屏权限。
>   2. 创建 `VirtualDisplay` 将屏幕内容投射到 `ImageReader` 的 Surface 上。
>   3. 通过 `ImageReader.setOnImageAvailableListener` 在后台线程实时获取 `Image` 对象，并转为 `Bitmap` 供 AI 消费。
> - **对比 ADB**：ADB 截图通常需要几百毫秒，且通过 USB 传输太慢；MediaProjection 是系统级内存拷贝，能达到 60FPS 的实时性。

**Q6: 项目中用到了 OCR（文字识别），是用来做什么的？**

> **参考回答：**
> OCR 主要用于 **结果评估和奖励机制**。
>
> - **场景**：当游戏结束时，我需要自动记录本次跑酷的得分。
> - **实现**：我集成了 **Google ML Kit** 的 Text Recognition SDK。
> - **触发机制**：我通过模板匹配（Template Matching）检测屏幕上是否出现了“保存/结束”按钮。一旦检测到游戏结束，就会截取屏幕右上角的分数区域，传给 ML Kit 识别出数字分数，并保存到本地 CSV 文件中，用于分析 AI 的表现。

### 👆 C. 自动化控制

**Q7: 在没有 Root 的情况下，你是怎么模拟滑动的？**

> **参考回答：**
> 我使用了 Android 的 **AccessibilityService（无障碍服务）**。
>
> - **服务实现**：编写了 `ScreenControlService`，继承自 `AccessibilityService`。
> - **手势生成**：利用 `dispatchGesture()` 方法和 `Path` 类。
>   - 比如 AI 决定“向左跳”，我就构建一个从屏幕中心向左滑动的 `Path`。
>   - 创建一个 `GestureDescription.StrokeDescription`，设置滑动时长（例如 50ms，模拟快速划动）。
> - **通信**：`ScreenCaptureService`（大脑）通过 `LocalBroadcastManager` 发送动作指令，`ScreenControlService`（手）接收到广播后立即执行手势。

***

## 3. 架构与工程能力（Architecture）

**Q8: 项目的整体架构是怎样的？**

> **参考回答：**
> 项目采用了 **Service-Oriented（面向服务）** 的解耦架构，主要分为三层：
>
> 1. **感知层 (ScreenCaptureService)**：负责“看”。运行在前台，持有 `MediaProjection` 实例，负责截屏、图像预处理、调用 TFLite 模型推理，以及通过 ML Kit 进行 OCR。
> 2. **执行层 (ScreenControlService)**：负责“做”。是一个独立的无障碍服务，负责将抽象的动作指令（如 `ACTION_JUMP`）转换为具体的屏幕手势。
> 3. **通信层**：两层之间通过 **LocalBroadcastManager** 进行进程内通信，保证了指令传输的高效和安全。
>    此外还有 `StatisticsManager` 负责数据统计，`ScoreStorage` 负责 CSV 数据持久化。

**Q9: 你提到用到了“强化学习”，具体体现在哪里？**

> **参考回答：**
> （*注：代码主要体现的是推理，这里需要结合训练流程回答*）
> 这个项目采用了 **“离线训练 + 端侧推理”** 的模式。
>
> - **训练阶段（PC端）**：我们收集了大量玩家的操作视频和对应的输入数据（Imitation Learning），以及通过模拟环境进行的强化学习训练（Reinforcement Learning），训练出一个策略网络。
> - **部署阶段（Android端）**：将训练好的模型导出为 `.tflite` 格式，部署到手机上。手机端主要负责执行这个策略（Inference），并通过 OCR 收集分数反馈，为下一轮模型的迭代优化提供数据支持。

***

## 4. 进阶技术与工程实践 (Advanced Engineering)

**Q10: 在高频截屏和推理下，你是如何管理内存防止 OOM (Out of Memory) 的？**

> **参考回答：**
> 这是一个非常关键的问题。
>
> 1. **Bitmap 显式回收**：在 `ScreenCaptureService` 中，我严格管理 Bitmap 的生命周期。每次从 `ImageReader` 获取到 Image 后，处理完会立即调用 `image.close()`。对于裁剪和缩放产生的中间 Bitmap（如 `regionBitmap` 和 `resizedBitmap`），我在使用完后都会在 `finally` 块中调用 `.recycle()`，确保 native 内存及时释放。
> 2. **缓冲区控制**：我限制了 `ImageReader` 的 maxImages（通常设为 2），避免生产速度过快导致堆积。
> 3. **主动 GC**：在 Service 销毁（`onDestroy`）时，我会主动调用 `System.gc()` 并清空所有引用，防止 Service 重启时内存累积。

**Q11: 项目中如何处理耗时操作？有没有用到多线程？**

> **参考回答：**
> 必须使用多线程，否则会阻塞主线程导致 ANR。
>
> 1. **HandlerThread**：我创建了一个独立的后台线程 `ScreenCaptureBackground`。`ImageReader` 的回调（OnImageAvailableListener）是绑定在这个后台 Handler 上的，因此图像转换、AI 推理（耗时约 20-50ms）和 OCR 识别都在后台线程执行，保证了 UI 的流畅。
> 2. **TFLite 并行**：在 AI 模型推理层面，我也开启了 4 线程并行计算（`setNumThreads(4)`），充分利用手机的多核 CPU。
> 3. **UI 更新**：当需要更新界面上的统计数据时，我会通过 `runOnUiThread` 切回主线程操作。

**Q12: 你是如何保证 AI 操作的准确性的？有什么机制防止乱操作？**

> **参考回答：**
> 我设计了 **置信度阈值 (Confidence Threshold)** 机制。
>
> - **动态阈值**：模型输出的是各个动作的概率分布。我设定了一个基准阈值（如 30%），只有当某个动作的概率超过这个值时，才会执行。
> - **差异化配置**：针对不同游戏，这个阈值是可配的。例如《地铁跑酷》节奏快，我把阈值设为 32%；而《神庙逃亡》设为 22%。
> - **误触防护**：我还引入了“疲劳检测”和“连续操作冷却”逻辑，防止 AI 在短时间内发出过多重复指令。

***

## 5. 业务场景与用户体验 (Business & UX)

**Q13: 你的项目不仅仅是一个“外挂”，它有哪些实际的应用场景？**
> **参考回答：**
> 是的，它不仅仅是一个自动脚本，而是一个智能 Agent。
> 1.  **辅助通关与教学**：对于卡关的新手玩家，AI 可以接管游戏进行演示，帮助玩家学习如何通过高难度路段。
> 2.  **疲劳驾驶接管**：我设计了 **“疲劳检测”** 功能。当系统通过图像匹配（Template Matching）检测到玩家连续多次失误（如频繁撞墙或掉坑），或者玩家主动开启辅助模式时，AI 会无缝接管游戏，让玩家休息片刻，避免因疲劳导致游戏失败，提升用户留存。
> 3.  **游戏测试自动化**：对于游戏开发者，这个 Agent 可以作为自动化测试工具（Monkey Test 的升级版），24 小时运行测试游戏的稳定性和关卡平衡性，通过 StatisticsManager 记录的 CSV 数据分析关卡难度。

**Q14: 你提到了“模拟真人操作”，具体是怎么体现的？**
> **参考回答：**
> 很多脚本操作生硬，容易被封号。我做了以下优化：
> *   **随机化延迟**：在 AI 做出决策后，我不会立即执行，而是加入了一个极短的随机延迟（10-30ms），模拟人的神经反应时间。
> *   **非线性滑动**：在 `ScreenControlService` 中，我使用 `Path` 构建贝塞尔曲线来模拟手指滑动的轨迹，而不是直线，使得操作看起来更像真人在划动屏幕。
> *   **动态时长**：滑动的时长（Duration）也不是固定的，而是根据滑动的距离动态计算（约 37ms），符合物理惯性。

---

## 6. 数据驱动与算法细节 (Data & Algorithm)

**Q15: 你是如何训练你的 AI 模型的？数据从哪来？**
> **参考回答：**
> 这是一个典型的 **“模仿学习 (Imitation Learning)”** 流程。
> 1.  **数据采集**：我开发了一个专门的数据采集模式。当我在玩游戏时，程序会在后台以 10Hz 的频率截取屏幕，并记录我当前的操作（如“左滑”、“跳跃”）。这样我就得到了一一对应的 `<Image, Label>` 数据对。
> 2.  **数据清洗**：人工剔除误操作和无效画面（如暂停界面、结算界面），保证数据的纯净度。
> 3.  **模型训练**：使用 TensorFlow 在 PC 端进行训练。我采用了 **MobileNetV2** 作为骨干网络（Backbone），因为它在移动端速度快且精度高。
> 4.  **量化压缩**：训练完成后，我使用了 **TFLite Converter** 进行权重量化（Quantization），将模型大小压缩到仅几 MB，大大降低了 APK 体积和推理耗时。

**Q16: 如果游戏更新了，界面变了，你的 AI 还能用吗？**
> **参考回答：**
> 这确实通过纯视觉方案面临的挑战，但我设计了 **配置化架构** 来应对。
> *   **ROI 动态配置**：如果只是 UI 布局微调（如分数栏变宽了），我只需要在 `AI.java` 中修改 `cropGameRegion` 的参数，调整裁剪区域即可，无需重新训练模型。
> *   **多模型切换**：如果是游戏场景大变（如《地铁跑酷》换了城市地图），我可以采集新地图的数据，训练一个新的 `.tflite` 模型，并通过 `assets` 动态下发或在设置里切换，保持系统的扩展性。

---

## 7. 开放性问题与反思 (Bonus)

**Q17: 遇到过最棘手的 Bug 是什么？怎么解决的？**

> **参考回答：**
>
> 1. **端侧训练 (On-device Training)**：目前 TFLite 主要是推理。未来可以尝试使用 TFLite 的 Transfer Learning 功能，让 AI 在玩家手机上边玩边学，根据玩家的习惯微调模型。
> 2. **模型结构优化**：引入 RNN 或 LSTM 处理时序信息。现在的模型主要是 CNN，只看当前这一帧画面。如果能结合过去几帧的画面，对动态障碍物的速度和方向判断会更准确。
> 3. **更通用的识别**：目前不同游戏的 ROI 需要手动配置，未来可以引入目标检测（如 YOLO-Nano）自动识别游戏区域和 UI 布局。

***

### 💡 给你的面试小贴士：

1. **展示代码熟悉度**：在回答时，可以不经意地提到类名（如 `ScreenCaptureService`、`AI.java`），这会让面试官觉得你对代码非常熟悉，是自己亲手写的。
2. **强调数据价值**：提到你不仅做了 AI，还做了数据收集（OCR 存 CSV），这体现了你具备数据驱动（Data-Driven）的思维，这是算法工程师非常看重的。
3. **自信表达**：毕竟是拿了一等奖的项目，在这个领域（移动端 AI + 游戏辅助）你已经是专家了。

