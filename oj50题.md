# OJ 50 题
- [1. 请你先做一个项目介绍](#1-请你先做一个项目介绍)
- [2. 这个系统后端整体架构是怎样的？](#2-这个系统后端整体架构是怎样的)
- [3. 为什么选择微服务架构，而不是单体架构？](#3-为什么选择微服务架构而不是单体架构)
- [4. 什么是微服务？它的优点是什么？](#4-什么是微服务它的优点是什么)
- [5. 你们为什么选择 Spring Cloud Alibaba？](#5-你们为什么选择-spring-cloud-alibaba)
- [6. 你们系统有哪些核心服务？](#6-你们系统有哪些核心服务)
- [7. 一次用户提交代码后的完整链路是什么？](#7-一次用户提交代码后的完整链路是什么)
- [8. 你在项目里主要负责哪些工作？](#8-你在项目里主要负责哪些工作)
- [9. 判题服务的核心职责是什么？](#9-判题服务的核心职责是什么)
- [10. 介绍一下判题机模块架构](#10-介绍一下判题机模块架构)
- [11. 为什么要抽象代码沙箱接口？](#11-为什么要抽象代码沙箱接口)
- [12. 你们有哪些代码沙箱实现类？](#12-你们有哪些代码沙箱实现类)
- [13. 为什么使用工厂模式来创建代码沙箱？](#13-为什么使用工厂模式来创建代码沙箱)
- [14. 为什么说你是“静态工厂模式 + Spring 配置化”？](#14-为什么说你是静态工厂模式--spring-配置化)
- [15. 为什么还用了代理模式封装代码沙箱？](#15-为什么还用了代理模式封装代码沙箱)
- [16. 为什么不同语言要用策略模式封装判题算法？](#16-为什么不同语言要用策略模式封装判题算法)
- [17. 你的策略模式是怎么落地的？](#17-你的策略模式是怎么落地的)
- [18. 判题结果是如何判断出来的？](#18-判题结果是如何判断出来的)
- [19. 你们支持哪些语言？](#19-你们支持哪些语言)
- [20. Java 原生代码沙箱是怎么实现的？](#20-java-原生代码沙箱是怎么实现的)
- [21. Docker 代码沙箱是怎么实现的？](#21-docker-代码沙箱是怎么实现的)
- [22. Java 原生代码沙箱和 Docker 代码沙箱的区别是什么？](#22-java-原生代码沙箱和-docker-代码沙箱的区别是什么)
- [23. 为什么要使用模板方法模式定义代码沙箱流程？](#23-为什么要使用模板方法模式定义代码沙箱流程)
- [24. 什么是 Java 安全管理器？你在项目里怎么用的？](#24-什么是-java-安全管理器你在项目里怎么用的)
- [25. 你们是怎么做超时控制的？](#25-你们是怎么做超时控制的)
- [26. 你们是怎么做内存限制的？](#26-你们是怎么做内存限制的)
- [27. 只用 `-Xmx` 够吗？](#27-只用--xmx-够吗)
- [28. Docker 代码沙箱的安全性是怎么保证的？](#28-docker-代码沙箱的安全性是怎么保证的)
- [29. 你们为什么要用 RabbitMQ 处理判题任务？](#29-你们为什么要用-rabbitmq-处理判题任务)
- [30. 为什么选择 RabbitMQ，而不是 Kafka 或 RocketMQ？](#30-为什么选择-rabbitmq而不是-kafka-或-rocketmq)
- [31. 你们怎么保证消息可靠性？](#31-你们怎么保证消息可靠性)
- [32. 什么是 Nacos？你们项目里怎么用的？](#32-什么是-nacos你们项目里怎么用的)
- [33. 什么是 OpenFeign？你们项目中哪些地方用了它？](#33-什么是-openfeign你们项目中哪些地方用了它)
- [34. 你们项目中 Gateway 的作用是什么？](#34-你们项目中-gateway-的作用是什么)
- [35. 你们的 Gateway 路由是怎么实现的？](#35-你们的-gateway-路由是怎么实现的)
- [36. Gateway 和 Nginx 的区别是什么？](#36-gateway-和-nginx-的区别是什么)
- [37. 你们是怎么解决跨域问题的？](#37-你们是怎么解决跨域问题的)
- [38. 为什么要在 Gateway 层做跨域，而不是在每个服务里做？](#38-为什么要在-gateway-层做跨域而不是在每个服务里做)
- [39. 你们如何通过 GlobalFilter 保护内部接口？](#39-你们如何通过-globalfilter-保护内部接口)
- [40. 你们的接口文档聚合是怎么做的？](#40-你们的接口文档聚合是怎么做的)
- [41. 你们库表是怎么设计的？](#41-你们库表是怎么设计的)
- [42. 为什么题目配置用 JSON 存，而不是拆成多个字段？](#42-为什么题目配置用-json-存而不是拆成多个字段)
- [43. 题目提交表里的状态字段有什么作用？](#43-题目提交表里的状态字段有什么作用)
- [44. 你们如何保证代码沙箱服务接口安全？](#44-你们如何保证代码沙箱服务接口安全)
- [45. 你们的 GPT 评测模块是做什么的？](#45-你们的-gpt-评测模块是做什么的)
- [46. 什么叫“上下文感知的智能代码建议”？](#46-什么叫上下文感知的智能代码建议)
- [47. 责任链模式在智能建议里是怎么用的？](#47-责任链模式在智能建议里是怎么用的)
- [48. 为什么这里用责任链模式，而不是简单 if-else？](#48-为什么这里用责任链模式而不是简单-if-else)
- [49. 你们如何避免 AI 直接把答案告诉学生？](#49-你们如何避免-ai-直接把答案告诉学生)
- [50. 这个项目最大的亮点和难点分别是什么？](#50-这个项目最大的亮点和难点分别是什么)

------

## 1. 请你先做一个项目介绍

我做的是一个 **OJ 在线判题系统 / 智能编程练习与评测平台**，整体采用 **B/S 架构**，前端使用 **Vue3**，后端基于 **Spring Boot + Spring Cloud Alibaba** 搭建微服务，核心模块包括 **用户服务、题目服务、判题服务、代码沙箱、GPT 评测模块、网关服务**。

这个系统除了具备传统 OJ 的题目管理、代码提交、自动判题功能外，还加入了 **AI 辅助教学能力**，能够结合题目、用户提交代码和判题结果，给出分层的智能建议。

我主要负责的部分包括：

- 判题机模块设计
- 代码沙箱抽象与实现
- 不同语言判题策略设计
- Gateway 路由与跨域处理
- 基于责任链的智能代码建议模块

我认为这个项目最有技术含量的点有两个：

1. **判题服务 + 代码沙箱的解耦设计**
2. **在自动评测基础上融合 AI 辅助教学能力**

------

## 2. 这个系统后端整体架构是怎样的？

系统后端是典型的微服务架构，主要分为以下模块：

- **用户服务**：负责用户注册、登录、用户信息管理
- **题目服务**：负责题目的增删改查、题目浏览、题目提交
- **判题服务**：负责获取题目信息和提交信息，调用代码沙箱执行代码，并完成结果判定
- **代码沙箱服务**：负责代码编译、运行、收集执行结果
- **GPT 评测模块**：负责结合上下文给出智能代码建议
- **网关服务**：统一接收前端请求，做路由转发、接口保护、跨域处理
- **公共模块**：放通用数据模型、响应封装、工具类、异常处理等

模块间关系主要是：

1. 前端所有请求先进入 **Gateway**
2. 题目服务在用户提交代码后，将判题任务交给 **判题服务**
3. 判题服务会调用 **题目服务** 获取题目和测试用例，再调用 **代码沙箱**
4. GPT 评测模块会结合题目、代码、判题结果生成建议

------

## 3. 为什么选择微服务架构，而不是单体架构？

主要原因是 **解耦和隔离重服务**。

在这个项目里，**判题服务**是最重的部分，因为它要调用代码沙箱执行用户代码，存在资源消耗高、执行慢、甚至异常阻塞的风险。如果把它和用户、题目这些核心业务都放在一个单体应用里，那么一旦判题服务压力过大，就可能影响整个系统。

采用微服务后：

- **用户服务、题目服务、判题服务** 能独立部署
- 判题服务可以单独扩容
- 即使判题服务压力大，也不会影响用户浏览题目、登录等核心功能
- 后期也更方便加入 AI 评测模块、消息队列等能力

所以这个项目使用微服务，核心目标是 **提升可维护性、可扩展性和容错性**。

------

## 4. 什么是微服务？它的优点是什么？

微服务是一种架构风格，它把一个大而全的单体应用拆分为多个职责单一、可独立部署和维护的小服务，服务之间通过网络通信协作完成系统功能。

它的主要优点有：

- **职责清晰**：每个服务只做一类事情
- **独立部署**：某个服务更新不影响整个系统
- **可扩展性强**：热点服务可以单独扩容，比如判题服务
- **容错性更好**：一个服务异常不会拖垮整个系统
- **便于团队协作**：不同人可以负责不同模块

在我的项目里，微服务主要帮助我们把 **判题重服务** 和 **核心业务服务** 解耦开。

------

## 5. 你们为什么选择 Spring Cloud Alibaba？

因为项目本身是 Spring Boot 技术栈，使用 Spring Cloud Alibaba 比较自然，它对微服务开发支持比较完整，尤其适合中小型分布式项目快速搭建。

它提供了很多常用组件，比如：

- **Nacos**：服务注册与配置中心
- **OpenFeign**：服务间远程调用
- **Gateway**：网关路由
- **RabbitMQ**：异步解耦
- **Sentinel**：熔断限流

在我的项目里，主要实际用到的是：

- **Nacos**
- **OpenFeign**
- **Spring Cloud Gateway**
- **RabbitMQ**

选择它的原因是：**生态比较完整，和 Spring 体系整合度高，开发效率高**。

------

## 6. 你们系统有哪些核心服务？

核心服务主要有 4 类：

1. **用户服务**
    负责注册、登录、用户管理
2. **题目服务**
    负责题目创建、修改、删除、浏览，以及题目提交记录管理
3. **判题服务**
    负责接收判题任务，拉取题目和提交信息，调用代码沙箱执行代码，再根据结果给出最终判题状态
4. **代码沙箱服务**
    负责真正执行用户代码，包括编译、运行、获取输出、异常处理、资源限制等

此外还有：

- **网关服务**：统一入口
- **GPT 评测模块**：提供智能辅助教学建议
- **公共模块**：沉淀通用代码

------

## 7. 一次用户提交代码后的完整链路是什么？

完整链路大致如下：

1. 用户在前端做题页面编写代码并点击提交
2. 前端调用网关暴露的题目提交接口
3. 网关把请求转发到题目服务
4. 题目服务先校验用户是否登录、题目是否存在、语言是否合法
5. 校验通过后，把提交记录写入题目提交表，状态初始设为“待判题”或“判题中”
6. 题目服务通过消息队列或服务调用触发判题服务
7. 判题服务根据提交 ID 获取提交信息和题目信息，包括测试用例、时间限制、内存限制等
8. 判题服务构造代码沙箱请求，调用代码沙箱执行用户代码
9. 代码沙箱返回编译结果、运行结果、耗时、内存等信息
10. 判题服务根据语言策略和测试用例进行判定，得出结果，如 AC、WA、TLE、MLE、RE、CE
11. 判题结果回写数据库
12. 前端查询提交记录或轮询获取最终结果
13. 如果开启 AI 辅助教学，还会进一步生成针对性的代码建议

------

## 8. 你在项目里主要负责哪些工作？

我在项目里主要负责以下几部分：

- 设计 **判题机模块架构**
- 抽象 **代码沙箱调用接口**，实现多种沙箱方式
- 使用 **工厂模式** 实现沙箱实例获取
- 使用 **代理模式** 对沙箱调用加日志增强
- 使用 **策略模式** 封装不同语言的判题逻辑
- 编写脚本测试代码沙箱，处理超时、中断、内存限制等异常场景
- 配置 **Spring Cloud Gateway** 做统一路由和跨域处理
- 实现智能代码建议模块，使用 **责任链模式** 做上下文感知的分层指导

------

## 9. 判题服务的核心职责是什么？

判题服务的核心职责不是直接执行代码，而是做 **判题调度和判题逻辑处理**，主要包括：

- 查询题目提交记录和题目信息
- 获取测试用例、时间限制、内存限制
- 调用代码沙箱执行用户代码
- 收集代码沙箱返回的输出、错误信息、耗时、内存等数据
- 根据判题规则比对测试用例结果
- 得出最终判题状态并更新数据库

所以可以理解为：

- **代码沙箱负责执行**
- **判题服务负责判断**

这样职责更清晰，也便于后续扩展不同的沙箱实现。

------

## 10. 介绍一下判题机模块架构

判题机模块的核心流程是：

1. 根据提交 ID 查询题目提交信息
2. 查询题目详情和测试用例
3. 组装代码沙箱请求，包括代码、输入用例、语言、时间限制、内存限制等
4. 调用代码沙箱执行用户代码
5. 获取执行结果
6. 使用判题策略做结果比对
7. 更新判题结果

为了让模块具备更好的扩展性，我做了几件事：

- 抽象了统一的 **代码沙箱接口**
- 提供了多种实现类，如示例沙箱、远程沙箱、第三方沙箱
- 用 **工厂模式** 统一创建沙箱实例
- 用 **代理模式** 对沙箱调用做日志增强
- 用 **策略模式** 封装不同语言的判题逻辑

这样后续新增沙箱类型或新增语言时，对原有代码影响比较小。

------

## 11. 为什么要抽象代码沙箱接口？

抽象代码沙箱接口主要是为了 **解耦和扩展性**。

因为代码沙箱的实现方式不止一种，比如：

- 示例沙箱：用于跑通流程
- 远程代码沙箱：调用自研沙箱服务
- 第三方代码沙箱：调用外部平台接口
- 本地 Java 原生沙箱
- Docker 沙箱

如果在业务里直接写死某一种实现，那么后续切换沙箱、增加新沙箱就要改很多业务代码。

所以我定义了统一的 `CodeSandbox` 接口，对外只暴露统一的执行方法，比如 `executeCode()`。
 业务层只依赖接口，不依赖具体实现，这样就实现了：

- 沙箱实现可插拔
- 更容易测试
- 更便于切换不同环境下的沙箱实现

------

## 12. 你们有哪些代码沙箱实现类？

根据项目材料，我这边主要设计过以下几种：

- **ExampleCodeSandboxImpl**
   用于快速跑通业务流程，不真正执行复杂逻辑
- **RemoteCodeSandboxImpl**
   调用自己开发的远程代码沙箱服务
- **ThirdPartyCodeSandboxImpl**
   调用第三方现成代码沙箱服务

此外在实现方式上，还做过：

- **Java 原生代码沙箱**
- **Docker 代码沙箱**

其中在实际项目中，主要使用的是 **远程代码沙箱**，因为这样可以把执行不可信代码的风险和判题业务本身隔离开。

------

## 13. 为什么使用工厂模式来创建代码沙箱？

因为代码沙箱有多种实现，如果每次都在业务里 `new` 具体类，或者写很多 if-else 来判断，就会让业务层和具体实现强耦合。

所以我使用了 **简单工厂模式**：

- 根据沙箱类型字符串
- 由 `CodeSandboxFactory` 返回对应的实现类

这样做的好处是：

1. **统一创建逻辑**
    业务层不需要关心具体实例化细节
2. **降低耦合**
    业务层只依赖抽象接口
3. **便于扩展**
    新增一个沙箱实现，只需要扩展工厂逻辑

此外，我还把沙箱类型做成了 **配置化**，可以通过修改 `application.yml` 切换具体实现。

------

## 14. 为什么说你是“静态工厂模式 + Spring 配置化”？

因为我的代码沙箱实例获取方式，本质上是通过一个工厂类根据配置返回对应实现，这个工厂方法本身是静态的，所以可以看作 **静态工厂** 或 **简单工厂**。

而沙箱具体使用哪一种实现，不是在代码里写死，而是通过配置文件指定，比如：

- example
- remote
- thirdParty

程序启动后读取配置，再通过工厂获取实例。这样做的价值是：

- 切换沙箱不需要改业务代码
- 本地开发、测试、生产环境可以用不同实现
- 提高系统灵活性

------

## 15. 为什么还用了代理模式封装代码沙箱？

因为我希望在不修改具体沙箱实现类的前提下，对沙箱调用过程做统一增强，比如：

- 打印请求参数
- 打印响应结果
- 记录调用耗时
- 便于排查问题和调试

所以我写了一个 `CodeSandboxProxy`，它也实现 `CodeSandbox` 接口，内部持有真实的沙箱实现对象。在调用 `executeCode()` 时，先做日志记录，再调用真实对象，最后再记录结果。

这样做符合代理模式的思想，也避免了把日志代码散落到各个沙箱实现类里。

------

## 16. 为什么不同语言要用策略模式封装判题算法？

因为不同语言的判题逻辑并不完全相同。

比如：

- Java 可能在运行时需要适当放宽时间和内存限制
- 不同语言的编译和执行行为不同
- 某些语言的错误信息、资源消耗特征也不同

如果把这些逻辑全部写在一个 service 里，用大量 if-else 区分，代码会非常臃肿，维护性也差。

所以我用了 **策略模式**：

- 定义统一的 `JudgeStrategy` 接口
- 针对不同语言实现不同的策略类
- 由 `JudgeManager` 根据语言选择对应策略执行

这样可以让不同语言的判题逻辑独立演化，提升代码的可维护性和扩展性。

------

## 17. 你的策略模式是怎么落地的？

我这边的落地方式大概是：

1. 定义一个统一的判题策略接口 `JudgeStrategy`

2. 接口中提供 `doJudge()` 之类的方法

3. 定义 

   ```
   JudgeContext
   ```

    作为上下文对象，封装判题需要的信息，比如：

   - 题目信息
   - 提交记录
   - 测试用例
   - 代码沙箱返回结果

4. 针对不同语言实现具体策略类，比如：

   - `DefaultJudgeStrategy`
   - `JavaLanguageJudgeStrategy`
   - `Python3LanguageJudgeStrategy`

5. 定义 `JudgeManager`，根据编程语言选择对应策略

业务层只需要把上下文交给 `JudgeManager`，不用自己写复杂判断。

------

## 18. 判题结果是如何判断出来的？

判题服务会先拿到代码沙箱返回的执行结果，然后按一定顺序做校验。

大体流程是：

1. **先看代码沙箱返回的 message**
   - 如果有编译错误或运行异常信息，优先判定为错误类结果
2. **对比输出结果数量**
   - 如果输出条数和预期测试用例条数不一致，通常说明结果有问题
3. **逐个对比输出值**
   - 如果有任意一个测试点输出和标准答案不一致，则判为错误答案
4. **检查资源限制**
   - 判断耗时是否超过题目时间限制
   - 判断内存是否超过题目内存限制

最后给出结果，比如：

- Accepted
- Wrong Answer
- Time Limit Exceeded
- Memory Limit Exceeded
- Runtime Error
- Compile Error

------

## 19. 你们支持哪些语言？

根据论文材料，当前系统主要支持：

- **Java**
- **Python**

并且在策略模式设计上，已经为多语言扩展预留了结构，比如：

- `JavaLanguageJudgeStrategy`
- `Python3LanguageJudgeStrategy`

如果后续需要支持 C++、Go 等语言，也可以继续新增对应的策略类和沙箱执行逻辑。

------

## 20. Java 原生代码沙箱是怎么实现的？

Java 原生代码沙箱的核心流程是：

1. 把用户提交的代码保存成文件
2. 调用 `javac` 编译，生成 class 文件
3. 通过 `Runtime.exec` 或 `ProcessBuilder` 执行 Java 程序
4. 读取进程的标准输出和错误输出
5. 收集运行时间、执行结果等信息
6. 执行完成后清理临时文件

它的优点是实现简单、容易验证流程；
 缺点是安全性较弱，因为本质上还是在宿主机环境直接运行代码，隔离能力有限。

------

## 21. Docker 代码沙箱是怎么实现的？

Docker 代码沙箱和原生沙箱在业务流程上类似，但运行环境不同。

大致流程是：

1. 把用户代码保存到宿主机的某个临时目录
2. 基于预先准备好的 Java / Python 镜像创建容器
3. 把代码目录挂载到容器内，或者将代码复制进容器
4. 通过 Docker 的 exec 能力，在容器内执行编译和运行命令
5. 获取容器中的标准输出、错误输出、资源占用信息
6. 任务结束后销毁容器并清理文件

相比原生沙箱，Docker 方案的主要优势是：

- 环境隔离更强
- 更适合执行不可信代码
- 更容易做 CPU、内存、网络等资源限制

------

## 22. Java 原生代码沙箱和 Docker 代码沙箱的区别是什么？

两者核心业务流程类似，都是“保存代码 -> 编译 -> 运行 -> 收集结果 -> 清理资源”，但主要区别在执行环境和安全性上。

**Java 原生沙箱：**

- 直接在宿主机执行命令
- 实现简单
- 调试方便
- 但安全性较弱，容易影响宿主机

**Docker 沙箱：**

- 在容器中执行代码
- 可以做资源隔离和权限隔离
- 更安全、更适合生产环境
- 但实现复杂度和运行成本更高

所以通常我会说：

- 原生沙箱适合开发验证
- Docker 沙箱更适合正式执行环境

------

## 23. 为什么要使用模板方法模式定义代码沙箱流程？

因为不同代码沙箱虽然实现方式不同，但核心执行流程是高度相似的，比如：

- 保存代码
- 编译代码
- 执行代码
- 收集输出
- 文件清理
- 错误处理

这些步骤可以抽象成一个固定的流程骨架，所以我用了 **模板方法模式**。

具体做法是：

- 定义抽象模板类，比如 `CommonCodeSandboxTemplate`
- 在模板类中定义统一的执行流程
- 把不同环节拆成独立方法
- 子类按需复用或重写某些步骤

这样做的好处是：

- 减少重复代码
- 流程更清晰
- 扩展新沙箱时更方便

------

## 24. 什么是 Java 安全管理器？你在项目里怎么用的？

Java 安全管理器是 Java 提供的一种安全控制机制，可以限制程序访问某些系统资源和执行某些危险操作。

在项目里，我通过继承 `SecurityManager` 自定义了一个安全管理器，重点重写了一些检查方法，比如：

- `checkWrite()`：限制用户代码写文件
- `checkExec()`：限制用户代码执行外部命令

然后在执行用户 Java 程序时，通过 JVM 参数绑定安全管理器，比如：

```bash
-Djava.security.manager=MySecurityManager
```

这样在一定程度上可以限制用户代码执行危险行为。

不过我也会补充说明：
 **Java 安全管理器只能作为辅助措施，真正更可靠的隔离还是要靠容器和系统级资源限制。**

------

## 25. 你们是怎么做超时控制的？

超时控制的核心目的是防止用户程序死循环或者长时间阻塞。

在项目里，我测试过通过线程等待和进程控制的方式处理中断逻辑，本质上就是：

- 启动用户程序后记录执行时间
- 如果超过设定时间阈值
- 就强制终止执行进程

如果是在 Docker 容器里执行，也可以在执行命令时设置超时参数，超时后中断容器内命令。

面试时我一般会补一句更稳妥的话：

> 从工程角度，比较可靠的方式是基于 `Process.waitFor(timeout, TimeUnit)` 或容器执行超时机制来做，而不是只靠普通线程 sleep。

这样会显得你考虑得更严谨。

------

## 26. 你们是怎么做内存限制的？

在 Java 程序执行时，我主要通过 JVM 参数来限制堆内存，比如：

- `-Xmx`：限制最大堆内存

例如设置：

```bash
-Xmx256m
```

这样用户程序如果申请过多堆内存，就可能触发 OOM，从而避免无限制占用资源。

在 Docker 沙箱场景下，我还可以通过容器配置进一步限制：

- 最大内存
- CPU 占用

所以项目中的内存限制主要有两层：

1. **JVM 层面的 `-Xmx`**
2. **容器层面的资源限制**

前者适用于 Java 程序，后者更适合做统一资源保护。

------

## 27. 只用 `-Xmx` 够吗？

严格来说，**不够**。

因为 `-Xmx` 只能限制 Java 堆内存，不能完全限制：

- 栈内存
- 直接内存
- 本地方法占用内存
- 其他系统资源

所以如果面试官深入追问，我会回答：

> `-Xmx` 只是一个基础措施，适合先限制 Java 程序的主要堆内存占用，但更完整的资源控制还需要结合 Docker 的内存限制、CPU 限制，以及文件权限、网络权限控制。

这样回答会比单纯说 `-Xmx` 更完整。

------

## 28. Docker 代码沙箱的安全性是怎么保证的？

Docker 本身提供了容器隔离，但它不是绝对安全的，所以我又从几个方面做了增强：

1. **超时控制**
    程序执行超时自动中断，防止死循环占用资源
2. **资源限制**
    创建容器时通过 `HostConfig` 限制最大内存和 CPU 占用
3. **网络限制**
    使用 `withNetworkDisabled(true)` 禁止容器访问网络
4. **权限限制**
    结合 seccomp 或 Java 安全管理器，限制危险系统调用和文件操作

所以整体思路是：
 **容器隔离 + 资源限制 + 权限限制 + 超时控制**

------

## 29. 你们为什么要用 RabbitMQ 处理判题任务？

因为判题服务是一个相对重的服务，执行用户代码比较耗时，如果让题目服务同步调用判题服务，会导致用户提交请求等待时间过长，也会增加服务间耦合。

引入 RabbitMQ 后，流程变成：

1. 用户提交代码
2. 题目服务把提交记录写库
3. 题目服务发送判题消息到队列
4. 判题服务异步消费消息并完成判题
5. 判题结果异步回写数据库

这样做有几个好处：

- **异步解耦**
- **削峰填谷**
- **提升用户体验**
- **即使判题服务繁忙，题目服务也能正常处理提交请求**

------

## 30. 为什么选择 RabbitMQ，而不是 Kafka 或 RocketMQ？

这是一个技术选型问题。我的回答一般会这样组织：

我了解过主流消息队列，比如：

- **RabbitMQ**
- **Kafka**
- **RocketMQ**

它们各有侧重点：

- **Kafka**：吞吐量高，适合日志、流处理、大数据场景
- **RocketMQ**：功能完善，适合复杂业务场景
- **RabbitMQ**：上手快、时效性好、支持确认机制、死信队列等，适合中小型业务系统

结合我的项目场景：

- 用户量不是特别大
- 判题任务更强调业务解耦和可靠投递
- 不需要超高吞吐的流式处理

所以选 RabbitMQ 更合适，原因是：

- 实现成本低
- 学习和开发效率高
- 功能足够满足需求

------

## 31. 你们怎么保证消息可靠性？

在 RabbitMQ 场景下，保证消息可靠性通常会从几个方面考虑：

1. **消息确认机制**
   - 消费成功后手动 ack
   - 避免消息还没处理完就丢失
2. **消费失败处理**
   - 失败重试
   - 多次失败进入死信队列
3. **幂等处理**
   - 防止消息重复消费导致重复判题
4. **持久化**
   - 队列和消息都做持久化配置

在项目里，判题任务本身也可以结合提交状态字段做幂等控制，比如如果某条提交已经完成判题，就不再重复执行。

------

## 32. 什么是 Nacos？你们项目里怎么用的？

Nacos 可以简单理解为 **服务注册中心 + 配置中心**。

在我的项目里，几乎所有微服务都会和 Nacos 交互，比如：

- 用户服务
- 题目服务
- 判题服务
- 网关服务

主要用途有两个：

1. **服务注册与发现**
   - 服务启动后注册到 Nacos
   - 其他服务通过服务名找到目标实例
2. **配置管理**
   - 把部分配置集中管理，便于维护和动态调整

例如 Gateway 会通过 Nacos 找到题目服务、用户服务、判题服务的地址，再完成请求转发。

------

## 33. 什么是 OpenFeign？你们项目中哪些地方用了它？

OpenFeign 是一个声明式的 HTTP 客户端，它可以把远程 HTTP 调用封装成 Java 接口调用，减少手写请求代码。

在我的项目里，主要用于微服务之间的调用，比如：

- 判题服务调用题目服务，获取题目信息和提交信息
- 题目服务或其他模块查询用户服务获取用户信息
- 更新题目提交状态时，服务之间也会有对应调用

使用 OpenFeign 的好处是：

- 开发方便
- 代码可读性更强
- 服务调用关系更清晰

------

## 34. 你们项目中 Gateway 的作用是什么？

Gateway 是系统的统一入口，主要作用包括：

- **统一接收前端请求**
- **路由转发到对应业务服务**
- **全局跨域处理**
- **接口保护**
- **统一做一些公共逻辑**

在我的项目里，Gateway 主要落地了这几件事：

1. 根据请求路径把请求转发到用户服务、题目服务、判题服务
2. 配置全局跨域，避免前后端联调时跨域问题
3. 通过自定义 `GlobalFilter` 拦截内部接口，防止外部直接访问

------

## 35. 你们的 Gateway 路由是怎么实现的？

路由主要是通过 **Nacos 服务注册发现 + Gateway 配置** 实现的。

比如在配置里定义：

- `/api/user/**` 转发到用户服务
- `/api/question/**` 转发到题目服务
- `/api/judge/**` 转发到判题服务

具体实现上：

- Gateway 收到请求后
- 根据 `Path` 规则匹配路由
- 通过 `lb://服务名` 去 Nacos 查找可用实例
- 最终完成转发

这种方式的好处是：

- 前端只需要访问一个统一入口
- 服务地址变化时前端无感知
- 方便统一做权限和公共逻辑处理

------

## 36. Gateway 和 Nginx 的区别是什么？

两者都可以做请求转发，但侧重点不一样。

**Nginx** 更偏基础设施层，擅长：

- 反向代理
- 静态资源服务
- 负载均衡
- 高性能网络转发

**Spring Cloud Gateway** 更偏应用层网关，擅长：

- 和 Spring 生态深度集成
- 动态路由
- 过滤器扩展
- 认证鉴权、统一日志、限流等业务逻辑

在我的项目里，Gateway 更适合承接微服务内部的路由与治理需求。

------

## 37. 你们是怎么解决跨域问题的？

我是在 Gateway 层通过自定义 `CorsWebFilter` 来全局解决跨域问题的。

这样做的原因是：

- Gateway 是统一入口
- 在这一层处理最集中
- 前端只要访问网关，就不需要每个服务单独配置跨域

配置的核心内容一般包括：

- 允许的源
- 允许的方法
- 允许的请求头
- 是否允许携带凭证

这种方式比在每个 Controller 上单独写 `@CrossOrigin` 更统一、更适合微服务场景。

------

## 38. 为什么要在 Gateway 层做跨域，而不是在每个服务里做？

主要是因为 Gateway 是统一流量入口。

如果在每个服务里分别处理跨域，会有几个问题：

- 配置分散，维护成本高
- 多个服务配置不一致容易出问题
- 前端实际上是统一访问 Gateway，不是直接访问下游服务

所以在 Gateway 层统一处理跨域更合理，可以做到：

- 统一配置
- 减少重复工作
- 更符合微服务统一入口的思想

------

## 39. 你们如何通过 GlobalFilter 保护内部接口？

我实现了一个自定义的 `GlobalFilter`，在请求进入网关后统一拦截。

具体思路是：

1. 获取当前请求路径
2. 判断路径里是否包含约定的内部接口标识，比如 `inner`
3. 如果是内部接口，则直接返回 `403 FORBIDDEN`
4. 否则放行

为了让这个过滤器尽早执行，我还实现了 `Ordered` 接口，并把优先级设得比较高。

这样做的目的是：
 **防止服务内部相互调用的接口被外部直接访问**。

------

## 40. 你们的接口文档聚合是怎么做的？

接口文档这块使用的是 **Knife4j**。

具体做法是：

1. 给各个业务服务引入 Knife4j 依赖，并开启接口文档
2. 在网关服务中引入 Knife4j Gateway 聚合依赖
3. 配置 discover 模式，让网关自动发现各服务文档
4. 最终通过网关统一访问 `doc.html`

这样做的好处是：

- 不需要分别访问各个服务的文档地址
- 对前后端联调和测试更方便
- 微服务接口文档统一管理更清晰

------

## 41. 你们库表是怎么设计的？

这个项目主要有 3 个核心表：

- **用户表**
- **题目表**
- **题目提交表**

其中比较关键的设计有：

1. **题目表里判题配置使用 JSON 字符串存储**
   - 比如 `timeLimit`、`memoryLimit`
   - 这样扩展性更强，不需要每新增一个配置就改表结构
2. **为常用查询字段加索引**
   - 题目表、题目提交表中的 `userId`、`questionId`
3. **题目提交表增加 status 状态字段**
   - 用 int 存储判题状态
   - 业务中通过枚举做语义映射
   - 节省存储，也方便状态流转管理

------

## 42. 为什么题目配置用 JSON 存，而不是拆成多个字段？

主要是考虑 **灵活性和扩展性**。

判题配置可能包括：

- 时间限制
- 内存限制
- 特殊判题参数
- 语言限制
- 其他扩展配置

如果每种配置都拆成一个字段，后续新增配置就得改表结构，不够灵活。

用 JSON 存储的好处是：

- 结构灵活
- 便于扩展
- 后端读取后可以映射成对象处理

当然它的代价是：

- 对单个配置项的 SQL 查询不如结构化字段方便

但对于这个项目来说，配置更多是“读取后用于判题”，不是复杂条件查询，所以 JSON 更合适。

------

## 43. 题目提交表里的状态字段有什么作用？

状态字段用于描述一次提交当前处于哪个阶段，是判题流程管理的关键字段。

例如可以有这些状态：

- 待判题
- 判题中
- 成功
- 判题失败

或者更细一点：

- Accepted
- Wrong Answer
- Runtime Error
- Time Limit Exceeded
- Memory Limit Exceeded
- Compile Error

它的作用包括：

1. **驱动前端展示**
   - 前端可以展示当前提交状态
2. **避免重复处理**
   - 比如已完成判题的提交不应重复消费
3. **支持异步判题流程**
   - 用户提交后不是同步立刻返回结果，而是经历状态变化

------

## 44. 你们如何保证代码沙箱服务接口安全？

代码沙箱是高风险服务，所以接口安全很重要。我总结有两种方式：

1. **约定请求头中的密钥**
   - 调用方必须携带约定字符串
   - 服务端校验通过才允许访问
   - 简单，但安全性一般
2. **API 签名认证**
   - 给调用方分配 `accessKey` 和 `secretKey`
   - 请求时对参数进行签名
   - 服务端按同样规则验签
   - 安全性更高，适合公网场景

如果是内网服务调用，简单密钥方案就能起到基础保护作用；
 如果面向公网或更严格场景，推荐签名机制。

------

## 45. 你们的 GPT 评测模块是做什么的？

GPT 评测模块是这个系统区别于传统 OJ 的亮点之一。

传统 OJ 只能告诉用户：

- 通过了还是没通过
- 哪个测试点失败了

但对于学习者来说，这种反馈还不够。
 所以我们加入了 GPT 评测模块，用来结合：

- 题目描述
- 用户提交代码
- 判题结果
- 报错信息
- 题目类型

给出更有教学意义的反馈，比如：

- 语法问题提示
- 边界条件提醒
- 复杂度优化方向
- 代码规范建议

目标不是直接给答案，而是起到 **辅助教学、帮助学习者提升编码能力** 的作用。

------

## 46. 什么叫“上下文感知的智能代码建议”？

所谓“上下文感知”，就是不是只看一段代码本身，而是结合更多信息来生成建议。

在这个项目里，上下文主要可以包括：

- 当前题目内容和题目类型
- 用户提交的代码
- 编程语言
- 编译错误或运行错误信息
- 测试用例执行结果
- 判题状态
- 用户当前卡住的层级

基于这些上下文，系统可以给出更准确的建议，而不是泛泛而谈。
 比如对于一个动态规划题，系统给的建议应该偏“状态设计、转移关系”；
 如果是语法错误，就优先提示语法问题，而不是直接讲算法优化。

------

## 47. 责任链模式在智能建议里是怎么用的？

在智能建议模块里，我用了 **责任链模式** 来做分层处理。

核心思想是把不同类型的建议拆成多个处理节点，每个节点只负责一种判断和建议生成，比如：

- 语法规范检查
- 运行错误分析
- 题型识别
- 算法优化建议
- 代码风格建议

请求进入后，会沿着责任链依次处理。
 每个节点都可以读取统一的上下文对象，根据条件决定：

- 是否处理
- 是否继续传递
- 输出什么建议

这样做的好处是：

- 结构清晰
- 可插拔性强
- 后续新增一种建议类型时，只需要新增责任链节点

------

## 48. 为什么这里用责任链模式，而不是简单 if-else？

因为智能建议这个场景的规则会不断增加，而且不同规则之间是有层级和顺序关系的。

如果全部写成 if-else，会有几个问题：

- 逻辑越来越长
- 各类建议耦合在一起
- 不方便扩展
- 后续调整顺序很麻烦

而责任链模式的优势在于：

- 每个节点职责单一
- 便于扩展新节点
- 可以灵活调整执行顺序
- 更适合处理“多阶段判断、逐层增强”的场景

所以责任链比简单 if-else 更适合这种上下文感知的智能建议流程。

------

## 49. 你们如何避免 AI 直接把答案告诉学生？

这是 AI 辅助教学里非常重要的一点。我们的目标是“教学辅助”，不是“代做答案”。

为了避免模型直接泄题，可以从几个方向控制：

1. **提示策略分层**
   - 先给语法和规范提示
   - 再给思路提示
   - 最后才给优化方向
   - 不直接给完整解法代码
2. **控制上下文内容**
   - 输入给模型的信息更偏题目、错误信息和用户代码问题，而不是标准答案
3. **输出约束**
   - 在 prompt 中明确要求“不能直接给完整答案，只能给启发式指导”
4. **模板化建议**
   - 对不同题型优先给出方向性建议，而不是最终实现

这样可以尽量保证 AI 发挥辅导作用，而不是变成“自动写题器”。

------

## 50. 这个项目最大的亮点和难点分别是什么？

我觉得这个项目最大的亮点有两个：

1. **在传统 OJ 的基础上做了工程化的判题架构设计**
   - 判题服务和代码沙箱解耦
   - 用工厂、模板方法、代理、策略等模式提升扩展性
2. **在自动评测基础上加入了 AI 辅助教学能力**
   - 不只是给对错结果
   - 还能结合上下文做分层指导

难点主要也有两个：

1. **代码沙箱的安全与资源控制**
   - 包括超时、中断、内存限制、容器隔离
2. **智能建议如何做到“有用但不泄题”**
   - 既要给用户帮助
   - 又不能直接把题目答案暴露出来

这两个点也是我在项目里投入精力最多的地方。

------

