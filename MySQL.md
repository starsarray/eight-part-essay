[TOC]

## SQL基础

### NOSQL和SQL的区别？

**SQL（关系型数据库）**：以二维表形式存储结构化数据，每一行代表一个实体，每一列代表一种属性。典型代表：MySQL、Oracle、PostgreSQL。

**NoSQL（非关系型数据库）**：存储方式灵活，可采用 JSON 文档、键值对、图等形式。典型代表：MongoDB、Redis。

**核心区别**：

- **事务特性**：SQL 支持 **ACID**（原子性、一致性、隔离性、持久性），适合银行等强一致场景；NoSQL 遵循 **BASE**（基本可用、软状态、最终一致性），适合社交等对一致要求不高的场景。
- **扩展性**：SQL 数据间关联性强，水平扩展较难，需处理跨服务器 JOIN 和分布式事务；NoSQL 数据无固定关系，易于水平扩展（如 Redis 集群）。

### **数据库三大范式**

- **第一范式（1NF）**：每一列都是不可分割的原子数据项。
- **第二范式（2NF）**：在1NF基础上，非主属性完全依赖于主键（消除部分依赖）。
- **第三范式（3NF）**：在2NF基础上，非主属性不依赖于其他非主属性（消除传递依赖）。

### **MySQL连表查询**

- **内连接（INNER JOIN）**：返回两表匹配的行。
- **左外连接（LEFT JOIN）**：返回左表所有行，右表无匹配则补NULL。
- **右外连接（RIGHT JOIN）**：返回右表所有行，左表无匹配则补NULL。
- **全外连接（FULL JOIN）**：返回两表所有行，MySQL需用LEFT JOIN + RIGHT JOIN + UNION模拟。

### **MySQL避免重复插入数据**

- **UNIQUE约束**：在列上添加唯一约束，保证数据唯一性。
- **INSERT ... ON DUPLICATE KEY UPDATE**：插入冲突时更新现有记录。
- **INSERT IGNORE**：插入冲突时忽略错误，不插入。

### **CHAR和VARCHAR区别**

- **CHAR**：定长字符串，存储时末尾补空格，适合长度固定的数据，效率较高。
- **VARCHAR**：变长字符串，按实际长度存储，节省空间，适合长度可变的数据。

## 索引

### 索引是什么？有什么好处？

索引类似于书籍的目录，可以减少扫描的数据量，提高查询效率。

- 如果查询的时候，没有用到索引就会全表扫描，这时候查询的时间复杂度是On
- 如果用到了索引，那么查询的时候，可以基于二分查找算法，通过索引快速定位到目标数据， mysql 索引的数据结构一般是 b+树，其搜索复杂度为O(logdN)，其中 d 表示节点允许的最大子节点个数为 d 个。

### 索引的分类是什么？

**按数据结构分类**

- **B+Tree索引**：最常见，InnoDB存储引擎默认使用，支持范围查询和排序。
- **Hash索引**：基于哈希表，适用于等值查询，不支持范围查询。
- **Full-text索引**：全文索引，用于文本内容的模糊搜索。

**按物理存储分类**

- **聚簇索引（主键索引）**：叶子节点存放整行数据，一个表只能有一个。
- **二级索引（辅助索引）**：叶子节点存放主键值，查询时可能需要回表。

**按字段特性分类**

- **主键索引**：建立在主键上，唯一且非空。
- **唯一索引**：建立在UNIQUE字段上，值唯一但允许空值。
- **普通索引**：无特殊约束，仅加速查询。
- **前缀索引**：对字符串的前几个字符建立索引，节省空间。

**按字段个数分类**

- **单列索引**：建立在单个列上。
- **联合索引**：建立在多个列上，遵循**最左匹配原则**，且范围查询后的字段无法使用索引。

### **哈希索引使用场景是什么？**

**哈希索引**的核心优势是**等值查询极快**，能直接通过哈希函数定位数据位置，但**不支持范围查询、排序和前缀匹配**。因此，它适用于**以等值查询为核心、无需复杂查询**的场景。例如，MySQL 的 **Memory 存储引擎默认使用哈希索引**，常用于缓存用户登录信息、权限信息等，通过用户 ID 快速查找，查询效率接近 **O(1)**，比 B+ 树索引更快。

### MySQL聚簇索引和非聚簇索引的区别是什么？

- **数据存储位置**：聚簇索引的叶子节点直接存储整行数据，数据与索引“聚簇”在一起；非聚簇索引的叶子节点存储的是主键值（或指向数据的指针），不包含完整数据行。
- **索引与数据关系**：聚簇索引中，数据行按索引键值顺序物理存储；非聚簇索引中，索引独立于数据，通过索引找到主键后再回表查询实际数据。
- **唯一性**：一个表只能有一个聚簇索引（因为数据只能有一种物理排序方式），但可以有多个非聚簇索引。
- **查询效率**：聚簇索引对范围查询和排序更高效，可直接获取数据；非聚簇索引在等值查询快，但若需回表则额外多一次I/O。若索引覆盖（即索引包含所需字段），则无需回表，效率更高。

### 如果聚簇索引的数据更新，它的存储要不要变化？

- 如果更新的数据是非索引数据，也就是普通的用户记录，那么存储结构是不会发生变化
- 如果更新的数据是索引数据，那么存储结构是有变化的，因为要维护 b+树的有序性