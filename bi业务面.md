# BI 项目二面业务面试题

## 1. 你这个 BI 项目的核心用户是谁？他们真实的使用场景是什么？

主要是**没有数据分析和前端开发能力的业务人员**，比如产品、运营、销售这类同学。
 他们真实的使用场景就是：**手里有一份 Excel / CSV 数据，想快速得到图表和分析结论，但自己不会写 SQL，也不会做可视化。**

**追问：**用户有遇到了什么问题吗，你们怎么解决的？

**1）不会提需求，描述很模糊**

比如用户只说一句“帮我分析一下这个表”，这种话其实没法直接得到高质量结果。

**我们的解决方式：**

- 在后端先做**任务场景识别**，判断他更像是趋势分析、对比分析还是异常分析；
- 再做**需求改写**，把模糊问题补全成结构化任务说明；
- 如果还是太模糊，就给默认分析思路，比如先看趋势、分布、TopN。

**2）数据很乱，AI直接分析效果不好**

比如表里有空值、字段名乱、说明文字多，甚至格式不规范。

**我们的解决方式：**

- 文件上传后先做**校验、解析、预处理**；
- 清理脏数据、统一字段、提取关键列；
- 不把整张原始表直接扔给 AI，而是先做**压缩和结构化摘要**，降低幻觉和误判。

**3）用户不信 AI，怕结果不准**

尤其是图表能画出来，不代表分析就是对的。

**我们的解决方式：**

- 把**统计和聚合这类确定性计算放在后端代码里做**，不交给 AI 自己“脑补”；
- AI 主要负责**结论生成和图表表达**；
- 对输出再做**格式校验、业务校验和失败兜底**，保证结果至少可解析、可展示。

------

## 2. 你觉得这个项目最应该看的 3 个核心业务指标是什么？为什么？

**1）任务成功率**

**定义：**用户发起分析任务后，最终成功生成可展示结果的比例。这里的“成功”不是单纯接口返回 200，而是任务状态真正走到成功，并且前端能正常展示图表和结论。

**来源：**主要来自任务表里的状态字段统计，比如总任务数里有多少条是 `success`，多少条是 `failed`。如果更细一点，也可以结合文件解析日志、AI 调用日志、结果校验日志一起看，定位失败发生在哪一层。

**反映：**

- 文件上传和解析是否稳定
- AI 输出是否可解析、可校验
- 异步任务链路是否完整可用
- 系统整体稳定性是否达标

**优化方案：**

- 前置做好文件校验、格式校验、数据预处理，减少脏数据导致的失败
- 对 AI 输出增加 JSON / Schema / 业务校验，避免结果不可渲染
- 加入自动纠错、有限重试和失败兜底，比如失败时退化成文字结论
- 做好任务状态监控和异常日志留存，方便快速定位哪一环最容易失败

------

**2）结果采纳率 / 使用率**

**定义：**生成结果后，用户是否真的查看、使用、保留，或者继续基于结果做后续操作。比如用户有没有继续编辑图表、导出结果、再次发起相关分析，或者第二次需求和第一次需求差异是否缩小。

**来源：**主要来自用户行为数据和埋点统计，比如结果页停留时长、是否点击导出、是否保存图表、是否再次生成、是否频繁修改需求、是否对结果点赞点踩等。也可以结合“第一次需求”和“第二次需求”的相似度，判断第一次结果有没有真正解决问题。

**反映：**

- 分析结果是否贴近用户真实诉求
- 图表和结论是否有参考价值
- 用户对 AI 结果是否信任
- 产品是否真的降低了数据分析门槛

**优化方案：**

- 在生成前先做任务场景识别和需求改写，减少“用户说不清”带来的偏差
- 让 AI 少做确定性计算，多基于后端算好的结果做表达，提升可信度
- 支持用户二次编辑、重新生成、补充追问，形成更自然的人机协同
- 沉淀用户反馈和常见 bad case，用来持续优化 Prompt、模板和知识库

------

**3）平均分析耗时**

**定义：**从用户提交任务到拿到最终结果的总耗时。这个时间看的是完整任务链路，而不是单次接口响应时间。

**来源：**主要来自任务生命周期里的时间戳，比如任务创建时间、开始执行时间、AI 返回时间、结果入库时间、任务完成时间。通过这些字段可以进一步拆出排队耗时、处理耗时、AI 调用耗时。

**反映：**

- 异步任务链路是否高效
- AI 调用是否是主要瓶颈
- 队列是否存在积压
- 用户整体等待体验好不好

**优化方案：**

- 把任务拆成异步处理，避免主流程阻塞
- 对大文件做预处理、裁剪和压缩，减少传给 AI 的数据量
- 做多模型路由，简单任务走快模型，复杂任务走强模型
- 增加任务队列监控、超时控制和降级策略，避免长尾任务拖慢整体体验

------

## 3. 用户上传一份表后，系统怎么判断“适不适合做 BI 分析”？比如合并单元格很多、标题不规范、说明文字很多，怎么识别它不是标准数据表？

用户上传表之后，我们不会直接进入 AI 分析，而是会先做一层**表结构识别**，判断它是不是适合做 BI 的**标准二维数据表**。

具体判断逻辑主要看几类特征：

- **表头是否清晰**
  - 会识别哪一行是真正表头，而不是默认第一行就是表头
  - 看列名是不是完整、是否有大量空字段、重复字段、异常字段名
- **数据区是否规整**
  - 看每一行的列数是不是基本一致
  - 如果有些行像标题、有些行像备注、有些行才像数据，说明结构不稳定
- **是否存在展示型特征**
  - 比如大量合并单元格
  - 前几行是“报表名称、统计周期、备注说明”这类内容
  - 一张 sheet 里有多块表，或者中间夹了很多说明文字
- **能不能稳定抽出字段和记录**
  - BI 分析要求的是“一列一个字段、一行一条记录”
  - 如果系统没法稳定识别出字段和数据边界，就会认为这不是标准数据表

对应到实现上，我们会在文件解析之后，先做一个**可分析性评分**。
 比如会综合看：表头完整度、列数稳定性、空行占比、说明文字占比、合并单元格情况。如果这些指标比较差，就说明这张表更像“展示报表”，不适合直接做 BI 分析。

**追问：**如果表不适合直接分析，你们是拒绝、提示用户修正，还是自动帮他整理？
 **不是统一拒绝，而是分层处理。**

- **轻度问题，自动整理**
  - 比如前面多了几行标题说明、字段名有空格或换行、夹杂少量空行
  - 这种我们会自动清洗，比如跳过说明行、统一字段名、过滤空行后再继续分析
- **中度问题，提示用户修正**
  - 比如表头不明确、字段缺失较多、一个 sheet 里有多段结构不同的数据
  - 这种我们会提示用户保留核心明细表、去掉说明文字和合并单元格后再上传
- **重度问题，直接拒绝**
  - 比如合并单元格很多、整张表是汇报样式、结构非常不规则
  - 这种即使强行解析，后面的图表和结论也不可靠，所以会直接拦截

------

## 4. 你们怎么做任务的状态设计？除了成功失败，还有没有更细的生命周期管理？

我一般会把状态分成这几类：

- **wait**：任务已创建，等待执行
- **running**：任务已被消费，开始处理
- **succeed**：任务执行成功，已经生成图表和分析结论
- **failed**：任务执行失败，并记录失败原因

如果项目再做细一点，我会继续往下拆中间态，比如：

- **validating**：文件校验中
- **parsing**：解析表格中
- **analyzing**：构造 Prompt / 数据处理中
- **generating**：模型生成中
- **saving**：结果落库中
- **timeout**：任务超时
- **canceled**：任务被取消

这样设计的好处是，系统能明确知道任务卡在哪一步，而不是最后只看到一个失败结果，定位问题会更快。

**追问：**如果任务卡在某个中间状态很久，比如数据处理中、模型生成中，怎么判断是慢还是挂了？状态流转有没有做幂等保护和状态机约束？

如果任务长时间停在某个中间状态，我们不会只看“它还没结束”，而是会结合**开始时间、更新时间、超时时间**来判断它到底是“执行慢”还是“已经挂了”。

一般会这么做：

- 每个任务都会记录：
  - **status**
  - **startTime**
  - **updateTime**
  - **endTime**
  - **execMessage**
- 系统会给不同阶段配置一个**超时阈值**
  - 比如表格解析可能几十秒
  - 模型生成可能 1~2 分钟
  - 超过阈值还没进入下一状态，就认为异常

判断逻辑一般是：

- 如果任务状态长时间没更新，但 worker 还存活、调用还在执行，说明是**慢任务**
- 如果状态停留太久，而且没有心跳、没有日志更新、消息消费也结束了，就更可能是**挂了**

所以实践上通常会配两层机制：

- **状态更新时间判断**
  - 比如 `running` 状态超过 5 分钟没更新，就标记为疑似超时
- **心跳 / 续租机制**
  - worker 在执行长任务时定期刷新 `updateTime`
  - 只要还在刷新，就说明任务还活着，只是慢
  - 长时间不刷新，才判定为挂掉

至于幂等保护和状态机约束，这两个其实很重要，不然异步任务很容易乱。

第一层是**幂等保护**：
 因为 MQ 可能重复消费，或者重试时重复执行，所以同一个任务不能被重复处理。
 一般会做这几件事：

- 消费任务前先查状态
  - 只有 `wait` 状态才能进入执行
- 更新状态时带条件
  - 例如只允许 `wait -> running`
- 如果已经是 `running / succeed / failed`，就拒绝重复执行

如果更新条数是 0，就说明这个任务已经被别人抢走了，或者已经执行过了，当前消费者直接退出。

第二层是**状态机约束**：
 不是任何状态都能随便跳。比如：

- `wait -> running`
- `running -> succeed`
- `running -> failed`
- `running -> timeout`

但是不能：

- `failed -> running`
- `succeed -> running`
- `wait -> succeed`

这样做的好处是状态流转是受控的，不会因为并发或者异常处理把任务状态写乱。

------

## 5. 如果一个用户连续点了 5 次“生成分析”，你们是把它当成 5 个任务，还是识别成重复请求？

一般会先区分两种情况：

- **请求内容不同**，那就是 5 个独立任务
   比如文件变了、分析目标变了、图表类型变了，这种本质上就是新的分析需求
- **请求内容相同**，那就要进一步判断是不是重复请求
   比如同一个用户、同一个文件、同一个 goal、同一个 chartType，在很短时间内连续提交，这种大概率不是新需求，而是重复点击或者网络重试

所以我们更倾向于做**重复请求识别**，而不是一律当成 5 个新任务。
 因为 AI 分析本身成本比较高，如果不做去重，很容易造成：

- 资源浪费
- 队列堆积
- 用户拿到多份几乎一样的结果
- 数据库里堆很多重复任务，后续统计也会失真

实现上通常会生成一个**任务幂等键**，比如由这些信息拼出来：

- userId
- 文件内容摘要，比如文件 hash
- goal
- chartType

然后对这些字段做 hash，得到一个 requestKey。
 如果短时间内收到相同 requestKey，就认为是重复请求。

**追问：**如果第一次任务还在执行，第二次同样请求来了，应该直接复用结果还是重新生成？为什么？

如果**第一次任务还在执行，第二次完全相同的请求又来了**，我一般会优先选择**复用正在执行的任务**，而不是重新生成。

原因主要有三个：

- **结果预期相同**
  - 文件、目标、图表类型都没变，理论上生成结果应该是同一类结果
  - 重新跑一遍，业务价值不大
- **避免重复消耗 AI 资源**
  - AI 调用通常是最贵的环节
  - 相同请求重复执行，会明显增加成本和排队时间
- **用户体验更稳定**
  - 如果同时生成多个相似任务，用户反而会看到多个状态、多个结果，不知道该看哪个
  - 直接复用一个任务，让前端轮询同一个 chartId，体验更清晰

所以一般做法是：

- 如果发现相同请求已经有一个 **wait / running** 状态的任务
  - 直接返回那个已有任务的 `chartId`
  - 告诉前端这是已存在任务，继续查询进度即可
- 如果之前已经有一个 **succeed** 的任务，而且结果仍然可用
  - 可以直接复用历史结果
  - 这其实相当于结果缓存
- 如果之前任务是 **failed**
  - 一般允许重新发起
  - 因为失败结果没有复用价值

当然，也不是所有场景都必须复用。
 如果你的产品强调“每次都要重新生成新的表达方式”，比如 AI 文案、开放式结论变化比较大，也可以允许重复生成。
 但对于 BI 分析这种场景，我更倾向于：
 **相同输入优先复用，相同请求不重复执行。**

------

## 6. 你们有没有考虑“历史结果复用”这件事？

**追问：**同样的数据、同样的问题，是否可以直接返回之前的分析结果？结果复用怎么判断命中？靠文件 hash、问题 hash 还是任务指纹？

------

## 10. BI 项目里其实有一个常见问题：用户并不信 AI。你们是怎么提升结果可信度的？

**追问：**

- 你们有没有给出“分析依据”，比如用了哪些字段、哪些统计结果？
- 如果结论里出现“可能原因”，这类推断怎么避免胡说？
- 你觉得业务用户最在意的是“结果漂亮”还是“结果能解释”？为什么？

------

## 11. 如果用户问的是一个非常强业务口径的问题，比如“ROI 为什么下降”，后端怎么避免 AI 按通用知识乱解释？

**追问：**

- 你们有没有业务口径中心、指标字典、知识库之类的东西参与生成？
- 如果不同部门对同一个指标定义不一样，你们怎么处理？
- 这种问题你觉得应该更多依赖规则、知识库，还是大模型？

------

## 12. 这个项目里，哪些逻辑你坚持放在后端代码里做，绝不交给 AI？

**追问：**

- 比如统计计算、过滤、排序、TopN、同比环比，这些哪些必须程序算？
- 为什么不能让模型直接根据原始表自己“算”？
- 你怎么划分“确定性逻辑”和“生成式逻辑”的边界？

------

## 13. 如果一张表非常大，但用户只问了一个很小的问题，你们怎么避免“为了回答一句话，处理整张表”？

**追问：**

- 你们有没有做字段裁剪、数据裁剪、问题相关性筛选？
- 谁来决定保留哪些列、哪些行？
- 如果裁剪错了，把关键数据裁掉了，后果怎么补救？

------

## 14. 你们有没有做过“多租户 / 不同企业隔离”的设计？

**追问：**

- 如果这是一个面向企业的 BI 平台，最关键的隔离点有哪些？
- 是不是不仅数据要隔离，Prompt、知识库、模型配置、缓存结果也都要隔离？
- 如果一个租户有自己的指标定义和分析模板，你会把这套配置放在哪？

------

## 15. 你们如何看待“分析结果可编辑”这件事？系统是一次性生成，还是支持人机协同修正？

**追问：**

- 用户如果想改图表标题、换图类型、补一句解释，这种修改如何落库？
- 下次同类任务时，能不能学习用户之前的编辑偏好？
- 你觉得 BI 产品里，AI 更像“自动生成器”还是“分析助手”？

------

## 16. 从后端视角看，这个项目里最容易被忽略但实际上很重要的数据表有哪些？

**追问：**

- 除了主任务表，你们还需要哪些辅助表？比如任务日志、模型调用记录、失败原因表、用户反馈表？
- 如果让你重新设计数据库表结构，你会新增什么字段？
- 哪些字段对于后续排障和运营分析最有价值？

------

## 17. 如果老板突然问你：“为什么这个月 AI 成本涨了 3 倍？” 你后端系统能不能回答这个问题？

**追问：**

- 你们有没有记录每次任务的大概 token 消耗、模型类型、任务来源、用户维度成本？
- 成本上涨可能来自哪些地方？
- 如果要做成本治理，你会优先从哪些抓手下手？

------

## 18. 假如用户对结果不满意，点了“重新生成”，这个“重新生成”到底意味着什么？

**追问：**

- 是同样输入重新跑一次，还是带着“用户不满意”这个反馈去调整生成策略？
- 重新生成和第一次生成相比，后端参数会有哪些变化？
- 如果一直重生成本很高，你们怎么限制？

------

## 19. 这个项目里有没有出现过“技术上可行，但业务上不该做”的需求？

**追问：**

- 比如用户希望 AI 直接给经营决策建议、自动归因、自动预测，这种你会怎么判断要不要做？
- 作为后端，你会如何向产品说明“这个能力有风险，不能直接上线”？
- 你觉得 BI 场景里最危险的 AI 幻觉，不是格式错，而是什么？

------

## 20. 如果现在让你把这个 BI 项目真正推到生产环境，你最担心的 3 个线上问题是什么？

**追问：**

- 这 3 个问题分别会怎么监控？
- 出现以后你的止损手段是什么？
- 如果只能优先补一个，你先补哪一个？为什么？