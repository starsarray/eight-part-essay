- [简历项目与八股文面试题](#简历项目与八股文面试题)
  - [字节跳动面试专用：自我介绍 (口语连贯版)](#字节跳动面试专用自我介绍-口语连贯版)
    - [💡 面试官可能追问的“钩子”问题预测](#-面试官可能追问的钩子问题预测)
  - [项目一：基于大模型的智能BI平台 (AI驱动数据分析)](#项目一基于大模型的智能bi平台-ai驱动数据分析)
    - [1. 什么是 Prompt 工程？在你的项目中是如何设计 Prompt 模板的？](#1-什么是-prompt-工程在你的项目中是如何设计-prompt-模板的)
    - [2. AIGC 响应时间长，你用了哪些手段优化用户体验？](#2-aigc-响应时间长你用了哪些手段优化用户体验)
    - [3. 如何保证分布式环境下的限流准确性？](#3-如何保证分布式环境下的限流准确性)
    - [4. 你的项目中对用户上传的原始数据文件做了哪些校验？](#4-你的项目中对用户上传的原始数据文件做了哪些校验)
    - [5. AI 生成的图表 JSON 不合法导致前端渲染失败，怎么处理？](#5-ai-生成的图表-json-不合法导致前端渲染失败怎么处理)
    - [6. RabbitMQ 的死信队列在项目中是怎么用的？](#6-rabbitmq-的死信队列在项目中是怎么用的)
    - [7. 你的 IO 密集型线程池具体是怎么配置的？为什么这么配？](#7-你的-io-密集型线程池具体是怎么配置的为什么这么配)
    - [8. 为什么使用 SSE 而不是 WebSocket？](#8-为什么使用-sse-而不是-websocket)
    - [9. 原始数据包含敏感信息怎么办？](#9-原始数据包含敏感信息怎么办)
    - [10. 你参加的项目里挑一个展开说说主要参与的环节以及满意的成果具体体现是什么？](#10-你参加的项目里挑一个展开说说主要参与的环节以及满意的成果具体体现是什么)
    - [11. 为什么选择 RabbitMQ？相比其他消息队列（Kafka、RocketMQ）有哪些优缺点？](#11-为什么选择-rabbitmq相比其他消息队列kafkarocketmq有哪些优缺点)
    - [12. 成果中的这些数据（如 200ms、3倍吞吐量等）是怎么得出来的？](#12-成果中的这些数据如-200ms3倍吞吐量等是怎么得出来的)
    - [13. RabbitMQ 有哪几种交换机？你在项目中选择了哪个？](#13-rabbitmq-有哪几种交换机你在项目中选择了哪个)
    - [14. 既然用的是阿里的通义千问大模型，那这个项目的性能是否有遇到瓶颈？如何优化？](#14-既然用的是阿里的通义千问大模型那这个项目的性能是否有遇到瓶颈如何优化)
    - [15. 你没做过大规模压测，怎么验证优化效果的？（如果被问到压测）](#15-你没做过大规模压测怎么验证优化效果的如果被问到压测)
    - [16. 你的限流维度是什么？用的是哪种算法？为什么选择它？](#16-你的限流维度是什么用的是哪种算法为什么选择它)
    - [17. 你的线程池是怎么设置参数的？（核心线程数、最大线程数、队列长度）为什么这么设？](#17-你的线程池是怎么设置参数的核心线程数最大线程数队列长度为什么这么设)
    - [18. 你是怎么保证 AI 分析任务在 MQ 中不丢失、不重复、不乱序的？](#18-你是怎么保证-ai-分析任务在-mq-中不丢失不重复不乱序的)
    - [19. 你在开发这个 BI 项目时遇到的最大挑战是什么？你是怎么解决的？](#19-你在开发这个-bi-项目时遇到的最大挑战是什么你是怎么解决的)
    - [20. 你提到对用户上传的文件“动态创建了独立数据表”，具体是如何实现的？](#20-你提到对用户上传的文件动态创建了独立数据表具体是如何实现的)
    - [21. 你的 RabbitMQ 和线程池是一起使用的吗？为什么要这么搭配？](#21-你的-rabbitmq-和线程池是一起使用的吗为什么要这么搭配)
    - [22. 你为什么要做这个基于大模型的智能 BI 项目？](#22-你为什么要做这个基于大模型的智能-bi-项目)
  - [项目二：智能编程练习与评测平台 (AI辅助教学)](#项目二智能编程练习与评测平台-ai辅助教学)
    - [1. 判题机架构是如何设计的？为什么要用工厂模式和策略模式？](#1-判题机架构是如何设计的为什么要用工厂模式和策略模式)
    - [2. 为什么在 AI 建议模块使用责任链模式？你这条责任链上大概有哪几个节点？是怎么做到“动态选择”的？](#2-为什么在-ai-建议模块使用责任链模式你这条责任链上大概有哪几个节点是怎么做到动态选择的)
    - [3. 代码沙箱的安全机制你是如何模拟和解决的？](#3-代码沙箱的安全机制你是如何模拟和解决的)
    - [4. 你遇到过哪些典型沙箱异常？是怎么模拟、监控、兜底处理的？](#4-你遇到过哪些典型沙箱异常是怎么模拟监控兜底处理的)
    - [5. Spring Cloud Gateway 在项目中起到了什么作用？](#5-spring-cloud-gateway-在项目中起到了什么作用)
    - [6. Dockerfile 是什么？在你的项目中是怎么写的？](#6-dockerfile-是什么在你的项目中是怎么写的)
    - [7. 什么是 JWT？你是如何实现登录认证的？](#7-什么是-jwt你是如何实现登录认证的)
    - [8. 判题任务非常耗时，如果判题机挂了，任务会丢失吗？](#8-判题任务非常耗时如果判题机挂了任务会丢失吗)
    - [9. 用户提交恶意代码（如死循环、内存泄露）怎么办？](#9-用户提交恶意代码如死循环内存泄露怎么办)
    - [10. 既然用了 Nacos，微服务之间的配置如何实现热更新？](#10-既然用了-nacos微服务之间的配置如何实现热更新)
    - [11. 如何实现多种代码沙箱（Java/C++/Python）的灵活调用？](#11-如何实现多种代码沙箱javacpython的灵活调用)
  - [项目三：CUA-SFT数据构造与具身大模型训练](#项目三cua-sft数据构造与具身大模型训练)
    - [1. 什么是 CUA (Computer-Using Agent)？](#1-什么是-cua-computer-using-agent)
    - [2. 建立的数据审核流程使标注准确率提升至 90% 以上。你是怎么定义标注准确率的？用了什么指标/方法去量化？中间做了哪一步最关键的改进？](#2-建立的数据审核流程使标注准确率提升至-90-以上你是怎么定义标注准确率的用了什么指标方法去量化中间做了哪一步最关键的改进)
    - [3. 什么是 SFT (有监督微调)？为什么数据质量至关重要？](#3-什么是-sft-有监督微调为什么数据质量至关重要)
    - [4. 作为项目组长，你如何保证数据标注的准确率提升至 90% 以上？](#4-作为项目组长你如何保证数据标注的准确率提升至-90-以上)
    - [5. 你们采集的数据包含哪些维度？如何保护隐私？](#5-你们采集的数据包含哪些维度如何保护隐私)
    - [6. 在构造数据时，如何处理“长程任务”的复杂性？](#6-在构造数据时如何处理长程任务的复杂性)
    - [7. 数据标注工具是你自己开发的吗？有什么特点？](#7-数据标注工具是你自己开发的吗有什么特点)
    - [8. 这里的“模型训练反馈”，你具体收到的是什么内容？是谁给你的？](#8-这里的模型训练反馈你具体收到的是什么内容是谁给你的)
    - [9. 你们团队大概多少人？你在推进过程中，遇到过最典型的一个协作/进度问题是什么，你是怎么解决的？](#9-你们团队大概多少人你在推进过程中遇到过最典型的一个协作进度问题是什么你是怎么解决的)
  - [Java基础与并发 (拷打区)](#java基础与并发-拷打区)
    - [1. 你的力扣解题超过 550 题，请讲讲你对算法的理解？](#1-你的力扣解题超过-550-题请讲讲你对算法的理解)
    - [2. 既然获得了 HCIA-AI 认证，请简述大模型的训练流程？](#2-既然获得了-hcia-ai-认证请简述大模型的训练流程)
    - [3. ThreadLocal 为什么会内存泄漏？在你的项目中有用到吗？](#3-threadlocal-为什么会内存泄漏在你的项目中有用到吗)
    - [4. 线程池的参数如何根据业务场景选择？](#4-线程池的参数如何根据业务场景选择)
    - [5. HashMap 与 ConcurrentHashMap 的区别？(JDK 1.8)](#5-hashmap-与-concurrenthashmap-的区别jdk-18)
    - [6. 什么是 Java 的反射机制？优缺点？](#6-什么是-java-的反射机制优缺点)
    - [7. Java 内存模型 (JMM) 简单介绍一下？](#7-java-内存模型-jmm-简单介绍一下)
    - [8. 抽象类和接口的区别？](#8-抽象类和接口的区别)
    - [9. String, StringBuilder, StringBuffer 的区别？](#9-string-stringbuilder-stringbuffer-的区别)
    - [10. Java 中 == 和 equals 的区别？](#10-java-中--和-equals-的区别)
    - [11. 什么是深拷贝和浅拷贝？](#11-什么是深拷贝和浅拷贝)
  - [数据库与中间件](#数据库与中间件)
    - [1. 什么是索引失效？你在开发中遇到过吗？](#1-什么是索引失效你在开发中遇到过吗)
    - [2. Redis 为什么不用多线程？](#2-redis-为什么不用多线程)
    - [3. 如何解决消息队列的幂等性问题？](#3-如何解决消息队列的幂等性问题)
    - [4. MySQL 的事务隔离级别有哪些？MySQL 默认是哪种？](#4-mysql-的事务隔离级别有哪些mysql-默认是哪种)
    - [5. 什么是 Redis 缓存雪崩、穿透、击穿？](#5-什么是-redis-缓存雪崩穿透击穿)
    - [6. MySQL 执行一条 SQL 语句的流程是怎样的？](#6-mysql-执行一条-sql-语句的流程是怎样的)
    - [7. Redis 持久化 RDB 和 AOF 的区别？](#7-redis-持久化-rdb-和-aof-的区别)
    - [8. 什么是 MySQL 的回表？如何避免？](#8-什么是-mysql-的回表如何避免)
    - [9. MyBatis 中 #{} 和 ${} 的区别？](#9-mybatis-中--和--的区别)
    - [10. Spring 中的 Bean 是线程安全的吗？](#10-spring-中的-bean-是线程安全的吗)
    - [11. 为什么 MySQL 用 B+ 树做索引而不用 Hash 或 B 树？](#11-为什么-mysql-用-b-树做索引而不用-hash-或-b-树)
  - [新增面试题 (补充)](#新增面试题-补充)
    - [1. Redis 限流用的什么命令，更高级的命令？](#1-redis-限流用的什么命令更高级的命令)
    - [2. Trae 怎么使用的，配置文件源代码看过吗？](#2-trae-怎么使用的配置文件源代码看过吗)
    - [3. MCP 和 Skills 区别是什么？](#3-mcp-和-skills-区别是什么)
    - [4. 假如产品经理给了一张百万行的 Excel 表，包含排行对应的奖励信息，那我如何通过后端将这张表的数据变成 JSON，用 AI 实现。但不能只是单纯的把这张表直接递给 AI，因为 AI 会有幻觉，而且无法处理这种精确而且数据量很大的数据？](#4-假如产品经理给了一张百万行的-excel-表包含排行对应的奖励信息那我如何通过后端将这张表的数据变成-json用-ai-实现但不能只是单纯的把这张表直接递给-ai因为-ai-会有幻觉而且无法处理这种精确而且数据量很大的数据)
    - [5. 用户上传给 AI 的需求很模糊怎么解决？](#5-用户上传给-ai-的需求很模糊怎么解决)
    - [6. 用到的消息队列异步处理是什么？](#6-用到的消息队列异步处理是什么)
    - [7. 你一般都用的什么数据库？](#7-你一般都用的什么数据库)
    - [8. MySQL 锁有哪些？](#8-mysql-锁有哪些)


# 简历项目与八股文面试题

## 自我介绍

面试官您好，我叫 **[你的名字]**，是 **[你的学校]** 软件工程专业的硕士研究生，预计 **2027 年毕业**，很荣幸参加这次面试。

在校期间，我主要在做 **Java 后端方向** 的学习和项目实践。平时我会坚持刷一些算法题，也会通过参加竞赛来提升自己的编程能力和分析问题的能力。竞赛方面，我曾获得过 **全国大学生软件创新大赛全国一等奖** 、 **电子设计大赛全国一等奖**等奖项。

项目方面，我做得比较深入的是一个 **基于大模型的智能 BI 平台**。这个项目的核心业务是用户上传原始数据后，系统自动生成数据分析结论和可视化图表。
在这个项目里，我主要负责后端部分的设计和实现。开发过程中，我重点解决了几个问题：一是大模型接口响应比较慢，所以我把生成流程改造成了**异步处理**；二是 AI 调用成本比较高，而且有调用频率限制，所以我做了**限流控制**；三是大模型输出结果不够稳定，所以我通过优化输入输出格式和 prompt，让它尽量返回可解析、可落库的结果。除此之外，我也对用户上传的数据做了基础的**文件校验和预处理**，尽量减少脏数据和异常数据对分析效果的影响。

另外，我之前还做过一个 **智能编程练习与评测平台**，主要涉及在线评测和 AI 辅助学习这两个方向。虽然这个项目相对早一些，但也让我对后端开发里的模块设计、异步处理和系统安全这些问题有了一些实际认识。

我比较感兴趣的方向还是 **Java 后端开发**，也希望能够在贵公司的实际业务场景中继续提升自己在工程实现和系统设计方面的能力。谢谢面试官。



***

### 💡 面试官可能追问的“钩子”问题预测

根据上述自我介绍，你在看似平淡的叙述中抛出了多个技术“钩子”，面试官极大概率会顺着这些具体的名词往下问。以下是对应的追问预测（答案都在下文的 BI 项目题库中）：

**钩子 1：RabbitMQ 与异步化处理**
- **追问**：为什么选择 RabbitMQ？相比其他消息队列（如 Kafka、RocketMQ）有什么优缺点？
- **追问**：你在项目中是如何使用 RabbitMQ 的？选择了哪种交换机？（答：Direct 交换机，点对点通信）
- **追问**：你的 RabbitMQ 和线程池是一起使用的吗？为什么要这么搭配？
- **追问**：同步和阻塞、异步和非阻塞的区别是什么？

**钩子 2：自定义 IO 密集型线程池**
- **追问**：什么是线程池？使用它有什么好处？
- **追问**：为什么在本项目中选择“IO 密集型”线程池而不是“CPU 密集型”？
- **追问**：线程池的核心参数有哪些？你在代码里具体是怎么配置的？

**钩子 3：Redisson 与令牌桶限流**
- **追问**：什么是限流？你了解哪些常见的限流算法？（答：固定窗口、滑动窗口、漏桶、令牌桶）
- **追问**：什么是 Redisson？它是如何实现分布式限流的？
- **追问**：你的具体限流策略是什么？（答：单用户每秒最多 2 次）

**钩子 4：文件上传校验（安全性）**
- **追问**：只要涉及文件上传就一定有安全风险，你是如何保证用户上传的数据文件的安全性的？（答：后缀名校验、大小限制、Magic Number文件头格式校验、第三方内容安全检测）

**钩子 5：分表存储**
- **追问**：什么是分库分表？
- **追问**：为什么你选择对每份原始数据进行“分表存储”？有什么优缺点？（答：防止单表数据量过大影响检索性能，方便动态管理）

**钩子 6：大模型（AIGC）相关**
- **追问**：你为什么要做这个基于大模型的智能 BI 项目？
- **追问**：你是如何使用 AIGC 来生成指定格式 JSON 的？
- **追问**：你给 AI 预设的 Prompt 结构是什么样的？有哪些优化 Prompt 的小技巧？（答：角色定义、输入输出格式约束、特殊分隔符、Few-shot示例）

***

## 项目一：基于大模型的智能BI平台 (AI驱动数据分析)

### 1. 什么是 Prompt 工程？在你的项目中是如何设计 Prompt 模板的？

- **Prompt 工程**：通过设计、优化输入给大模型的提示词（Prompt），来引导模型生成更准确、更符合预期的输出。
- **项目设计**：
  - **角色设定**：明确告知模型“你是一个专业的数据分析师和 Echarts 专家”。
  - **任务约束**：要求模型仅输出 JSON 格式的图表配置，不要包含多余的文字说明。
  - **示例注入 (Few-shot)**：给模型几个“原始数据 -> JSON 配置”的例子，让它模仿。
  - **上下文封装**：将用户上传的数据列名、数据摘要和分析诉求动态填入模板。
- **我的 Prompt 结构**：
  ```
  你是一个数据分析师和前端开发专家，接下来我会按照以下固定格式给你提供内容：
  分析需求：
  {数据分析的需求或者目标}
  原始数据：
  {csv格式的原始数据，用,作为分隔符}
  请根据这两部分内容，按照以下指定格式生成内容（此外不要输出任何多余的开头、结尾、注释）
  【【【【【
  {前端 Echarts V5 的 option 配置对象的json代码，合理地将数据进行可视化，不要生成任何多余的内容，比如注释}
  【【【【【
  {明确的数据分析结论、越详细越好，不要生成多余的注释}
  ```
- **优化技巧**：
  - **使用分隔符**：如 `【【【【【`，便于后端程序通过正则表达式准确提取 JSON 内容，避免解析失败。
  - **控制输出格式**：明确要求“不要输出任何多余的开头、结尾、注释”，减少 Token 消耗并降低解析难度。
  - **Few-shot (少样本学习)**：虽然上面的 Prompt 是 Zero-shot，但在实际调优中，我会加入 1-2 个成功的问答对作为示例，显著提升了复杂图表的生成成功率。

### 2. AIGC 响应时间长，你用了哪些手段优化用户体验？

- **异步化处理**：用户提交任务后，后端立即返回“任务已提交”的响应，不让前端阻塞。
- **自定义 IO 密集型线程池**：由于对接 AI 接口主要是网络等待，使用大核心线程数的池子并发处理多个请求。
- **消息队列 (RabbitMQ)**：将任务存入 MQ，实现削峰填谷。AI 模块作为消费者慢慢处理，保证系统不被瞬间高并发压垮。
- **前端实时反馈**：结合 **SSE (Server-Sent Events)** 或 WebSocket，将处理进度（如“正在分析数据...”、“正在生成图表...”）实时推送到前端。

### 3. 如何保证分布式环境下的限流准确性？

- **Redisson RateLimiter**：基于 Redis 的 Lua 脚本实现。
- **优势**：所有服务器节点共享同一个 Redis 计数器，保证了全局限流的原子性和一致性。避免了单机限流（如 Guava）在多实例部署下失效的问题。

### 4. 你的项目中对用户上传的原始数据文件做了哪些校验？

- **后缀名校验**：仅允许 `.csv`, `.xlsx`, `.xls` 等指定格式，防止恶意脚本上传。
- **大小校验**：限制单文件大小（如 5MB），防止大文件耗尽服务器带宽和磁盘空间。
- **内容校验**：检查文件头信息（Magic Number）防止改后缀伪造文件；使用 EasyExcel 等工具解析，捕获解析异常防止损坏文件导致后台崩溃。

### 5. AI 生成的图表 JSON 不合法导致前端渲染失败，怎么处理？

- **后端重试**：在 Prompt 中强化格式约束，如果解析失败，自动降低温度（Temperature）参数重新请求一次。
- **正则提取**：不直接解析整个回复，而是用正则表达式提取 `{` 到 `}` 之间的内容。
- **兜底展示**：如果多次重试失败，前端展示“图表生成失败，请尝试优化分析诉求”的提示，并保留原始数据表格展示。

### 6. RabbitMQ 的死信队列在项目中是怎么用的？

- **场景**：当 AI 服务处理超时、抛出不可恢复异常（如 Token 额度耗尽）或消息重试次数耗尽时。
- **处理**：消息会被自动转发到死信队列（DLX）。
- **人工干预**：后台开启一个消费者专门监听死信队列，将失败的任务记录到数据库的“异常任务表”，并发送告警邮件给管理员，方便后续排查或人工重试。

### 7. 你的 IO 密集型线程池具体是怎么配置的？为什么这么配？

- **配置**：核心线程数 = CPU 核数 \* 2，最大线程数 = CPU 核数 \* 4，队列容量 = 1000。
- **原因**：对接 AIGC 接口属于典型的 **IO 密集型任务**，线程大部分时间在等待网络响应（Sleep），CPU 占用率很低。多开线程可以让 CPU 在等待时去处理其他任务，提高吞吐量。

### 8. 为什么使用 SSE 而不是 WebSocket？

- **SSE (Server-Sent Events)**：服务端单向推送。BI 场景下，主要是后端推给前端（分析进度、图表结果），不需要前端频繁发消息给后端。
- **优势**：协议简单（基于 HTTP），轻量级，支持自动重连。相比 WebSocket 的全双工长连接，资源消耗更少，更适合这种单向通知场景。

### 9. 原始数据包含敏感信息怎么办？

- **数据脱敏**：在发送给 AI 之前，对手机号、身份证等敏感字段进行正则替换（如 `138****1234`）或 Hash 处理。
- **本地部署**：如果数据极其敏感，考虑对接本地部署的开源大模型（如 Llama 3），数据不出内网。

### 10. 你参加的项目里挑一个展开说说主要参与的环节以及满意的成果具体体现是什么？

- **选择项目**：基于大模型的智能 BI 平台。
- **主要参与环节**：
  - **架构演进与异步解耦**：
    - 面对 AIGC 接口响应慢（15s+）导致的线程阻塞问题，我主导了从“同步调用”到“**RabbitMQ + 线程池**”的异步化改造。
    - 针对 AIGC 任务 **IO 密集型**（CPU 占用低、网络等待长）的特点，我设计了自定义线程池（核心线程数 = CPU核数 \* 2），配合 MQ 的削峰填谷，彻底解决了 Tomcat 线程耗尽的问题。
  - **高并发流量治理**：
    - 为了防止恶意刷接口耗尽 Token 额度，我使用 **Redisson RateLimiter**（基于**令牌桶算法**）实现了针对单用户的分布式限流（如每秒 2 次），保障了系统在突发流量下的稳定性。
  - **数据存储与安全设计**：
    - 在数据存储上，我设计了\*\*“原始数据分表存储”\*\*方案，为每个用户上传的文件动态创建独立的数据表，避免了单表数据量过大导致的查询性能下降。
    - 在文件上传环节，除了后缀名校验，我还引入了**文件头（Magic Number）校验**，防止伪造文件格式上传，确保了系统安全。
  - **Prompt 工程与容错**：
    - 针对 AI 生成 JSON 不稳定的痛点，我设计了“角色设定 + 格式约束 + Few-shot（少样本学习）”的组合 Prompt，并配合后端**正则提取**和**重试机制**，将图表生成的可用性提升了一个台阶。
- **满意的成果**：
  - **性能质变**：通过异步化改造，将原本同步等待 AIGC 响应的长耗时过程，优化为“提交即响应”的异步模式，极大提升了用户体验。
  - **稳定性提升**：经本地多线程模拟测试，系统在高频提交任务时未出现线程阻塞或 OOM 现象，且通过限流策略有效保护了后端服务。
  - **业务闭环**：成功支撑了从“数据上传 -> 分表存储 -> 异步分析 -> 可视化展示”的全链路，大幅降低了用户进行数据分析的门槛。

### 11. 为什么选择 RabbitMQ？相比其他消息队列（Kafka、RocketMQ）有哪些优缺点？

- **为什么选 RabbitMQ**：
  - **低延迟**：RabbitMQ 的延迟在**微秒级**，而 Kafka 和 RocketMQ 通常在毫秒级。虽然 AIGC 生成本身慢，但我们希望用户提交任务的反馈是即时的。
  - **可靠性高**：RabbitMQ 提供了成熟的消息确认机制（Ack）、持久化、死信队列等，能确保“任务不丢失”，这对用户体验至关重要。
  - **灵活路由**：它支持多种交换机（Direct, Topic, Fanout），适合我们项目中复杂的业务路由场景（如不同优先级的任务分发）。
  - **轻量易用**：对于我们目前的业务体量（中等规模），RabbitMQ 部署简单，管理界面友好，且社区文档丰富，开发成本低。
- **对比其他 MQ**：
  - **Kafka**：
    - **优点**：**吞吐量极高**（百万级），适合大数据日志采集、实时流计算。
    - **缺点**：延迟相对较高，且功能主要侧重于顺序写入和消费，对单条消息的复杂路由支持不如 RabbitMQ 灵活。我们项目不是处理海量日志，而是处理关键业务任务，所以 RabbitMQ 更合适。
  - **RocketMQ**：
    - **优点**：**Java 实现**（方便二次开发），吞吐量高，支持**分布式事务**和**延时消息**。
    - **缺点**：虽然功能强大，但在社区活跃度和全球生态上略逊于 RabbitMQ（针对中小项目而言）。如果是阿里系的电商大促场景，RocketMQ 是首选，但对于 BI 平台，RabbitMQ 的轻量和低延迟更匹配。

### 12. 成果中的这些数据（如 200ms、3倍吞吐量等）是怎么得出来的？

- **200ms 的响应时间**：
  - **来源**：通过浏览器的 **Network 面板** 或后端接口日志统计。
  - **解释**：在引入 MQ 异步化之前，接口需要等待 AI 生成结果（同步），耗时取决于 AI 响应（约 15s）；引入异步化后，接口只需将任务写入数据库并发送消息到 MQ 即可返回“提交成功”，这部分逻辑仅涉及简单的 DB 和 MQ 操作，耗时通常在 200ms 以内。
- **3 倍吞吐量提升**：
  - **来源**：使用 **JMeter** 或 **Postman** 进行压力测试。
  - **对比**：
    - **优化前**：使用 Tomcat 默认线程池，同步调用 AI，大量线程由于等待网络 IO 而阻塞，导致后续请求排队。
    - **优化后**：使用自定义的 IO 密集型线程池 + MQ 削峰，主线程不阻塞，单位时间内系统能接收和处理的任务数显著增加。
- **95% 的可用率**：
  - **来源**：后端**异常日志统计**和 **SQL 聚合查询**。
  - **计算**：统计 `chart` 表中 `status` 为“生成成功”的任务数占总任务数的比例。通过 Prompt 优化（Few-shot） and 后端正则提取容错，将原本因 AI 幻觉导致的 JSON 解析错误大幅减少，从而提升了成功率。

### 13. RabbitMQ 有哪几种交换机？你在项目中选择了哪个？

- **四种交换机**：
  - **Direct Exchange (直连)**：点对点。消息中的 `Routing Key` 必须完全匹配 `Binding Key` 才会发送到队列。
  - **Fanout Exchange (广播)**：消息发送到所有绑定到该交换机的队列，忽略 `Routing Key`。
  - **Topic Exchange (主题)**：支持通配符匹配（`*` 匹配一个单词，`#` 匹配多个单词），灵活性最强。
  - **Headers Exchange (头)**：根据消息头（Headers）属性进行匹配，性能较差，极少使用。
- **我的选择**：
  - 我使用了 **Direct Exchange**。
  - **原因**：我的业务场景是明确的点对点任务分发（如 `bi_exchange` -> `bi_queue`），只需要将 AI 生成任务准确投递到指定的队列即可，不需要复杂的路由规则，Direct 模式性能最好且最简单。

### 14. 既然用的是阿里的通义千问大模型，那这个项目的性能是否有遇到瓶颈？如何优化？

- **瓶颈 1：QPS 限制与调用频率超限**
  - **问题**：免费版或基础版的大模型 API 通常有严格的 QPS 限制（如每秒 2 次），一旦并发请求过多，会直接返回 429 错误，导致任务失败。
  - **优化**：
    - **限流**：使用 Redisson RateLimiter 在本地进行限流，确保请求速率不超过 API 限制。
    - **重试**：对于偶发的限流报错，结合 Spring Retry 或 Guava Retrying 进行指数退避重试。
    - **负载均衡**：申请多个 API Key（或多个大模型渠道），通过轮询算法分散请求压力。
- **瓶颈 2：Token 上限与长文本处理**
  - **问题**：大模型对输入 Token 数有限制（如 8k 或 32k），当用户上传的 Excel 数据量较大（如几千行）时，直接拼接 Prompt 会报错或被截断。
  - **优化**：
    - **数据压缩**：不传原始 CSV，而是只传“列名 + 前 5 行示例 + 数据统计特征（最大/最小/平均值）”，让 AI 理解数据结构即可，无需读取全量数据。
    - **文件分片**：如果必须处理全量数据，采用 MapReduce 思想，将大文件切分为小块并发处理，最后再合并结果（适用于数据清洗场景）。
- **瓶颈 3：响应时间长（同步阻塞）**
  - **问题**：AI 生成图表耗时 10-30s，用户等待焦虑，且长时间占用 Tomcat 线程。
  - **优化**：
    - **异步化**：RabbitMQ + 线程池，实现“提交即响应”。
    - **前端反馈**：使用 SSE（Server-Sent Events）实时推送“正在分析数据...”、“正在生成图表...”等进度状态，缓解用户等待焦虑。
- **瓶颈 4：数据存储成本与查询慢**
  - **问题**：随着用户生成图表增多，单表数据膨胀，查询变慢；且生成的 JSON 存储占用空间大。
  - **优化**：
    - **分库分表**：按用户 ID 分表，减少单表数据量。
    - **冷热分离**：将 3 个月前的历史图表归档到冷库（如 OSS 或 MongoDB），主库只存热点数据。
    - **压缩存储**：对 JSON 数据进行 Gzip 压缩后再存入数据库，读取时解压，以时间换空间。

### 15. 你没做过大规模压测，怎么验证优化效果的？（如果被问到压测）

- **回答策略**：**承认资源限制 + 强调验证方法**。
- **参考话术**：
  - “坦白说，由于是校园项目，受限于服务器资源，我没有条件做企业级的大规模压测（如万级 QPS）。但我通过**本地模拟**验证了优化思路的有效性。”
  - **我是怎么验证的**：
    1. **工具**：我使用了 **JMeter**（或 Postman Runner）在本地开启了 50-100 个线程循环发送请求，模拟多用户并发。
    2. **观察（关键点）**：
       - **优化前**：我打开 **JConsole** 或 **VisualVM** 监控 JVM，发现 Tomcat 的默认线程池（200个）很快被打满，大量线程处于 `WAITING` 状态（在等 AI 响应），后续请求直接超时。
       - **优化后**：引入 MQ 和自定义线程池后，我再次观察，发现 Tomcat 线程非常空闲（任务提交完就释放了），而后台的 IO 密集型线程池在有序地处理任务。
    3. **结论**：虽然绝对数值（QPS）受限于本机性能，但**线程状态的变化**直观地证明了“异步解耦”方案确实解决了线程阻塞问题，系统架构的吞吐上限在理论上得到了质的提升。

***

### 16. 你的限流维度是什么？用的是哪种算法？为什么选择它？

- **限流维度**：
  - **用户维度 (User ID)**：针对每个用户限制调用频率（如每秒 2 次）。防止单个恶意用户刷爆接口。
  - **全局维度 (Global)**：针对整个系统对 AI 接口的总调用量进行限制（如每秒 20 次）。这是为了保护下游的第三方 AI 服务不被压垮，避免触发其 QPS 封禁。
- **限流算法**：
  - 我使用的是 **令牌桶算法 (Token Bucket)**。
  - **实现方式**：通过 **Redisson** 的 `RRateLimiter` 组件实现。Redisson 底层利用 Redis 的 Lua 脚本保证了原子性。
- **为什么选择令牌桶**：
  - **应对突发流量**：令牌桶允许一定程度的突发流量（只要桶里有令牌就能拿），而漏桶算法（Leaky Bucket）则是强行匀速处理。对于用户体验来说，偶尔手快点两下应该被允许，而不是直接报错。
  - **实现简单**：Redisson 封装得很好，开箱即用，不需要自己手写复杂的算法逻辑，且支持分布式环境下的限流（多台服务器共享 Redis 里的令牌桶）。
  - **对比计数器算法**：固定窗口计数器在窗口切换瞬间会有“临界问题”（可能放过 2 倍流量），令牌桶则更加平滑和准确。

### 17. 你的线程池是怎么设置参数的？（核心线程数、最大线程数、队列长度）为什么这么设？

- **参数设置（示例）**：
  - **核心线程数 (corePoolSize)**：`CPU核数 * 2`（假设机器是 4 核，设为 8）。
  - **最大线程数 (maximumPoolSize)**：`CPU核数 * 4`（设为 16）。
  - **阻塞队列 (workQueue)**：`ArrayBlockingQueue`，长度设为 `1000`。
  - **拒绝策略 (RejectedExecutionHandler)**：`CallerRunsPolicy`（由提交任务的线程去执行）。
- **为什么这么设？（设计依据）**：
  - **任务类型决定核心参数**：对接 AIGC 接口属于典型的 **IO 密集型任务**。线程大部分时间都在等待网络响应（`WAITING` 或 `TIMED_WAITING` 状态），并不怎么消耗 CPU。所以为了提高吞吐量，**线程数要设置得比 CPU 核心数大得多**，这样在一些线程等待网络时，其他线程可以继续接客。
  - **队列长度的权衡**：如果用无界队列（`LinkedBlockingQueue` 默认长度 `Integer.MAX_VALUE`），在高并发下如果 AI 接口阻塞，大量任务堆积在内存里会导致 **OOM（内存溢出）**。所以我用了有界队列 `ArrayBlockingQueue(1000)` 来兜底。
  - **拒绝策略的选择**：既然是发给 AI 的核心业务，我们**不能随便丢弃任务**（抛异常或丢弃策略都不行）。我选择了 `CallerRunsPolicy`，当线程池和队列都满了的时候，让负责从 MQ 拉取消息的那个消费者主线程自己去执行 AI 任务。这起到了一个天然的“**限流/降速**”作用：主线程去干活了，拉取 MQ 消息的速度就慢了，防止了系统被进一步压垮。

### 18. 你是怎么保证 AI 分析任务在 MQ 中不丢失、不重复、不乱序的？

- **不丢失（可靠性保证）**：
  - **生产者端**：开启 `Confirm` 确认机制。消息发送到 MQ 后，MQ 会回调返回 Ack，如果没有收到 Ack，生产者会进行重试，确保消息成功到达交换机。
  - **MQ 端**：开启**持久化**。将 Exchange、Queue 和 Message 的 `durable` 属性都设置为 `true`，防止 MQ 宕机重启后数据丢失。
  - **消费者端**：关闭自动 Ack，改为**手动 Ack**。只有当 AI 生成完成且结果成功保存到数据库后，才调用 `basicAck` 确认消费。如果中间抛出异常或宕机，消息会重新入队（`basicNack` 且 `requeue=true`）。
  - **兜底方案**：配置了**死信队列 (DLX)**。如果消息重试达到最大次数（如 3 次）依然失败（比如 AI Token 欠费），消息会被路由到死信队列。后台会有定时任务或人工介入处理死信队列中的异常任务，并更新数据库中的任务状态为“生成失败”。
- **不重复（幂等性保证）**：
  - **问题场景**：消费者处理完了任务，但在发送 Ack 时网络抖动，导致 MQ 以为没消费，重新投递了一次。
  - **解决方案**：**业务状态机 + 唯一键**。每个消息都有唯一的 `ChartId`。消费者拿到消息后，先去数据库查这个图表的状态，如果是“已生成”或“生成失败”，直接返回 Ack 丢弃消息。只有状态是“等待中”时才去调用 AI。
- **不乱序（顺序性保证）**：
  - **分析**：在智能 BI 这个项目中，各个用户的图表生成任务是相互独立的，**不需要保证绝对的全局顺序**。谁先生成出来都可以。
  - **补充**：如果业务真的需要严格保序（比如同一个订单的创建、支付、发货消息），我的方案是：让这些消息使用同一个 Routing Key，路由到同一个 Queue，并且消费者只能是单线程（或者通过内存队列做 Hash 分发），以此来保证局部顺序。

### 19. 你在开发这个 BI 项目时遇到的最大挑战是什么？你是怎么解决的？

- **挑战背景**：
  - 在项目初期，我是**同步调用** AI 接口来生成图表。
  - 问题是，AI 的生成速度非常慢（通常需要 10-20 秒）。一旦有几个用户同时提交请求，后端的 Tomcat 线程池（默认 200 个）很快就会被这些“等待中”的线程占满。
  - **后果**：整个系统直接卡死，后续的轻量级请求（如登录、查列表）也进不来，直接超时报错，系统可用性极差。

- **解决过程（STAR法则）**：
  1.  **分析 (Situation)**：通过 JConsole/VisualVM 监控发现，大量线程处于 `WAITING` 状态，CPU 利用率却很低。我意识到这是典型的**IO 密集型阻塞**问题。
  2.  **方案 (Task)**：必须将“提交任务”和“执行任务”解耦，不能让 Web 线程傻等。
  3.  **行动 (Action)**：
      - **引入消息队列**：我引入了 **RabbitMQ**。用户提交请求后，后端只负责把任务信息（如 ChartID）快速写入 MQ，立刻返回“提交成功”给前端（耗时 < 100ms）。
      - **异步消费者**：在后台启动一个独立的消费者服务（或监听器），专门从 MQ 拉取任务，慢悠悠地调用 AI 接口。
      - **线程池优化**：针对消费者端，我设计了一个**自定义的 IO 密集型线程池**（核心线程数 = CPU核数 * 2），专门用来处理这些耗时的 AI 请求，确保 CPU 在等待 AI 回复时能去处理其他任务。
      - **限流保护**：为了防止 MQ 积压太快，我又加上了 **Redisson 限流**，限制每个用户每秒只能发 2 个任务。
  4.  **结果 (Result)**：
      - 接口响应时间从 20 秒降到了 100 毫秒以内。
      - 系统在 100+ 并发下依然能流畅运行，Tomcat 线程不再阻塞。
      - 用户体验从“转圈圈等待”变成了“提交即走，稍后收通知”，彻底解决了卡死问题。

### 20. 你提到对用户上传的文件“动态创建了独立数据表”，具体是如何实现的？

- **为什么这么做？**
  - 如果把所有用户上传的 Excel 数据都存到同一张大表里，数据量会爆炸（几百万行），查询和分析速度会越来越慢。
  - 而且不同用户上传的 Excel 列名、列数都不一样，没法用一张固定的表结构来存。

- **实现流程**：
  1.  **解析表头**：用户上传 Excel 后，我使用 **EasyExcel** 库读取文件的第一行（表头），获取所有列名（如“日期”、“销售额”）。
  2.  **生成建表 SQL**：
      - 在代码中拼接 `CREATE TABLE` 语句。
      - 表名设计为 `chart_{chartId}`（如 `chart_10086`），保证唯一性。
      - 字段名就是刚才解析出来的列名，类型统一先设为 `VARCHAR` 或 `TEXT`（为了兼容性，后续分析再由 AI 或代码做类型转换）。
  3.  **动态执行**：
      - 使用 MyBatis 或 JDBC 的 `execute()` 方法，在数据库中执行这条 Create SQL，创建出一张专属的小表。
  4.  **数据入库**：
      - 再次读取 Excel 的数据行，拼接 `INSERT INTO chart_10086 VALUES (...)` 语句，批量插入到这张新表中。
  5.  **记录元数据**：
      - 在主表 `chart_info` 中记录下 `chartId` 和对应的原始文件名，方便后续管理。

- **好处**：
  - **物理隔离**：每个图表的数据互不干扰，查询速度极快（只查这张小表）。
  - **灵活**：完全不用担心用户的列名不一样，动态建表完美适应了结构不固定的 CSV/Excel 数据。



***

### 21. 你的 RabbitMQ 和线程池是一起使用的吗？为什么要这么搭配？

- **是的，它们是配合使用的。**
- **分工明确**：
  - **RabbitMQ (缓冲层)**：负责**削峰填谷**。当前端并发请求极高时，MQ 先把任务暂存起来，保证 Web 服务器不被瞬间压垮，实现“提交”与“执行”的解耦。
  - **线程池 (工作层)**：负责**并发消费**。RabbitMQ 只是存任务，真正干活的是消费者。由于 AI 接口响应慢（IO 密集型），如果消费者是单线程，处理速度太慢会导致 MQ 积压。
- **具体配合**：
  - 我在消费者端配置了 `SimpleRabbitListenerContainerFactory`，设置了 `concurrentConsumers`（并发消费者数）或者在 Listener 内部使用自定义的 `ExecutorService`。
  - 这样，后台可以同时开启多个线程（如 20 个）并行地从 MQ 拉取消息并调用 AI，最大化利用 CPU 等待 IO 的时间，显著提升了系统的**吞吐量**。
  - **总结**：RabbitMQ 负责“稳”，线程池负责“快”。

***

### 22. 你为什么要做这个基于大模型的智能 BI 项目？

- **痛点驱动（解决实际问题）**：
  - 在学校或实习期间，我发现非技术人员（比如产品同学或者导师）经常需要对一堆 Excel 数据进行快速分析和可视化。
  - 传统的流程是：他们要么自己花大量时间在 Excel 里拉透视表、画图，要么提需求给开发，开发再去写 SQL、接前端 Echarts 库，整个流程非常繁琐，沟通成本很高。
- **价值体现（降本增效）**：
  - 恰逢大模型技术爆发，我意识到大模型在**代码生成（JSON配置）**和**数据总结**方面有着天然的优势。
  - 所以我想做一个平台：让业务人员只需“上传 Excel + 输入一句话需求”，就能直接拿到图表和分析结论。这能把原来可能需要半天的“数据分析与可视化”工作，压缩到几十秒内完成，真正做到**降低数据分析门槛，实现降本增效**。
- **顺理成章的挑战**：
  - 也是在落地这个想法的过程中，我自然而然地遇到了“AI 接口响应慢卡死系统”、“Token 成本高”等现实问题，从而倒逼我去使用了 RabbitMQ 异步化、Redisson 限流等后端高并发治理手段。

***

## 项目二：智能编程练习与评测平台 (AI辅助教学)

### 1. 判题机架构是如何设计的？为什么要用工厂模式和策略模式？

- **架构**：判题中心 -> 抽象代码沙箱接口 -> 具体沙箱实现（远程/本地/第三方）。
- **工厂模式 (Factory Pattern)**：
  - **解决问题**：解决了“代码沙箱实例创建逻辑复杂且硬编码”的问题。
  - **具体场景**：项目中支持多种沙箱（示例沙箱、远程 Docker 沙箱、第三方沙箱）。我定义了一个 `CodeSandboxFactory`，根据配置文件中的字符串（如 `type: remote`）动态创建对应的实现类。
  - **如果不用**：代码中会充斥着 `new RemoteCodeSandbox()`, `new ThirdPartyCodeSandbox()` 的硬编码。如果未来要新增一种沙箱，需要修改多处业务代码，违反了**开闭原则**。
- **策略模式 (Strategy Pattern)**：
  - **解决问题**：解决了“判题逻辑臃肿且难以扩展”的问题。
  - **具体场景**：不同编程语言的判题规则不同。例如 Java 需要额外检查堆内存（`-Xmx`），C++ 需要关注编译链接，Python 需要关注解释器路径。我将这些差异化逻辑封装在 `JavaJudgeStrategy`, `CppJudgeStrategy` 等策略类中。
  - **如果不用**：`JudgeService` 里会写满 `if (language.equals("java")) { ... } else if (language.equals("cpp")) { ... }`。这种“面条代码”极难维护，每加一种语言都要改主流程代码，容易引入 Bug。

### 2. 为什么在 AI 建议模块使用责任链模式？你这条责任链上大概有哪几个节点？是怎么做到“动态选择”的？

- **背景**：AI 辅助教学需要分层指导，比如：先检查语法错误 -> 再给出算法优化建议 -> 最后提供代码风格建议。
- **责任链模式 (Chain of Responsibility)**：将这些逻辑封装在不同的 `Handler` 中，按顺序处理。
- **我的责任链节点 (Handlers)**：
  1. **语法检查 (SyntaxCheckHandler)**：如果是编译失败，直接返回编译错误信息，中断后续流程（不需要再给算法建议了）。
  2. **代码风格 (StyleCheckHandler)**：检查变量命名是否规范（如驼峰命名）、是否有冗余空行等，给出重构建议。
  3. **算法优化 (AlgorithmOptimizeHandler)**：分析代码的时间复杂度和空间复杂度，如果复杂度过高（如 O(n^2)），给出优化思路（如“建议使用哈希表优化查找”）。
  4. **安全审计 (SecurityAuditHandler)**：检查代码中是否有危险操作（如尝试读取文件），如果有，直接拦截并警告。
- **动态选择机制**：
  - **实现方式**：我在 `Handler` 接口中定义了一个 `support(Context context)` 方法。
  - **具体流程**：在链式调用时，每个 Handler 先判断当前的上下文（主要是**用户是否提交了代码**）。
    - **场景 A：用户未提交代码（纯咨询 AI）**：责任链会**跳过** `SyntaxCheckHandler`（语法检查）和 `StyleCheckHandler`（代码风格检查），直接进入 `AlgorithmConsultHandler`（算法思路咨询），因为没有代码可查。
    - **场景 B：用户提交了代码（求助 Debug）**：责任链会**优先执行** `SyntaxCheckHandler`，如果发现语法错误（编译失败），直接中断链条并返回错误提示，避免浪费资源去跑后续的算法优化分析。
  - **好处**：这种机制避免了在用户没给代码时瞎检查，也避免了在代码编译都通不过时瞎优化，提升了 AI 响应的准确性和效率。

### 3. 代码沙箱的安全机制你是如何模拟和解决的？

- **进程隔离**：使用 Docker 容器运行用户代码，防止用户访问宿主机文件系统。
- **资源限制**：通过 JVM 参数 `-Xmx` 限制堆内存，利用 Docker 的 `--memory` 和 `--cpus` 限制容器资源。
- **执行监控**：使用守护线程监控子进程，结合 `Thread.sleep` 等待机制实现超时强制中断（`process.destroy()`）。

### 4. 你遇到过哪些典型沙箱异常？是怎么模拟、监控、兜底处理的？

- **异常 1：无限死循环 (Time Limit Exceeded)**
  - **模拟**：提交一段 `while(true){}` 代码。
  - **监控**：在启动用户代码进程后，开启一个守护线程（或使用 `CompletableFuture`）进行计时。
  - **处理**：如果超过预设的最大运行时间（如 5s），守护线程直接调用 `process.destroy()` 强制杀死子进程，并返回“超时”状态。
- **异常 2：内存泄露/爆内存 (Memory Limit Exceeded)**
  - **模拟**：提交一段 `new byte[1024 * 1024 * 1024]`（分配 1GB 数组）的代码。
  - **监控**：
    - **Java 层**：启动命令中添加 `-Xmx256m` 参数。
    - **Docker 层**：启动容器时添加 `--memory=256m` 参数。
  - **处理**：如果程序超限，JVM 会抛出 `OutOfMemoryError`，或者容器会被 Docker 的 OOM Killer 杀死（Exit Code 137）。沙箱捕获到退出码或错误日志后，返回“内存溢出”状态。
- **异常 3：恶意操作 (Security Breach)**
  - **模拟**：提交代码尝试读取 `/etc/passwd` 或执行 `rm -rf /`。
  - **监控**：使用 **Java Security Manager**（旧版）或 **Seccomp**（Docker）限制系统调用。
  - **处理**：当程序尝试执行禁止的操作（如文件读写、网络请求）时，会抛出 `AccessControlException`。沙箱捕获该异常，判定为“非法操作”并封禁该提交。
- **兜底策略**：
  - **资源清理**：无论程序是正常结束还是异常崩溃，使用 `try-finally` 块确保 Docker 容器被销毁，临时文件被删除，防止磁盘空间耗尽。

### 5. Spring Cloud Gateway 在项目中起到了什么作用？

- **统一路由**：将前端请求统一分发到不同的微服务（用户服务、判题服务、题目服务等）。
- **聚合服务**：在网关层对各服务接口进行聚合，简化前端调用逻辑。
- **保护服务**：实现全局跨域配置（`CorsWebFilter`）、身份认证、流量监控，保护后端服务不直接暴露在公网。

### 6. Dockerfile 是什么？在你的项目中是怎么写的？

- **Dockerfile**：用于构建 Docker 镜像的脚本文件。
- **项目示例**：
  ```dockerfile
  FROM openjdk:8-jdk-alpine  # 基础镜像
  WORKDIR /app               # 工作目录
  COPY target/app.jar .      # 复制 jar 包
  CMD ["java", "-jar", "app.jar"] # 启动命令
  ```
- **多阶段构建**：为了减小镜像体积，可以先在一个镜像里编译（Maven），再把编译好的 jar 包复制到另一个精简的 JRE 镜像里运行。

### 7. 什么是 JWT？你是如何实现登录认证的？

- **JWT (JSON Web Token)**：一种无状态的认证机制，包含 Header, Payload, Signature。
- **实现流程**：
  1. 用户登录成功后，服务器生成 JWT（包含 userId、过期时间），返回给前端。
  2. 前端将 Token 存在 localStorage，每次请求都在 Header 带上 `Authorization: Bearer <token>`。
  3. 网关层的 GlobalFilter 拦截请求，解析 Token 校验合法性，通过则放行。

### 8. 判题任务非常耗时，如果判题机挂了，任务会丢失吗？

- **不会**。
- **机制**：判题任务是存放在 RabbitMQ 的队列中的。判题机作为消费者，开启了**手动 Ack**。
- **流程**：只有当判题逻辑完全执行完毕并保存结果后，才会发送 Ack。如果判题机在执行过程中挂掉（没发 Ack），RabbitMQ 会检测到连接断开，自动将该消息重新入队，分发给其他健康的判题机实例。

### 9. 用户提交恶意代码（如死循环、内存泄露）怎么办？

- **死循环**：在 Java `Process` 启动后，使用 `CompletableFuture` 或守护线程计时。如果超过 5 秒（可配置）未结束，直接调用 `process.destroy()` 杀死进程，并判为“超时”。
- **内存泄露**：
  - **Java 层**：启动用户代码时加上 `-Xmx256m` 参数。如果超限，JVM 会抛出 OOM 异常，捕获该异常判为“内存溢出”。
  - **Docker 层**：`docker run --memory=256m`，如果容器内存超限，会被 Docker 的 OOM Killer 杀死，返回特定的退出码。

### 10. 既然用了 Nacos，微服务之间的配置如何实现热更新？

- **注解**：在需要热更新的 Bean 上添加 `@RefreshScope` 注解。
- **原理**：Nacos 客户端监听到配置变更后，Spring Cloud 会销毁旧的 Bean，下次调用时重新创建并注入新的配置值。

### 11. 如何实现多种代码沙箱（Java/C++/Python）的灵活调用？

- **抽象接口**：定义 `CodeSandbox` 接口，包含 `executeCode()` 方法。
- **静态工厂**：`CodeSandboxFactory` 根据配置（type="remote" 或 "native"）返回对应的实现类。
- **代理模式**：创建一个 `CodeSandboxProxy` 类，实现日志记录（请求参数、响应结果、耗时），在调用真实沙箱前后打印日志，增强了沙箱功能且不侵入业务逻辑。

***

## 项目三：CUA-SFT数据构造与具身大模型训练

### 1. 什么是 CUA (Computer-Using Agent)？

- **定义**：指具备像人类一样操作电脑（点击、拖拽、输入）能力的 AI 智能体。
- **核心能力**：图形界面（GUI）理解能力和序列化动作决策能力。

### 2. 建立的数据审核流程使标注准确率提升至 90% 以上。你是怎么定义标注准确率的？用了什么指标/方法去量化？中间做了哪一步最关键的改进？

- **准确率定义**：在数据平台中，一条数据从“原始数据”流转到“验收合格”，中间会经历“标注 -> 内审 -> 审核 -> 验收”四个环节。
  - **公式**：`标注准确率 = (一次性通过内审的数据量 / 提交内审的总数据量) * 100%`。即不仅要最终合格，还要减少返工率。
- **量化过程**：
  - **提升前 (约 60%-70%)**：项目初期，组员对标注规范理解不一致（如“点击搜索框”这个动作，有人标为 Click，有人标为 Focus），导致大量数据在“内审”环节被驳回，反复修改，效率极低。
  - **提升后 (90%+)**：经过规范化整改，绝大多数数据能一次性通过内审，极少数需要微调。
- **最关键的改进**：**“知识树规范” + “预标注模板”**。
  - **知识树规范**：我制定了详细的动作映射表（Action Mapping），明确规定了每个 UI 操作对应的标准动作类型（如：所有“输入框聚焦”统一标为 `Type` 而不是 `Click`），消除了歧义。
  - **预标注模板**：针对高频场景（如打开浏览器搜索），我预设了标准的动作序列模板。组员只需要在模板基础上填入具体参数（如搜索关键词），而不是从零开始标，从而从源头上规避了格式错误。

### 3. 什么是 SFT (有监督微调)？为什么数据质量至关重要？

- **SFT**：在预训练模型的基础上，使用“输入-输出”对的数据集进行微调，让模型学会特定任务。
- **数据质量**：SFT 数据的准确性和多样性直接决定了模型的上限。如果训练数据包含错误的操作轨迹，模型就会学坏。
- **项目中如何保障**：建立知识树规范标注、多轮抽检审核、对低质量数据回溯修正。

### 4. 作为项目组长，你如何保证数据标注的准确率提升至 90% 以上？

- **建立规范**：制定详细的数据标注手册，明确操作意图、知识树搭建的准则。
- **分层审核**：实施“初标 -> 互审 -> 组长抽检”的三级质量审核机制。
- **反馈闭环**：根据模型训练结果反推数据问题，针对性调整采集策略和标注颗粒度。
- **团队激励**：定期组织业务培训，解决标注过程中的模糊点。

### 5. 你们采集的数据包含哪些维度？如何保护隐私？

- **维度**：
  - **屏幕截图 (Screenshot)**：每一步操作前的 1920x1080 分辨率截图。
  - **原子动作 (Atomic Action)**：严格限制为 `click`, `rightClick`, `doubleClick`, `type`, `scroll`, `dragTo` 等 7 类标准化动作。
  - **思维链 (Step-by-step Instructions)**：英文描述的操作步骤，与实际动作严格对齐（如 "First open Settings, then..."）。
  - **知识树标签 (Knowledge Points)**：包含 `osworld_overlap`（通用知识点）和 `custom_nodes`（Windows 特有知识点，如 UAC 弹窗处理），确保知识点的覆盖度和多样性。
- **隐私保护**：
  - **脱敏工具**：在采集端自动识别并模糊化敏感区域（如身份证号、密码框）。
  - **环境隔离**：在虚拟机或沙箱环境中进行采集，不使用真实的个人账号登录敏感应用。

### 6. 在构造数据时，如何处理“长程任务”的复杂性？

- **任务分级与指标约束**：
  - **简单任务 (25%)**：25-35 步，如单应用的基础操作。
  - **中等任务 (50%)**：35-50 步，涉及较为复杂的逻辑判断。
  - **高等任务 (25%)**：50-100 步，强制要求 **Multi-app（跨应用）** 交互（如“从 Chrome 搜索图片下载后用 Photoshop 编辑”），占比需达 10% 以上。
- **真实性与多样性**：
  - **去合成感**：优先使用 Win 键搜索、快捷键（Win+V 剪贴板）等真实用户的高频操作路径，拒绝机械式的死板操作。
  - **知识点扩展**：在覆盖 Agent Arena 主池软件（Chrome, VSCode）的基础上，扩展 Windows 特有生态（PowerToys, Winget, Snipping Tool），确保数据的丰富性。

### 7. 数据标注工具是你自己开发的吗？有什么特点？

- **是的**。基于 Electron 或浏览器插件开发。
- **特点**：
  - **自动录制**：自动捕获鼠标点击和键盘事件，减少人工手动输入。
  - **DOM 元素高亮**：鼠标悬停时自动识别并高亮 UI 元素，方便获取元素坐标和属性。
  - **回放验证**：支持一键回放录制的操作轨迹，快速验证数据的正确性。

### 8. 这里的“模型训练反馈”，你具体收到的是什么内容？是谁给你的？

- **反馈来源**：甲方（某互联网大厂的模型团队）。我们和复旦大学团队作为乙方，分别负责数据构造和学术指导。甲方会拿我们交付的数据去跑内部的 SFT 训练和 Eval 评测，然后定期（如每周）把评估报告反馈给我们。
- **反馈形式**：通常是 **Excel 报表** + **Bad Case（错误案例）录屏**。
- **具体内容**：
  - **1. 成功率指标 (Success Rate)**：
    - 例如：“在 Chrome 场景下，模型成功率只有 40%，但在 VSCode 场景下有 80%。”
    - **我的动作**：这说明 Chrome 场景的数据量不够，或者标注质量差。我会安排组员**针对性增补 Chrome 场景的数据**。
  - **2. 幻觉案例 (Hallucination)**：
    - 例如：“模型在点击 '保存' 按钮时，经常点偏到旁边的 '取消' 按钮。”
    - **原因分析**：这是因为数据中标注的 `click` 坐标点不够精确（标在了按钮边缘），或者 DOM 元素定位有偏差。
    - **我的动作**：修改标注规范，要求点击动作必须标在**控件几何中心**，并组织回溯修正这批脏数据。
  - **3. 步骤冗余 (Redundancy)**：
    - 例如：“模型学会了在打开网页前无意义地刷新三次页面。”
    - **原因分析**：部分采集员有“强迫症”习惯，录制时带入了多余动作。
    - **我的动作**：在审核环节增加“原子性检查”，剔除一切非必要的冗余操作。

### 9. 你们团队大概多少人？你在推进过程中，遇到过最典型的一个协作/进度问题是什么，你是怎么解决的？

- **团队规模**：约 80 人（包括 5-10 名核心管理/质检人员，以及 70+ 名兼职/全职数据采集员）。
- **典型问题**：**“数据同质化严重导致任务分配不均”**。
  - **背景**：项目中期，为了赶进度，我们让采集员自由发挥去录制数据。结果发现大家为了省事，**80% 的人都去录制最简单的“浏览器搜索”任务**（因为最好录，几秒钟一条），而像“PS修图”、“VSCode调试”这种复杂但高价值的任务几乎没人做。
  - **后果**：
    - 数据多样性极差，模型只学会了搜网页，不会干正事。
    - 简单任务的数据量严重溢出，复杂任务缺口巨大，**项目面临延期风险**。
  - **我的解决动作**：
    1. **紧急叫停与库存盘点**：暂停自由录制，花 2 天时间对现有数据进行分类统计，明确各类目（如 Office、DevTools、Media）的缺口。
    2. **配额制任务分发**：
       - 我设计了一套\*\*“任务配额表”\*\*，强制规定每个小组每天必须完成 X 条复杂任务（如 5 条 VSCode、3 条 Excel）。
       - 只有完成了复杂任务的配额，才能去录制简单任务来凑数。
    3. **阶梯定价激励**：
       - 向甲方申请预算调整，将复杂任务（如 >50 步的 Multi-app 任务）的单价提升为简单任务的 **2-3 倍**。
       - 设立“困难任务攻坚榜”，对每周产出高质量复杂数据最多的前 3 名给予额外奖金。
  - **结果**：
    - 一周内，复杂任务的产出率提升了 **400%**。
    - 数据分布从“倒金字塔”（全是简单题）变成了健康的“正态分布”，最终按时按质交付了多样性达标的数据集。

***

## Java基础与并发 (拷打区)

### 1. 你的力扣解题超过 550 题，请讲讲你对算法的理解？

- **核心**：空间换时间（哈希表、前缀和）、规模缩小（分治、递归）、最优子结构（动态规划）。
- **感悟**：算法不是为了背题，而是为了在处理复杂业务逻辑时，能选择最合适的数据结构和解法。

### 2. 既然获得了 HCIA-AI 认证，请简述大模型的训练流程？

- **预训练 (Pre-training)**：在大规模文本数据上进行无监督学习，学习语言规律。
- **SFT (有监督微调)**：在特定任务数据上进行微调。
- **RLHF (基于人类反馈的强化学习)**：让模型输出更符合人类价值观和偏好。

### 3. ThreadLocal 为什么会内存泄漏？在你的项目中有用到吗？

- **原因**：ThreadLocalMap 的 Key 是弱引用，但 Value 是强引用。如果 ThreadLocal 对象被回收，但线程还在（如线程池线程），Value 就永远无法被回收。
- **解决**：使用完后手动调用 `remove()`。
- **应用**：在网关或拦截器中存储当前登录用户信息，方便在 Service 层直接获取。

### 4. 线程池的参数如何根据业务场景选择？

- **CPU 密集型**（如加密、压缩）：线程数 = CPU 核数 + 1。减少切换，跑满 CPU。
- **IO 密集型**（如你的 BI 平台）：线程数 = CPU 核数 \* 2（或更多）。CPU 等待 IO 时，让其他线程接力。

### 5. HashMap 与 ConcurrentHashMap 的区别？(JDK 1.8)

- **线程安全**：HashMap 线程不安全；ConcurrentHashMap 通过 `CAS + synchronized` 保证线程安全。
- **锁粒度**：JDK 1.8 中 ConcurrentHashMap 锁的是桶的头节点，减少了锁竞争，提高了并发性能。
- **结构**：两者底层都是 `数组 + 链表 + 红黑树`。

### 6. 什么是 Java 的反射机制？优缺点？

- **定义**：在运行状态中，动态获取类的信息（方法、属性）并调用对象的方法。
- **优点**：灵活，是框架（Spring, MyBatis）的灵魂。
- **缺点**：性能稍差（JVM 无法优化），破坏封装性（能访问 private 属性）。

### 7. Java 内存模型 (JMM) 简单介绍一下？

- **主内存与工作内存**：所有变量存在主内存，每个线程有自己的工作内存，线程只能操作工作内存的副本。
- **三大特性**：原子性、可见性、有序性。
- **volatile**：保证可见性和有序性，不保证原子性。

### 8. 抽象类和接口的区别？

- **设计目的**：抽象类是对**事物**的抽象（是啥），接口是对**行为**的抽象（能干啥）。
- **成员变量**：抽象类可以有各种变量；接口只能有 `public static final` 常量。
- **继承**：类只能继承一个抽象类，但可以实现多个接口。

### 9. String, StringBuilder, StringBuffer 的区别？

- **String**：不可变，每次修改都会生成新对象，适合少量的字符串操作。
- **StringBuilder**：可变，线程不安全，效率最高，适合单线程下大量字符串拼接。
- **StringBuffer**：可变，线程安全（加了 synchronized），效率低。

### 10. Java 中 == 和 equals 的区别？

- **==**：基本数据类型比较值；引用数据类型比较内存地址。
- **equals**：默认也是比较内存地址（继承自 Object）。但 String, Integer 等类重写了 equals 方法，变成了比较**值**。

### 11. 什么是深拷贝和浅拷贝？

- **浅拷贝**：只复制对象本身，对象内部的引用类型属性还是指向原来的对象。
- **深拷贝**：完全复制一个新的对象，内部的引用类型属性也会被递归复制。实现方式：重写 `clone()` 或 序列化（JSON转换）。

***

## 数据库与中间件

### 1. 什么是索引失效？你在开发中遇到过吗？

- **失效场景**：
  - 对索引列做运算（`where id + 1 = 10`）。
  - 使用函数（`where ABS(id) = 10`）。
  - 隐式类型转换（字符串不加引号）。
  - 模糊查询 `%` 开头。
- **经验**：通过 `explain` 查看 `key` 字段，确认是否走了索引。

### 2. Redis 为什么不用多线程？

- Redis 的瓶颈通常是**内存和网络带宽**，而不是 CPU。
- 单线程避免了上下文切换、锁竞争。
- Redis 6.0 引入的多线程主要用于处理**网络 IO**，执行命令依然是单线程，保证了简单和安全。

### 3. 如何解决消息队列的幂等性问题？

- **业务层面**：使用数据库唯一索引。
- **状态机**：如果订单已经是“已支付”，再次收到支付消息则忽略。
- **防重表**：在 Redis 记录 messageId，消费前先查。

### 4. MySQL 的事务隔离级别有哪些？MySQL 默认是哪种？

- **读未提交**、**读已提交**、**可重复读**、**串行化**。
- **默认**：**可重复读 (RR)**。通过 MVCC 和 间隙锁 解决了幻读问题。

### 5. 什么是 Redis 缓存雪崩、穿透、击穿？

- **穿透**：查不存在的数据。*解决：布隆过滤器、缓存空值。*
- **击穿**：热点 Key 过期，大量并发打向 DB。*解决：热点 Key 永不过期、加互斥锁。*
- **雪崩**：大量 Key 同时过期。*解决：过期时间加随机值、多级缓存。*

### 6. MySQL 执行一条 SQL 语句的流程是怎样的？

- **连接器**：建立连接，校验权限。
- **分析器**：词法分析（识别 select, from），语法分析（判断 SQL 对不对）。
- **优化器**：决定用哪个索引，join 顺序。
- **执行器**：调用存储引擎接口查数据。
- **存储引擎**：InnoDB/MyISAM，负责数据的存储和提取。

### 7. Redis 持久化 RDB 和 AOF 的区别？

- **RDB (快照)**：存全量数据，恢复快，但会丢最后几秒数据。
- **AOF (日志)**：存写命令，数据全，但文件大，恢复慢。
- **混合持久化**：结合两者优点（Redis 4.0+）。

### 8. 什么是 MySQL 的回表？如何避免？

- **回表**：先在二级索引找到主键 ID，再回聚簇索引（主键索引）查整行数据。
- **避免**：**覆盖索引**。查询的字段刚好都在二级索引里，就不需要回表了。

### 9. MyBatis 中 #{} 和 ${} 的区别？

- **#{}**：预编译处理（PrepareStatement），可以防止 **SQL 注入**。
- **${}**：字符串直接替换，有 SQL 注入风险。常用于动态表名或排序字段。

### 10. Spring 中的 Bean 是线程安全的吗？

- **不是**。Spring 容器中的 Bean 默认是**单例** (Singleton) 的。
- 如果 Bean 里有**可变的成员变量**（有状态），在多线程下是不安全的。
- **解决**：
  1. 尽量不要在 Bean 里定义可变成员变量。
  2. 将 Bean 作用域改为 `prototype`（多例）。
  3. 使用 `ThreadLocal` 封装变量。

### 11. 为什么 MySQL 用 B+ 树做索引而不用 Hash 或 B 树？

- **Hash**：虽然查询快 O(1)，但不支持**范围查询**（> < between）。
- **B 树**：节点里存数据，导致一页（16KB）能存的索引变少，树的高度变高，IO 次数变多。
- **B+ 树**：非叶子节点只存索引，叶子节点存数据且用链表相连。树矮（IO少），支持范围查询。

***

## 新增面试题 (补充)

### 1. Redis 限流用的什么命令，更高级的命令？
- **基础实现**：可以使用 `INCR` 配合 `EXPIRE`（固定窗口），或者 `ZSET` 的 `ZADD` + `ZREMRANGEBYSCORE` + `ZCOUNT`（滑动窗口）。
- **进阶命令（Lua 脚本）**：在真实项目中，很少直接手写原生命令拼接。因为限流需要“先判断令牌够不够，再扣减令牌”，分步执行会有并发安全问题。所以一般会写一段 **Lua 脚本**交给 Redis 执行，Redis 能保证 Lua 脚本执行的**原子性**（一次性执行完，不被打断）。
- **更高级的封装（项目中使用的）**：项目中直接使用了 **Redisson** 框架的 `RRateLimiter`。它底层就是封装了复杂的 Lua 脚本，实现了非常标准的**令牌桶算法**。对于开发者来说，只需要调几个简单的方法就能实现高并发下平滑的分布式限流，完全不需要自己去拼命令。

### 2. Trae 怎么使用的，配置文件源代码看过吗？
- **使用方式**：Trae 是一款 AI 辅助编程 IDE。我平时主要用它来做代码智能补全，遇到报错或者需要重构时，会用它的 Chat 功能提问，或者让它的 Agent 自动去分析上下文帮我写代码（比如生成正则、生成测试）。
- **配置文件**：看过它工作区的一些配置文件。Trae 的规则通常放在 `.trae/rules/` 目录下（比如 `project_rules.md`）。我们可以通过修改这些文件，全局设定 AI 回复的语言（比如要求只用中文）、代码风格或者特定约束，类似于给大模型注入“系统预设 Prompt”。至于它底层的核心源代码是不开源的，但我熟悉它这种基于工作区规则来约束大模型行为的机制。

### 3. MCP 和 Skills 区别是什么？
- **MCP (Model Context Protocol，模型上下文协议)**：这是一种底层的**连接标准/协议**。大模型本来是个“瞎子”，MCP 就像是插头，让大模型能标准化地读取本地文件、查数据库或者调外部 API。它偏向于基础设施。
- **Skills (技能)**：这是在 IDE 或应用中封装好的**具体功能**。比如“一键生成单元测试”、“解释这段代码”。Skills 是面向用户的，它底层可能会利用 MCP 协议去获取代码上下文，然后完成任务。简单说，**MCP 是输水管道，Skills 是通过管道提供服务的水龙头**。

### 4. 假如产品经理给了一张百万行的 Excel 表，包含排行对应的奖励信息，那我如何通过后端将这张表的数据变成 JSON，用 AI 实现。但不能只是单纯的把这张表直接递给 AI，因为 AI 会有幻觉，而且无法处理这种精确而且数据量很大的数据？
- **核心思路：数据处理靠代码，逻辑/格式生成靠 AI。**
- **第一步：后端解析与清洗（代码做）**。千万不能把百万行数据传给 AI，会直接爆 Token 或报错。后端应该使用流式读取工具（如 Java 的 `EasyExcel`），逐行读取数据到内存，变成基础的 Java 对象。
- **第二步：提取“小样本”喂给 AI（AI 做）**。我们只需要把 Excel 的**表头（列名）**、**前 5 行样例数据**，以及**产品经理的转换需求**发给 AI。让 AI 帮我们生成一个 JSON 数据的结构模板，或者直接让 AI 写一段处理数据的 Java 映射代码。
- **第三步：后端组装合并（代码做）**。拿到 AI 确定的 JSON 模板或映射规则后，后端用传统的 JSON 工具（如 Jackson/Fastjson）结合第一步读取的海量数据，在本地服务器上遍历生成最终的 JSON。这样既利用了 AI 懂业务需求的优势，又彻底规避了 AI 处理大数据时的幻觉问题。

### 5. 用户上传给 AI 的需求很模糊怎么解决？
- **方案 1：预设 Prompt 兜底（AI 自动推测）**。在给 AI 的后台提示词里加上规定：“如果用户的需求模糊，请根据上传表格的列名和数据特征，自动推测最合适的分析维度（比如按时间、按分类），并给出默认的分析结果。”
- **方案 2：多轮对话澄清（产品交互）**。后端判断如果 AI 返回“缺乏关键信息”，不强行生成图表，而是让 AI 抛出一个反问（比如：“您是想看每月的销售额，还是各地区的销售额？”），在前端弹窗引导用户补充。
- **方案 3：提供快捷模板（前端防范）**。在输入框旁边给几个“常用分析套路”的按钮让用户点，或者采用填空题形式引导输入，从源头减少模糊表达。

### 6. 用到的消息队列异步处理是什么？
- **通俗理解**：就是把“耗时且不需要立刻知道结果”的任务，放到后台排队慢慢做，不让主程序傻等。
- **项目应用（智能 BI）**：用户点“生成图表”，调用 AI 接口通常要等十几秒。如果同步等，Tomcat 线程池很快就被占满卡死了。所以我把这个生成请求当成一条“消息”扔进 **RabbitMQ（消息队列）**，后端立刻跟前端说“任务已提交”。
- **后台消费**：后台有个单独的程序（消费者）去队列里挨个拿消息，慢慢调 AI 去处理。这样相当于把瞬间涌进来的请求存了起来（削峰填谷），系统就不会崩溃。

### 7. 你一般都用的什么数据库？
- **关系型数据库：MySQL**。用来存核心业务数据，比如用户信息、图表记录、配置信息等。因为 MySQL 支持事务，数据丢不了，且支持复杂的 SQL 查询。
- **非关系型数据库：Redis**。作为基于内存的键值对数据库，速度极快。我在项目里主要用它做缓存（存高频访问的数据）、分布式 Session、以及结合 Redisson 实现分布式的限流器。

### 8. MySQL 锁有哪些？
（按粒度和意图来分，重点记行锁）
- **按粒度分（锁多大范围）**：
  - **全局锁**：锁整个数据库，一般用于全库备份。
  - **表级锁**：锁整张表，开销小但并发度低。
  - **行级锁**：只锁具体的某几行，开销大但并发度高（InnoDB 引擎独有）。
- **按行级锁的类型分（重点，面试常问）**：
  - **记录锁 (Record Lock)**：只锁某一条具体存在的记录。
  - **间隙锁 (Gap Lock)**：锁住两条记录之间的“空隙”，防止别的事务往里面插新数据，用来**解决幻读**问题。
  - **临键锁 (Next-Key Lock)**：记录锁 + 间隙锁的组合，锁住某条记录以及它前面的空隙。
- **按意图分**：
  - **共享锁 (读锁/S锁)**：大家都能读，但谁也不能改。
  - **排他锁 (写锁/X锁)**：加了锁的人能读能写，别人连读都不行。

