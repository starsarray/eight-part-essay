[TOC]

# 简历项目与八股文面试题

## 项目一：Star智能数据分析平台 (SpringBoot + MQ + AIGC)

### 1. 你的项目中提到了使用Redisson的RateLimiter实现分布式限流，为什么要限流？为什么选择Redisson？
- **为什么要限流**：
  - 防止恶意用户或爬虫频繁请求接口，耗尽系统资源。
  - AIGC接口调用成本高、响应慢，如果不限流，大量请求堆积会导致服务雪崩。
- **为什么选Redisson**：
  - **分布式支持**：如果只用Guava的RateLimiter，它仅在单机生效。在微服务或集群部署下，必须用基于Redis的分布式限流来控制全局流量。
  - **开箱即用**：Redisson封装好了API，使用简单，不需要自己写Lua脚本实现令牌桶。

### 2. 这里的RateLimiter底层原理是什么？
- Redisson的RateLimiter底层主要基于**令牌桶算法**（Token Bucket）。
- **原理**：系统以恒定的速率向“桶”里放入令牌。请求进来时，必须先去桶里拿到令牌才能执行；拿不到令牌则被限流（阻塞或拒绝）。
- **相比漏桶算法**：令牌桶允许一定程度的**突发流量**（只要桶里有存货），更适合互联网应用场景。

### 3. 你提到了自定义IO密集型线程池，参数是怎么设置的？
- **IO密集型场景**：AIGC生成、数据库读写、文件上传等，CPU计算少，大部分时间在等待网络/磁盘IO。
- **参数设置**：
  - **核心线程数 (Core Pool Size)**：通常设置为 `CPU核数 * 2` 或更多。让CPU在等待IO时去处理其他线程的任务。
  - **最大线程数 (Max Pool Size)**：可以设置得较大（如 `CPU核数 * 4`），用于应对突发的高并发。
  - **任务队列**：使用有界队列（如 `LinkedBlockingQueue`），容量设为适中值（如1000），防止内存溢出（OOM）。
- **拒绝策略**：
  - 可以选 `CallerRunsPolicy`（调用者运行）：让提交任务的线程自己去执行，既不丢弃任务，又能减缓提交速度。
  - 或者 `AbortPolicy`：直接抛异常，让前端提示“服务器繁忙”。

### 4. 为什么要引入RabbitMQ？直接用线程池的队列不行吗？
- **直接用线程池的缺点**：
  - **内存限制**：线程池的队列在内存中，如果AIGC任务积压太多，会撑爆内存（OOM）。
  - **数据丢失**：如果服务重启或宕机，内存里的任务就全丢了，用户体验极差。
- **引入RabbitMQ的优势**：
  - **持久化**：MQ可以将消息存入磁盘，服务重启后任务还在，保证**可靠性**。
  - **削峰填谷**：高峰期请求先存入MQ，消费者按自己的能力慢慢处理，不会压垮系统。
  - **解耦**：接收请求的模块和执行AI生成的模块解耦，互不影响。

### 5. 你的RabbitMQ是怎么保证消息不丢失的？
- **生产者端**：开启 **Confirm Callback** 机制，确保消息成功发送到了交换机。
- **MQ服务端**：将队列和消息都设置为 `durable`（持久化），写入磁盘。
- **消费者端**：开启 **手动Ack**（Manual Acknowledgment）。只有当业务逻辑完全执行成功后，才发送ack给MQ删除消息；如果执行失败，可以发送nack让消息重新入队。

### 6. Spring Boot 自动装配原理是什么？（必问）
- **核心注解**：`@SpringBootApplication`，它包含 `@EnableAutoConfiguration`。
- **流程**：
  1. 启动时扫描 `META-INF/spring.factories`（或新版的 `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports`）文件。
  2. 加载文件中配置的所有自动配置类（`xxxAutoConfiguration`）。
  3. 通过 `@ConditionalOnClass` 等注解判断是否符合条件（比如classpath下有没有某个jar包）。
  4. 如果条件满足，就自动创建 Bean 并注册到 Spring 容器中。

### 7. 如果AIGC生成很慢，前端如何获取结果？
- **轮询 (Polling)**：前端每隔几秒发请求问后端“好了没？”。实现简单，但浪费服务器资源。
- **WebSocket**：建立长连接，后端处理完主动推送到前端。实时性好，适合聊天式AI。
- **SSE (Server-Sent Events)**：服务端单向推送流式数据。ChatGPT 就是用的这个，可以一个字一个字地蹦出来，用户体验最好。

### 8. 既然用到了Redisson，它还能做分布式锁吗？原理是什么？
- **能**，最常用的就是 `redissonClient.getLock("key")`。
- **原理**：基于 Redis 的 Hash 结构和 Lua 脚本（保证原子性）。
- **看门狗 (Watch Dog)**：如果你没有设置锁的过期时间，Redisson 会默认给锁30秒过期。然后启动一个后台线程，每隔10秒（1/3时间）检查一下，如果线程还在运行，就自动给锁续期。这样防止业务没跑完锁就过期了。

### 9. RabbitMQ 的消息积压怎么办？（生产环境常见问题）
- **紧急方案**：
  1. 修复消费者逻辑（如果是Bug导致的）。
  2. 临时扩容：增加消费者数量，提高消费速度。
  3. 如果积压太严重，可以写一个临时程序，把消息从队列取出来，转发到容量更大的临时队列（或写入数据库），后续再慢慢处理。
- **预防**：设置消息TTL（过期时间）+ 死信队列（DLX），让处理不完的消息进入死信队列，避免阻塞主流程。

### 10. Redis 的数据结构有哪些？在你的项目中用了哪些？
- **常见5种**：String, Hash, List, Set, ZSet。
- **项目中应用**：
  - **String**：缓存热点数据（如用户信息）、分布式锁（setnx）。
  - **Hash**：存储对象数据，方便修改单个字段。
  - **List**：简单的消息队列（LPUSH/RPOP）。
  - **ZSet (Sorted Set)**：排行榜（如每日刷题榜单），RateLimiter 底层也用到了 ZSet 来记录时间戳。

### 11. RabbitMQ 重复消费问题（幂等性）怎么解决？
- **原因**：消费者消费成功，但 Ack 发送失败，MQ 认为消息没消费，重发。
- **解决**：
  - **数据库唯一键**：利用 MySQL 主键冲突（Insert Ignore），重复插入会报错。
  - **Redis 记录**：消费前用 `setnx(messageId, 1)` 查一下，如果存在说明消费过了。

---

## 项目二：Star代码练习平台 (SpringCloud + Docker + 设计模式)

### 1. 你在这个项目中用了策略模式，是为了解决什么问题？
- **背景**：不同编程语言（Java, Python, C++）的判题逻辑不同（编译命令、运行参数、内存限制检查方式都不同）。
- **问题**：如果用大量的 `if-else` 判断语言类型，代码会极其臃肿，且难以维护。
- **解决方案**：
  - 定义一个 `JudgeStrategy` 接口。
  - 为每种语言实现一个策略类（如 `JavaJudgeStrategy`, `CppJudgeStrategy`）。
  - 通过 `Context` 类根据传入的语言参数，动态选择对应的策略去执行。
- **好处**：符合**开闭原则**。如果以后要支持 Go 语言，只需要新增一个策略类，不需要修改原有代码。

### 2. 代码沙箱是怎么实现的？如何防止用户代码恶意破坏（如死循环、内存溢出）？
- **核心**：隔离与资源限制。
- **超时控制（防止死循环）**：
  - 创建一个守护线程或使用 `Future.get(timeout)` 来监控执行进程。
  - 如果超过规定时间（如5秒）还在运行，直接调用 `process.destroy()` 强行杀死进程。
- **内存限制（防止OOM）**：
  - **Java**：启动用户代码时加上 `-Xmx` 参数（如 `java -Xmx256m Main`）。
  - **Docker**：使用 `docker run --memory=256m` 直接限制容器内存（更安全）。
- **权限控制**：
  - 使用 Java 的 `SecurityManager`（旧方案）禁止文件读写、网络连接。
  - **最佳实践**：使用 Docker 容器，天然隔离了宿主机的文件系统和网络，用户怎么折腾都出不去容器。

### 3. Spring Cloud Gateway的作用是什么？
- **统一入口**：前端只需访问网关地址，不需要知道后端几十个微服务的具体IP和端口。
- **路由转发**：根据请求路径（如 `/api/user`）转发到对应的 User 服务。
- **鉴权认证**：在网关层通过 Filter 统一校验 Token，拦截非法请求，减轻后端服务负担。
- **解决跨域**：统一配置 `CorsWebFilter`。

### 4. 什么是跨域？你的项目是怎么解决的？
- **跨域 (CORS)**：浏览器的同源策略限制。当 协议、域名、端口 任意一个不同时，前端JS无法请求后端接口。
- **解决**：在 Spring Cloud Gateway 中配置 `CorsWebFilter` Bean。
  - 设置 `Access-Control-Allow-Origin: *`（或指定域名）。
  - 设置 `Access-Control-Allow-Methods: POST, GET, ...`。
  - 这样所有经过网关的请求都会自动带上跨域响应头，后端微服务不需要单独处理。

### 5. Docker 常用命令有哪些？镜像和容器的区别？
- **区别**：
  - **镜像 (Image)**：是静态的只读模板（类比 Java 的 Class）。
  - **容器 (Container)**：是镜像运行时的实例（类比 Java 的 Object 对象）。
- **常用命令**：
  - `docker ps`：查看运行中的容器。
  - `docker images`：查看本地镜像。
  - `docker run -d -p 80:80 nginx`：后台启动并映射端口。
  - `docker exec -it <id> /bin/bash`：进入容器内部。
  - `docker build -t myapp .`：根据 Dockerfile 构建镜像。

### 6. 你的项目中除了策略模式，还用了什么设计模式？
- **静态工厂模式**：在创建代码沙箱实例时，根据配置（Remote/ThirdParty/Native）使用工厂类直接返回对应的沙箱实现，对调用方隐藏创建细节。
- **单例模式**：Spring 的 Bean 默认都是单例的。
- **模板方法模式**：代码沙箱的执行流程（编译 -> 运行 -> 收集结果 -> 清理）是固定的，可以定义一个抽象父类实现这个流程，具体的“编译”和“运行”步骤留给子类去实现。

### 7. 微服务之间是怎么调用的？OpenFeign 的原理？
- **OpenFeign**：是一个声明式的 HTTP 客户端。你只需要定义一个接口，加上 `@FeignClient` 注解，像调用本地方法一样调用远程服务。
- **原理**：
  1. 启动时，Feign 会扫描注解，生成接口的**动态代理**对象。
  2. 当你调用接口方法时，代理对象会将请求参数封装成 HTTP 请求。
  3. 通过 Ribbon/LoadBalancer 进行**负载均衡**，选择一个健康的服务实例。
  4. 发送 HTTP 请求并获取响应。

### 8. Nacos 在项目中起什么作用？
- **注册中心**：所有微服务启动时，都会把自己（IP+Port）注册到 Nacos。服务之间调用时，先去 Nacos 拉取可用列表，实现服务发现。
- **配置中心**：将 application.yml 中的配置（如数据库密码、开关配置）放到 Nacos 上统一管理。修改配置后，服务可以**动态刷新**（`@RefreshScope`），不需要重启。

### 9. CAP 理论了解吗？Nacos 是 CP 还是 AP？
- **CAP**：一致性 (Consistency)、可用性 (Availability)、分区容错性 (Partition tolerance)。P 是必选的，只能在 C 和 A 之间权衡。
- **Nacos**：默认是 **AP**（高可用），但也支持切换为 CP（强一致，类似 Zookeeper）。适合服务注册发现，允许短暂的数据不一致。

### 10. 什么是服务雪崩？熔断降级（Sentinel）是什么？
- **服务雪崩**：A 调用 B，B 挂了，A 的线程卡死在等 B，导致 A 也挂了，连锁反应。
- **熔断**：保险丝。当检测到错误率过高时，直接切断请求，不再调用 B，保护 A。
- **降级**：熔断后，返回一个兜底数据（如“系统繁忙”），保证用户体验。

---

## Java基础与并发 (必问八股)

### 1. ArrayList 和 LinkedList 的区别？
- **底层数据结构**：ArrayList 是**数组**；LinkedList 是**双向链表**。
- **查询性能**：ArrayList 支持随机访问（下标），查询快 `O(1)`；LinkedList 需要遍历，查询慢 `O(n)`。
- **增删性能**：
  - ArrayList 增删需要数组扩容或拷贝（System.arraycopy），较慢。
  - LinkedList 只需要修改指针，增删快。
- **使用场景**：绝大多数情况用 ArrayList，只有在频繁在头部/中间插入删除时才考虑 LinkedList。

### 2. HashMap 的底层原理？（高频）
- **JDK 1.8 结构**：**数组 + 链表 + 红黑树**。
- **Put 流程**：
  1. 计算 key 的 hash 值，定位到数组下标。
  2. 如果无冲突，直接存入。
  3. 如果有冲突（Hash Collision），用链表串起来（拉链法）。
  4. **树化**：当链表长度 > 8 且 数组长度 > 64 时，链表转为**红黑树**，将查找复杂度从 `O(n)` 降低到 `O(log n)`。
- **扩容**：当元素数量 > 数组长度 * 0.75（负载因子）时，数组扩容为2倍，并重新计算Hash位置。

### 3. synchronized 和 Lock 的区别？
| 特性 | synchronized | Lock (如 ReentrantLock) |
| :--- | :--- | :--- |
| **层面** | JVM 关键字 | JDK API 接口 |
| **释放** | 自动释放（代码块结束或异常） | 必须手动 `unlock()`（建议在 finally 块中） |
| **灵活性** | 不支持判断锁状态 | 支持 `tryLock()`（尝试获取）、超时获取 |
| **公平性** | 非公平锁 | 支持公平锁和非公平锁 |

### 4. ThreadLocal 是什么？原理？
- **作用**：线程本地变量。每个线程都有自己独立的副本，互不干扰。常用在保存用户登录信息（UserContext）、数据库连接等。
- **原理**：每个 Thread 对象里都有一个 `ThreadLocalMap`。
  - key 是 `ThreadLocal` 对象本身。
  - value 是你存的值。
- **内存泄漏问题**：ThreadLocalMap 的 key 是弱引用，但 value 是强引用。如果 ThreadLocal 被回收了，value 还在。**必须在使用完后调用 `remove()` 方法清理**。

### 5. volatile 关键字的作用？
- **保证可见性**：一个线程修改了变量，其他线程立刻可见（通过强制刷回主内存）。
- **禁止指令重排序**：保证代码执行顺序，比如在单例模式的双重检查锁（DCL）中防止对象初始化未完成就被使用。
- **注意**：它**不保证原子性**（比如 `i++` 用 volatile 也不安全，还是得加锁）。

### 6. 线程池的核心状态有哪些？
- **RUNNING**：接受新任务，处理队列任务。
- **SHUTDOWN**：不接新任务，但处理队列里剩下的。
- **STOP**：不接新任务，不处理队列任务，中断正在执行的任务（调用 `shutdownNow()`）。
- **TERMINATED**：所有任务结束，线程池关闭。

### 7. ConcurrentHashMap 1.7 和 1.8 的区别？
- **1.7**：**分段锁 (Segment)**。把大数组切成16段，每段配一把锁（ReentrantLock）。并发度是16。
- **1.8**：**CAS + synchronized**。抛弃了Segment，直接锁数组的头节点（Node）。只要hash不冲突，CAS直接插入；冲突了就synchronized锁这一个链表/红黑树。并发度大大提高。

### 8. JVM 的内存区域有哪些？
- **堆 (Heap)**：存对象实例（new 出来的都在这），GC 主要管理区域。
- **方法区 (Method Area)**：存类信息、常量、静态变量。JDK 1.8 后叫元空间 (Metaspace)，使用本地内存。
- **虚拟机栈 (VM Stack)**：存局部变量表、操作数栈。每个方法调用都会创建一个栈帧。
- **本地方法栈 (Native Method Stack)**：给 native 方法用的。
- **程序计数器 (PC Register)**：记录当前线程执行到哪一行字节码了。

### 9. 垃圾回收算法有哪些？GC Roots 是什么？
- **常见算法**：
  - **标记-清除**：标记垃圾，直接清除。缺点：产生内存碎片。
  - **复制算法**：内存分两半，每次只用一半，存活的复制到另一半。缺点：内存利用率低（年轻代常用）。
  - **标记-整理**：标记存活对象，往一端移动，清除剩下的。无碎片。
- **GC Roots**：垃圾回收的起点。如果一个对象从 GC Roots 出发引用不到，就是垃圾。
  - 栈中引用的对象（局部变量）。
  - 方法区中静态属性引用的对象。

### 10. 什么是 CAS？ABA 问题怎么解决？
- **CAS (Compare And Swap)**：比较并交换。是乐观锁的核心。包含三个值：内存地址V、旧预期值A、新值B。只有当V等于A时，才把V改成B。
- **ABA 问题**：A 变成了 B，又变回了 A。CAS 会觉得它没变。
  - **解决**：加**版本号**（AtomicStampedReference）。1A -> 2B -> 3A。

### 11. AQS 原理简单介绍一下？
- **AQS (AbstractQueuedSynchronizer)**：是 Java 并发包的核心框架，ReentrantLock, CountDownLatch 都基于它。
- **核心**：
  - 一个 `volatile int state` 变量代表同步状态（0无锁，1有锁）。
  - 一个 **FIFO 双向队列**。
- **流程**：线程抢锁失败，就封装成节点加入队列等待；当锁释放时，唤醒队列头的线程去抢锁。

### 12. Spring IOC 和 AOP 的原理？
- **IOC (控制反转)**：对象不由我们 `new`，而是交给 Spring 容器管理。通过 **依赖注入 (DI)** 将对象注入到需要的地方。底层是 Map<String, Object> 单例池。
- **AOP (面向切面)**：将日志、事务等非业务代码抽取出来，动态切入到业务方法中。底层是 **动态代理**（接口用 JDK Proxy，类用 CGLIB）。

### 13. Spring Bean 的生命周期？（简述）
- **实例化**：`createBeanInstance` (new 对象)。
- **属性赋值**：`populateBean` (依赖注入)。
- **初始化**：
  - 调用 `BeanNameAware`, `BeanFactoryAware` 等接口。
  - 执行 `BeanPostProcessor` 的前置方法。
  - 调用 `init-method`。
  - 执行 `BeanPostProcessor` 的后置方法 (AOP 在这步完成)。
- **销毁**：容器关闭时调用 `destroy`。

### 14. 类加载机制？什么是双亲委派模型？
- **加载器**：Bootstrap (启动类) -> Extension (扩展类) -> Application (应用类) -> Custom (自定义)。
- **双亲委派**：类加载时，先委托给父加载器去加载，父加载器找不到才自己加载。
- **好处**：安全。防止用户自己写一个 `java.lang.String` 类替换核心类。

---

## 数据库与中间件 (MySQL, Redis)

### 1. 事务的四大特性 (ACID)？
- **原子性 (Atomicity)**：要么全部成功，要么全部失败回滚（Undo Log 实现）。
- **一致性 (Consistency)**：数据必须从一个合法状态变为另一个合法状态。
- **隔离性 (Isolation)**：并发事务之间互不干扰（锁 + MVCC 实现）。
- **持久性 (Durability)**：事务提交后，数据永久保存（Redo Log 实现）。

### 2. 索引为什么要用 B+ 树？
- **磁盘IO少**：B+树非叶子节点只存索引，不存数据，一个节点能存更多索引，树的高度更低（通常3-4层），只需要3-4次磁盘IO就能找到数据。
- **范围查询快**：B+树的叶子节点用**双向链表**串联，非常适合 `order by` 或 `where id > 10` 这种范围查询。

### 3. Redis 缓存穿透、击穿、雪崩的区别？
- **缓存穿透**：查**不存在**的数据。请求直接打到数据库。
  - *解决*：布隆过滤器、缓存空值。
- **缓存击穿**：**热点Key**突然过期。大量并发直接打到数据库。
  - *解决*：逻辑过期（永不过期）、加互斥锁。
- **缓存雪崩**：**大量Key同时过期**或Redis宕机。
  - *解决*：过期时间加随机值、Redis集群高可用。

### 4. MySQL 的事务隔离级别有哪些？
- **读未提交 (Read Uncommitted)**：脏读（读到别人没提交的数据）。
- **读已提交 (Read Committed, RC)**：避免脏读，但不可重复读（由于别人提交了，两次读不一样）。Oracle/SQLServer 默认。
- **可重复读 (Repeatable Read, RR)**：避免不可重复读。**MySQL 默认级别**。
- **串行化 (Serializable)**：最慢，最安全。

### 5. Redis 的持久化机制 RDB 和 AOF？
- **RDB (快照)**：
  - 把某一时刻的数据全部 dump 到磁盘文件。
  - *优点*：恢复快，文件小。
  - *缺点*：会丢最后几秒的数据。
- **AOF (日志)**：
  - 记录每一条写命令。
  - *优点*：数据丢得少（可以配置每秒刷盘）。
  - *缺点*：文件大，恢复慢。
- **混合持久化**：RDB + AOF 结合（Redis 4.0+），既快又安全。

### 6. 什么是聚簇索引和非聚簇索引？
- **聚簇索引 (Clustered Index)**：
  - 数据和索引存在一起。MySQL InnoDB 的**主键索引**就是聚簇索引，叶子节点存的是整行数据。
  - 一张表只能有一个。
- **非聚簇索引 (Secondary Index)**：
  - 叶子节点存的是**主键ID**。
  - 查询时通常需要**回表**（先拿到ID，再去聚簇索引里找整行数据），除非查询字段全在索引里（覆盖索引）。

### 7. MySQL 的 MVCC (多版本并发控制) 是什么？
- **作用**：实现 **读已提交 (RC)** 和 **可重复读 (RR)** 隔离级别，让读写操作不用加锁也能并发执行。
- **原理**：
  - **隐藏字段**：每行数据都有隐藏的事务ID (trx_id) 和 回滚指针 (roll_pointer)。
  - **Undo Log**：记录历史版本数据，通过回滚指针串成一条链。
  - **ReadView**：读数据时生成一个快照，判断当前事务能看到哪个版本的数据。

### 8. SQL 优化有哪些常见手段？Explain 怎么看？
- **优化手段**：
  - **建索引**：对 WHERE, JOIN, ORDER BY 的字段建索引。
  - **避免 Select ***：只查需要的列，尽量走覆盖索引。
  - **避免索引失效**：不要在索引列上做计算、函数、类型转换；`like` 不要以 `%` 开头。
  - **Limit 优化**：大数据量分页时，用 `ID > xxx limit 10` 代替 `limit 10000, 10`。
- **Explain 关键字段**：
  - **type**：system > const > eq_ref > ref > range > index > all (全表扫描，最差)。
  - **key**：实际用到的索引。
  - **rows**：扫描行数，越少越好。

### 9. Redis 为什么这么快？(单线程模型)
- **纯内存操作**：读写都在内存，纳秒级。
- **单线程**：避免了线程切换和锁竞争的开销。
- **IO 多路复用**：使用 epoll 模型，一个线程能处理成千上万个 Socket 连接。

### 10. MySQL 的锁机制？（行锁、表锁、间隙锁）
- **行锁 (Row Lock)**：锁住一行数据。InnoDB 默认支持。并发高。
- **表锁 (Table Lock)**：锁住整张表。MyISAM 默认。并发低。
- **间隙锁 (Gap Lock)**：在 RR 隔离级别下，锁住索引之间的空隙，防止其他事务插入新数据，解决**幻读**问题。

### 11. Redis 的过期策略和内存淘汰策略？
- **过期策略**：
  - **惰性删除**：查的时候发现过期了再删。
  - **定期删除**：每隔 100ms 随机抽查一些 key 删除。
  - *Redis 是两者结合使用。*
- **内存淘汰策略** (内存满了怎么办？)：
  - **allkeys-lru**：移除最久未使用的 key (最常用)。
  - **volatile-lru**：只在设置了过期时间的 key 里移除最久未使用的。
  - **noeviction**：不移除，直接报错 (默认)。
