[toc]

# 一、AI 工具使用与学习方法

## 1. 你提到会用 Claude Code、Trae 等工具高效学习，能具体讲讲你平时是怎么使用这些工具的吗？

**参考回答：**

我一般把 AI 工具当成“技术学习的加速器”，但不会完全依赖它。我的使用方式通常是一个四步流程。

第一步是**先让 AI 帮我建立知识地图**。比如我接触一个新框架时，我会先让 Claude Code 或 Trae 帮我梳理：这个技术是解决什么问题的、核心概念有哪些、最小可运行 Demo 需要哪些组件、常见坑有哪些。这样我能先建立整体认知，而不是一上来就陷在细节里。

第二步是**结合官方文档做最小闭环**。我通常会先看 Quick Start，然后让 AI 帮我把官方示例改成更贴合我当前场景的 Demo，比如改成我熟悉的语言、项目结构或者业务场景。这样我学习速度会快很多。

第三步是**把 AI 用在实验和验证上**。比如我会让它帮我生成最小样例、补测试用例、解释报错、比较不同写法。但最终我会通过跑通代码、看日志、对照官方文档来验证，而不是直接相信它。

第四步是**做总结沉淀**。我会让 AI 帮我把这次学习整理成一页式笔记，比如核心概念、关键 API、常见问题、适用场景。这样后面复习和迁移到新项目会更快。

所以对我来说，Claude Code、Trae 这类工具最大的价值不是“替我思考”，而是帮我**更快建立认知、缩短试错时间、提高信息获取效率**。

------

## 2. 你能举一个具体例子，说明你是如何借助 AI 工具快速上手一个之前没接触过的技术的吗？

**参考回答：**

可以。比如我之前快速上手 **MCP 或者某个 LLM SDK/Agent 框架** 时，就是这么做的。

我先让 AI 帮我回答三个问题：
 第一，这个技术的定位是什么；
 第二，它最核心的概念有哪些；
 第三，最小可运行案例应该长什么样。

然后我去看官方文档的 Quick Start 和核心章节，把最小 Demo 先跑起来。这个过程中，AI 的作用主要是把文档里的信息“翻译成我当前更容易执行的步骤”，比如根据我的开发语言和环境，生成一个本地可运行版本。

等 Demo 跑通以后，我会继续让 AI 帮我做两件事：
 一个是**解释关键代码**，让我知道每一段在干什么；
 另一个是**扩展场景**，比如加一个新工具、加配置、改成异步调用、改成更接近业务的结构。

最后我会自己做一个小总结，比如：

- 这个技术解决什么问题
- 核心对象或模块是什么
- 最容易踩坑的是哪里
- 什么时候该用，什么时候不该用

这样一套下来，通常我能在很短时间里从“完全没接触过”到“能跑起来、能解释、能做简单改造”。我觉得 AI 最大的帮助是**压缩了从文档理解到动手实践的时间**。

------

## 3. 你在使用 Claude Code 或 Trae 辅助写代码时，通常会把它们用在哪些环节？

**参考回答：**

我一般会把它们用在五类环节。

第一类是**需求理解和任务拆解**。有时候需求描述很口语化，我会先让 AI 帮我拆成更明确的技术任务，比如输入输出是什么、有哪些边界条件、哪些点适合写测试。

第二类是**方案探索**。比如一个功能可以有几种实现方式，我会让它先给出多种方案，对比复杂度、可维护性和适用场景。这个阶段我不一定直接拿代码，而是先拿思路。

第三类是**样板代码和最小实现**。像接口封装、DTO、基础 CRUD、测试骨架、配置代码这些重复性比较强的部分，我会让 AI 先生成初稿，然后我自己调整。

第四类是**调试和报错分析**。遇到编译错误、依赖冲突、接口调用异常、返回结构不符合预期时，我会把报错信息、上下文代码、运行环境一起给它，让它帮我缩小排查范围。这个环节节省时间很明显。

第五类是**重构和可读性优化**。比如把长函数拆开、统一命名、抽公共方法、补注释、补测试、优化异常处理，这些 AI 做初步整理很快。

但我不会把核心业务判断直接全交给 AI，尤其是涉及架构边界、数据一致性、安全合规这些地方，还是要自己做主导。

------

## 4. 你有没有遇到过 AI 工具“看起来说得对，实际上是错的”这种情况？你通常是怎么发现并修正的？

**参考回答：**

有，而且这种情况其实挺常见，尤其在新版本框架、细节 API、配置项、或者边界场景上。

我一般是通过三种方式发现它不对。
 第一是**跑不通**，比如方法名、参数、依赖版本看起来都很合理，但实际编译不过或者运行报错。
 第二是**和官方文档对不上**，比如它说某个配置这么写，但文档里已经变了。
 第三是**结果不符合预期**，虽然代码能跑，但行为不对，比如返回结构错了、异常没处理好、性能有问题。

我修正的流程通常是：

1. **先缩小问题范围**，确认是版本问题、上下文缺失，还是 AI 直接幻觉了。
2. **回到官方文档和示例**，尤其看 Quick Start、API reference、Release Notes。
3. **做最小复现**，把问题缩成最小可运行样例。
4. **重新给 AI 更完整的上下文**，比如明确版本号、错误日志、项目结构，再让它修正。

所以我的习惯是：
 **AI 可以帮我加速，但不能代替验证。**
 真正可靠的是“AI + 文档 + 运行结果”三者交叉验证。

------

## 5. 你通常怎么读官方文档？是通读、先看 Quick Start，还是带着问题去读？

**参考回答：**

我一般不会一上来通读全量文档，而是**先建立目标，再按层次阅读**。

通常第一步我会先看 **Overview / Introduction**，明确这个技术是干什么的、核心概念是什么。
 第二步我会看 **Quick Start**，因为我更倾向先把最小 Demo 跑起来。
 第三步是带着问题读，比如我当前要做的是工具调用、流式输出、会话管理还是部署，那我就定向去看对应章节。
 第四步才是看更细的 API 文档、配置项、最佳实践、FAQ、Release Notes。

我的经验是，文档不是拿来“从头到尾背”的，而是拿来**支撑当前任务闭环**的。
 所以我更偏向“**先跑通，再补理论；先做闭环，再做深入**”。

------

## 6. 很多官方文档内容很多、很杂，你怎么快速定位自己真正需要的内容？

**参考回答：**

我主要靠三个方法。

第一是**先把需求拆成具体问题**。比如不是笼统地学某个框架，而是拆成“怎么接模型”“怎么接工具”“怎么流式输出”“怎么做记忆”“怎么部署”。问题一具体，文档定位就快很多。

第二是**优先看文档结构**。我会先看目录、Getting Started、Core Concepts、How-to Guides、API Reference、Examples。一般来说：

- Quick Start 解决“怎么跑起来”
- Concepts 解决“为什么这么设计”
- How-to 解决“怎么完成具体任务”
- API Reference 解决“参数细节”

第三是**配合关键词检索**。包括在文档站内搜核心术语、报错信息、配置项名称、版本差异关键词。必要时我也会看 changelog 和 GitHub issue，但最终还是以官方文档为准。

所以我的方法不是“从海量信息里硬看”，而是先把信息需求结构化，再去定位。

------

## 7. 如果让你快速学习一个 AI 相关框架，比如 LangChain、Spring AI、OpenAI SDK，你会怎么制定学习路径？

**参考回答：**

我会按“**认知—跑通—扩展—总结**”四个阶段来学。

第一阶段是**认知**。
 先搞清楚这个框架的定位：它解决什么问题，和同类方案相比有什么特点，适合什么场景，不适合什么场景。

第二阶段是**跑通最小闭环**。
 我会先完成一个最小 Demo，比如：

- 发起一次模型调用
- 接收返回结果
- 做一次简单的 prompt 输入输出
   如果是 Agent 框架，那就再加一个简单工具调用。
   这个阶段的目标不是做复杂，而是尽快建立“可运行的感知”。

第三阶段是**围绕核心能力逐步扩展**。
 比如按模块学习：

- 模型接入
- 提示词管理
- 结构化输出
- 工具调用
- 记忆 / 上下文
- RAG
- 流式输出
- 异常处理
- 可观测性
- 部署方式

第四阶段是**做一个贴近业务的小项目**。
 比如做一个知识问答、代码助手、文档总结器、工单助手等。只有做到这一步，才算真正会用，而不是只会跑示例。

最后我会总结一份自己的笔记，回答这几个问题：

- 它最核心的抽象是什么
- 它在哪些地方明显提升开发效率
- 它的局限是什么
- 以后在什么项目里会优先考虑它

------

# 二、Claude Code / Trae 的使用体验与比较

## 8. Claude Code 你实际用下来感觉怎么样？你会怎么评价它的优点和不足？

**参考回答：**

我对 Claude Code 的整体感觉是：**在代码理解、长上下文处理、重构建议和工程任务拆解上体验比较好**，比较适合做“协作式开发”。

它给我的一个明显感觉是，不只是生成几行代码，而是更像一个能参与上下文理解的开发助手。比如当项目代码比较长、涉及多个文件、需要先理解结构再动手改的时候，它表现会比较自然。尤其是在让我解释某段代码为什么这么写、帮我梳理重构思路、或者基于已有代码风格续写时，我感觉它的连贯性比较好。

优点方面，我会总结成几点：

- **理解上下文能力强**，对多文件、多步骤任务更友好
- **解释型能力较好**，不只是给答案，也比较擅长说明原因
- **重构、补测试、读代码**这类任务体验不错
- 对于“先分析，再实施”的工作方式比较匹配

不足也有：

- 有时候会**过度自信**，尤其在版本细节、具体 API 上看起来很像对的
- 如果上下文给得不完整，它也可能按自己的理解补全，结果方向就偏了
- 在一些非常依赖本地环境、构建链、工程细节的场景里，还是需要开发者自己兜底
- 对于特别强约束、特别工程化的修改，还是要靠明确指令和反复校正

所以我的真实感受是，它不是“自动写代码机器”，而是一个**很强的代码协作助手**。你越会给上下文、越会拆任务，它的效果越好。

------

## 9. Trae 你使用下来感觉怎么样？和 Claude Code 相比，它在体验上有什么不同？

**参考回答：**

Trae 给我的感觉更偏向**IDE 内嵌式、面向日常开发流的 AI 编程助手**，整体使用门槛比较低，融入编辑器工作的感觉会更强一些。

和 Claude Code 相比，我觉得最大的差异不只是模型本身，而是**交互方式和工作场景**。
 Claude Code 更像一个偏“任务协作”和“上下文分析”的助手，适合你把一个相对完整的问题交给它，让它先分析再行动。
 Trae 这种工具则更像嵌在编码流里的帮手，适合你边写边补、边改边问、边报错边修。

我的实际体验是：

- Trae 在**代码补全、局部修改、快速问答、日常开发辅助**上会更顺手
- Claude Code 在**较复杂任务拆解、长上下文理解、跨文件修改建议**上给我的感觉更强一些
- 如果我要处理的是“一个明确的小任务”，Trae 这类工具会更轻量
- 如果是“要先理解一堆上下文，再改动多个地方”，我更倾向 Claude Code 这类模式

当然，这两类产品也在不断互相靠近，边界没有那么绝对。但如果从真实使用感来说，我会这么区分。

------

## 10. 你觉得 Claude Code 和 Trae 分别有什么特点？各自更适合什么场景？

**参考回答：**

如果做一个比较务实的区分，我会这么看：

**Claude Code 更适合：**

- 阅读和理解已有项目
- 多文件联动修改
- 重构建议
- 先分析再编码
- 学习陌生代码或陌生框架
- 让它帮你梳理技术方案

**Trae 更适合：**

- 日常编码过程中的即时辅助
- 快速补全、局部改写
- 报错排查
- 生成样板代码
- 小步快跑式迭代
- 作为 IDE 开发流的一部分持续使用

如果从“感觉”上说，Claude Code 更像是一个**能参与思考的协作者**，Trae 更像是一个**贴着编码动作走的助手**。

所以我一般不是二选一，而是按场景用：

- **学习新技术、读代码、做方案**时，我更倾向 Claude Code
- **日常开发、写样板、快速修改、处理局部问题**时，我更倾向 Trae 这类工具

------

## 11. 你一般会用哪个模型来辅助开发或学习？为什么选它？实际体验怎么样？

**参考回答：**

我一般会按任务选模型，而不是固定只用一个。

如果是**代码理解、技术解释、长上下文分析、重构思路**这类任务，我会优先选偏强推理、长上下文能力更好的模型。
 如果是**日常编码补全、快速生成样板、简单问答**，我会选响应更快、交互成本更低的模型。
 如果是**查概念、对比方案、做总结**，我也会看模型在表达清晰度和结构化输出上的表现。

我的真实体验是，没有哪个模型在所有维度都最好。
 有的模型在代码推理和解释上很强，但速度可能慢一些；
 有的模型响应很快，适合频繁交互，但复杂任务上可能不够稳；
 有的模型适合“先分析再执行”，有的适合“高频小步协作”。

所以我更倾向的做法是：

- **复杂任务用强模型**
- **高频开发用快模型**
- **最终结果自己验证**

我觉得真正提升效率的关键，不是“永远选最强模型”，而是**按任务类型做合适的匹配**。

------

## 12. 这些 AI 编程工具对你的开发效率具体带来了哪些提升？你觉得目前它们还有哪些明显欠缺？

**参考回答：**

提升我觉得主要体现在四个方面。

第一是**缩短了信息获取时间**。以前我要在搜索引擎、博客、文档之间来回跳，现在很多问题可以先通过 AI 快速得到一个结构化答案，再去验证。

第二是**减少了样板工作**。像基础代码骨架、接口封装、测试模板、注释整理、重构初稿，这些都能节省不少时间。

第三是**加快了排错速度**。尤其是当我把错误日志、代码片段、依赖版本一起给到 AI 时，它能帮我更快定位可能原因，这对开发效率帮助很明显。

第四是**提高了学习速度**。以前学新技术需要先自己摸索很久，现在 AI 可以先帮我建立知识框架，我再结合官方文档实践，整体上手更快。

但明显欠缺也很现实：

- **版本感知还不够稳定**，容易把旧版本写法当成新版本答案
- **对隐含上下文不敏感**，如果你不给完整信息，它会自己“脑补”
- **在复杂工程约束下不够可靠**，比如依赖治理、性能瓶颈、架构边界、安全合规等问题
- **缺少真正的责任意识**，代码能生成，但是否适合上线还是要开发者判断

所以我对它们的定位是：
 **非常好的效率工具，但不是替代工程判断的工具。**

------

# 三、MCP 的理解、发展与工程实践

## 13. 你对 MCP 的理解是什么，它解决的核心问题是什么？

**参考回答：**

我对 MCP 的理解是：**它本质上是一种让模型或 AI Agent 以更标准化方式接入外部能力和上下文的协议/接口思路**。
 它解决的核心问题，不是“模型能不能调用工具”，而是**如何用统一、可扩展、可复用的方式，把工具、资源、提示模板等能力接给模型使用**。

以前很多 AI 应用接工具，都是每个系统自己写一套对接逻辑。这样会有几个问题：

- 每接一个工具都要单独适配
- 不同 Agent / 不同模型之间很难复用
- 上下文资源、工具定义、调用方式不统一
- 工程上维护成本高

MCP 的价值就在于，它试图把这件事做成一种更标准的“连接层”。
 你可以把它理解成：**在模型和外部世界之间提供一个相对统一的能力暴露方式**。
 这样模型客户端、IDE、Agent 框架就可以更方便地接入各种外部能力，比如文件系统、数据库、知识库、代码仓库、内部系统接口等。

所以如果一句话概括，我会说：
 **MCP 解决的是 AI 系统接入外部能力时的标准化和工程复用问题。**

------

## 14. MCP 和传统的 function calling / tool calling 有什么区别？为什么现在大家会越来越关注 MCP？

**参考回答：**

我觉得它们不是完全对立关系，而是**抽象层次不同**。

**Function calling / tool calling** 更像是模型层能力：
 模型识别到用户意图后，按约定输出一个工具调用请求，开发者再去执行这个函数。它解决的是“**模型如何触发某个工具**”。

而 **MCP** 更像是协议层或接入层思路：
 它关注的是“**外部能力如何以标准化方式暴露给模型或 Agent 使用**”。
 这里面不只是工具，还可能包括资源、提示模板、上下文对象等，而且强调客户端和服务端之间的统一对接方式。

简单说：

- **Tool calling** 更偏“调用动作”
- **MCP** 更偏“接入标准和能力组织”

现在大家关注 MCP，我觉得有几个原因：

1. **Agent 应用越来越多，外部工具接入越来越复杂**
    单纯靠每个项目自己硬编码工具适配，扩展性很差。
2. **生态开始追求标准化**
    大家都希望模型、IDE、Agent 平台、工具服务之间能更容易互通。
3. **上下文不只是 prompt 了**
    现在除了工具调用，资源访问、知识注入、系统提示管理、工作流协作都在变复杂，大家需要更统一的框架。

所以我会把 MCP 看成是 AI 工程从“能调用工具”走向“**可标准化接入外部能力**”的一步。

------

## 15. 如果让你设计一个基于 MCP 的 AI 应用接入方案，你觉得它的核心组成部分有哪些？

**参考回答：**

如果我设计一个基于 MCP 思路的 AI 应用接入方案，我会把它拆成几层。

**1. 模型/Agent 客户端层**

负责和用户交互、管理会话、组织上下文，并决定什么时候要调用外部能力。

**2. MCP 客户端适配层**

负责和 MCP 服务建立连接，发现有哪些可用能力，比如工具、资源、提示模板等，并把这些能力暴露给上层 Agent。

**3. MCP 服务层**

这是能力提供方。它负责把某类外部能力标准化暴露出来，比如：

- 文件系统访问
- 数据库查询
- Git 仓库操作
- 企业内部 API
- 知识库检索
- 工单系统、搜索系统等

**4. 能力定义层**

要明确每个能力的：

- 名称
- 描述
- 输入输出 schema
- 调用权限
- 错误处理方式
- 超时和重试策略

**5. 上下文与资源管理层**

除了工具，还要考虑资源如何注入上下文，比如文件内容、文档片段、数据库结果、历史会话摘要等。

**6. 安全与治理层**

这层我觉得非常重要，包括：

- 身份认证
- 权限控制
- 审计日志
- 调用频率限制
- 敏感操作确认
- 可观测性与追踪

**7. 编排与回退机制**

当工具调用失败、结果不可信、资源不可达时，要有回退策略，比如降级到纯模型回答、提示用户补充信息、或者切换备用能力。

所以在我看来，一个成熟的 MCP 接入方案，重点不只是“接上工具”，而是要把**能力发现、调用治理、安全控制、上下文组织和可观测性**一起考虑进去。

------

## 16. 你在实际项目里有没有接触过 MCP 风格的工具接入？即使不是严格 MCP，也可以说说你怎么理解它在工程上的价值。

**参考回答：**

如果严格说标准协议级别的 MCP，我接触可能更多是在学习和实践层面；但从工程思路上看，我接触过很多**MCP 风格的能力接入方式**。

比如在一些 AI 应用里，我们会把外部能力抽成统一的工具层：

- 一个工具负责查知识库
- 一个工具负责查数据库
- 一个工具负责调用内部接口
- 一个工具负责读文件或生成报告

虽然不一定完全按 MCP 协议实现，但它背后的思路其实很像：
 **把外部能力标准化封装，让上层 Agent 或模型按统一方式调用。**

我觉得它在工程上的价值很大，主要体现在：

1. **降低耦合度**
    上层不需要关心每个外部系统的接入细节。
2. **提高复用性**
    同一个工具能力可以被多个 Agent、多个业务场景复用。
3. **便于扩展**
    新增一个能力时，只要按统一规范接入，不需要大改主流程。
4. **便于治理**
    统一记录调用日志、控制权限、处理失败重试，会比散落在业务代码里更可控。

所以我对 MCP 的理解，不只是一个协议名词，而是一种非常典型的**AI 外部能力工程化接入思路**。

------

## 17. 你觉得 MCP 在 AI 工程化里最大的价值是什么？是标准化、可扩展性、上下文管理，还是别的？为什么？

**参考回答：**

如果只能选一个，我会优先说是**标准化**，因为它会带动后面的可扩展性、复用性和治理能力。

原因是这样的：
 AI 应用早期很多时候是“能跑就行”，工具调用往往是业务里写死的。这样短期开发快，但一旦工具多了、场景多了、Agent 多了，就会出现维护困难、重复开发、迁移成本高的问题。

MCP 的最大价值在于，它把外部能力接入这件事，从“项目内临时实现”往“标准接口能力”方向推了一步。
 标准化之后，才有可能进一步做到：

- **可扩展**：新增工具不需要每次都重新设计
- **可复用**：同一能力可供多个系统共享
- **可治理**：权限、日志、审计、限流更容易统一
- **可组合**：能力之间可以像积木一样组合进不同 Agent 工作流

上下文管理也很重要，但我觉得上下文管理是更广泛的问题，而 MCP 的一个很突出价值是：
 **让“模型如何接外部世界”这件事更有工程秩序。**

------

## 18. 你能简单讲一下 MCP 的发展背景吗？为什么会出现这样的协议或思路？

**参考回答：**

我觉得 MCP 这类思路的出现，背后是 AI 应用从“单轮对话”走向“可调用外部世界的 Agent 系统”的必然结果。

早期大模型应用很多是纯 prompt 驱动，最多加一点检索增强。
 后来大家越来越希望模型能真正做事，比如：

- 查文件
- 调接口
- 读代码
- 操作数据库
- 调企业内部系统

这时候就出现一个问题：
 每个平台、每个工具、每个 Agent 框架都各自定义一套接法，生态非常分散。
 开发者每做一个场景都要重新适配，复用性很差。

所以行业自然会往两个方向演进：

1. **能力调用从“临时拼接”走向“规范化接口”**
2. **模型上下文从“纯文本 prompt”走向“结构化资源 + 工具 + 提示模板”的组合**

MCP 就是在这样的背景下被关注起来的。
 本质上它反映的是：
 **AI 应用已经不满足于只生成文字，而是需要更标准地连接真实系统和真实数据。**

------

## 19. 你平时接触或使用过哪些比较通用的 MCP 服务/能力？哪些在提升效率上最有帮助？

**参考回答：**

如果说比较通用、很多场景都适合的能力，我觉得主要有这几类：

**1. 文件系统相关**

比如读取项目文件、配置文件、日志文件。
 这类能力在开发辅助、代码理解、排查问题时特别实用。

**2. Git / 仓库相关**

比如查看代码变更、读取仓库结构、分析提交记录。
 对代码助手、研发提效类场景很有帮助。

**3. 文档/知识库检索**

比如检索内部文档、产品说明、技术规范。
 这是企业里最通用的能力之一，很多问答、客服、研发助手都离不开。

**4. 数据库/查询类能力**

比如只读查询某些业务数据、配置数据、统计结果。
 适合报表、运营、分析辅助等场景。

**5. HTTP/API 调用类能力**

这类非常通用，本质上可以把很多内部系统能力统一包装起来。

如果说对效率提升最明显，我会优先选：

- **文档检索**
- **文件系统读取**
- **代码仓库访问**
- **通用 API 调用**

因为这些能力覆盖面广，而且能直接增强模型对“真实上下文”的获取能力。

------

# 四、Skills 的理解、发展与工程实践

## 20. 你理解的 Skills 是什么？它和普通 Prompt、Tool 有什么区别？

**参考回答：**

我理解的 **Skills**，本质上是对某类任务能力的**结构化封装**。
 它通常不只是一个 prompt，也不只是一个 tool，而是把**任务目标、调用条件、执行步骤、所需工具、输入输出约束、结果格式**等内容打包成一个可复用单元。

它和普通 Prompt 的区别在于：
 Prompt 更像一次性的指令文本，重点是告诉模型“你现在怎么回答”。
 而 Skill 更像一个“**面向任务的能力模块**”，强调复用、可组合、可维护。

它和 Tool 的区别在于：
 Tool 更偏执行层，是一个具体动作，比如查数据库、调接口、读文件。
 Skill 更偏任务层，它可能会**组合多个 prompt、多个 tool，甚至带上规则和流程**，来完成一个相对稳定的任务目标。

所以我会这样概括：

- **Prompt**：告诉模型怎么说
- **Tool**：让系统能做什么
- **Skill**：把“怎么说 + 怎么做 + 怎么组织过程”封装成可复用任务能力

这也是为什么 Skills 很适合 AI 工程化，因为它比单个 prompt 更稳定，比单个 tool 更接近业务目标。

------

## 21. 如果在一个 AI Agent 系统里引入 Skills，你会怎么设计 Skills 的组织方式、调用方式和复用机制？

**参考回答：**

如果我来设计，我会把 Skills 当成一层“任务能力模块”，重点做好三件事：组织、调用、复用。

**1. 组织方式**

我会按任务域来组织，比如：

- 文档处理类 Skill
- 数据分析类 Skill
- 代码辅助类 Skill
- 客服问答类 Skill
- 报告生成类 Skill

每个 Skill 里明确：

- 适用场景
- 输入输出定义
- 使用哪些工具
- 依赖哪些上下文
- 成功标准和失败处理方式

**2. 调用方式**

调用上我会支持两种：

- **显式路由**：由上层工作流或规则决定调用哪个 Skill
- **智能路由**：由模型根据任务描述和 Skill 元数据做选择

但为了稳定性，关键业务场景我会尽量采用“模型判断 + 规则约束”的混合方式，而不是完全自由选择。

**3. 复用机制**

复用上我会把 Skill 做成配置化、模块化设计，比如：

- Prompt 模板参数化
- 工具依赖可插拔
- 输出 schema 标准化
- 支持版本管理
- 支持评估与回归测试

这样一个 Skill 不只是一次性逻辑，而是可以在多个 Agent、多个业务场景里复用。

我觉得核心思想是：
 **把 AI 的任务能力做成可管理的软件模块，而不是散落在各处的一堆 prompt。**

------

## 22. 你觉得在实际项目中，Skills 最适合解决哪类问题？为什么它比把所有逻辑都堆在一个大 Prompt 里更好？

**参考回答：**

我觉得 Skills 最适合解决的是那些**高频出现、任务边界相对清晰、流程有一定稳定性、但又需要模型灵活性的任务**。

比如：

- 文档总结
- 信息抽取
- 表格分析
- 代码审查建议
- 工单分类
- 知识问答
- 报告生成
- 多步骤的数据整理

这些任务有一个共同点：
 不是纯规则任务，但也不是完全开放式创作。
 它们既需要模型理解，又需要一定流程控制和输出约束。

相比把所有逻辑都堆进一个大 Prompt，Skills 的好处有几个：

1. **更容易维护**
    大 Prompt 一长，后面很难改。Skill 模块化后，每个能力单独迭代。
2. **更容易复用**
    同一个 Skill 可以被多个场景调用，不用每次重写 prompt。
3. **更容易测试**
    Skill 可以做针对性的评估，比如准确率、格式稳定性、工具使用成功率。
4. **更容易治理**
    可以做版本控制、灰度发布、效果回归，而不是改一段 prompt 后全局受影响。

所以我认为，Skills 的本质优势是：
 **把原本隐含在 Prompt 里的任务知识和流程约束，显式地工程化出来。**

------

## 23. 你能讲一下 Skills 的发展背景吗？为什么大家会从 Prompt 逐步走向 Skill 化、模块化？

**参考回答：**

我觉得 Skills 的出现，本质上是 AI 应用从“Prompt 试验阶段”走向“工程化落地阶段”的自然结果。

早期很多项目是靠一个大 Prompt 快速验证效果，这种方式上线快、试错成本低。但随着项目复杂度上来，就会暴露几个问题：

- Prompt 越写越长，维护困难
- 一个任务里混了很多意图，稳定性下降
- 不同业务场景之间难复用
- 修改一个 Prompt，可能影响整个系统行为
- 很难做系统化测试和评估

所以大家就会逐步把能力拆出来，形成更稳定的任务单元。
 这个过程中，Skills 就变成一种很自然的工程抽象：
 它把提示词、工具、规则、流程、输出格式约束整合成一个可复用能力模块。

换句话说，Prompt 更适合“告诉模型怎么做一件事”，
 而 Skill 更适合“把一类任务沉淀成产品级能力”。

所以从 Prompt 走向 Skill 化，本质是从“写提示词”走向“**构建可治理的 AI 能力模块**”。

------

## 24. 你平时会用哪些比较通用的 Skills？哪些是你觉得在很多项目里都能复用、能明显提升效率的？

**参考回答：**

如果说通用性比较强、很多项目都能复用的 Skills，我会优先想到这几类：

**1. 文档总结与结构化提取**

适合会议纪要、需求文档、说明书、报告等场景。
 这个在知识管理、客服、研发、运营里都很常见。

**2. 问答与检索增强**

把检索结果和回答生成做成一个 Skill，很多企业知识库、内部助手都能复用。

**3. 分类与路由**

比如意图识别、工单分类、文本归类、请求分发。
 这是很多 Agent 系统的入口能力。

**4. 结构化输出**

比如把自然语言提取成 JSON、表格字段、数据库入参。
 这个能力在系统对接时非常通用。

**5. 代码辅助**

比如代码解释、接口生成、单测补全、日志分析。
 对研发提效非常有用。

**6. 报告生成**

比如周报、分析报告、摘要报告。
 很多办公自动化和数据辅助场景都适合。

我觉得最值得沉淀成通用 Skill 的，一般都是那些：
 **重复出现、输入变化但任务模式稳定、能明显节省人工时间** 的能力。

------

# 五、MCP 与 Skills 的关系辨析

## 25. 你觉得 MCP 和 Skills 的区别是什么？它们分别解决的是哪一层的问题？

**参考回答：**

我觉得可以把它们理解成**不同层次的抽象**。

**MCP 解决的是“外部能力怎么标准化接入”的问题**

它更偏协议层、连接层、能力暴露层。
 重点在于：模型或 Agent 怎么以统一方式访问工具、资源、上下文来源。

**Skills 解决的是“任务能力怎么模块化封装”的问题**

它更偏任务层、编排层、业务能力层。
 重点在于：针对某类业务任务，怎么把 prompt、tool、规则、流程封装成可复用模块。

所以如果做一个类比：

- **MCP 更像标准化插座**
- **Tool 更像具体电器**
- **Skill 更像一个完成某类工作的组合装置**

面试里我会强调：
 **MCP 和 Skills 不是同一个层次的概念，它们可以协同。**
 MCP 让 Skill 更容易接到统一外部能力；
 Skill 则把这些能力组织成对业务有价值的任务模块。

------

## 26. 如果在一个 AI Agent 项目里同时使用 MCP 和 Skills，你会怎么划分它们的职责？

**参考回答：**

如果同时使用，我会这样划分：

**MCP 负责底层能力接入**

比如：

- 文件系统
- 数据库查询
- 知识库检索
- 内部 API
- Git 仓库能力

它的职责是把这些外部能力以统一方式暴露出来，让上层可以稳定访问。

**Skills 负责上层任务封装**

比如：

- “生成需求摘要”
- “根据知识库回答用户问题”
- “分析日志并给出排查建议”
- “根据代码变更生成评审意见”

一个 Skill 内部可以调用多个 MCP 暴露出来的能力，再结合 prompt、规则和输出格式，完成一个完整任务。

所以职责划分上，我会把它理解为：

- **MCP 管接入**
- **Skills 管任务**
- **Agent 管编排和决策**

这样的分层比较清晰，也更容易维护和扩展。

------

# 六、Harness Engineering / Prompt Engineering / Context Engineering

## 27. 最近有个词叫 Harness Engineering，你怎么理解它？它和 Prompt Engineering、Context Engineering 有什么区别？

**参考回答：**

这个词目前在业界还没有像 Prompt Engineering 那样完全统一的严格定义，但从主流语境来看，我理解 **Harness Engineering** 更偏向：
 **围绕模型构建一整套“可控、可评估、可运行”的任务执行框架**，让模型能力真正稳定地被“ harness（驾驭/利用）”起来。

如果做区分的话：

**Prompt Engineering**

重点是**怎么写提示词**，让模型更好地理解任务、输出更符合预期。
 关注的是“指令层”。

**Context Engineering**

重点是**给模型什么上下文、怎么组织上下文**。
 比如检索结果怎么拼接、历史对话怎么裁剪、系统提示和工具结果怎么安排。
 关注的是“信息供给层”。

**Harness Engineering**

我理解更偏**运行与治理层**。
 也就是不仅关心 prompt 和 context，还关心：

- 任务如何编排
- 工具如何接入
- 结果如何评估
- 失败如何回退
- 如何做测试、监控、回归、约束

如果用一句更口语的话说：

- Prompt Engineering 是“**怎么跟模型说**”
- Context Engineering 是“**给模型什么信息**”
- Harness Engineering 是“**怎么把模型能力系统化地用起来，并且稳定运行**”

------

## 28. 如果让你从工程实践角度解释 Prompt Engineering、Context Engineering、Harness Engineering 三者的关系，你会怎么说？

**参考回答：**

从工程实践角度，我会把这三者看成一个逐层扩展的关系。

最基础的是 **Prompt Engineering**。
 因为模型最终还是要靠指令理解任务，所以 prompt 是入口。没有合适的 prompt，很多事情第一步就做不好。

再往上是 **Context Engineering**。
 因为只靠 prompt 不够，模型效果很大程度取决于你给了什么信息、这些信息是不是干净、完整、相关、顺序合理。
 很多时候模型效果差，不是模型不会，而是上下文工程没做好。

再往上我会理解成 **Harness Engineering**。
 也就是当你不只是做一个 Demo，而是要把模型放进真实系统里，你就要考虑：

- 工作流怎么编排
- 工具怎么调用
- 输出怎么校验
- 异常怎么处理
- 效果怎么监控
- 怎么做回归测试和持续优化

所以三者关系我会总结成：

- **Prompt Engineering** 解决“表达任务”
- **Context Engineering** 解决“提供信息”
- **Harness Engineering** 解决“系统化运行和治理”

也可以说，前两个更偏“让模型答得好”，
 后一个更偏“让模型在真实系统里稳定地把事做好”。

------

# 七、这几个“发展史”问题，面试里怎么稳妥回答

你特别加了“**skills 和 mcp 的发展史说一下**”，这里我建议你面试时不要说得太像背百科，更稳妥的回答方式是讲**演进逻辑**。

------

## MCP 的发展史，稳妥答法

**参考话术：**

我理解 MCP 的发展不是孤立出现的，而是 AI 应用从单纯对话走向 Agent 化之后，对“外部能力标准化接入”的需求推动出来的。
 早期大家更多是自己写 tool calling 或接口封装，能用但不统一。随着工具种类、数据源、系统接口越来越多，大家就越来越需要一种统一的接入方式。
 所以 MCP 背后的趋势，其实是：
 **从零散工具调用，走向标准化能力连接。**

------

## Skills 的发展史，稳妥答法

**参考话术：**

我理解 Skills 的发展路径，是从最开始大家直接堆 Prompt，到后来发现复杂任务需要拆分、复用和治理，于是逐渐把一类任务沉淀成可复用的能力模块。
 所以它背后的趋势是：
 **从 prompt 试验，走向任务能力模块化。**

------

# 八、如果面试官问“你一般用哪些 Skills 和 MCP”，你可以这样答

## 通用 Skills 可以答这些

- 文档总结
- 结构化信息提取
- 知识问答
- 分类路由
- 代码解释/单测补全
- 报告生成
- 日志分析

## 通用 MCP 能力可以答这些

- 文件系统读取
- Git / 仓库访问
- 文档检索 / 知识库
- 数据库只读查询
- 通用 HTTP / API 调用
- 配置/日志读取

这几类都很通用，不会显得太虚。