[toc]

# 一、智能数据分析平台：20 个问题 + 参考回答

------

## 1. 你这个智能数据分析平台的核心业务流程是什么？从用户上传数据到最终返回图表和分析结论，中间完整链路怎么走？

我的项目完整链路大致是这样的：

1. 用户在前端上传原始数据文件，比如 Excel 或 CSV，并输入自己的分析诉求。
2. 后端先对上传文件做校验，包括文件后缀、大小、文件内容格式校验，防止非法文件或者异常大文件进入系统。
3. 校验通过后，后端解析文件内容，把原始数据转成适合传给 AI 的文本格式，一般是结构化的表格数据。
4. 同时把用户输入的分析目标和解析后的数据，按预设 Prompt 模板进行封装。
5. 用户发起生成请求时，会先经过限流校验。我这里是基于 Redisson 的 RateLimiter 做单用户限流，防止某个用户恶意高频调用 AI 接口。
6. 校验通过后，不会同步阻塞等待 AI 返回，而是把生成任务提交到线程池或者 RabbitMQ 中异步执行。
7. AI 生成模块消费任务，调用第三方 AIGC 接口，请它返回 ECharts 的 option 配置 JSON 和文字分析结论。
8. 后端对 AI 返回结果做解析和简单校验，成功的话就把图表配置和结论保存起来，并更新任务状态。
9. 前端提交任务后可以快速拿到“任务已受理”的响应，之后通过轮询任务状态的方式获取最终结果，并渲染图表。

这个流程的核心就是：**上传校验 -> Prompt 封装 -> 限流 -> 异步任务化 -> AI 生成 -> 结果解析 -> 前端展示**。

------

## 2. 你为什么要用 AIGC 来做 BI，而不是传统规则引擎或者固定模板生成报表？这个项目的核心价值是什么？

我觉得传统 BI 更偏向于“会用工具的人”去拖拉拽图表、写 SQL、配置指标，但很多普通用户其实不具备这个能力。
 而 AIGC 的优势在于，它可以让用户只需要提供**原始数据 + 自然语言分析诉求**，系统就能自动生成图表和结论，这样大大降低了数据分析门槛。

相比传统规则引擎或者固定模板，我觉得 AIGC 更适合这个场景，主要原因有三点：

1. **灵活性更强**
    传统模板适用于固定业务场景，但用户分析诉求可能很多样，比如“帮我看销量趋势”“分析不同地区销售差异”“给我一个适合对比的图表”，这些很难靠固定模板完全覆盖。
2. **降低人工成本**
    用户不需要自己选图表类型，也不需要自己整理结论，AI 可以自动生成初步结果。
3. **提升交互效率**
    用户和系统的交互从“配参数”变成了“提需求”，更接近自然语言使用方式。

这个项目的核心价值我总结成一句话就是：
 **把数据分析从“专业工具操作”变成“自然语言驱动”，实现数据分析的降本增效。**

------

## 3. 你是怎么设计 Prompt 模板的？如何把用户的分析诉求和原始数据封装进去，才能尽量让模型稳定输出你想要的图表 JSON 和分析结论？

我在 Prompt 设计上主要做了三件事：

1. **给模型设定明确角色**
2. **固定输入格式**
3. **严格约束输出格式**

比如我会在系统预设里告诉模型：
 “你是一个数据分析师和前端开发专家”，这样让模型更倾向于从“分析 + 可视化”的角度回答。

然后输入会拆成两部分：

- 分析需求
- 原始数据

像这样：

- 分析需求：用户想看什么、想分析什么
- 原始数据：我会把解析出来的数据按 csv 或者结构化文本拼接进去

输出格式我会强制约束，比如要求它只输出两部分：

1. ECharts option 的 JSON
2. 分析结论

并且使用特殊分隔符隔开，防止模型输出多余废话，方便后端解析。

为了让结果更稳定，我还做了这些优化：

- 多轮调试 Prompt，观察 AI 最容易出错的地方
- 减少歧义描述，要求“不要输出注释，不要输出额外解释”
- 给少量示例，也就是 few-shot
- 对输出字段做格式约束，比如要求返回合法 JSON

所以本质上，我的 Prompt 优化思路就是：
 **通过角色约束 + 输入模板化 + 输出结构化，尽量把大模型从“开放问答”收敛成“半结构化生成器”。**

------

## 4. 如果 AIGC 返回的结果格式不稳定，比如不是合法 JSON、字段缺失、图表类型错误，你是怎么处理的？有没有做结果校验和兜底？

这是 AI 项目里一定会遇到的问题，因为大模型并不能百分百保证每次都返回严格合法的数据。

我这里的处理思路是 **“Prompt 约束 + 后端校验 + 失败兜底”**。

**第一层：Prompt 约束**

在 Prompt 里明确要求：

- 只输出 JSON 和分析结论
- 不要输出注释、解释、markdown
- 使用固定分隔符分隔结果

这样先尽量降低格式出错概率。

**第二层：后端解析校验**

后端拿到 AI 返回内容后，会先按分隔符拆分，再对 JSON 部分做解析校验，比如：

- 是否是合法 JSON
- 是否包含 ECharts option 的核心字段
- 图表类型是否在允许范围内
- 数据字段是否为空

如果 JSON 解析失败，说明模型输出不符合预期。

**第三层：兜底策略**

如果解析失败或者字段缺失，我会把任务标记为失败，返回前端一个友好提示，比如“生成失败，请重试”。
 如果要进一步增强，也可以考虑两种方式：

1. 自动重试一次，重新请求模型
2. 给一个默认图表模板，至少让前端能展示基础结果

如果面试官继续追问，我会补充：
 在 AI 应用里，不要把模型输出当成绝对可信数据，后端一定要把它当作**不可信输入**处理。

------

## 5. 用户上传的数据文件你做了哪些校验？为什么要校验后缀名、大小、内容？如果有人上传恶意文件或者超大文件会怎么样？

我这里主要做了三类校验：

**1）文件类型校验**

检查文件后缀名和 MIME 类型，比如只允许上传 csv 或 xlsx，防止用户上传脚本、可执行文件等非法内容。

**2）文件大小校验**

限制文件大小，比如 1MB 到 10MB 范围内，避免超大文件占用过多内存和 IO 资源，影响系统稳定性。

**3）文件内容校验**

只看后缀是不够的，因为后缀可以伪造。
 所以我还会进一步校验文件内容格式，比如：

- Excel 是否能正常解析
- CSV 是否符合预期分隔结构
- 文件头是否符合对应格式特征

如果更严格一些，还可以接入第三方内容安全服务做内容检测。

为什么要做这些校验？
 因为上传文件本身就是高风险入口，如果不校验，可能导致：

- 恶意文件执行风险
- 超大文件拖垮服务
- 非法格式导致解析异常
- 污染后续 AI 分析结果

所以文件上传校验本质上是为了兼顾 **安全性、稳定性和数据质量**。

------

## 6. 你说做了“内容校验”，具体是怎么判断文件内容是否合法的？只是看后缀够吗？有没有考虑后缀伪造的问题？

不是只看后缀，后缀只能作为第一层快速校验，确实存在伪造风险。
 比如用户把一个非法文件改名成 `.xlsx` 上传，这种情况只看扩展名是识别不出来的。

所以我会结合以下几种方式：

1. **检查文件后缀**
2. **检查 MIME 类型**
3. **尝试按目标格式解析文件**
4. **必要时校验文件头特征**

例如：

- 如果是 Excel，我会尝试通过解析库去读取，如果根本读不出来，说明内容和格式不匹配。
- 如果是 CSV，就会检查分隔结构、列数是否基本正常。
- 如果需要做更严格校验，也可以读取文件头的 magic number 或格式标识。

所以我的理解是：
 **后缀校验只能拦住低级错误，真正有效的是“格式解析验证”。**

------

## 7. 如果用户上传的是 Excel / CSV，数据量特别大，你是一次性读到内存里还是流式处理？这样设计的原因是什么？

如果数据量不大，一次性读取实现更简单。
 但如果数据量较大，更合理的方式肯定是 **流式处理**，因为一次性读入内存可能导致内存占用过高，尤其是 Excel 文件。

我在设计上更倾向于这样处理：

- 小文件：可以直接读取到内存中处理，开发简单
- 大文件：用流式解析，边读边处理，降低内存峰值

这样设计的原因主要有两个：

1. **避免内存压力**
2. **提升系统稳定性**

尤其这个项目后面还要把数据再封装给 AI，如果一开始就把特别大的原始文件整个读入内存，再进行字符串拼接，内存消耗会更明显。

另外 AI 接口本身对输入长度也有限制，所以即使上传大文件，最终也不能无限制把所有数据都喂给模型。
 所以实际工程里还要配合：

- 采样
- 压缩
- 只取关键列
- 截断过大内容

------

## 8. 如果上传的数据中包含脏数据、空值、字段类型混乱，AIGC 分析效果可能会变差，你们是怎么做预处理的？

这个问题我会从“数据质量影响 AI 效果”来回答。

在项目里，如果原始数据本身就很乱，比如：

- 空值很多
- 数值字段混入字符串
- 列名不清晰
- 重复行、异常值比较多

那 AI 很可能会理解偏差，导致图表和结论不靠谱。

所以我觉得预处理至少要做这些：

1. **空值处理**
    对空值做识别，可以选择过滤、补默认值，或者在传给 AI 时注明该列存在缺失。
2. **字段规范化**
    比如统一列名、去掉多余空格、特殊字符。
3. **类型识别**
    识别哪些列是数值型、时间型、类别型，这样更利于决定适合的图表类型。
4. **异常数据过滤**
    明显错误的数据可以提前清洗掉，避免误导模型。
5. **数据裁剪**
    如果数据过大，不会整表全量传给 AI，而是做采样、汇总或者提取关键字段。

所以我会强调：
 这个项目并不是“把原始数据原封不动丢给 AI”，而是要先做一定的数据治理，至少做基础清洗和结构化处理，这样 AI 输出才更稳定。

------

## 9. 你为什么选择基于 Redisson 的 RateLimiter 做分布式限流？而不是本地限流或者 Sentinel？

我当时选择 Redisson 的 RateLimiter，主要是结合这个项目的规模和需求做的技术选型。

**为什么不用本地限流？**

因为系统是分布式部署的情况下，本地限流只能限制单实例，不能全局生效。
 比如一个用户的请求落到不同机器上，就可能绕过单机限流。

**为什么选 Redisson？**

因为项目本身已经用了 Redis，Redisson 又提供了现成的分布式限流组件，接入成本低，开发效率高。
 它底层基于 Redis，天然适合分布式场景，能够让多个实例共享同一套限流状态。

**为什么不是 Sentinel？**

Sentinel 更偏平台化治理，功能更全，适合更复杂的流控、熔断、降级场景。
 但我这个项目需求相对聚焦，就是针对“单用户 AI 生成功能限流”，Redisson 足够轻量直接。

所以这个选择本质上是：
 **在满足分布式限流需求的前提下，优先选接入简单、和现有技术栈贴合度高的方案。**

------

## 10. 你的限流维度是怎么设计的？是按用户 id、接口、IP，还是组合维度？如果一个用户有多个接口请求，怎么控制更合理？

我的核心限流维度是 **按用户 id + 功能接口** 来做的。
 因为这个项目最贵、最耗资源的操作是 AI 生成图表，所以重点不是限制所有接口，而是限制某个用户调用“生成图表”这个接口的频率。

比如 key 可以设计成类似：

```
ratelimiter:ai_generate:userId
```

这样每个用户都有独立限流器，互不影响。

如果继续优化，我觉得可以做成组合维度：

- 用户 id 维度：防止单用户恶意刷接口
- IP 维度：防止未登录场景下的恶意请求
- 接口维度：只限制高成本接口，不影响普通查询接口
- 全局维度：防止全站级别资源被打爆

如果一个用户有多个接口请求，更合理的方式是分层控制：

1. **普通查询接口**：限流宽松
2. **上传接口**：根据文件处理成本单独限制
3. **AI 生成接口**：严格限制

因为不同接口的资源消耗不一样，不能一刀切。

------

## 11. Redisson 的 RateLimiter 底层原理你了解吗？它为什么能在分布式场景下保证限流一致性？

我了解它底层本质是基于 **Redis + Lua 脚本 + 令牌桶思想** 来做的。

它之所以适合分布式场景，关键点在于：

1. **限流状态存在 Redis 中**
    Redis 是中心化存储，多台服务实例访问的是同一份限流状态，而不是各自本地维护。
2. **通过 Lua 脚本保证原子性**
    获取令牌、更新令牌数量这些操作要么一起成功，要么一起失败，避免并发下状态错乱。
3. **基于时间计算令牌补充**
    按设定速率往桶里补充令牌，请求来了就尝试获取令牌，拿到就放行，拿不到就拒绝。

所以它能保证分布式一致性的原因，不是说“所有机器自动同步”，而是因为**所有节点都依赖 Redis 这一个共享状态中心，并且关键操作具备原子性**。

------

## 12. 你提到 AIGC 响应比较慢，所以用了“自定义 IO 密集型线程池 + 任务队列”，为什么这是 IO 密集型任务？线程池参数是怎么设置的？

因为这个任务主要是在**等待第三方 AI 接口返回结果**，而不是在本地做大量 CPU 计算。
 线程真正消耗 CPU 的时间并不长，大部分时间都在网络请求、等待响应，所以它本质上是 IO 密集型任务。

对于这种任务，线程数通常可以适当比 CPU 核数高一些，因为很多线程都会处于等待状态。

我在项目里使用的是 `ThreadPoolExecutor` 自定义线程池，核心关注几个参数：

- corePoolSize：核心线程数
- maximumPoolSize：最大线程数
- workQueue：任务队列
- keepAliveTime：空闲线程存活时间
- rejectedExecutionHandler：拒绝策略

我的思路是：

- 核心线程数设置得相对大一些
- 配合有界阻塞队列，避免无限堆积
- 根据业务压力设置最大线程数
- 明确拒绝策略，避免线程池打满时失控

如果面试官追问为什么不能无限加线程，我会说：
 线程太多会带来上下文切换开销、内存消耗和下游接口压力，所以线程池参数一定要和**本机资源 + 下游承载能力**一起考虑。

------

## 13. 为什么不能每来一个请求就直接开一个线程调用 AIGC？线程池相比直接创建线程的优势是什么？

不能每来一个请求就 new 一个线程，主要是因为这样做在高并发下不可控。

问题主要有几个：

1. **线程创建和销毁有开销**
    如果请求量大，频繁创建线程会浪费大量系统资源。
2. **线程数量失控**
    高峰期可能瞬间创建大量线程，导致内存占用上升，甚至拖垮系统。
3. **缺乏统一管理**
    没法很好地做线程复用、监控、队列控制、拒绝策略处理。

线程池的优势就在于：

- 复用线程，降低创建销毁成本
- 可以控制最大并发数
- 可以通过队列做缓冲
- 可以设置拒绝策略
- 更利于监控和调优

所以对于 AIGC 这种慢任务，线程池不是为了“更快”，而是为了**更稳、更可控**。

------

## 14. 你的任务队列如果堆积太多怎么办？有没有考虑任务积压、任务超时、线程池打满、拒绝策略这些问题？

这个问题是一定要考虑的，不然异步化只是把问题从前台挪到后台。

我的思路是分几个层面处理：

**1）任务积压**

如果任务堆积太多，说明系统处理速度跟不上请求速度。
 可以从两个方向优化：

- 限流，减少进入系统的任务量
- 扩容消费者，提高处理能力

**2）任务超时**

AIGC 调用可能很慢，所以任务要设置超时机制。
 如果超过一定时间还没完成，就标记失败或者重试，避免任务一直卡住。

**3）线程池打满**

线程池参数要合理设置，不能无限接收任务。
 如果线程池满了，我会结合业务选择拒绝策略，比如直接拒绝并提示“系统繁忙，请稍后重试”。

**4）有界队列**

任务队列最好用有界队列，而不是无限队列，不然高峰期可能把内存撑爆。

**5）监控告警**

如果队列长度持续上升、任务平均耗时过长，就应该告警并排查。

所以我的理解是：
 异步不是万能的，真正要解决的是**生产速度和消费速度的平衡问题**。

------

## 15. 提交任务后立即响应前端，前端怎么知道任务执行完成了？你们是轮询、回调、WebSocket，还是别的方案？

这个场景下最简单稳定的方案其实是 **轮询**。

我的做法可以是这样：

1. 用户提交生成任务后，后端立即返回一个任务 id，并告诉前端任务已受理。
2. 前端拿着任务 id，定期调用查询接口，查看任务状态。
3. 当状态变成“成功”时，前端再获取图表配置和分析结论并展示。

任务状态一般会设计成：

- waiting：待处理
- running：执行中
- succeed：成功
- failed：失败

为什么优先选轮询？
 因为实现简单、兼容性好，尤其在这种不是特别高频实时推送的业务里，已经够用了。

如果以后要优化用户体验，也可以换成：

- WebSocket 推送
- SSE
- 消息通知

但对于中小型项目来说，轮询是最稳妥的。

------

## 16. 你一开始用本地任务队列，后来换成 RabbitMQ，为什么本地队列会有问题？RabbitMQ 解决了哪些核心痛点？

本地任务队列最大的问题是：**任务状态只存在当前进程内存里，不可靠，也不利于扩展**。

具体问题有几个：

1. **服务重启任务丢失**
    如果任务还没执行完，服务重启后内存中的队列就没了。
2. **单机局限**
    本地队列只能当前实例消费，不适合分布式扩容。
3. **缺乏可靠投递机制**
    任务失败后怎么重试、怎么确认、怎么补偿，自己做会比较麻烦。

RabbitMQ 解决的核心痛点就是：

- **消息持久化**：服务重启后消息还在
- **生产消费解耦**：提交任务和执行任务分离
- **支持多消费者扩展**：可以横向扩容处理能力
- **有确认机制**：处理成功再 ack，提高可靠性
- **支持死信、重试等机制**：便于处理异常任务

所以从本地队列切到 RabbitMQ，本质上是从“单机异步”升级成了“可靠的分布式异步任务系统”。

------

## 17. RabbitMQ 为什么选择 Direct 交换机？不用 Topic 或 Fanout 的原因是什么？

因为我的这个场景其实比较简单，属于典型的**点对点任务投递**。

项目里：

- 生产者就是业务服务
- 消费者就是 AI 生成模块
- 消息类型也比较单一，就是“生成图表任务”

在这种情况下，Direct 交换机就足够了。
 它根据 routing key 精确路由到指定队列，简单直接，也便于理解和维护。

为什么不用 Fanout？
 因为 Fanout 是广播模式，会把消息发给所有绑定队列，这明显不适合单任务单消费的场景。

为什么不用 Topic？
 Topic 更适合复杂路由，比如不同业务类型、不同模块通过通配符订阅消息。
 我这个项目消息类型没有复杂到那个程度，用 Topic 有点设计过度。

所以选择 Direct 的原因很简单：
 **业务简单、路由明确、实现成本低。**

------

## 18. RabbitMQ 的消息持久化你是怎么做的？如果消费者挂了、服务重启了、消息消费失败了，任务会不会丢？你怎么保证可靠性？

为了保证消息可靠性，我主要会从这几个方面做：

**1）队列持久化**

创建队列时设置 durable=true，让队列本身持久化。

**2）消息持久化**

发送消息时把消息标记为持久化，这样 RabbitMQ 重启后消息还能恢复。

**3）消费者手动确认**

消费者不是拿到消息就算成功，而是**业务处理成功后再 ack**。
 如果处理失败，就不确认，消息可以重新投递或者进入异常处理流程。

**4）失败处理**

如果消息反复消费失败，可以把它投递到死信队列，避免一直无限重试。

**5）任务状态落库**

除了 MQ 层的可靠性，业务层面也会把任务状态存数据库。
 这样即使出了问题，也可以通过任务状态做补偿或者排查。

所以如果消费者挂了、服务重启了，只要消息还没被成功 ack，通常不会轻易丢失。
 当然严格来说，要做到“绝对不丢”还要考虑生产者投递确认、消费者幂等等，但对于这个项目，我会强调我已经从**持久化 + 手动 ack + 死信处理 + 状态落库**几个方面做了可靠性增强。

------

## 19. AI 生成模块和业务主流程为什么要解耦？如果 AI 服务异常、响应超时、调用失败，你们的系统怎么做降级或者重试？

AI 生成模块必须和主业务解耦，原因主要有三点：

1. **AI 调用慢**
    如果同步耦合在主流程里，会直接拖慢接口响应时间。
2. **AI 不稳定**
    第三方接口可能超时、限流、失败，如果耦合得太紧，会影响整个系统可用性。
3. **便于扩展**
    未来如果要切换模型、增加多个 AI 服务提供商，解耦后改动会更小。

如果 AI 服务异常，我的处理思路是：

**1）超时控制**

调用第三方服务时设置超时时间，避免线程长期阻塞。

**2）失败重试**

对于偶发网络问题，可以有限次数重试，但不能无限重试，避免放大压力。

**3）任务状态管理**

任务失败后更新状态为 failed，前端可感知，不至于一直卡在处理中。

**4）降级处理**

如果 AI 服务不可用，可以提示“系统繁忙，请稍后重试”；
 如果有能力，也可以切换备用模型服务。

**5）死信队列**

如果通过 MQ 驱动任务，消费失败达到阈值后可以放进死信队列做后续人工或自动补偿。

------

## 20. 如果现在让你继续优化这个平台，面向更高并发和更大数据量，你觉得瓶颈会在哪？你会优先优化哪几个点？

如果继续优化，我觉得瓶颈主要会在以下几个地方：

**1）AI 接口调用能力**

这是最明显的瓶颈，因为外部 AIGC 接口本身响应慢、QPS 受限、调用成本高。
 优化方向：

- 多账号轮询
- 模型路由
- 限流
- 失败重试
- 请求缓存

**2）大文件处理能力**

上传文件更大时，解析、清洗、传输都会变慢。
 优化方向：

- 流式读取
- 分片处理
- 数据压缩
- 只抽取关键字段给 AI

**3）任务积压**

用户量上来后，异步队列容易堆积。
 优化方向：

- 扩容消费者
- 优化线程池参数
- 做任务优先级
- 做超时淘汰

**4）查询性能**

图表记录变多后，历史图表查询可能变慢。
 优化方向：

- 加索引
- 分页优化
- Redis 缓存热点数据

**5）稳定性和可观测性**

需要补足：

- 任务监控
- 队列堆积监控
- 调用链追踪
- 告警机制

所以如果只能优先做几件事，我会先做：
 **AI 调用治理 + 异步任务稳定性 + 大文件处理优化 + 缓存和监控体系。**

------

# 二、智能编程评测系统：20 个问题 + 参考回答

------

## 21. 你这个智能编程评测系统的完整业务流程是什么？从用户提交代码到判题结果返回，中间经历了哪些模块？

完整流程可以概括为：

1. 用户在前端选择题目并提交代码。
2. 请求先经过网关，由网关做统一路由、跨域处理和接口保护。
3. 题目服务接收提交请求，保存题目提交记录，初始状态一般是“待判题”。
4. 然后把判题任务交给判题服务，可以通过 MQ 异步触发判题。
5. 判题服务先通过 OpenFeign 调用题目服务，获取题目信息、测试用例、判题配置等。
6. 再根据当前语言和判题策略，组织代码和输入用例，调用代码沙箱编译执行用户代码。
7. 代码沙箱返回编译结果、运行结果、运行时间、内存占用、错误信息等。
8. 判题服务根据预期输出和实际输出，结合时间、内存限制执行判题逻辑。
9. 最后把判题结果更新到题目提交表中，前端查询后展示结果。

如果项目中还加了 AI 辅助教学，那么在判题结果基础上，还可以进一步触发智能代码建议模块，给用户返回规范建议或者算法优化建议。

------

## 22. 你为什么要把代码沙箱抽象成统一接口？这个抽象解决了什么问题？

把代码沙箱抽象成统一接口，核心是为了 **解耦和扩展**。

因为项目里并不只有一种沙箱实现方式，我当时有：

- 示例沙箱
- 远程代码沙箱
- 第三方代码沙箱
- 还可以扩展 Docker 沙箱

如果在业务代码里直接硬编码某一种实现，比如直接写死调用某个远程接口，那后续一旦切换沙箱方案，改动面会很大。

抽象成统一接口之后，好处主要有：

1. **调用层统一**
    上层判题服务只依赖接口，不关心底层到底是本地执行、远程执行还是第三方执行。
2. **实现可替换**
    后续切换沙箱时，只需要替换实现类，不需要改核心业务逻辑。
3. **更利于测试**
    比如开发阶段可以用示例沙箱快速跑通流程，正式环境再切换到真实沙箱。
4. **扩展性更好**
    如果以后新增一种更安全或者更高性能的沙箱，只需要新增实现类。

所以这个抽象解决的核心问题是：
 **避免业务逻辑和具体沙箱实现强耦合。**

------

## 23. 你提到支持远程/第三方代码沙箱，多种沙箱实现是怎么切换的？静态工厂模式 + Spring 配置化具体是怎么配合的？

我当时的做法是这样的：

**第一步：定义统一接口**

先定义一个代码沙箱接口，比如提供执行代码的方法。

**第二步：为不同沙箱写实现类**

比如：

- ExampleCodeSandbox
- RemoteCodeSandbox
- ThirdPartyCodeSandbox

它们都实现同一个接口。

**第三步：用工厂模式统一创建实例**

我写了一个工厂类，根据传入的沙箱类型字符串返回对应的实现类。
 比如：

- `example` -> 示例沙箱
- `remote` -> 远程沙箱
- `thirdParty` -> 第三方沙箱

**第四步：通过 Spring 配置化动态选择**

在 `application.yml` 里配置当前使用哪一种沙箱，比如：

```yaml
code-sandbox:
  type: remote
```

启动后读取这个配置，把对应类型传给工厂类，就能拿到当前要用的实现。

这样做的好处是：
 切换沙箱时不需要改代码，只需要改配置。
 所以“静态工厂模式 + Spring 配置化”的配合，本质上就是：
 **用工厂屏蔽创建细节，用配置决定运行时选择。**

------

## 24. 如果后续要新增一种新的代码沙箱实现，你这个架构要改哪些地方？这样设计的扩展性体现在哪？

如果要新增一种新沙箱，比如 DockerSandbox，我觉得改动很小，主要就几步：

1. 新增一个实现类，实现统一的代码沙箱接口
2. 在工厂类里增加一种类型映射
3. 在配置文件里增加可选类型
4. 如果有代理或者日志增强，也自动复用

上层判题服务基本不需要改，因为它只依赖抽象接口。

这个设计的扩展性主要体现在：

- 新增实现不会影响调用层
- 不需要大面积修改原有业务逻辑
- 配置切换灵活
- 更符合开闭原则，扩展开放、修改封闭

如果继续追问，我也会说：
 如果以后沙箱种类非常多，工厂类里的 if-else 也可能膨胀，那还可以进一步优化成 **Spring 容器自动注入 + 策略映射表** 的方式。

------

## 25. 判题机模块为什么会比较复杂？判题逻辑复杂具体体现在哪些方面？

判题机复杂，不只是因为“执行代码”这一步，而是因为它要处理很多边界情况和多维度判断。

复杂点主要在几个方面：

1. **多语言差异**
    不同语言的编译命令、运行方式、资源消耗特征不同。
2. **结果状态多**
    不只是正确和错误，还包括：
   - 编译失败
   - 运行时异常
   - 超时
   - 内存超限
   - 输出错误
   - 沙箱异常
3. **题目配置差异**
    不同题目可能有不同时间限制、内存限制，甚至特殊判题逻辑。
4. **安全问题**
    用户提交的是不可信代码，必须防止越权操作和恶意攻击。
5. **异常处理复杂**
    比如死循环、疯狂占内存、无限输出、恶意 fork 等场景都要考虑。

所以判题机不是简单执行一段代码，而是一个集成了
 **代码执行、安全隔离、资源控制、结果判定、异常处理** 的综合模块。

------

## 26. 你为什么在判题逻辑里选择策略模式，而不是 if-else？策略模式在你这个项目里具体是怎么落地的？

因为不同语言、不同题目的判题逻辑并不完全一致，如果全部写在一个 service 里用 if-else 区分，会导致代码越来越臃肿，后面很难维护。

策略模式更适合这种“同一类业务行为有多种变体”的场景。

我具体是这样落地的：

1. 定义判题策略接口 `JudgeStrategy`

2. 为不同语言或者不同场景写具体策略类，比如 `JavaLanguageJudgeStrategy`

3. 定义一个 

   ```
   JudgeContext
   ```

   ，把判题需要的数据封装进去，比如：

   - 输入输出
   - 题目限制
   - 执行结果

4. 再写一个 `JudgeManager`，根据语言或者条件选择对应策略执行

这样做的好处是：

- 不同判题逻辑职责清晰
- 后续新增语言时只要加新策略类
- service 层更简洁
- 更符合开闭原则

所以我会说，策略模式主要是为了解决 **判题规则多变、if-else 膨胀** 的问题。

------

## 27. 不同语言的判题规则有什么差异？比如 Java 题目为什么要额外增加空间限制？

不同语言在运行时特性上是有差异的，所以判题不能完全一刀切。

比如：

**Java**

Java 程序运行在 JVM 上，本身就有虚拟机开销，启动速度比 C/C++ 慢一些，而且堆内存是 JVM 管理的。
 所以在时间和内存限制上，往往需要更宽松一些，或者单独设置 `-Xmx` 来限制堆大小。

**C/C++**

运行效率通常更高，但如果不做隔离，越界、非法访问风险也比较大。

**Python**

解释型语言，执行速度通常比编译型语言慢，所以一些题目如果统一时间限制，Python 可能天然吃亏。

所以不同语言的判题差异主要体现在：

- 编译方式不同
- 运行方式不同
- 启动开销不同
- 内存模型不同
- 时间 / 空间限制可能不同

Java 额外增加空间限制，本质上是因为它的运行依赖 JVM，内存消耗模式和原生语言不一样，所以我需要通过 JVM 参数对用户程序占用的堆内存做更细粒度控制。

------

## 28. 你的判题结果一般会包含哪些维度？比如编译错误、运行超时、内存超限、答案错误，这些状态是怎么识别出来的？

判题结果通常至少包含这些维度：

- 是否编译成功
- 是否运行成功
- 每组用例是否通过
- 总体判题状态
- 运行时间
- 内存占用
- 错误信息

具体状态一般有：

1. **编译错误**
    编译阶段命令执行失败，或者编译器输出错误信息。
2. **运行超时**
    程序执行超过设定时限，还没结束，就强制中断并标记超时。
3. **内存超限**
    运行过程中内存占用超过限制值。
4. **运行时错误**
    比如空指针、数组越界、除零异常等。
5. **答案错误**
    程序能正常执行，但实际输出和标准答案不一致。
6. **通过**
    所有测试用例输出都符合预期，且资源使用在限制范围内。

怎么识别这些状态？

- 编译错误：看编译命令退出码和错误流
- 超时：通过定时控制或 watchdog 线程判断
- 内存超限：读取运行时资源指标
- 运行错误：看进程退出码和错误输出
- 答案错误：逐个比对输出结果

------

## 29. 你自测代码沙箱时模拟了哪些异常场景？为什么这些异常场景很重要？

我自测的时候会重点模拟这些异常场景：

1. **死循环**
    比如 `while(true)`，验证超时机制是否有效。
2. **疯狂占内存**
    比如不断 new 大对象，验证内存限制是否生效。
3. **非法文件操作**
    比如尝试写文件、删文件，验证权限控制是否生效。
4. **执行外部命令**
    比如 `Runtime.getRuntime().exec()`，验证能否拦截危险操作。
5. **无限输出**
    看日志或输出流处理是否会被撑爆。
6. **编译失败**
    验证错误收集和返回逻辑。
7. **运行时异常**
    比如空指针、数组越界，验证状态识别是否准确。

这些异常场景很重要，因为代码沙箱面对的是用户提交的**不可信代码**。
 如果只测正常路径，真正上线后很容易被边界情况打穿。
 所以我会强调，这些测试不是为了“功能好看”，而是为了验证沙箱在**安全性、稳定性、健壮性**上的表现。

------

## 30. 你说用“守护线程 + Thread.sleep 等待机制”实现超时中断，这个方案具体是怎么做的？有没有什么风险？

这个方案的基本思路是：

1. 主线程启动用户程序对应的进程
2. 再启动一个守护线程或者监控线程
3. 监控线程 sleep 指定超时时间
4. 时间到了之后，检查用户程序是否还在运行
5. 如果还在运行，就强制销毁进程，标记为超时

这个方案比较直观，适合快速实现超时控制。

但它也有风险：

1. **精度有限**
    `Thread.sleep` 不是特别精准，可能存在小幅偏差。
2. **不是最优雅的取消机制**
    如果只是中断 Java 线程，不一定能真正杀掉底层进程，所以必须针对进程做 destroy。
3. **资源清理问题**
    杀掉进程后要注意释放流、删除临时文件，不然可能残留资源。
4. **高并发下监控线程成本**
    如果每个任务都额外开监控线程，也会增加开销。

所以这个方案能用，但如果进一步优化，可以考虑更成熟的超时控制方式，比如基于 Future、进程等待超时 API、容器级限制等。

------

## 31. 如果用户程序进入死循环，你的沙箱是怎么中断它的？仅仅 interrupt 线程够吗？为什么？

如果用户程序是一个独立进程在执行，**仅仅 interrupt Java 线程通常是不够的**。
 因为 interrupt 只能中断当前 Java 线程的阻塞状态，不能保证把外部进程真正停掉。

所以正确做法应该是针对进程本身处理：

1. 启动用户程序后拿到 `Process` 对象
2. 超时后判断进程是否还活着
3. 调用 `destroy()` 或必要时 `destroyForcibly()` 强制结束进程

如果是在 Docker 容器里执行，那还可以进一步通过容器层面：

- 限制 CPU / 内存
- 超时后停止容器内命令
- 必要时杀掉容器

所以这个问题的重点是：
 **要区分“中断线程”和“杀掉用户进程”不是一回事。**

------

## 32. 你通过 JVM `-Xmx` 参数限制用户程序最大堆内存，这种方式能完全保证安全吗？还有哪些资源限制手段？

不能完全保证安全，它只是限制了 **JVM 堆内存**，不是全方位资源隔离。

局限点主要有：

1. 它主要限制 Java 堆，不一定能覆盖所有 native 内存使用
2. 不能限制 CPU 占用
3. 不能限制磁盘 IO
4. 不能限制网络访问
5. 不能阻止危险系统调用

所以除了 `-Xmx`，还应该结合其他手段：

- **超时控制**：防止死循环长期占用 CPU
- **容器资源限制**：限制内存、CPU 配额
- **禁用网络**：防止用户代码访问外网
- **权限控制**：限制文件操作、执行命令
- **安全管理器或 seccomp**：限制危险调用
- **输出限制**：防止无限打印撑爆日志

所以 `-Xmx` 更像是资源保护的一部分，而不是全部。

------

## 33. 除了内存限制，CPU、磁盘 IO、网络访问、恶意系统调用这些风险你怎么处理？

如果从更完整的沙箱安全视角看，除了内存，还要考虑这些：

**1）CPU 风险**

典型是死循环或者高计算任务。
 处理方式：

- 执行超时控制
- Docker 容器设置 CPU 限额

**2）磁盘 IO 风险**

典型是疯狂读写文件。
 处理方式：

- 限制文件系统权限
- 只给临时目录
- 执行后清理文件

**3）网络访问风险**

用户代码可能去请求外部接口甚至攻击别的系统。
 处理方式：

- 容器禁网
- 禁止特定系统调用

**4）恶意系统调用**

比如执行系统命令、fork 炸弹。
 处理方式：

- Java 安全管理器或其他权限控制
- seccomp 限制危险 syscall
- 容器隔离

所以如果面试官问安全，我会强调：
 **真正的代码沙箱安全要从进程、容器、权限、资源、网络多个层面综合限制。**

------

## 34. 代码沙箱为什么不能直接在宿主机上随便执行用户代码？这里最大的安全风险是什么？

因为用户提交的代码是不可信的，如果直接在宿主机执行，相当于把服务器暴露给了用户。

最大的安全风险包括：

1. **越权访问系统资源**
    比如读服务器文件、删文件、执行危险命令。
2. **资源耗尽攻击**
    比如死循环、疯狂占内存、无限创建线程，直接把宿主机拖垮。
3. **网络攻击**
    利用宿主机网络去扫描、请求外部系统。
4. **提权或逃逸**
    更严重的话可能进一步攻击宿主环境。

所以代码沙箱的核心目标就是：
 **让用户代码只能在受控、隔离、可回收的环境里运行，不能直接碰宿主机。**

------

## 35. 你用了 Spring Cloud Gateway 做聚合和路由，为什么网关层适合做这件事？它给系统带来了哪些好处？

网关层天然适合做统一入口，因为所有请求都会先经过它。
 所以像路由、鉴权、跨域、日志、限流这些横切逻辑，放网关比放到每个服务里都实现一遍更合适。

我项目里 Gateway 主要做了这些事：

- 请求路由转发
- 聚合接口文档
- 全局跨域处理
- 接口保护
- 统一管理服务入口

它带来的好处主要有：

1. **简化客户端调用**
    前端不需要记多个服务地址，只请求网关即可。
2. **集中治理**
    像跨域、认证、过滤这些逻辑统一做。
3. **保护内部服务**
    内部服务不直接暴露给外部，提升安全性。
4. **更利于后续扩展**
    后面做限流、灰度、日志追踪都方便。

------

## 36. 你自定义 CorsWebFilter 解决跨域问题，这和在 Controller 上加 `@CrossOrigin` 相比有什么区别？为什么选全局方案？

`@CrossOrigin` 一般适合单个接口或者少量 Controller 的跨域处理。
 但在微服务项目里，接口多、服务多，如果每个地方都单独加，维护成本很高，也容易漏。

而 `CorsWebFilter` 是在网关层全局处理的，好处是：

1. **统一**
    所有经过网关的请求都能统一处理跨域。
2. **集中维护**
    规则改一处就行，不用改多个服务。
3. **更适合微服务架构**
    因为外部请求本来就统一走网关。

所以我选全局方案的原因是：
 **跨域属于典型的系统级横切问题，放在网关统一处理更合理。**

------

## 37. 你这个系统里为什么要用 MQ？判题任务或者 AI 建议任务放进消息队列的价值是什么？

因为判题和 AI 建议这类任务都比较重，不适合同步阻塞主流程。

用 MQ 的价值主要有：

1. **异步化**
    用户提交代码后，不需要一直等判题完成，系统可以先返回“提交成功”。
2. **解耦**
    题目服务不需要直接依赖判题服务的实时可用性。
3. **削峰填谷**
    高峰期任务先进入队列排队，避免瞬时把判题服务打爆。
4. **便于扩展**
    消费者可以水平扩容，提升吞吐能力。
5. **提高可靠性**
    队列具备持久化、重试、确认机制，任务处理更可控。

所以 MQ 在这个项目里本质上承担的是：
 **异步任务中心 + 系统缓冲层**。

------

## 38. 你提到“基于责任链模式实现上下文感知的智能代码建议”，责任链模式具体怎么设计的？每一层责任分别处理什么？

责任链模式适合这种“一个请求可能经过多层处理，每层处理不同规则”的场景。

我理解的设计方式可以是这样：

1. 定义一个统一的处理器接口，比如 `CodeSuggestionHandler`
2. 每个处理器只关注一个维度，比如：
   - 代码规范建议处理器
   - 语法错误提示处理器
   - 时间复杂度优化建议处理器
   - 空间复杂度优化建议处理器
   - 题型特定建议处理器
3. 每个处理器决定：
   - 自己是否处理当前请求
   - 处理后是否继续传给下一个处理器

所谓“上下文感知”，就是在责任链执行时，会把题目类型、用户代码、判题结果、错误信息这些上下文传进去。
 每个处理器根据上下文判断要不要触发自己的建议逻辑。

这样设计的好处是：

- 各种建议逻辑解耦
- 新增建议类型时只加新处理器
- 链路顺序可配置
- 扩展性好

------

## 39. 你说可以根据不同题目类型动态选择算法优化模板，这个“动态选择”是怎么实现的？规则驱动还是 AI 判断？

这个可以做成 **规则驱动 + AI 生成** 的结合方式。

我的理解是：

**第一层：规则驱动**

先根据题目标签、题型、判题结果、用户代码特征，判断属于哪一类问题，比如：

- 数组题
- 动态规划题
- 图论题
- 暴力超时
- 空间超限

规则层先把问题归类，选择一个更匹配的优化模板。

**第二层：AI 生成**

再把题目上下文、用户代码、错误点、选中的优化模板一起喂给 AI，生成更具体的建议内容。

为什么不完全靠 AI 判断？
 因为纯 AI 判断会更不稳定，结果可控性差。
 如果先用规则筛一层，再让 AI 做文本生成，效果会更稳。

所以我会说这是一个：
 **“规则做路由，AI 做增强表达”** 的方案。

------

## 40. 如果现在线上用户量增长很快，你觉得这个评测系统最容易出现的性能瓶颈和稳定性风险分别是什么？你会怎么优化？

如果用户量快速上涨，我觉得这个系统最可能出问题的地方主要有：

**性能瓶颈**

**1）代码沙箱执行能力不足**

因为真正重的操作在编译和执行用户代码。
 优化方式：

- 增加沙箱实例
- 任务异步化
- MQ 削峰
- 容器池化 / 复用

**2）判题服务压力过大**

高峰期大量提交会把判题服务压满。
 优化方式：

- 判题服务独立扩容
- 优化线程池
- 拆分同步和异步逻辑

**3）数据库写压力**

题目提交记录、判题状态更新会变多。
 优化方式：

- 索引优化
- 批量更新
- 分库分表或冷热分离

**稳定性风险**

**1）恶意代码冲击沙箱**

可能导致资源耗尽或者安全问题。
 优化方式：

- 更严格的资源隔离
- 容器限制
- 安全审计

**2）MQ 堆积**

高峰期消费速度跟不上生产速度。
 优化方式：

- 扩容消费者
- 死信队列
- 监控堆积长度

**3）服务之间调用雪崩**

微服务之间相互调用，如果某个服务挂了，可能连锁影响。
 优化方式：

- 超时控制
- 熔断降级
- 重试限次
- 隔离线程池

如果面试官让我总结，我会说：
 这个系统未来最大的挑战，一是**安全地执行不可信代码**，二是**高峰期稳定处理大量判题任务**。

