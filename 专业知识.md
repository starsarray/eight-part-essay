# 一、Java 技术栈

------

## 1. 集合

### 问题 1

**Java 常见集合有哪些？分别适用于什么场景？**

#### 追问

**ArrayList 和 LinkedList 实际开发中怎么选？**

#### P6/P7 级回答

Java 集合我一般按 **List、Set、Map、Queue** 四类去理解，而不是只背类名。

- List
  - `ArrayList`：底层是动态数组，特点是随机访问快，尾插效率高，适合读多写少场景
  - `LinkedList`：双向链表，理论上中间插入删除成本低，但因为遍历定位和缓存不友好，实际业务里使用频率远低于 `ArrayList`
- Set
  - `HashSet`：本质是基于 `HashMap`，适合做去重
  - `TreeSet`：有序去重，底层红黑树
- Map
  - `HashMap`：最常用，通用 KV 存储
  - `LinkedHashMap`：在 `HashMap` 基础上维护插入顺序或访问顺序，适合做 LRU
  - `TreeMap`：按 key 排序
  - `ConcurrentHashMap`：并发读写场景
- Queue / Deque
  - `ArrayDeque`：性能通常优于 `LinkedList`
  - `PriorityQueue`：适合优先级调度、TopK

如果从工程实践角度看，我选择集合主要考虑四件事：

1. **是否需要顺序**
2. **是否允许重复**
3. **查询多还是修改多**
4. **是否有并发要求**

**追问回答：**
 `ArrayList` 和 `LinkedList` 我在实际项目里几乎都会优先选 `ArrayList`。
 原因不是因为 `LinkedList` 不支持插入，而是因为实际性能不能只看时间复杂度。

`LinkedList` 的问题主要有三个：

1. **内存不连续，CPU 缓存命中差**
2. **每个节点有额外指针开销**
3. **很多插入操作前提是先找到位置，定位本身就是 O(n)**

所以虽然教材里常说链表适合插入删除，但在真实业务代码里，大部分场景 `ArrayList` 依然更优。除非明确是队列/双端操作，或者频繁操作首尾节点，否则我一般不会选 `LinkedList`。

------

### 问题 2

**HashMap 底层原理是什么？**

#### 追问

**为什么 JDK 1.8 要引入红黑树？**

#### P6/P7 级回答

`HashMap` 底层可以概括为 **数组 + 链表 + 红黑树**。

核心流程是：

1. 先对 key 计算 hash
2. 再通过位运算定位到数组桶位
3. 如果桶为空，直接插入
4. 如果桶中已有元素，就比较 hash 和 `equals`
5. 如果发生冲突，在链表或红黑树中继续查找/插入

这里有几个我认为面试里值得展开的点：

### 1）为什么数组长度通常是 2 的幂

因为定位桶位时使用的是：

```java
index = (n - 1) & hash
```

这样比取模更高效，而且当数组长度为 2 的幂时，可以更均匀利用 hash 的低位。

### 2）为什么既需要 hashCode 又需要 equals

- `hashCode` 决定落在哪个桶
- `equals` 决定桶内是不是同一个 key

只比较 hashCode 不够，因为不同对象可能 hash 冲突。

### 3）扩容为什么是性能敏感点

扩容不仅是数组变大，还涉及 **rehash / 节点迁移**，这是一个有成本的操作。
 所以如果业务预估数据量比较明确，我会建议初始化时给一个相对合理的容量，减少扩容次数。

**追问回答：**
 JDK 1.8 引入红黑树，本质上是为了优化哈希冲突严重时的最坏性能。

在 JDK 1.7 中，如果大量 key 落到同一个桶里，链表会很长，查询复杂度就会退化到 O(n)。
 JDK 1.8 在链表长度超过阈值，并且数组容量达到一定条件时，会把链表树化成红黑树，把最坏查找复杂度优化到 O(log n)。

不过我一般也会补一句：
 **红黑树不是为了提升常规场景性能，而是为了避免极端冲突下性能雪崩。**
 在大部分情况下，良好的 hash 分布比树化更重要。

------

### 问题 3

**ConcurrentHashMap 和 HashMap 的区别是什么？**

#### 追问

**JDK 1.8 的 ConcurrentHashMap 为什么不再采用分段锁？**

#### P6/P7 级回答

两者最核心的区别是：**一个面向单线程/外部同步场景，一个面向高并发场景。**

我一般会从四个层面回答：

### 1）线程安全

- `HashMap` 非线程安全
- `ConcurrentHashMap` 线程安全

### 2）底层并发控制

- JDK 1.7 的 `ConcurrentHashMap` 基于 `Segment` 分段锁
- JDK 1.8 改成 **CAS + synchronized + 桶级别锁**

### 3）null 值支持

- `HashMap` 允许 `null key` 和 `null value`
- `ConcurrentHashMap` 不允许
   这是为了避免并发场景下 `get(key)==null` 时无法区分“key 不存在”还是“value 就是 null”

### 4）使用场景

如果只是单线程上下文或者调用方自己已经做了同步控制，我会使用 `HashMap`。
 如果是多线程共享读写数据结构，一般直接用 `ConcurrentHashMap`，而不是自己在外面套一层粗粒度锁。

**追问回答：**
 JDK 1.8 去掉分段锁，我理解主要是两个原因：

1. **结构更简单**
    分段锁虽然提升了并发度，但数据结构更复杂，维护成本高
2. **锁粒度进一步细化**
    1.8 直接对桶位进行同步，并结合 CAS 处理空桶插入、size 统计等场景，通常能获得更好的吞吐和更灵活的并发控制

工程上这件事带来的价值是：
 **在读多写少、热点分散的场景下，ConcurrentHashMap 的性能和可伸缩性会更好。**

------

## 2. JVM

### 问题 1

**JVM 内存结构有哪些？**

#### 追问

**堆和栈的区别是什么？**

#### P6/P7 级回答

JVM 运行时内存结构我一般分成线程私有和线程共享两类来讲：

### 线程私有

- **程序计数器**
- **虚拟机栈**
- **本地方法栈**

### 线程共享

- **堆**
- **方法区**（HotSpot 在 Java 8 之后主要是元空间实现）

如果展开讲：

- **程序计数器**：记录当前线程执行到哪条字节码指令
- **虚拟机栈**：每次方法调用会创建一个栈帧，里面存局部变量表、操作数栈、动态链接、返回地址等
- **堆**：存放对象实例和数组，是 GC 的主要区域
- **方法区 / 元空间**：存类元数据、运行时常量池、静态变量、JIT 编译后的代码等

我在排查线上问题时，通常会把不同异常和内存区域关联起来理解：

- `StackOverflowError`：一般先想到递归太深或方法调用链异常
- `OutOfMemoryError: Java heap space`：优先看堆对象增长
- `OutOfMemoryError: Metaspace`：优先看类加载或动态代理是否过多

**追问回答：**
 堆和栈的区别，我不会只说“堆放对象、栈放变量”，那样太浅。
 我会从以下几个维度说：

### 1）作用不同

- 堆主要承载对象生命周期
- 栈主要承载方法调用上下文

### 2）共享性不同

- 堆是线程共享的
- 栈是线程私有的

### 3）性能特征不同

- 栈分配释放非常快，方法结束自动出栈
- 堆对象生命周期更复杂，需要 GC 介入

### 4）问题类型不同

- 堆更容易出现内存泄漏、频繁 GC
- 栈更容易出现递归过深导致溢出

所以从工程角度，很多性能问题最终都能落到一句话：
 **短生命周期、线程内使用的对象，尽量不要让它变成长生命周期堆对象。**

------

### 问题 2

**类加载过程是什么？**

#### 追问

**双亲委派模型的作用是什么？**

#### P6/P7 级回答

类加载生命周期一般包括：

1. **加载**
2. **验证**
3. **准备**
4. **解析**
5. **初始化**
6. 使用
7. 卸载

面试里我通常重点讲前五步，因为最常考。

### 加载

把 class 文件字节流读入内存，生成对应的 `Class` 对象。

### 验证

校验字节码是否符合 JVM 规范，防止恶意或非法字节码破坏虚拟机。

### 准备

给类变量分配内存，并赋默认值。
 这里要注意，是**默认值**，不是代码里写的显式初值。

### 解析

把常量池中的符号引用替换成直接引用。

### 初始化

执行类构造器 `<clinit>`，也就是静态变量显式赋值和静态代码块执行的阶段。

我一般还会补一个细节：
 **只有到了初始化阶段，类才算真正“开始执行开发者定义的逻辑”。**

**追问回答：**
 双亲委派模型的核心价值，我认为有两个：

### 1）避免类重复加载

如果每个类加载器都可以随意加载同名类，类的唯一性会被破坏。

### 2）保护核心类库安全

比如 `java.lang.String` 这类核心类，必须由引导类加载器优先加载。
 否则用户自定义一个同名类就可能污染整个运行环境。

不过在实际工程里，我会强调：
 双亲委派不是不能打破，而是**默认遵守**。像 Tomcat、OSGi、SPI、热部署场景，都会在特定边界下做定制化类加载。

------

### 问题 3

**垃圾回收如何判断对象是否可回收？**

#### 追问

**为什么 Java 不采用引用计数法作为主方案？**

#### P6/P7 级回答

Java 判断对象是否可回收，主流采用的是**可达性分析**，而不是引用计数。

做法是从一组 **GC Roots** 出发向下搜索，如果某个对象无法从 Roots 到达，就认为它是可回收的。

常见 GC Roots 包括：

- 虚拟机栈中引用的对象
- 方法区静态属性引用的对象
- 常量引用的对象
- JNI 引用的对象
- 活跃线程本身相关引用

这套机制的好处是，它不是只看“有没有人引用我”，而是看“这个引用链是否仍然对程序可达”。这更符合真实运行语义。

**追问回答：**
 Java 不单纯采用引用计数法，最大原因是它无法解决**循环引用**。

比如 A 引用 B，B 引用 A，即使这两个对象已经和业务完全断开，只要互相还持有引用，引用计数就不会归零，最终无法回收。

而可达性分析不会受这个问题影响。
 从 GC Roots 走不到它们，就会被判定为垃圾。

如果面试官继续深挖，我还会补充：
 Java 虽然不把引用计数作为主回收依据，但某些实现细节或资源管理策略里，引用计数思想仍然可能出现；只是它不是 JVM 垃圾回收的核心判定标准。

------

## 3. synchronized

### 问题 1

**synchronized 的作用对象有哪些？**

#### 追问

**静态方法和实例方法上的 synchronized 有什么区别？**

#### P6/P7 级回答

`synchronized` 可以加在三类位置上：

1. **实例方法**：锁当前对象实例
2. **静态方法**：锁当前类的 `Class` 对象
3. **代码块**：锁指定对象

我一般会特别强调：
 `synchronized` 锁的不是代码，而是**某个对象监视器**。
 代码只是同步范围，真正决定互斥关系的是“锁对象是否相同”。

**追问回答：**
 静态方法和实例方法上的锁不是同一把锁。

- 实例方法：锁的是当前实例 `this`
- 静态方法：锁的是类对象 `Class`

所以：

- 两个不同实例调用实例同步方法，彼此不一定互斥
- 所有实例调用同一个类的静态同步方法，会互斥

这个点在代码 review 里很常见。很多人以为“都写了 synchronized 就一定互斥”，其实要看锁对象是不是同一个。

------

### 问题 2

**synchronized 的底层实现原理是什么？**

#### 追问

**什么是锁升级？**

#### P6/P7 级回答

`synchronized` 底层依赖 JVM 的 **Monitor 机制**。
 对象头中的 Mark Word 会记录锁相关状态，线程进入同步块时，本质上是在竞争对象关联的监视器。

如果从字节码层看：

- 同步代码块对应 `monitorenter` / `monitorexit`
- 同步方法则通过方法访问标志来体现同步语义

我在回答这个问题时，一般不会只停留在“底层是 Monitor”，还会讲它的工程含义：

1. JVM 层实现，语义简单
2. 异常情况下也能自动释放锁
3. 随着 JDK 优化越来越多，性能已经不像早期版本那么差

所以在很多业务代码里，**如果不需要可中断、公平锁、条件队列等高级能力，synchronized 是完全够用的。**

**追问回答：**
 锁升级本质上是 JVM 为了减少不同竞争强度下锁开销做的优化策略。
 通常会经历：

- 无锁
- 偏向锁
- 轻量级锁
- 重量级锁

它的核心思想是：
 如果没有竞争，或者竞争很轻，就不要一上来就走操作系统级别的阻塞唤醒，因为那样上下文切换成本太高。

从工程角度理解，就是一句话：
 **JVM 试图把“低竞争场景下的锁”做得尽可能便宜。**

------

### 问题 3

**synchronized 和 ReentrantLock 怎么选？**

#### 追问

**什么场景更适合 ReentrantLock？**

#### P6/P7 级回答

我一般不会简单说“哪个好”，而是说它们的**能力边界**不同。

### synchronized 的特点

- JVM 原生关键字
- 语义简单
- 自动释放锁
- 可读性好，适合大多数互斥场景

### ReentrantLock 的特点

- JDK 层显式锁
- 支持可中断获取锁
- 支持超时获取锁
- 支持公平锁
- 支持多个 `Condition`

所以如果只是单纯的互斥保护，我通常优先 `synchronized`。
 如果需要更灵活的锁控制，比如：

- 避免线程长期阻塞
- 支持超时降级
- 一个锁上挂多个条件队列
- 需要更可控的并发编排

那我会用 `ReentrantLock`。

**追问回答：**
 更适合 `ReentrantLock` 的典型场景有：

1. **需要 `tryLock` 做快速失败**
    比如高并发下避免请求堆积
2. **需要 `lockInterruptibly`**
    避免线程无法响应中断
3. **多个条件队列**
    比如生产者消费者模型中把“非空”和“非满”分开管理
4. **复杂并发组件封装**
    很多中间件、框架级代码会更偏向显式锁，因为可控性更强

------

## 4. 线程池

### 问题 1

**线程池的核心参数有哪些？**

#### 追问

**线程池接收任务后的执行流程是什么？**

#### P6/P7 级回答

`ThreadPoolExecutor` 我一般会重点关注七个核心参数：

- `corePoolSize`
- `maximumPoolSize`
- `keepAliveTime`
- `unit`
- `workQueue`
- `threadFactory`
- `RejectedExecutionHandler`

但如果是 P6/P7 级回答，我不会只背参数，而是强调：
 **线程池本质上是“线程数上限 + 排队策略 + 拒绝策略”的组合。**

这是资源治理问题，不只是 API 问题。

### 我看线程池主要看三件事：

1. 线程数怎么控制
2. 任务怎么排队
3. 扛不住时怎么拒绝

**追问回答：**
 任务提交后的执行流程可以概括为：

1. 如果当前线程数小于核心线程数，直接创建核心线程执行
2. 否则先尝试进入阻塞队列
3. 如果队列已满且线程数还没到最大线程数，则创建非核心线程处理
4. 如果线程数也到上限了，就执行拒绝策略

我一般会补一个工程经验：
 **线程池问题本质上不是“线程少了”，而是“系统总容量设计不清楚”。**
 如果队列无界，就可能把压力从“线程”转移成“内存”；
 如果线程无界，就可能把压力从“排队”转移成“CPU 和上下文切换”。

------

### 问题 2

**为什么不建议直接使用 Executors 创建线程池？**

#### 追问

**生产环境应该怎么创建线程池？**

#### P6/P7 级回答

不建议直接使用 `Executors`，核心原因是它隐藏了关键容量参数，容易把问题延后到线上暴露。

例如：

- `FixedThreadPool` 和 `SingleThreadExecutor` 默认使用无界队列，任务堆积多了可能导致 OOM
- `CachedThreadPool` 线程数理论上可以无限增长，容易把机器打满

这类问题在压测不充分或流量突增时很容易出事故。

**追问回答：**
 生产环境我一般建议显式 new `ThreadPoolExecutor`，并结合业务类型设置：

1. 核心线程数
2. 最大线程数
3. 有界队列
4. 明确拒绝策略
5. 自定义线程工厂，方便日志定位
6. 配置监控指标，比如活跃线程数、队列长度、拒绝次数、任务耗时

如果再往上走一步，我会把线程池纳入配置中心和监控平台，让它成为可观测、可调优的资源池，而不是散落在代码里的一个工具类。

------

### 问题 3

**线程池大小怎么设置？**

#### 追问

**CPU 密集型和 IO 密集型任务分别怎么配置？**

#### P6/P7 级回答

线程池大小我一般不会给绝对值，而是给**方法论**。

核心要看：

1. 机器 CPU 核数
2. 任务类型
3. 任务平均耗时
4. 阻塞比例
5. 系统整体负载
6. 下游承载能力

如果是 CPU 密集型任务，线程太多没有意义，反而会增加上下文切换。
 如果是 IO 密集型任务，因为线程大量时间在等待网络/磁盘/外部服务返回，可以适当放大线程数。

**追问回答：**
 常见经验值：

- **CPU 密集型**：线程数接近 `CPU 核数` 或 `CPU 核数 + 1`
- **IO 密集型**：可以适当更大，比如 `CPU 核数 * 2`，甚至更高，但要结合阻塞时间和下游限制

不过我会强调，经验值只是起点，不是结论。
 真正靠谱的方式还是：

- 先按任务类型设初值
- 再通过压测看吞吐、延迟、拒绝率、CPU 使用率
- 最后动态调优

------

## 5. ThreadLocal

### 问题 1

**ThreadLocal 的作用是什么？**

#### 追问

**它和 synchronized 的区别是什么？**

#### P6/P7 级回答

`ThreadLocal` 的核心作用不是“共享”，而是**隔离**。
 它为每个线程提供一份独立变量副本，让线程之间互不干扰。

我常见它的使用场景有：

- 请求上下文透传
- 用户身份信息
- TraceId
- 数据源路由
- 事务上下文
- 一些线程内复用对象

**追问回答：**
 `ThreadLocal` 和 `synchronized` 解决的是两类完全不同的问题：

- `synchronized`：多个线程访问同一份共享资源，靠加锁保证安全
- `ThreadLocal`：干脆不共享，每个线程保存自己的副本

所以：

- 一个是**同步**
- 一个是**隔离**

如果场景本身是共享写，那 `ThreadLocal` 解决不了问题；
如果本来就适合线程内独享，那用 `synchronized` 反而会过度设计。

------

### 问题 2

**ThreadLocal 的底层原理是什么？**

#### 追问

**为什么 key 要设计成弱引用？**

#### P6/P7 级回答

`ThreadLocal` 的底层不是“ThreadLocal 存 value”，而是：

**每个 Thread 持有一个 ThreadLocalMap，ThreadLocal 作为 key，业务对象作为 value。**

调用 `set/get/remove` 时，本质上都是在操作当前线程内部的那张 `ThreadLocalMap`。

这也是为什么它能做到线程隔离：
 因为数据根本不在 `ThreadLocal` 对象里，而在各自线程内部。

**追问回答：**
 key 设计成弱引用，是为了避免 `ThreadLocal` 对象本身已经没有外部强引用了，却还因为线程里的 Map 被强行保活。

但这里要注意一个经常被误解的点：

- **key 是弱引用**
- **value 不是弱引用**

所以如果 `ThreadLocal` 被回收后，entry 的 key 变成了 `null`，但 value 还挂在线程对象上，只要线程没结束，这个 value 就可能一直占内存。

这也就是为什么大家总说 ThreadLocal 有内存泄漏风险。

------

### 问题 3

**ThreadLocal 为什么容易内存泄漏？怎么避免？**

#### 追问

**为什么在线程池场景下问题更明显？**

#### P6/P7 级回答

ThreadLocal 内存泄漏的核心不在“弱引用”，而在**长生命周期线程 + 未及时 remove**。

线程池里的工作线程通常不会结束。
如果请求处理完后不调用 `remove()`，即使 `ThreadLocal` key 被回收，value 仍可能残留在线程的 `ThreadLocalMap` 里，后续长期不释放。

这类问题不仅会导致内存泄漏，还可能引发**线程上下文串数据**，比如用户信息串到下一个请求。

**追问回答：**
 在线程池场景下更明显，是因为：

- 普通线程执行完会销毁，Map 也就跟着释放
- 线程池线程会被复用，生命周期非常长

避免方式我一般会从工程规范上回答：

1. **使用后必须 remove**
2. 最好在统一拦截器、过滤器、AOP finally 中清理
3. 对上下文透传场景建立统一封装，不让业务方手动乱用
4. 异步线程切换时不要默认依赖 ThreadLocal，必要时使用显式上下文传递方案

------

## 6. CAS

### 问题 1

**什么是 CAS？**

#### 追问

**CAS 为什么能保证原子性？**

#### P6/P7 级回答

CAS 是一种典型的无锁并发原语，全称是 **Compare And Swap**。

它包含三个值：

- 内存位置 V
- 预期值 A
- 新值 B

只有当内存位置当前值等于预期值 A 时，才把它更新为 B；否则更新失败。

CAS 的核心思想是：
 **先假设不会冲突，冲突了再重试。**
 这就是典型的乐观锁思路。

**追问回答：**
 CAS 之所以具备原子性，不是因为 Java 代码天然原子，而是因为底层依赖 CPU 提供的原子指令支持。
 也就是说，比较和交换在硬件层面是一个不可分割的操作。

从工程角度理解，CAS 的价值在于：

- 避免线程阻塞
- 减少上下文切换
- 在低冲突场景下性能很好

------

### 问题 2

**CAS 有哪些问题？**

#### 追问

**什么是 ABA 问题？怎么解决？**

#### P6/P7 级回答

CAS 不是银弹，它主要有三类问题：

### 1）ABA 问题

值从 A 变成 B，又变回 A，CAS 只看最终值，看不出中间是否被改过。

### 2）自旋开销

如果竞争很激烈，线程不断重试，会消耗大量 CPU。

### 3）只能天然保证单变量原子性

复杂对象或多字段一致性更新，单纯 CAS 不够，需要额外设计。

**追问回答：**
 ABA 问题的本质是：
 **“值看起来没变”，不代表“过程没变”。**

典型解决思路是给值加版本号或时间戳。
 JDK 里可以用 `AtomicStampedReference` 这类工具，把“值 + 版本”一起比较。

我在回答时通常会强调，是否真的需要处理 ABA，要看业务语义。
 比如有些纯计数场景，A->B->A 不一定有问题；
 但在链表、栈、对象状态机这类结构里，ABA 可能就是致命问题。

------

### 问题 3

**CAS 和 synchronized 怎么选？**

#### 追问

**什么场景更适合 CAS？**

#### P6/P7 级回答

我会把它理解为两种不同并发策略：

- `CAS`：乐观锁，先尝试，无阻塞
- `synchronized`：悲观锁，默认会有竞争，需要互斥保护

CAS 的优点是：

- 不阻塞线程
- 在低冲突场景性能很好

缺点是：

- 高冲突下重试成本高
- 代码复杂度更高
- 复杂共享状态不容易维护

**追问回答：**
 更适合 CAS 的场景通常具备几个特点：

1. 更新逻辑简单
2. 竞争不高
3. 对吞吐量敏感
4. 可接受失败后重试

比如：

- 原子计数器
- 状态位切换
- 无锁队列/栈中的部分操作

如果状态复杂、临界区大、业务逻辑长，我反而更倾向直接加锁，代码更稳、更容易维护。

------

# 二、Spring

------

## 7. IoC

### 问题 1

**什么是 IoC？**

#### 追问

**IoC 和 DI 的关系是什么？**

#### P6/P7 级回答

IoC 我理解成一句话：
 **对象的创建和依赖关系管理，从业务代码手里转移给容器。**

以前是业务代码自己 `new` 依赖对象；
 用了 Spring 之后，由容器统一装配、生命周期管理、扩展增强。

它的价值不只是“少写 new”，更重要的是：

1. **降低耦合**
2. **统一生命周期管理**
3. **便于扩展和测试**
4. **为 AOP、事务、配置化装配提供基础**

**追问回答：**
 IoC 是思想，DI 是实现 IoC 的主要手段。

- **IoC**：控制权反转给容器
- **DI**：容器通过构造器、setter、字段注入等方式，把依赖注入进去

我更推荐强调构造器注入，因为：

- 依赖关系更明确
- 更利于单元测试
- 更符合不可变设计

------

### 问题 2

**Spring Bean 生命周期是什么？**

#### 追问

**BeanPostProcessor 的作用是什么？**

#### P6/P7 级回答

Bean 生命周期如果按主线说，大致是：

1. 实例化
2. 属性填充
3. `Aware` 回调
4. `BeanPostProcessor` 前置处理
5. 初始化
6. `BeanPostProcessor` 后置处理
7. 使用
8. 销毁

如果是面试，我一般会补一个更关键的点：
 **Spring 很多扩展能力，其实都挂在生命周期钩子上。**

比如：

- AOP 代理创建
- 自定义初始化逻辑
- 参数校验
- 配置绑定
- 各种框架整合

**追问回答：**
 `BeanPostProcessor` 是 Spring 非常核心的扩展点。
 它允许我们在 Bean 初始化前后做统一处理。

很多 Spring 能力都建立在它之上，比如：

- 自动代理创建
- 注解驱动能力增强
- 一些中间件框架的自动装配扩展

我一般会把它理解为：
 **Spring 容器给开发者和框架层留出的“Bean 装配拦截器”。**

------

## 8. AOP

### 问题 1

**什么是 AOP？**

#### 追问

**AOP 主要解决什么问题？**

#### P6/P7 级回答

AOP 是面向切面编程，我的理解是：
 **把和主业务无关、但又反复出现的横切逻辑抽离出来，统一治理。**

典型场景包括：

- 日志
- 事务
- 权限校验
- 监控埋点
- 接口幂等
- 参数校验补充处理

它的价值不只是“少写重复代码”，更重要的是让这些公共逻辑具备统一入口，便于维护和演进。

**追问回答：**
 AOP 解决的是**横切关注点散落在各业务模块中**的问题。

如果没有 AOP，很多服务方法里都会有类似模板代码：

- 先打印日志
- 再鉴权
- 再开启事务
- 最后做监控上报

这样会导致业务代码被污染，职责边界不清。
 AOP 的价值就在于把这些逻辑从业务主线中剥离出来。

------

### 问题 2

**Spring AOP 的实现原理是什么？**

#### 追问

**JDK 动态代理和 CGLIB 怎么选？**

#### P6/P7 级回答

Spring AOP 的核心原理是**动态代理**。
 也就是说，Spring 不直接改目标类源码，而是在运行期生成代理对象，在代理对象中织入增强逻辑。

一般规则是：

- 目标类实现了接口，优先使用 **JDK 动态代理**
- 没有实现接口，通常使用 **CGLIB**

Spring AOP 是基于方法级别的代理增强，不是像 AspectJ 那样更彻底的字节码织入，所以它的能力边界也更清晰。

**追问回答：**
 怎么选通常不是业务手动选，而是 Spring 根据条件决定。

- **JDK 动态代理**：基于接口，生成实现接口的代理类
- **CGLIB**：基于继承，为目标类生成子类

所以限制也不同：

- final 类不能被 CGLIB 代理
- final 方法也不能被子类重写增强

如果项目里用到了大量基于类的代理能力，设计时就要避免不必要的 final 限制。

------

### 问题 3

**为什么 @Transactional 有时会失效？**

#### P6/P7 级回答

`@Transactional` 失效是一个非常经典的 Spring 面试题。
 核心原因通常都可以归结为一句话：

**事务增强没有经过 Spring 代理对象。**

常见失效场景包括：

1. 方法不是 `public`
2. 异常被吞掉，没有抛出
3. 捕获异常后没有手动回滚
4. 配置的异常类型不匹配
5. 方法内部自调用
6. Bean 没有交给 Spring 管理
7. 多线程切换导致事务上下文丢失

**追问：自调用为什么会导致事务失效？**
 同一个类内部 `this.xxx()` 调用，不会经过 Spring 生成的代理对象，而是直接调用目标方法本身。

事务逻辑是织入在代理对象里的，所以绕过代理，事务自然就不生效。

这个问题在工程里常见的解决方案有：

- 拆到另一个 Spring Bean 中
- 通过代理对象调用
- 调整事务边界设计

我一般更倾向第一种：
 **通过职责拆分让事务边界更清晰，而不是为了“让注解生效”去写绕的代码。**

------

## 9. Spring Boot

### 问题 1

**Spring Boot 为什么能简化开发？**

#### 追问

**它的核心能力是什么？**

#### P6/P7 级回答

Spring Boot 简化开发，不是因为“封装了一堆依赖”，而是因为它把 Spring 项目里最繁琐的几件事做了标准化：

1. **自动配置**
2. **Starter 依赖管理**
3. **内嵌 Web 容器**
4. **约定优于配置**
5. **生产运维支持，比如 Actuator**

本质上，它做的是**工程脚手架 + 自动装配体系**，让开发者把精力从框架装配切回业务实现。

**追问回答：**
 我认为 Spring Boot 的核心能力其实就两个：

- **自动配置**
- **约定化集成**

自动配置解决“怎么装”；
 Starter 解决“装什么”；
 这两者叠加，才形成了 Spring Boot 的开发体验。

------

### 问题 2

**Spring Boot 自动配置原理是什么？**

#### 追问

**它为什么能做到按需装配？**

#### P6/P7 级回答

Spring Boot 自动配置的核心链路，我一般概括成三步：

1. 通过主启动类上的自动配置入口启用自动装配机制
2. 框架通过约定的配置导入方式加载候选自动配置类
3. 自动配置类再结合一系列条件注解决定是否真正生效

这些条件通常包括：

- 类路径下是否存在某个类
- 容器中是否已有某个 Bean
- 配置文件里是否开启某个开关
- 当前是不是 Web 环境等

**追问回答：**
 它能做到按需装配，关键就在于**条件注解 + 配置元信息 + Spring 容器上下文状态**。

------

### 问题 3

**如果让你设计一个自定义 Starter，你会怎么做？**

#### 追问

**怎么让这个 Starter 更适合团队复用？**

#### P6/P7 级回答

我设计自定义 Starter，通常会按“依赖入口”和“自动装配实现”分层：

1. 定义配置属性类，承接外部配置
2. 编写 AutoConfiguration
3. 用条件注解决定在什么场景下生效
4. 导出统一的 Bean 能力
5. 提供默认配置，但保留覆盖能力
6. 补充自动提示和使用文档

**追问回答：**
 如果是团队复用，我会特别关注这几个点：

1. **开箱即用**
2. **可配置但不过度配置**
3. **默认值合理**
4. **失败时有清晰日志**
5. **提供监控/埋点能力**
6. **兼容多个业务线场景**

因为 Starter 本质上不是“把代码包起来”，而是把一套最佳实践产品化。

------

# 三、MySQL

------

## 10. ACID

### 问题 1

**事务的 ACID 分别是什么？**

#### 追问

**MySQL/InnoDB 是怎么实现这些特性的？**

#### P6/P7 级回答

ACID 是事务的四个核心特性：

- **A：Atomicity 原子性**
- **C：Consistency 一致性**
- **I：Isolation 隔离性**
- **D：Durability 持久性**

如果是 P6/P7 级回答，我不会只翻译缩写，而会把它和底层机制关联起来。

**追问回答：**
 在 InnoDB 里大致可以这样理解：

- **原子性**：主要依赖 `undo log`
- **持久性**：主要依赖 `redo log`
- **隔离性**：主要依赖锁 + MVCC
- **一致性**：本质上是事务机制和业务约束共同作用后的结果

我通常会强调：
 **一致性不是单靠数据库某一个机制“实现”的，而是原子性、隔离性、持久性以及应用约束共同保证的。**

------

### 问题 2

**一致性和隔离性有什么区别？**

#### 追问

**一致性完全由数据库负责吗？**

#### P6/P7 级回答

这两个概念很容易被混淆。

- **隔离性**：关注并发事务之间互不干扰到什么程度
- **一致性**：关注事务执行前后，数据是否仍满足业务规则和约束

比如转账场景里：

- 隔离性解决的是多个转账事务并发时会不会互相读脏数据、覆盖更新
- 一致性解决的是转账前后总金额是否守恒

**追问回答：**
 一致性不完全由数据库负责。
 数据库可以保证：

- 主键唯一
- 外键约束
- 事务原子提交
- 并发控制

但像“订单支付后库存必须扣减成功”、“账务总额必须平衡”这类业务一致性，通常还需要应用层、消息机制、补偿机制共同保证。

------

### 问题 3

**原子性是怎么实现的？**

#### 追问

**事务回滚时底层发生了什么？**

#### P6/P7 级回答

InnoDB 的原子性主要依赖 `undo log`。
 可以理解为：在修改数据前，先把旧版本信息保留下来，这样事务失败或回滚时就有机会恢复。

**追问回答：**
 回滚时，数据库会根据 `undo log` 进行逆向恢复，把已经做过的修改撤销掉。

从工程角度我会补一句：
 `undo log` 不只是为了回滚，它同时也是 MVCC 的重要基础。
 所以它在事务系统里承担的是“双角色”。

------

## 11. 事务隔离级别

### 问题 1

**MySQL 的四种隔离级别是什么？**

MySQL 四种隔离级别是：

1. **Read Uncommitted**
2. **Read Committed**
3. **Repeatable Read**
4. **Serializable**

它们是数据库为了解决事务并发时的数据一致性问题而提供的四种隔离强度，主要对应三类问题：

- **脏读**：读到别人**未提交**的数据
- **不可重复读**：同一事务里两次读同一行，结果不一样
- **幻读**：同一事务里两次按条件查询，记录条数不一样

### 1. Read Uncommitted

隔离性最弱，能直接读到其他事务未提交的数据。
 所以：

- **脏读、不可重复读、幻读都可能发生**

### 2. Read Committed

只能读到其他事务**已提交**的数据。
 它是通过 **MVCC 的可见性判断** 实现的：未提交版本对当前事务不可见，只能读已提交的旧版本。
 所以：

- **解决脏读**
- **不可重复读、幻读仍可能发生**

因为每次读都会生成新的可见性视图，别的事务提交后，你下次读就可能看到新结果

### 3. Repeatable Read

MySQL InnoDB 默认级别。
 它也是基于 **MVCC**，但和 RC 不同的是：

> **一个事务里复用同一个 Read View**

所以同一事务中多次读取同一行，看到的是同一份快照。
 因此：

- **解决脏读**
- **解决不可重复读**

对于幻读：

- 普通 `select` 用**快照读**，结果保持一致
- **当前读**（如 `update`、`select ... for update`）通过 **间隙锁 / Next-Key Lock** 减少幻读

### 4. Serializable

隔离级别最高。
 它通过 **加更强的锁，让事务尽量串行执行**。
 所以：

- **脏读、不可重复读、幻读都可以避免**
- 但并发性能最差

------

### 问题 2

**MySQL 默认为什么是 Repeatable Read？**

InnoDB 默认使用 `Repeatable Read`，本质上是在一致性和性能之间取得一个较好的平衡点。

相比 `Read Committed`，它能在一个事务内提供更稳定的读视图；
 相比 `Serializable`，它又不会把并发能力压得太低。

#### 追问

**InnoDB 的 RR 为什么和标准 SQL 里的理解不完全一样？**

**追问回答：**
 InnoDB 的 RR 和很多教材里的标准定义不完全一样，是因为 InnoDB 在实现上引入了：

- **MVCC**
- **间隙锁 / 临键锁**

所以它在很多场景下对幻读的处理比大家想象中更强。
 特别是：

- **快照读**：主要靠 MVCC
- **当前读**：主要靠锁机制防止范围内插入

因此在 MySQL 面试里，谈幻读时最好区分“快照读”和“当前读”，否则回答容易显得不够深入。

------

### 问题 3

**什么是 MVCC？**

MVCC 是多版本并发控制。
 它的核心目标是：**让读尽量不阻塞写，写也尽量不阻塞读。**

它不是简单“给数据加版本号”，而是一整套机制，通常依赖：

- 隐藏字段
- undo log
- Read View

用户看到的是“当前事务可见的那个版本”，而不是唯一的一份物理数据。

#### 追问

**RC 和 RR 下 MVCC 的差异是什么？**

**追问回答：**
 RC 和 RR 的关键差异在于 **Read View 的生成时机**：

- **RC**：每次快照读都生成新的 Read View
- **RR**：事务第一次快照读生成 Read View，后续通常复用

这就导致：

- RC 下同一事务里两次查询结果可能不同
- RR 下同一事务里的快照读结果通常一致

这个点是理解不可重复读和事务可见性的核心。

------

## 12. 索引

### 问题 1

**MySQL 为什么使用 B+ 树索引？**

#### 追问

**为什么不用红黑树或 Hash？**

MySQL 选择 B+ 树，核心原因是它更适合**磁盘/页式存储 + 范围查询**场景。

我一般会从三个角度回答：

### 1）树高度低，磁盘 IO 少

数据库索引不是纯内存结构，节点通常对应磁盘页。
 B+ 树是多路平衡树，单节点可容纳很多 key，因此高度低，查找时磁盘访问次数更少。

### 2）范围查询友好

叶子节点天然有序，并且通常通过链表相连，范围查询、排序、分页都很高效。

### 3）非叶子节点只存索引，不存完整数据

这样每个节点能放更多 key，进一步降低树高。

**追问回答：**
 为什么不用红黑树或 Hash：

- **红黑树**：二叉结构，树高相对高，磁盘 IO 次数更多，不适合数据库索引
- **Hash**：等值查询快，但不支持范围查询、排序、最左匹配等复杂能力

所以数据库索引要解决的不是单点查找，而是综合查询能力，B+ 树是更平衡的工程选择。

------

### 问题 2

**索引失效的常见原因有哪些？**

#### 追问

**联合索引为什么要遵循最左前缀原则？**

索引失效本质上不是“索引坏了”，而是**优化器判断走索引不划算，或者条件写法破坏了索引可用性**。

常见情况包括：

1. 对索引列做函数或表达式运算
2. 隐式类型转换
3. 前导 `%` 模糊匹配
4. 联合索引未满足最左前缀
5. 选择性太差
6. `or` 使用不当
7. 回表成本过高，优化器认为全表扫描更划算

**追问回答：**
 联合索引的最左前缀原则，本质上来自索引的有序组织方式。
 比如联合索引 `(a, b, c)`，它先按 `a` 排，再在 `a` 相同的范围内按 `b` 排，最后按 `c` 排。

所以如果你跳过 `a` 直接查 `b`、`c`，数据库通常无法有效利用这个有序结构。

我一般会强调一句：
 **最左前缀不是死记规则，而是索引底层排序结构的自然结果。**

------

### 问题 3

**覆盖索引是什么？它为什么重要？**

#### 追问

**什么是回表？**

覆盖索引指的是：
 **查询所需的字段都能直接从索引结构中拿到，不需要再回主表取数据。**

它重要的原因很直接：

1. 减少一次主键索引访问
2. 降低随机 IO
3. 提升查询性能
4. 在高并发读场景下收益明显

**追问回答：**
 回表通常发生在二级索引查询里。
 流程是：

1. 先从二级索引查到主键值
2. 再根据主键去聚簇索引取完整行数据

如果查询字段已经都包含在二级索引里，就不需要这第二步，也就是不需要回表。

所以很多 SQL 优化，本质上就是在想办法让查询“尽量覆盖”。

------

## 13. 日志机制

### 问题 1

**MySQL 常见日志有哪些？**

#### 追问

**redo log 和 binlog 的区别是什么？**

#### P6/P7 级回答

MySQL 常见日志我一般分成引擎层和 Server 层来理解：

### InnoDB 相关

- `redo log`
- `undo log`

### Server 层相关

- `binlog`
- `slow query log`
- `error log`
- `general log`（了解即可）

**追问回答：**
 `redo log` 和 `binlog` 是高频考点，我一般从四个维度区分：

### 1）层次不同

- `redo log`：InnoDB 引擎层
- `binlog`：MySQL Server 层

### 2）内容不同

- `redo log`：偏物理日志，记录页的修改
- `binlog`：逻辑日志，记录 SQL 或行变更

### 3）用途不同

- `redo log`：崩溃恢复
- `binlog`：主从复制、归档恢复

### 4）写入方式不同

- `redo log` 是循环写
- `binlog` 是追加写

这个区分讲清楚，面试官一般会认为你对事务提交链路理解比较完整。

------

### 问题 2

**为什么需要 redo log？**

#### 追问

**什么是 WAL？**

#### P6/P7 级回答

如果每次事务提交都直接把脏页刷回磁盘，随机 IO 成本会非常高，吞吐上不去。
 所以 InnoDB 引入了 `redo log`，先把修改以顺序写日志的方式落盘，后续再异步刷数据页。

这套机制本质上是用**顺序写替代随机写**，大幅提升性能。

**追问回答：**
 WAL 是 **Write-Ahead Logging**，意思是“先写日志，再写数据”。

好处是：

- 即使真正的数据页还没来得及落盘
- 只要日志已经持久化
- 宕机后也能通过日志恢复出已提交事务

所以 WAL 是事务持久性的重要基础。

------

### 问题 3

**两阶段提交解决什么问题？**

#### 追问

**为什么 redo log 和 binlog 必须协调一致？**

#### P6/P7 级回答

MySQL 里两阶段提交主要是为了解决：
 **事务提交时 redo log 和 binlog 之间的一致性问题。**

如果没有协调机制，就可能出现：

- redo log 成功，binlog 失败
- binlog 成功，redo log 失败

这样会导致：

- 崩溃恢复结果不一致
- 主从复制数据不一致
- 数据恢复链路混乱

**追问回答：**
 之所以必须协调，是因为它们分别服务两个不同目标：

- `redo log` 保证本机存储引擎恢复
- `binlog` 保证复制和归档恢复

如果一个事务对引擎来说“提交了”，但对复制链路来说“没提交”，系统状态就会分裂。
 两阶段提交就是为了把这两套状态对齐。

------

## 14. 锁机制

### 问题 1

**MySQL 有哪些锁？**

#### 追问

**行锁和表锁怎么理解？**

#### P6/P7 级回答

我一般把 MySQL 锁分成两个维度：

### 1）按粒度

- 表锁
- 行锁
- 页锁（了解即可）

### 2）按语义

- 共享锁
- 排他锁
- 意向锁
- 间隙锁
- 临键锁

如果是 InnoDB，实际最常讨论的还是**行锁体系**，因为它直接关系到并发性能和死锁问题。

**追问回答：**
 行锁和表锁的核心差异是粒度不同：

- **表锁**：加锁开销小，但并发度低
- **行锁**：并发度高，但锁管理更复杂

不过要注意一句经常被忽略的话：
 **InnoDB 的行锁是基于索引实现的。**
 如果 SQL 没走索引，或者索引条件不精确，表面看你想加行锁，实际可能锁的范围会扩大很多，甚至退化成更重的锁效果。

------

### 问题 2

**什么是间隙锁和临键锁？**

#### 追问

**它们主要解决什么问题？**

#### P6/P7 级回答

- **间隙锁（Gap Lock）**：锁的是索引记录之间的间隙，不锁具体已有记录
- **临键锁（Next-Key Lock）**：记录锁 + 间隙锁，锁住一个范围

它们本质上不是为了“锁住已有数据”，而是为了**防止范围内新增数据破坏当前事务的结果集稳定性**。

**追问回答：**
 它们主要是为了解决**幻读问题**，尤其是在 `Repeatable Read` 下的当前读场景。

比如你做范围更新、范围查询加锁时，如果不锁间隙，别的事务可以在这个范围插入新记录，那你前后两次看到的结果集就变了。

所以间隙锁本质上是在保护“范围”，而不是只保护“行”。

------

### 问题 3

**死锁是怎么产生的？怎么避免？**

#### 追问

**遇到线上死锁你怎么排查？**

#### P6/P7 级回答

死锁本质上是多个事务形成了循环等待。
 最常见的场景是：

- 事务 A 持有资源 1，等待资源 2
- 事务 B 持有资源 2，等待资源 1

InnoDB 能自动检测死锁，并回滚其中一个事务，让系统继续往前走。

**追问回答：**
 避免死锁我一般从设计层面入手：

1. 固定访问顺序
2. 缩小事务范围
3. 提高索引命中率，减少锁范围
4. 避免长事务
5. 把大事务拆成小事务

如果线上排查，我通常会看：

- 死锁日志
- 涉及的 SQL
- 执行计划
- 是否走索引
- 是否有热点行竞争
- 是否有批量更新/范围更新

很多死锁问题最后不是“锁机制太复杂”，而是 SQL 设计和事务边界设计不合理。

------

# 四、Redis

------

## 15. Redis 线程模型

### 问题 1

**Redis 是单线程的吗？**

#### 追问

**为什么 Redis 还能这么快？**

#### P6/P7 级回答

更准确地说，Redis 的**命令执行主线程**长期以来是单线程模型。
 但如果说 Redis “完全单线程”就不严谨了，因为在新版本里，网络读写、持久化等部分已经有多线程参与。

所以我通常会说：
 **Redis 的核心命令执行路径是单线程，但整体系统不是只有一个线程。**

**追问回答：**
 Redis 快的原因我一般总结为五点：

1. **内存数据库**，避免磁盘随机 IO
2. **数据结构高效**
3. **命令执行单线程，避免锁竞争**
4. **IO 多路复用**
5. **大多数操作时间复杂度低**

我还会补一句工程视角：
 Redis 快不是因为“单线程本身神奇”，而是因为它把问题设计成了“单线程就够快”。
 如果单次命令很重，比如大 key 扫描、复杂 Lua、批量删除，Redis 依然会被拖慢。

------

### 问题 2

**Redis 6 为什么引入多线程？**

#### 追问

**为什么命令执行还保留单线程？**

#### P6/P7 级回答

Redis 6 引入多线程，主要不是为了把命令执行并行化，而是为了解决**高并发网络 IO 成为瓶颈**的问题。

随着连接数和吞吐量上升，网络读写和协议解析的成本越来越明显，所以 Redis 把这部分工作拆出来并行处理。

**追问回答：**
 命令执行依然保留单线程，核心是两个考虑：

1. **保持模型简单**
    不需要复杂锁控制，很多数据结构实现和原子语义更容易保证
2. **收益不一定划算**
    对 Redis 这种以内存操作为主、单命令通常很快的系统来说，把执行层多线程化不一定比增加复杂度更值

所以 Redis 的策略是：
 **把网络层并行化，把数据结构修改路径保持简单。**

------

### 问题 3

**什么是 IO 多路复用？**

#### 追问

**epoll 为什么更适合高并发？**

#### P6/P7 级回答

IO 多路复用可以理解为：
 **一个线程同时监听多个连接上的事件，在某个连接真正可读/可写时再去处理。**

这样就避免了“一连接一线程”的高成本模型。

Redis 正是利用这种机制，在单线程命令执行模型下仍然能高效处理大量客户端连接。

**追问回答：**
 `epoll` 更适合高并发，主要是因为：

1. 事件驱动，不需要每次遍历所有 fd
2. 支持更大规模连接
3. 内核态和用户态之间的拷贝与调度成本更低

所以在 Linux 高并发网络编程里，`epoll` 基本是事实标准。

------

## 16. 持久化

### 问题 1

**Redis 的持久化方式有哪些？**

#### 追问

**RDB 和 AOF 怎么取舍？**

#### P6/P7 级回答

Redis 持久化主要有三种形态：

1. **RDB**
2. **AOF**
3. **混合持久化**

它们不是互斥关系，而是不同目标下的权衡。

**追问回答：**
 如果要比较：

### RDB

- 优点：文件紧凑、恢复快、适合备份
- 缺点：两次快照之间的数据可能丢失

### AOF

- 优点：数据更完整，丢失窗口更小
- 缺点：文件更大，恢复速度可能更慢

### 混合持久化

兼顾启动恢复效率和数据完整性

我在生产环境中通常会优先考虑：

- 对数据完整性要求高：AOF 或混合
- 对恢复速度、备份传输要求高：RDB 也很重要

------

### 问题 2

**AOF 重写是什么？**

#### 追问

**为什么不能一直追加？**

#### P6/P7 级回答

AOF 重写本质上是：
 **把“历史执行过程”压缩成“当前状态的最简恢复命令集合”。**

比如某个 key 被 set 了 100 次，最终恢复时根本不需要那 100 条历史命令，只需要保留能恢复到最终状态的命令即可。

**追问回答：**
 如果一直追加，会有两个问题：

1. 文件越来越大，恢复越来越慢
2. 大量历史无效命令会浪费存储和 IO

所以重写的意义不是“修复 AOF”，而是**对恢复成本做治理**。

------

### 问题 3

**如何尽量减少 Redis 数据丢失？**

#### 追问

**AOF 的刷盘策略怎么选？**

#### P6/P7 级回答

减少数据丢失，一般要从单点持久化提升到整体架构可靠性：

1. 开启 AOF 或混合持久化
2. 选择合理刷盘策略
3. 主从复制
4. 哨兵或集群提高可用性
5. 关键链路做多副本和降级方案

**追问回答：**
 AOF 刷盘策略常见有：

- `always`：每次写都刷盘，最安全，性能最差
- `everysec`：每秒刷一次，安全和性能折中，最常用
- `no`：交给 OS 决定刷盘时机，性能高但风险大

如果让我做工程选择，我通常会优先 `everysec`，因为它在大多数业务里是比较合理的平衡点。

------

## 17. 缓存策略

### 问题 1

**常见缓存更新策略有哪些？**

#### 追问

**为什么很多业务用 Cache Aside？**

#### P6/P7 级回答

常见缓存策略包括：

- Cache Aside
- Read Through
- Write Through
- Write Behind

但在 Java 业务系统里，用得最多的还是 **Cache Aside**，也就是旁路缓存模式。

**追问回答：**
 Cache Aside 常见，是因为它最符合业务系统的职责划分：

- 缓存不是主数据源
- 数据库才是最终可信来源
- 应用自己控制读写流程

读时：

- 先查缓存
- 未命中查库并回填

写时：

- 更新数据库
- 删除缓存

它的优势不是理论最优，而是**实现简单、可控、容易落地**。

------

### 问题 2

**为什么通常是“更新数据库，删除缓存”，而不是“更新缓存”？**

#### 追问

**删除缓存失败怎么办？**

#### P6/P7 级回答

因为数据库是最终数据源，而缓存的职责是加速读取。
 如果改成“更新缓存”，会引入更多问题：

- 缓存和数据库双写顺序复杂
- 缓存节点可能有多个副本
- 更新失败补偿更麻烦
- 一些冷数据根本没必要主动写缓存

所以“更新数据库，删除缓存”更符合工程上的最小复杂度原则。

**追问回答：**
 删除缓存失败时，常见补偿方案有：

1. 重试
2. 异步消息补偿
3. 延迟双删
4. 订阅 binlog 做最终修复

如果对一致性要求比较高，我更倾向做“数据库变更事件驱动缓存修复”，而不是把一致性全部赌在一次删除操作上。

------

### 问题 3

**如何设计一个高可用缓存系统？**

#### 追问

**缓存层设计最重要的边界是什么？**

#### P6/P7 级回答

高可用缓存系统我会从四个维度设计：

1. **性能**
   - 高命中率
   - 热点识别
   - 避免大 key、慢查询
2. **高可用**
   - 主从 + 哨兵 / Cluster
   - 故障自动转移
   - 多级缓存
3. **一致性**
   - 明确最终一致性策略
   - 删除失败补偿
   - 热点数据串行化更新
4. **稳定性**
   - 限流、熔断、降级
   - 防穿透、防击穿、防雪崩
   - 监控告警

**追问回答：**
 缓存层最重要的边界是：
 **缓存不是唯一真相来源。**

也就是说：

- 缓存可以失效
- 缓存可以重建
- 缓存可以短时间不一致
- 但数据库不能依赖缓存反向证明正确性

把这个边界守住，系统设计才不会跑偏。

------

## 18. 分布式锁

### 问题 1

**Redis 分布式锁一般怎么实现？**

#### 追问

**为什么要设置唯一 value？**

#### P6/P7 级回答

最基础的 Redis 分布式锁实现是：

```text
SET lock_key requestId NX PX 30000
```

这里关键点有三个：

- `NX`：只在 key 不存在时加锁
- `PX`：设置过期时间，避免死锁
- `requestId`：标识锁持有者

我会强调，分布式锁不是“加上就安全”，而是要同时考虑：

1. 获取锁原子性
2. 释放锁安全性
3. 业务执行时间是否超过 TTL
4. 故障切换一致性

**追问回答：**
 唯一 value 的作用是标识锁归属。
 否则你只知道“有锁”，但不知道“是不是我加的锁”。

这会导致一个严重问题：
 如果线程 A 的锁过期后，线程 B 获取了同一个锁，A 再去执行删除，就可能把 B 的锁删掉。

所以 value 唯一标识是必须的，不是可选优化。

------

### 问题 2

**为什么释放锁要用 Lua 脚本？**

#### 追问

**为什么不能先 get 再 delete？**

#### P6/P7 级回答

释放锁要确保两个动作原子执行：

1. 校验 value 是否是自己
2. 如果是，再删除 key

Lua 脚本可以把这两步放在 Redis 服务端一次性执行，避免并发窗口问题。

**追问回答：**
 如果先 `get` 再 `delete`，中间存在竞态：

1. 线程 A 读到 value 还是自己的
2. 锁刚好过期
3. 线程 B 获得了新锁
4. 线程 A 再执行 delete，把 B 的锁删掉

这就是典型的“非原子释放锁”问题。

------

### 问题 3

**Redis 分布式锁有哪些风险？**

#### 追问

**Redisson 相比手写锁有什么工程优势？**

#### P6/P7 级回答

Redis 分布式锁常见风险有：

1. 锁过期但业务没执行完
2. 主从切换导致锁丢失
3. 高可用架构下的一致性争议
4. 不支持可重入、续期、阻塞等待等高级能力
5. 锁粒度和持锁时间设计不合理导致吞吐下降

所以我通常会说：
 **Redis 锁适合做“业务层互斥协调”，不适合替代数据库事务。**

**追问回答：**
 Redisson 的工程价值主要在于它把很多复杂细节封装了：

- 可重入
- 看门狗自动续期
- 阻塞等待
- 公平锁
- 读写锁
- 更完善的 API 语义

如果是中大型项目，我一般不建议业务自己手写一套分布式锁细节，维护成本和踩坑概率都很高。

------

## 19. 缓存一致性

### 问题 1

**缓存和数据库为什么会出现双写不一致？**

#### 追问

**为什么不建议先删缓存再更新数据库？**

#### P6/P7 级回答

双写不一致的根本原因是：
 **数据库和缓存是两个独立系统，不存在天然事务。**

所以只要出现这些情况，就可能不一致：

- 更新数据库成功，删除缓存失败
- 删除缓存成功，但旧值被并发请求重新写回
- 消息异步修复延迟
- 多副本缓存同步延迟

这不是某个框架 bug，而是分布式系统的自然结果。

**追问回答：**
 不建议先删缓存再更新数据库，是因为中间存在窗口期：

1. 缓存被删掉
2. 数据库还没更新完成
3. 并发读请求查库读到旧值
4. 旧值重新写回缓存

结果就是缓存里反而又回填了脏数据。

所以业界更常见的是：
 **先更新数据库，再删除缓存。**

------

### 问题 2

**什么是延迟双删？**

#### 追问

**它能彻底解决一致性问题吗？**

#### P6/P7 级回答

延迟双删的典型流程是：

1. 更新数据库
2. 删除缓存
3. 延迟一小段时间后再删一次缓存

第二次删除的目的，是尽量清理掉在并发窗口中被旧值回填进去的缓存。

**追问回答：**
 它不能彻底解决问题，只能降低问题发生概率。
 因为延迟多久合适、是否覆盖所有并发路径、删除是否一定成功，这些都不是绝对可控的。

所以如果系统对一致性要求更高，我会倾向结合：

- MQ 重试
- binlog 订阅修复
- 版本号校验
- 热点 key 串行化更新

而不是把希望全部寄托在延迟双删上。

------

### 问题 3

**如果让你设计缓存一致性方案，你怎么做？**

#### 追问

**怎么在一致性和性能之间做权衡？**

#### P6/P7 级回答

我通常先分场景，而不是上来就给统一方案。

### 一般读多写少场景

- 更新数据库
- 删除缓存
- 删除失败异步补偿
- 设置合理过期时间

### 对一致性要求更高的场景

- 数据变更事件化
- MQ / binlog 驱动缓存修复
- 热点 key 做串行更新
- 必要时引入版本控制

### 对强一致性要求极高的场景

我会反过来问：
 这个场景是不是根本不适合走缓存？
 比如某些金融强一致链路，缓存只能作为旁路优化，不能承载核心正确性。

**追问回答：**
 一致性和性能的权衡，本质是业务决策：

- 大多数缓存系统追求的是**最终一致性**
- 不是每一毫秒都必须强一致

我的原则通常是：

1. 先明确数据是否允许短暂脏读
2. 再确定是同步修复还是异步修复
3. 最后补齐监控和补偿机制

因为没有监控的一致性方案，等于没有方案。

------

## 20. 缓存穿透 / 击穿 / 雪崩

### 问题 1

**什么是缓存穿透、击穿、雪崩？**

#### 追问

**三者的本质区别是什么？**

#### P6/P7 级回答

这三个问题本质上都是：
 **缓存没挡住流量，压力打到了后端存储。**

但触发方式不同：

- **穿透**：请求的 key 根本不存在，缓存和数据库都查不到
- **击穿**：某个热点 key 失效瞬间，大量请求同时回源
- **雪崩**：大量 key 同时失效，或者缓存整体不可用，导致系统性回源

**追问回答：**
 区别我一般会用“范围”来记：

- 穿透：**不存在的数据**
- 击穿：**单个热点数据**
- 雪崩：**大面积缓存失效或服务故障**

面试里如果能把“问题定义”和“系统后果”一起讲，显得会更成熟。

------

### 问题 2

**缓存穿透怎么解决？**

#### 追问

**为什么布隆过滤器适合这个场景？**

#### P6/P7 级回答

缓存穿透的思路不是单点解决，而是分层防御：

1. 参数合法性校验
2. 缓存空值
3. 布隆过滤器
4. 限流和风控

如果是恶意攻击流量，仅靠缓存空值通常不够，还要配合流量治理。

**追问回答：**
 布隆过滤器适合这个场景，是因为它能以很小空间成本快速判断“一个元素大概率存在 / 一定不存在”。

所以当请求来的 key 明显不在布隆过滤器里时，就没必要继续查 Redis 和 DB，直接拦截即可。

它特别适合大规模 key 集合的预过滤。

------

### 问题 3

**缓存击穿和雪崩怎么处理？**

#### 追问

**为什么热点 key 常配合互斥锁？**

#### P6/P7 级回答

### 缓存击穿

核心是保护热点 key：

- 热点不过期
- 逻辑过期
- 单飞 / 互斥锁回源
- 本地缓存兜底

### 缓存雪崩

核心是避免同时大面积失效：

- TTL 加随机值
- 多级缓存
- Redis 高可用
- 限流、降级、熔断
- 数据预热

**追问回答：**
 热点 key 常配合互斥锁，是为了避免 key 一失效，大量请求同时打到数据库。

本质上是把“并发回源”变成“单线程回源 + 其他线程等待/降级”。
 这不是让系统更快，而是让系统不被瞬时流量击穿。

------

# 五、新技术

------

## 21. RAG

### 问题 1

**什么是 RAG？**

#### 追问

**它和微调的核心区别是什么？**

#### P6/P7 级回答

RAG 是检索增强生成。
 我一般把它理解成：
 **先从外部知识源检索相关内容，再把检索结果作为上下文交给大模型生成答案。**

它的价值在于把“模型能力”和“企业私有知识、实时知识”结合起来。

**追问回答：**
 RAG 和微调的区别，本质上是：

- **RAG**：不改变模型参数，通过外部知识增强回答
- **微调**：改变模型参数，让模型学习新任务、新风格或领域能力

工程上我会优先考虑 RAG，因为：

1. 知识更新快
2. 成本相对低
3. 可解释性更强
4. 更适合企业知识库场景

------

### 问题 2

**RAG 的典型链路是什么？**

#### 追问

**为什么 chunk 和召回策略很关键？**

#### P6/P7 级回答

RAG 的典型链路我会拆成离线和在线两部分：

### 离线

1. 文档接入
2. 清洗去噪
3. 切分 chunk
4. 向量化
5. 建索引

### 在线

1. 用户提问
2. Query 改写
3. 检索召回
4. 重排
5. 上下文拼装
6. 模型生成
7. 返回引用和答案

**追问回答：**
 chunk 和召回策略直接决定“模型拿到什么上下文”。

如果 chunk 太大：

- 噪声多
- 检索不精准
- 上下文浪费

如果 chunk 太小：

- 语义不完整
- 召回碎片化

所以 RAG 项目做得好不好，往往不在模型本身，而在检索工程。
 很多答案不准，本质是“检索错了”或“给模型的证据不对”。

------

### 问题 3

**RAG 项目落地最大的难点是什么？**

#### 追问

**你怎么提升答案质量？**

#### P6/P7 级回答

RAG 落地的难点，我觉得不在“能不能接一个向量库”，而在工程闭环：

1. 文档质量参差不齐
2. 切分策略不稳定
3. 召回不准
4. 重排效果一般
5. 模型容易幻觉
6. 权限边界复杂
7. 缺少评测体系

**追问回答：**
 提升答案质量，我通常会从链路上逐段优化：

1. 优化 chunk 粒度和 overlap
2. 向量检索 + 关键词检索做混合召回
3. 增加 rerank
4. 对 query 做改写和意图识别
5. Prompt 中强化“基于检索结果回答、无法确认就拒答”
6. 返回引用片段，增强可追溯性
7. 建评测集，持续做离线评估和线上反馈闭环

------

## 22. Harness Engineering

### 问题 1

**你怎么理解 Harness Engineering？**

#### 追问

**它和传统后端开发最大的差异是什么？**

#### P6/P7 级回答

我理解的 Harness Engineering，不是单纯“接个大模型接口”，而是围绕大模型应用建立一套**可运行、可评估、可治理、可持续迭代**的工程体系。

它通常包括：

- Prompt 管理
- 模型路由
- RAG 编排
- 工具调用
- 效果评测
- 反馈闭环
- 安全与权限控制
- 成本与延迟治理

**追问回答：**
 它和传统后端开发最大的差异在于：
 传统后端处理的是**确定性系统**，而大模型应用处理的是**概率型系统**。

所以工程重点也会变化：

- 不只是“服务能跑”
- 还要“结果可解释、效果可衡量、问题可回放、策略可迭代”

这也是为什么大模型应用里评测体系和观测能力会变得非常重要。

------

### 问题 2

**一个企业级大模型平台要具备哪些能力？**

#### 追问

**为什么评测体系是核心能力？**

#### P6/P7 级回答

企业级平台我会重点看这几层：

1. **模型接入层**
   - 多模型适配
   - 路由和降级
2. **知识增强层**
   - 文档接入
   - 检索召回
   - 权限过滤
3. **编排层**
   - Prompt 模板
   - 工具调用
   - 工作流 orchestration
4. **治理层**
   - 监控
   - 日志
   - 审计
   - 成本控制
   - 内容安全
5. **评测层**
   - 离线评测
   - A/B 实验
   - 用户反馈闭环

**追问回答：**
 评测体系之所以重要，是因为大模型结果不是确定的。
 如果没有评测，你很难回答这些问题：

- 是检索差了，还是 prompt 退化了？
- 新模型版本到底更好还是更差？
- 成本上升换来了多少效果提升？
- 哪类问题持续答不好？

所以我会认为：
 **没有评测和回放能力的大模型平台，基本不具备真正的工程可迭代性。**

------

### 问题 3

**如果让你做企业知识库问答系统，你怎么设计？**

#### 追问

**如何保证答案可追溯、可控、可优化？**

#### P6/P7 级回答

如果让我设计，我会按“数据、检索、生成、治理”四层来搭：

### 1）数据层

- 支持多源接入：文档、Wiki、工单、数据库
- 清洗、切分、去重、打标签
- 做权限和租户隔离

### 2）检索层

- 向量检索 + 关键词检索
- rerank
- 召回日志保留

### 3）生成层

- Prompt 模板化
- 严格约束“基于证据回答”
- 返回引用来源

### 4）治理层

- 问答日志
- 反馈闭环
- 评测集
- 效果监控
- 成本监控

**追问回答：**
 保证可追溯、可控、可优化，我会重点做这几件事：

1. **返回引用片段**
2. **记录检索结果、Prompt、模型输出**
3. **支持问题回放**
4. **建立拒答机制**
5. **构建评测基线**
6. **让用户反馈进入迭代闭环**

这套设计思想和后端系统治理是一脉相承的，只是对象从“接口性能”变成了“模型效果”。